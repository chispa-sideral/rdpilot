//! Windows local-user-scoped transport: named pipe + explicit DACL +
//! `first_pipe_instance(true)` (Plan 12-07; DAEMON-02).
//!
//! `#[cfg(windows)]`-only — never compiled on this Linux development host.
//! Stub for now (Plan 12-02) — filled in by Plan 12-07 (the live-gate plan,
//! the only consumer of the `[ASSUMED]` `windows-permissions` crate).
#![cfg(windows)]
