# Requirements: rdpilot — v1.1 (Consumer Surfaces & File Transfer)

**Defined:** 2026-07-10
**Core Value:** A local AI agent can connect to a remote Windows desktop over RDP and read/inspect a program that is only reachable via RDP — using both screenshots and structured accessibility data, without installing or running the agent itself on the remote machine.

## v1.1 Requirements

Requirements for the v1.1 milestone. Each maps to roadmap phases.

### Session Daemon

- [ ] **DAEMON-01**: A long-lived daemon holds N live RDP sessions with keepalive, decoupled from any CLI process lifetime
- [x] **DAEMON-02**: A local IPC transport (unix socket / Windows named pipe) carries requests between clients (CLI, MCP) and the daemon, restricted to the local user (permission/DACL-scoped) — Unix path complete (12-04); Windows DACL path live-verified (15-05)
- [x] **DAEMON-03**: The daemon auto-starts on first client connect and reaps idle sessions / self-shuts-down when the registry empties
- [x] **DAEMON-04**: On restart, the daemon detects and tears down orphaned Windows-side sessions (in-memory registry; no reattach) — LIVE-VERIFIED (15-06): kill -9 mid-session against a real Azure VM target + restart surfaces the session as `Orphaned` (never forgotten/auto-killed), explicit disconnect reconciles it

### Session Lifecycle & Identity

- [x] **SESSION-01**: User can open a session under a caller-supplied name, or receive an auto-generated id when unnamed — LIVE-VERIFIED (15-06): explicit-name connect against a real Azure VM target succeeds end-to-end
- [x] **SESSION-02**: Every perception/input/file command explicitly targets a session by name/id — no implicit default
- [x] **SESSION-03**: User can list active sessions (name/id, target, status) — LIVE-VERIFIED (15-06): `list` reports correct name/id/host/status/timestamps against a real Azure VM session
- [x] **SESSION-04**: User can disconnect a named session; names/ids are unique (collisions rejected) — LIVE-VERIFIED (15-06): explicit disconnect against a real Azure VM session acknowledges and reconciles

### CLI Surface

- [x] **CLI-01**: A `rdpilot` CLI manages session lifecycle (connect [--name] / list / disconnect) over the daemon — COMPLETE (13-05): the `rdpilot-cli` crate/binary implements `connect [--name] / list / disconnect`, transparently auto-starting the daemon via the relocated `rdpilot_ipc::transport::connect_or_spawn` (13-01); offline-proven end-to-end against the real `rdpilot-daemon` binary (fake connector) in `tests/cli_lifecycle.rs`; `cargo tree -p rdpilot-cli` confirmed free of `ironrdp`/`rustls`/`rdpilot`/`rdpilot-daemon` (thin-client invariant, D-17)
- [x] **CLI-02**: CLI exposes the full perception + input + launch verb set (screenshot, world_state, UIA, window/process list, click/type/key/scroll/drag, launch, foreground), each targeting a named session — LIVE-VERIFIED (15-06): real screenshot pixels (before/after captures differ, PNG-magic confirmed), real click-coordinate landing (verified via `perceive uia` focus after a live-diagnosed deeper-subtree-scope + foreground-before-click fix), and real UIA tree shape all pass against a real Azure VM target (7-Zip File Manager)
- [x] **CLI-03**: CLI exposes file put/get and reports errors clearly (session-not-found, daemon-unreachable, transfer failure) — COMPLETE (13-07): `rdpilot put`/`get` (flat) and `rdpilot file put|get` (grouped) absolutize `--local` via `current_dir()` join before sending (no relative path ever hits the wire — the daemon's cwd differs from the invoking shell's); `get`'s no-clobber is fully CLI-side enforced (`exists()` check before the request is sent, `CliError::NoClobber`, exit 8, `--force` override) while `put`'s remote no-clobber is a documented known gap this phase (`--force` accepted for forward-compat only; `put --help` names the gap; tracked as backlog Phase 999.5 — symmetric remote no-clobber via a C# sensor `File.Exists` check, not built this phase); the finalized `exit_codes.rs` taxonomy maps every `WireErrorCode` plus the CLI-local `DaemonUnreachable`/`NoClobber` classes to 8 distinct non-zero exit codes, and `--json` errors emit `{"error":{"code":"<kebab>","message":"..."}}`; offline-proven in `tests/cli_errors.rs` against the real `rdpilot-daemon` binary (session-not-found exit 2, daemon-unreachable exit 3 — CLI binary copied without a sibling daemon binary so auto-spawn fails immediately, no-clobber exit 8 then `--force` succeeds). LIVE-VERIFIED (15-06): a real 8 MiB put/get round trip through the CLI surface reports `bytes_transferred` == local file size and a matching checksum both directions

### MCP Server Surface

- [x] **MCP-01**: An `rmcp`-based MCP server exposes rdpilot over MCP to any MCP client (e.g. Claude)
- [x] **MCP-02**: MCP exposes a single Anthropic computer-use-compatible `computer` tool (screenshot + action-discriminated mouse/keyboard/scroll) mapping onto the SDK input/capture verbs
- [x] **MCP-03**: MCP exposes rdpilot-native tools (world_state, UIA, window/process list, launch, foreground, session connect/list/disconnect, file put/get) as MCP Tools
- [x] **MCP-04**: The computer-use surface bridges rdpilot's 96-DPI physical-pixel coordinates to the tool's expected scaled screenshot/coordinate space — LIVE-VERIFIED (15-06): real `computer` `left_click` actions near the advertised-space top-left/top-right corners land on the intended 7-Zip File Manager elements (menu-bar "File", title-bar "Minimize"), verified via `rdpilot_uia` focus / window-state transition, never via pre-click coordinate computation (Pitfall 3); required a live-diagnosed deeper-UIA-scope fix plus a genuine-fresh-maximize + re-foreground fix (7-Zip FM's own stale remembered window placement otherwise defeats blind corner-coordinate targeting)
- [x] **MCP-05**: MCP file put/get operate on local disk paths and return path/size/checksum metadata (never inline file bytes)
- [x] **MCP-06**: Slow RDP round-trips (file transfer, launch waits) do not block the MCP transport event loop

### Bidirectional File Transfer

- [x] **FILE-01**: User can upload a file local→remote to a named session — LIVE-VERIFIED end-to-end (10-05): `Session::upload_file` round-trips against a real Azure VM
- [x] **FILE-02**: User can download a file remote→local from a named session — LIVE-VERIFIED end-to-end (10-05): `Session::download_file`'s bytes_transferred/checksum independently confirmed against a real Azure VM
- [x] **FILE-03**: Transfer validates remote paths via canonicalization to prevent path traversal / arbitrary write (blocking security requirement) — LIVE re-confirmed (10-05): the mixed-separator and Windows-drive-absolute adversarial cases both reject as `Error::PathTraversal` on the real Windows target, both directions
- [x] **FILE-04**: Large files transfer reliably (chunked); partial-transfer failure is detected and surfaced — LIVE-VERIFIED (10-05): a 3 MiB file transfers correctly (102-137 read IRPs / 48 write IRPs observed, real per-IRP write chunk = 65,536 bytes), and an abruptly-interrupted download leaves no final-named file

### Connection Config

- [x] **CONFIG-01**: Consumers supply target host + credentials via layered config — a clearly-named gitignored file, overridable by env vars and CLI flags / MCP init params
- [x] **CONFIG-02**: The config file follows common CLI-tool convention (platform config dir / clearly-named `.rdpilot.*`), discoverable and self-explanatory
- [x] **CONFIG-03**: Credentials never leak through IPC/MCP wire responses or logs — Serialize paths redact secrets (closing the v1.0 D-14 Debug-only redaction gap)

### Proof

- [x] **PROOF-02**: A scripted harness proves the CLI surface end-to-end against a real remote-only Windows program (no live LLM)
- [x] **PROOF-03**: A scripted harness proves the MCP surface end-to-end (tool calls exercised programmatically, no live LLM)
- [x] **PROOF-04**: A capstone live-LLM demo drives a read/inspect + file-transfer task through the MCP surface against a real remote-only Windows program — LIVE-VERIFIED (15-08): `claude -p --permission-mode bypassPermissions --strict-mcp-config` drove `rdpilot_connect` → `rdpilot_launch` (7-Zip::FM) → `rdpilot_window_list`/`rdpilot_foreground` → `rdpilot_uia` (menu bar: File, Edit, View, Favorites, Tools, Help) → `rdpilot_put`/`rdpilot_get` entirely through the real MCP server against the live Azure VM; PROOF: PASS on both required signals (transcript tool-call evidence AND an independent second-client byte-for-byte side-effect verification, never model self-report). Required one live-diagnosed fix: the rendered `--mcp-config` env block was missing `RDPILOT_SENSOR_BINARY_PATH` (identical root cause to 15-06/15-07's finding) — added and re-verified genuinely green.

## Future Requirements

Deferred to future release. Tracked but not in current roadmap.

- PyO3 / NAPI-RS bindings exposing the SDK to Python and TypeScript consumers
- Clipboard read/write over CLIPRDR (text sync from remote apps)
- MCP progress notifications (notifications/progress) for long transfers
- Durable session reattach across daemon restart (v1.1 uses in-memory registry + orphan cleanup)
- Remote-assist / session shadowing (Backlog Phase 999.4 / SEED-001)

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Running the AI agent on the remote session | Explicitly rejected; intelligence stays local |
| Non-Windows RDP targets (Linux/xrdp, macOS) | Windows-only to lean on UIA/WinRM/WMI |
| Multi-session orchestration / concurrency AT SCALE | The registry holds multiple named sessions, but load/scale orchestration is out of scope |
| Published-package polish (public API stability guarantees, comprehensive docs, multi-registry distribution) | Remains personal tooling first |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| FILE-01 | Phase 10 | Complete (live-verified 10-05) |
| FILE-02 | Phase 10 | Complete (live-verified 10-05) |
| FILE-03 | Phase 10 | Complete (live-verified 10-05, BLOCKING re-confirm passed) |
| FILE-04 | Phase 10 | Complete (live-verified 10-05, BLOCKING re-confirm passed) |
| SESSION-02 | Phase 11 | Complete |
| CONFIG-01 | Phase 11 | Complete |
| CONFIG-02 | Phase 11 | Complete |
| CONFIG-03 | Phase 11 | Complete |
| DAEMON-01 | Phase 12 | In Progress (12-01/02/03: registry-level leak-free session holding proven — SC#3 BLOCKING thread+RSS soak passes across N=50 real connect/disconnect cycles; the long-lived daemon *process* with keepalive, decoupled from any CLI process lifetime, is Wave 4/6, 12-04/12-06) |
| DAEMON-02 | Phase 12 / 15-01 / 15-05 | **Complete** (12-04: Unix path complete — `0700` runtime dir (atomic mode-at-creation) + per-connection `peer_cred()` uid check; SC#4 [BLOCKING] offline proof passes `cargo test -p rdpilot-daemon --test ipc_security` — `authorize_uid` rejects a mismatched uid, accepts a matching one, socket dir confirmed mode 0700; a real cross-account variant is gated behind `RDPILOT_SECOND_UID`+`#[ignore]`. 15-01: Windows path AUTHORED — explicit owner-only DACL (`windows-sys` raw Win32 calls) + `first_pipe_instance(true)` anti-squatting in `ipc/windows.rs`, closing 12-07's Task 2 scope; `tests/live_daemon_windows_dacl.rs` authored the cross-account-rejection/anti-squatting/DACL-present assertions, `#[cfg(windows)]`-gated (0 tests on Linux) + `RDPILOT_LIVE`-gated. **15-05: LIVE-VERIFIED on the Azure Windows VM** — the FIRST real compile of `ipc/windows.rs` surfaced 2 genuine windows-sys 0.61.2 API-path bugs (`OpenProcessToken`/`SECURITY_DESCRIPTOR_REVISION` moved modules; `HANDLE` is now `*mut c_void`, not an integer), both fixed and re-verified; all 3 live DACL tests PASS: owner-only DACL present (same-account control case), anti-squatting fails loudly (`AddrInUse`), and — the critical assertion — a genuinely different Windows account (`rdpilot2`, provisioned this plan, verified via `whoami` inside a one-shot Scheduled Task after `runas` was live-diagnosed as non-functional from the non-interactive SYSTEM execution context) receives a real `ERROR_ACCESS_DENIED` opening the pipe) |
| DAEMON-03 | Phase 12 | Complete (12-06: auto-start on first client connect (`autostart::connect_or_spawn`, bind-as-mutex) + idle reap (routed through the awaited `Registry::close`) + empty-registry grace-period self-shutdown (D-31 anti-thrash) all proven OFFLINE against the REAL compiled `rdpilot-daemon` binary — `tests/autostart_lifecycle.rs`'s `#[ignore]`-gated SC#5 [BLOCKING] test, run via `cargo test -p rdpilot-daemon --test autostart_lifecycle -- --include-ignored`. This requirement's full text (auto-start + idle-reap + self-shutdown-on-empty) has no live/Windows component of its own — DAEMON-03 is fully satisfied by this plan. The broader ROADMAP success criterion 5 [BLOCKING] also combines DAEMON-04's remote-liveness confirmation, which remains pending in 12-07) |
| DAEMON-04 | Phase 12 / 15-01 / 15-06 | **Complete** (12-05: offline crash-restart reconciliation mechanics proven — kill -9 surrogate + restart surfaces the prior session as `Orphaned` in `list`, never silently forgotten, reconciled only via explicit `close`; SC#5 [BLOCKING] offline portion passes `cargo test -p rdpilot-daemon --test crash_restart_reconcile`. 15-01: `tests/live_daemon_e2e.rs` authored the real-target gate. **15-06: LIVE-VERIFIED** against the real Azure VM — `kill -9` mid-session + restart genuinely surfaces the possibly-still-live remote session as `Orphaned` (correct host preserved, never forgotten/auto-killed), and an explicit disconnect reconciles it, then `list` confirms it is gone. Required two live-diagnosed daemon-side fixes to even reach a real session (see DAEMON-02/SESSION-01 entries): none needed for this test itself beyond a Unix-socket-path-length fix (`SUN_LEN`) in the test harness) |
| SESSION-01 | Phase 12 / 15-01 / 15-06 | **Complete** (12-01/02/03: wire verb `Request::Connect` + registry `open()` with caller-name/auto-id (D-29) both implemented and tested. 15-01: `tests/live_daemon_e2e.rs` authored the real-target Connect assertion. **15-06: LIVE-VERIFIED** against the real Azure VM — explicit-name connect succeeds end-to-end with the correct session id returned. **Live-diagnosed, required TWO real production bug fixes to reach this point** (previously masked by the offline fake-connector test suite): (1) the daemon's `Connect` handler never sourced a sensor binary path at all — added `ResolvedConfig::sensor_binary_path` (mirrors `share_root`) and wired it into `ConnectionConfig`; (2) even with a sensor path configured, nothing in the daemon ever called `Session::deploy_and_launch` — added `ManagedSession::deploy_and_launch` and wired it into the `Connect` handler. Both fixes are prerequisites for every sensor-backed verb (CLI-02, CLI-03, MCP-04) live-verified in this same plan |
| SESSION-03 | Phase 12 / 15-06 | **Complete** (12-04: `dispatch`'s `List {}` arm returns the registry's credential-free `SessionList`, with `connected_since`/`last_activity` now wall-clock-populated for live entries. **15-06: LIVE-VERIFIED** against the real Azure VM — `list` reports exactly one `Live` session with correct id/name/host/status/`connected_since` after a real connect, and correctly shows `Orphaned` after a crash-restart) |
| SESSION-04 | Phase 12 / 15-01 / 15-06 | **Complete** (12-01/02/03: wire verb `Request::Disconnect` + registry `close()`/uniqueness enforcement both implemented and tested — SC#1 proves N=16 same-name contention yields exactly one winner. 15-01: `tests/live_daemon_e2e.rs` authored the real-target Disconnect assertion. **15-06: LIVE-VERIFIED** against the real Azure VM — explicit disconnect acknowledges and removes the session from `list`, both for a normal e2e teardown and for reconciling a surfaced orphan) |
| CLI-01 | Phase 13 | Complete (13-05: the `rdpilot-cli` crate/binary implements `connect [--name] / list / disconnect`, transparently auto-starting the daemon via the relocated `rdpilot_ipc::transport::connect_or_spawn` (13-01) — proven offline end-to-end against the real compiled `rdpilot-daemon` binary with the fake connector in `tests/cli_lifecycle.rs` (no daemon pre-started; auto-start observed; `Live` status + name/host round-trip through `list --json`; `SessionNotFound` maps to exit code 2). `cargo tree -p rdpilot-cli` confirmed free of `ironrdp`/`rustls`/`rdpilot`/`rdpilot-daemon` (thin-client invariant, D-17). The shared clap tree/config-flag layer/exit-code taxonomy/table renderer this plan also laid down are reused by 13-06/13-07) |
| CLI-02 | Phase 13 / 15-02 / 15-06 | **Complete** (13-03: daemon-side seam complete. 13-04: `dispatch.rs`'s exhaustive match wires every operational verb through `Registry::call`. 13-06: the actual `rdpilot` CLI binary issuing these requests over IPC, offline against the canned `FakeTestSession`. 15-02: the live-deferred half AUTHORED as `tests/live_cli_verbs.rs`. **15-06: LIVE-VERIFIED** against the real Azure VM — real screenshot pixel content (before/after captures byte-differ, PNG-magic confirmed), real click-coordinate landing (verified via `perceive uia` focus), and real UIA tree shape all pass. Live-diagnosed fixes required: (1)/(2) the two daemon-side sensor-deployment fixes shared with SESSION-01 (`sensor_binary_path` wiring + `deploy_and_launch` call); (3) `--scope children` only reaches UIA depth-1 (the Phase 9 D-9.1 finding recurring) — switched to `--scope subtree --max-depth 3`; (4) the naive "first focusable element" selection always picked the root `Window` element itself — now prefers an interactive `MenuItem`/`Button` leaf; (5) a freshly launched window is not guaranteed OS foreground focus (the Phase 6/9 finding recurring) — added an explicit `input foreground` + settle before the click) |
| CLI-03 | Phase 13 / 15-02 / 15-06 | **Complete** (13-03: daemon-side seam complete. 13-04: `dispatch.rs`'s `Put`/`Get` arms call `upload_file`/`download_file` via `Registry::call`. 13-07: `rdpilot put`/`get` (flat) + `rdpilot file put\|get` (grouped) absolutize `--local` client-side; `get`'s no-clobber is fully CLI-side enforced (exit 8, `--force` override), `put`'s remote no-clobber is a documented known gap (backlog Phase 999.5); `exit_codes.rs` finalized to 8 distinct non-zero exit codes; offline-proven in `tests/cli_errors.rs`. 15-02: the real multi-MB re-exercise AUTHORED in `tests/live_cli_verbs.rs`. **15-06: LIVE-VERIFIED** against the real Azure VM — an 8 MiB `put` then `get` through the CLI surface reports `bytes_transferred` == local file size and a matching SHA-256 checksum both directions, and the round-tripped local file's on-disk size matches; re-exercises the CLI path only, does not re-prove the underlying SDK transfer already live-verified in Phase 10 (FILE-01/02/04) |
| MCP-01 | Phase 14 | Complete |
| MCP-02 | Phase 14 | Complete |
| MCP-03 | Phase 14 | Complete |
| MCP-04 | Phase 14 / 15-03 / 15-06 | **Complete** (14-03: the [BLOCKING] `scale_to_native` bridge + precision edge/corner-tie proof is offline-complete. 15-03: the live half AUTHORED as `tests/live_mcp_computer.rs`. **15-06: LIVE-VERIFIED** against the real Azure VM — real `computer` `left_click` actions near the advertised-space top-left ("File" menu, verified via UIA focus) and top-right ("Minimize" button, verified via window-state transition) corners both land correctly, still computed blind in the fixed advertised 1280x800 space and verified only after the click (Pitfall 3 preserved). Required 3 live-diagnosed fixes: (1) the same `--scope children`-too-shallow issue as CLI-02 — switched `verify_focus_via_uia` to `subtree`/`max_depth: 3`; (2) 7-Zip FM persists its own stale remembered "maximized" placement across launches, independent of the session's real negotiated desktop size, making a `"win+up"` sent to an already-"maximized" window a no-op that never recomputes fresh bounds — an unconditional `"win+down"` before `"win+up"` forces a genuinely fresh, deterministic full-screen maximize every time; (3) `"win+down"` genuinely minimizes (not merely restores) this window, which loses OS foreground focus entirely, so the follow-up `"win+up"` global hotkey was landing on the wrong context — added an explicit re-foreground between the two key presses. Corner coordinates re-tuned against the true full-screen maximize bounds) |
| MCP-05 | Phase 14 | Complete |
| MCP-06 | Phase 14 | Complete |
| PROOF-02 | Phase 15 | In Progress (15-02: the PROOF-02 scripted CLI end-to-end proof harness is now AUTHORED as a gated `tests/live_proof.rs` test in `rdpilot-cli` — `env!("CARGO_BIN_EXE_rdpilot")` subprocess spawn of the REAL compiled `rdpilot` + `rdpilot-daemon` binaries with the Phase 13 fake connector `RDPILOT_DAEMON_TEST_CONNECTOR` deliberately UNSET (D-15.1 full fidelity), targeting the v1.0 7-Zip File Manager window (`class_name` `"7-Zip::FM"`) for continuity with the Phase 9 proof. Drives connect (D-29 explicit name) -> screenshot -> launch -> polled window-list assertion -> put -> get (checksum round trip) -> disconnect, plus a D-28 distinct-non-zero-exit-code assertion (`get` against an unknown session exits 2, never a generic 1) — all with a D-9.4 per-step `PASS`/`FAIL` trace and a final `PROOF: PASS`/`PROOF: FAIL` line. Compiles offline and lists as ignored (`cargo test -p rdpilot-cli --test live_proof -- --list`); no new dependency added (`cargo tree -p rdpilot-cli` confirmed free of `ironrdp`/`rustls`/`rdpilot`/`rdpilot-daemon`, thin-client invariant D-17 intact); live execution against the Azure VM deferred to Plan 15-07) |
| PROOF-03 | Phase 15 | In Progress (15-03: the PROOF-03 scripted MCP end-to-end proof harness is now AUTHORED as a gated `tests/live_proof.rs` test in `rdpilot-mcp` — spawns the REAL compiled `rdpilot-mcp` binary as an rmcp CLIENT subprocess (`TokioChildProcess` + `().serve(...)`, the SAME `rmcp` crate the production server links, so client/server are wire-compatible by construction) and drives it programmatically over actual MCP JSON-RPC with NO live LLM: `tools/list` discovery -> `rdpilot_connect` -> `rdpilot_list` (D-30 status) -> `rdpilot_world_state` perception read -> `rdpilot_put`/`rdpilot_get` (MCP-05 metadata-only checksum/bytes_transferred round trip) -> `rdpilot_disconnect`, each session-scoped call carrying an explicit `session` (D-29), with a D-9.4 per-step `PASS`/`FAIL` trace. Credentials are passed only as MCP tool-call JSON arguments over the child's stdin, never via env/argv. `rmcp`'s `client`+`transport-child-process` features were added as `[dev-dependencies]` only (additive on the same D-26-approved version); `cargo tree -p rdpilot-mcp -e normal` (the shipped, non-dev graph) confirmed byte-for-byte unchanged, thin-client invariant (D-17) intact. Compiles offline and lists as ignored (`cargo test -p rdpilot-mcp --test live_proof -- --list`); live execution against the Azure VM deferred to Plan 15-07) |
| PROOF-04 | Phase 15 | **Complete** (15-04: the PROOF-04 live-LLM capstone AUTHORED as a gated `tests/live_capstone.rs` test in `rdpilot-mcp`, per binding direction 1 — the LLM driving it is the LOCAL, already-authenticated `claude` CLI in headless print mode (`claude -p "<task>" --mcp-config <rendered config>`), NEVER `ANTHROPIC_API_KEY`/`reqwest`/an SDK/a hardcoded model id. **15-08: LIVE-VERIFIED** against the real Azure VM — `claude -p --permission-mode bypassPermissions --strict-mcp-config` (the constant's documented default, no fallback flag needed) drove `rdpilot_connect` → `rdpilot_launch` (7-Zip::FM, pid observed) → `rdpilot_window_list`/`rdpilot_foreground` → `rdpilot_uia` (menu bar enumerated: File, Edit, View, Favorites, Tools, Help) → `rdpilot_put` (60 bytes, checksum `1dcd191b...`) → `rdpilot_get` round trip (matching checksum) entirely through the real compiled `rdpilot-mcp` binary. `PROOF: PASS` on BOTH required signals: (1) the captured `stream-json` transcript shows `mcp__rdpilot__rdpilot_uia` (read/inspect) AND `mcp__rdpilot__rdpilot_put`/`rdpilot_get` (file-transfer) tool-use blocks; (2) a SECOND, independent `rmcp` client subprocess downloaded `rdpilot-capstone-04-upload.bin` fresh and matched it byte-for-byte against the known local seed — never model self-report. **Live-diagnosed fix required:** the rendered `--mcp-config`'s env block was missing `RDPILOT_SENSOR_BINARY_PATH` (identical root cause to 15-06/15-07's finding for CLI-02/03 and PROOF-02/03) — first run FAILED with every sensor-backed call timing out (connect succeeded, `rdpilot_launch`/`window_list`/`put` all failed); added a `sensor_binary_path()` helper (mirrors `live_proof.rs`) threaded into `render_mcp_config`'s env block plus the fixture template's documented substitution tokens, re-ran genuinely green (`PROOF: PASS`, exit 0, 62s). No dependency/Cargo.toml change. Full offline workspace suite (`cargo test --workspace`) green after the live run's leftover daemon process was cleaned up (pre-existing local-daemon-socket test-isolation artifact, not caused by this fix).) |

**Coverage:**

- v1.1 requirements: 27 total
- Mapped to phases: 27 ✓
- Unmapped: 0

---
*Requirements defined: 2026-07-10*
*Last updated: 2026-07-11 after Plan 15-08 (PROOF-04 live-verified — capstone PASS; VM teardown pending developer authorization)*
