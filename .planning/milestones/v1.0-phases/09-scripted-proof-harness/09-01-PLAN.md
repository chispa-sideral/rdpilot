---
phase: 09-scripted-proof-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot/examples/spike_7zip_uia_dump.rs
autonomous: false
requirements: [PROOF-01]
user_setup:
  - service: azure
    why: "The D-9.1 fidelity spike must launch the REAL 7-Zip File Manager on a genuine Windows desktop and dump its live UIA tree — 7zFM.exe and IUIAutomation have no meaning on this Linux dev host"
    dashboard_config:
      - task: "Provision + tear down the disposable Windows test VM"
        location: "infra/manage-env.ps1 up -VmSize Standard_B2s_v2 / infra/manage-env.ps1 down"
      - task: "AOT-publish the sensor on the VM (WinRM auth fails from this host)"
        location: "az vm run-command invoke (Phase 5/6/7 proven mechanism)"
must_haves:
  truths:
    - "The flat UiaElement[] for a live 7zFM.exe main window is dumped to stdout for manual human inspection (role/name/bbox legible, no assertions)"
    - "The first post-launch screenshot is captured so any unexpected 7-Zip first-run UI is caught visually (Open Q#2)"
    - "The real observed 7-Zip window class_name + title are recorded verbatim (Pitfall 1 — no guessed match predicate)"
    - "A decision is recorded: 7-Zip adequate at TreeScope_Children, OR fall back to Notepad per D-9.1 criteria, OR flag a scoped deeper-walk (D-7.4 re-assessment)"
  artifacts:
    - path: .planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md
      provides: "Recorded spike findings: exact SC#2 target elements (name/role/bbox), the D-9.2 navigation target, the D-9.6 seeding decision, and the 7-Zip window match predicate — the explicit inputs to Plan 09-02"
      contains: "SC#2"
  key_links:
    - from: crates/rdpilot/examples/spike_7zip_uia_dump.rs
      to: crates/rdpilot/src/session.rs
      via: "calls get_uia_tree(hwnd) once on the launched 7zFM window and dumps the result"
      pattern: "get_uia_tree"
---

<objective>
Run the D-9.1 fidelity spike as a throwaway, disposable-VM, dump-and-inspect step — mirroring the proven Phase 7 D-7.5 risk-gate (07-03-PLAN.md): launch `7zFM.exe` on a real Azure VM, call `get_uia_tree(hwnd)` once, and dump the flat `UiaElement[]` plus the first screenshot for manual human inspection. NO assertions, NO harness code, NO pass/fail logic.

This is THE risk gate for Phase 9. The exact SC#2 element assertions (which named elements 7-Zip actually exposes at `TreeScope_Children` depth) and the D-9.2 navigation target are OUTPUTS of this spike, not inputs — writing assertion code before seeing a real dump risks guessing wrong against a real Win32 non-instrumented app (the class of surprise Notepad already produced in Phase 7's empty-`AutomationId` finding). Everything downstream (09-02 harness, 09-03 live gate) consumes what this spike records.

Purpose: Resolve the phase's single concentrated unknown — is 7-Zip's UIA tree fidelity-adequate under the locked D-7.4 `TreeScope_Children` scope? — before any real harness code is written, and capture the real window match predicate + navigation target the harness will hard-code.
Output: A captured live dump of 7zFM's flat `UiaElement[]` + first screenshot + observed window class/title, a recorded decision (7-Zip adequate / fall back to Notepad / flag deeper-walk), the throwaway spike file removed, and the VM torn down.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scripted-proof-harness/09-RESEARCH.md
@.planning/phases/09-scripted-proof-harness/09-CONTEXT.md
@.planning/phases/07-uia-tree-module/07-03-PLAN.md
@crates/rdpilot/examples/screenshot.rs
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/src/session.rs
@infra/manage-env.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the throwaway 7-Zip UIA dump spike, provision the VM, and run it live (dump-only, no assertions)</name>
  <files>crates/rdpilot/examples/spike_7zip_uia_dump.rs</files>
  <read_first>
    crates/rdpilot/examples/screenshot.rs (the example-binary shape: main() -> ExitCode, current-thread tokio, local load_config() reading .secrets/connection.json, password never printed),
    crates/rdpilot/tests/live_session.rs (launch_notepad_and_find_window ~lines 1222-1242 — the launch_process + poll get_window_list idiom to adapt for 7-Zip; the uia_tree_returns_populated_elements preamble ~1252-1262 — the sensor_exe_path().exists() precondition + sensor_binary_path + deploy_and_launch order),
    crates/rdpilot/src/session.rs (deploy_and_launch, launch_process(image, working_dir, args), get_window_list, get_uia_tree(hwnd), screenshot),
    .planning/phases/07-uia-tree-module/07-03-PLAN.md (the D-7.5 risk-gate structure this spike mirrors: auto dump task + blocking checkpoint, VM up -> spike -> capture -> VM down),
    .planning/phases/09-scripted-proof-harness/09-RESEARCH.md (Pattern 2, Pitfalls 1/3, Code Examples: launch_7zip_and_find_window template; Open Questions 1/2)
  </read_first>
  <action>
    Create `crates/rdpilot/examples/spike_7zip_uia_dump.rs` as an explicitly THROWAWAY diagnostic example (doc-comment must say so). It composes the public API only: local `load_config()` (copied from examples/screenshot.rs), read the published sensor path from `RDPILOT_SENSOR_EXE` (or the same default `common::sensor_exe_path()` uses), set it via `ConnectionConfig::sensor_binary_path`, `Session::connect`, then `deploy_and_launch`. Then `launch_process("7zFM.exe", ...)` seeding 7-Zip at a known path per D-9.6 (the classic `7zFM.exe <path>` starting-folder form pointed at `C:\Program Files` — confirm the actual argument shape empirically here, this spike is exactly where A1 is resolved). Poll `get_window_list()` in a bounded loop (mirror `launch_notepad_and_find_window`), and the FIRST time any newly-appeared top-level window is found after the 7-Zip launch, `eprintln!` its FULL `WindowInfo` (class_name + title + rect) so the real match predicate values are captured from a live observation, never guessed (Pitfall 1). Do NOT hard-code a class-name guess; match loosely (e.g. any window whose title contains "7-zip" or the seeded path) purely to obtain an hwnd for the dump.

    Capture the first `screenshot()` after the window appears and write it to a PNG (mirror screenshot.rs's write path) so any unexpected 7-Zip first-run dialog is caught visually (Open Q#2). Then call `get_uia_tree(hwnd)` ONCE and `println!` the flat `UiaElement[]` in a legible per-element form (id, role, name, bbox x/y/w/h, enabled, visible, focusable, focused, depth) for manual inspection. This is dump-only: NO `assert!`, NO pass/fail exit logic beyond mapping a hard connect/launch error to `ExitCode::FAILURE`. The example uses `.expect`/`String`-mapped errors freely (it is an `examples/` crate root, outside the library's `#![deny(expect_used)]` scope per lib.rs:20-31).

    Provision the disposable VM (`infra/manage-env.ps1 up -VmSize Standard_B2s_v2` — default Standard_B2ms is SkuNotAvailable in westeurope, STATE.md pending todo). AOT-publish `rdpilot-sensor.exe` win-x64 ON the VM via `az vm run-command invoke` (WinRM Negotiate/NTLM auth fails from this Linux dev host — the Phase 5/6/7 pattern: build on the VM, relay the artifact via a short-lived Azure Storage blob SAS if needed) and point the spike's sensor path at it. Run the spike against the live VM (`RDPILOT_LIVE=1 cargo run -p rdpilot --example spike_7zip_uia_dump`, or drive the built binary through the established live-run mechanism) and capture its full stdout dump + the observed window class/title + the screenshot. Do NOT tear the VM down until the Task 2 checkpoint has captured the dump.
  </action>
  <acceptance_criteria>
    - crates/rdpilot/examples/spike_7zip_uia_dump.rs exists, compiles (`cargo build -p rdpilot --example spike_7zip_uia_dump`), and contains no `assert!`/`assert_eq!` (it is dump-only).
    - The example's doc comment states it is a THROWAWAY D-9.1 spike, to be removed once findings are recorded.
    - The live run's stdout contains a legible per-element dump of the 7zFM window's flat UiaElement[] (id/role/name/bbox/flags/depth).
    - The run captures and records the real observed 7-Zip window class_name + title, and writes a first-launch screenshot PNG.
    - No password is printed anywhere in the spike's output (ConnectionConfig Debug redaction respected).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p rdpilot --example spike_7zip_uia_dump 2>&1 | tail -n 5</automated>
    <human-check>The live `az vm run-command` / `cargo run --example spike_7zip_uia_dump` output shows the flat 7zFM UiaElement[] dump and the recorded window class/title.</human-check>
  </verify>
  <done>The throwaway spike compiles and, run live against a real disposable Azure VM, dumps 7zFM's flat UiaElement[] + first screenshot + real window class/title for manual inspection — with zero assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Inspect the 7-Zip UIA dump, record the SC#2/SC#3 findings, remove the throwaway spike, and tear the VM down</name>
  <action>Pause for the operator to review the live 7zFM UIA dump + first-launch screenshot, then RECORD in the SUMMARY the decisions that Plan 09-02 depends on, remove the throwaway spike file, and tear the VM down before proceeding.</action>
  <what-built>A throwaway, disposable-VM diagnostic that launches the real 7-Zip File Manager over RDP and dumps its live flat UiaElement[] (via the full Rust SDK -> DVC -> C# UIA handler -> FindAll(TreeScope_Children) path) plus a first-launch screenshot — resolving the phase's single concentrated unknown (is 7-Zip's UIA tree fidelity-adequate for SC#2 assertions?) before any real harness code is written (D-9.1, mirroring the Phase 7 D-7.5 gate).</what-built>
  <how-to-verify>
    1. Confirm the dump shows 7zFM's UIA tree with legible, distinguishable elements. Record VERBATIM the exact SC#2 target elements (name + role + bbox) to assert on downstream — e.g. the menu bar's "File" item, a named toolbar button, the listview — with valid (nonzero-area) bounding boxes.
    2. Choose and record the D-9.2 navigation target from what the dump surfaces (safe default: mouse-click the "File" menu bar item's bbox center; prefer the mouse-click-on-UIA-bbox path over Alt+letter mnemonics per Pitfall 4). Record its expected verify signal (submenu appears via screenshot-diff region, or a follow-up get_uia_tree re-query shows the submenu items).
    3. Record the D-9.6 seeding decision: if the chosen target is always-present menu/toolbar chrome, seeding may be unnecessary; otherwise record the confirmed `7zFM.exe <path>` argument form (A1). Note any first-run dialog seen in the screenshot.
    4. Record the D-7.4 re-assessment: is `TreeScope_Children` depth SUFFICIENT for the chosen SC#2 elements? If the meaningful elements sit below children depth, FLAG a scoped, caller-configurable deeper-walk as an explicit capability addition (do NOT silently absorb it) — this becomes a flagged input to Plan 09-02's scope.
    5. Record the real 7-Zip window class_name + title (the Plan 09-02 match predicate), OR invoke the D-9.1 fallback to Notepad ONLY if 7-Zip's tree is genuinely inadequate (toolbar buttons all report generic `role: "Button"` with empty/indistinct `name`, or the "File" menu item is absent/indistinct at children depth).
    6. Remove the throwaway spike: delete `crates/rdpilot/examples/spike_7zip_uia_dump.rs` (Pitfall 2 — it must NOT evolve into `run_proof_harness`; the real harness is Plan 09-02, built fresh from these recorded findings).
    7. Tear the VM down: `infra/manage-env.ps1 down`, then confirm `az group exists -n rdpilot-test` returns false.
  </how-to-verify>
  <resume-signal>Type "approved" once the SC#2 elements + D-9.2 target + seeding/depth decisions + window predicate are recorded in the SUMMARY, the throwaway spike is deleted, and the VM is torn down — or describe why 7-Zip is inadequate and the fallback chosen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev host -> disposable Azure VM | Ephemeral credentials + short-lived blob SAS relay for the AOT sensor artifact |
| Live RDP+DVC session -> sensor | The full untrusted transport exercised end-to-end to fetch the live UIA tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Credential leak via the spike's stdout dump | high | mitigate | The spike composes the public API only and never prints the password (ConnectionConfig Debug redacts it, config.rs); acceptance criteria assert no password in output — same discipline as examples/screenshot.rs. |
| T-09-02 | Information Disclosure / cost | A left-up disposable VM after the spike | medium | mitigate | Blocking human-verify Step 7 requires `manage-env.ps1 down` + `az group exists -n rdpilot-test` false before approval (every prior phase's teardown discipline). |
| T-09-03 | Tampering (of scope) | The throwaway spike silently becoming real harness code | medium | mitigate | Checkpoint Step 6 mandates deleting `spike_7zip_uia_dump.rs`; the real harness (09-02) is built fresh — enforces the CONTEXT "pure composition, spike is throwaway" boundary (Pitfall 2). |
| T-09-SC | Tampering | Dependency supply chain | low | accept | Zero new packages — the spike reuses the crate's existing dependency graph unchanged (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- The live 7zFM UIA dump is captured and the SC#2 elements + D-9.2 target + D-9.6 seeding + D-7.4 depth decisions + window predicate are recorded in 09-01-SUMMARY.md.
- The throwaway `spike_7zip_uia_dump.rs` is deleted (does not survive into Wave 2).
- VM torn down and confirmed absent (`az group exists -n rdpilot-test` == false).
</verification>

<success_criteria>
- The phase's single concentrated unknown (7-Zip UIA fidelity under TreeScope_Children) is empirically resolved to a recorded decision before any assertion code exists.
- Plan 09-02 has a concrete, live-observed input set: exact assert-on elements, the navigation target, the seeding decision, and the window match predicate.
</success_criteria>

<output>
Create `.planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md` when done (record the SC#2 elements, D-9.2 navigation target + verify signal, D-9.6 seeding decision, D-7.4 depth adequacy/flag, and the real 7-Zip window class/title).
</output>
