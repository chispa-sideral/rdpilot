---
phase: 04-dvc-transport-channel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot/Cargo.toml
  - crates/rdpilot/src/sensor.rs
  - crates/rdpilot/src/error.rs
  - crates/rdpilot/src/lib.rs
autonomous: true
requirements: [SENSOR-03]
user_setup: []

must_haves:
  truths:
    - "The durable envelope { version, req_id, type, payload } round-trips through serde_json for the Version, Ping, and Pong message types ONLY — no WindowList/ProcessTree/Uia types exist yet (D-4.3, D-4.4)"
    - "RdpilotSensorProcessor::channel_name() returns \"RDPILOT_SENSOR\" and start() returns exactly one Version envelope as the first outbound message, unconditionally (SC#1 name reservation, SC#3 first-message)"
    - "Receiving a Version reply whose version != PROTOCOL_VERSION sets HandshakeState::Mismatched{local,remote}; a matching version sets HandshakeState::Ok (SC#3 negative path, unit-tested offline with no VM)"
    - "Malformed / oversized / unknown-type inbound JSON is dropped or mapped to an internal error, never unwrap/expect/panic (API-01, ASVS V5 input-validation boundary)"
    - "Error::Dvc(String) exists mirroring Error::CoordinateOutOfBounds exactly: variant + #[error(...)] + pub(crate) dvc() constructor + a category() arm returning \"dvc\""
  artifacts:
    - path: "crates/rdpilot/src/sensor.rs"
      provides: "Envelope + MsgType (Version/Ping/Pong), JsonDvcMessage (DvcEncode/Encode wrapper), SensorShared (pending map + handshake), HandshakeState, RdpilotSensorProcessor (channel_name/start/process/encode_ping), PROTOCOL_VERSION"
      contains: "pub(crate) struct RdpilotSensorProcessor"
    - path: "crates/rdpilot/src/error.rs"
      provides: "Error::Dvc(String) variant + constructor + category arm"
      contains: "Dvc(String)"
    - path: "crates/rdpilot/src/lib.rs"
      provides: "mod sensor; (crate-internal — no ironrdp-dvc type is re-exported publicly)"
      contains: "mod sensor"
    - path: "crates/rdpilot/Cargo.toml"
      provides: "serde (derive) + serde_json promoted to runtime [dependencies]"
      contains: "serde = { version"
  key_links:
    - from: "crates/rdpilot/src/sensor.rs"
      to: "ironrdp-dvc DvcProcessor + DvcEncode"
      via: "impl DvcProcessor for RdpilotSensorProcessor + impl_as_any! + JsonDvcMessage: DvcEncode"
      pattern: "impl DvcProcessor"
    - from: "crates/rdpilot/src/sensor.rs"
      to: "serde_json"
      via: "to_vec on outbound envelope, from_slice on inbound payload (Result-mapped, never unwrap)"
      pattern: "serde_json::(to_vec|from_slice)"
---

<objective>
Build the offline foundation of the DVC transport: the durable JSON envelope, the
`RdpilotSensorProcessor` (IronRDP `DvcProcessor`) with its version-handshake `start()` and
reply-parsing `process()`, the `SensorShared` correlation state, and the `Error::Dvc` variant —
plus the `Cargo.toml` serde promotion. This is the interface every later plan consumes:
Plan 02 registers the processor and calls `encode_ping`; Plan 03 exercises it live. No `connect.rs`,
no `session.rs`, no VM is touched here — all logic is pure and offline-unit-tested.

Purpose: Interface-first ordering. The fiddly, source-verified pieces — the `{version, req_id,
type, payload}` envelope (D-4.3), the `DvcEncode`/`Encode` JSON wrapper, the handshake state
machine, and the unconditional first-message Version send (SC#3) — live here as self-contained,
VM-free code so Plans 02/03 become thin wiring. The version-mismatch → `Error::Dvc` path (SC#3
negative) is only ever unit-testable (the phase's own responder always matches — RESEARCH
Assumption A3), so it MUST be fully covered here.

Output: `crates/rdpilot/src/sensor.rs` (new), `Error::Dvc` in `error.rs`, `mod sensor;` in `lib.rs`,
serde/serde_json under `[dependencies]` in `Cargo.toml`. Compiles and passes offline
`cargo test -p rdpilot` with no VM.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-dvc-transport-channel/04-CONTEXT.md
@.planning/phases/04-dvc-transport-channel/04-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Phase 4 turns the empty DVC seam at `connect.rs:87-96` into a working bidirectional
`RDPILOT_SENSOR` channel. No new crate; no new public SDK type beyond `Session::ping`. Full symbol
set across the phase:
- `sensor.rs` (this plan): `PROTOCOL_VERSION: u32`; `Envelope { version: u32, req_id: u64, msg_type: MsgType, payload: Option<serde_json::Value> }` (serde-tagged `type` field); `enum MsgType { Version, Ping, Pong }`; `struct JsonDvcMessage(Vec<u8>)` implementing `Encode` + `DvcEncode`; `pub(crate) enum HandshakeState { Pending, Ok, Mismatched { local: u32, remote: u32 } }`; `pub(crate) struct SensorShared { pending: Mutex<HashMap<u64, oneshot::Sender<()>>>, handshake: Mutex<HandshakeState> }`; `pub(crate) struct RdpilotSensorProcessor` with `new(Arc<SensorShared>)`, `channel_name()`, `start()`, `process()`, `close()`, and `encode_ping(req_id) -> Vec<DvcMessage>`.
- `error.rs` (this plan): `Error::Dvc(String)`.
- `connect.rs` (Plan 02): `DrdynvcClient::new().with_dynamic_channel(RdpilotSensorProcessor::new(shared))`; `connect()` returns the extra `Arc<SensorShared>`.
- `session.rs` (Plan 02): `Session::ping() -> Result<Duration>`; `sensor: Arc<SensorShared>` + `next_req_id: AtomicU64` fields.
- `session_loop.rs` (Plan 02): `RdpInputEvent::Ping(u64)` variant + `select!` arm.
- Live suite + throwaway responder (Plan 03): `tests/live_session.rs::sensor_ping_pong_under_500ms`, `tests/fixtures/sensor-responder.ps1`, `tests/fixtures/deploy-responder.ps1`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Promote serde to runtime deps + Error::Dvc + the JSON envelope vocabulary (D-4.3, D-4.4)</name>
  <files>crates/rdpilot/Cargo.toml, crates/rdpilot/src/error.rs, crates/rdpilot/src/sensor.rs, crates/rdpilot/src/lib.rs</files>
  <read_first>
    - crates/rdpilot/Cargo.toml (the [dependencies] block L10-43 and the [dev-dependencies] serde_json line L47 — move it up and add serde)
    - crates/rdpilot/src/error.rs (the full CoordinateOutOfBounds precedent: variant L64-85, coordinate_out_of_bounds constructor L112-125, category() arm L142 — mirror this shape for Dvc exactly)
    - crates/rdpilot/src/lib.rs (mod list L20-28 and pub use list L30-34 — add `mod sensor;` only; do NOT pub-use any sensor type)
    - .planning/phases/04-dvc-transport-channel/04-RESEARCH.md (Q2 exact JSON shapes ~L156-172; Standard Stack "Cargo.toml change required" ~L298; Code Examples "DvcEncode-wrapping JSON message" ~L434-461; Security Domain V5 row ~L555)
    - .planning/phases/04-dvc-transport-channel/04-CONTEXT.md (D-4.3 envelope + Version/Ping/Pong only; D-4.4 JSON via serde; Claude's Discretion on Error::Dvc wording)
  </read_first>
  <action>
  In `Cargo.toml`: remove `serde_json = "1"` from `[dev-dependencies]` and add BOTH
  `serde = { version = "1", features = ["derive"] }` and `serde_json = "1"` to `[dependencies]`
  (serde_json alone does not provide the derive macros — D-4.4). Do not change any ironrdp pins.
  In `error.rs`: add `Error::Dvc(String)` as a tuple variant with an `#[error("DVC transport error: {0}")]`
  message (wording is Claude's discretion per CONTEXT, but MUST mirror the existing single-String
  variants' style, e.g. Session/Connect), a `pub(crate) fn dvc(msg: impl Into<String>) -> Self`
  constructor placed beside `coordinate_out_of_bounds`, and a new `Error::Dvc(_) => "dvc"` arm in
  `category()`. The enum is `#[non_exhaustive]` — additive only; do not reorder existing variants.
  Create `crates/rdpilot/src/sensor.rs` and add `mod sensor;` to `lib.rs` (crate-internal — NOTHING from
  sensor is re-exported; the owned-types-only public rule D-09 means no `ironrdp`/`ironrdp-dvc` type may
  ever appear in a `pub` signature). In `sensor.rs` define: `pub(crate) const PROTOCOL_VERSION: u32 = 1;`
  the `Envelope` struct deriving `Serialize, Deserialize, Debug, Clone, PartialEq` with fields
  `version: u32`, `req_id: u64`, `#[serde(rename = "type")] msg_type: MsgType`, and
  `payload: Option<serde_json::Value>` (payload is `None` for all three v1 types but MUST stay in the
  wire shape for Phase 6-7 extensibility — D-4.3); and `enum MsgType { Version, Ping, Pong }` deriving
  `Serialize, Deserialize, Debug, Clone, Copy, PartialEq, Eq` with the default (externally-tagged unit)
  representation so it serializes as the bare strings `"Version"`/`"Ping"`/`"Pong"` — verify the on-wire
  string form against RESEARCH Q2's JSON samples. Add `#[cfg(test)]` unit tests asserting: serializing
  `Envelope { version: 1, req_id: 0, msg_type: MsgType::Version, payload: None }` produces JSON whose
  `type` key is exactly `"Version"` and whose `req_id` is `0`; a Ping envelope round-trips
  (to_vec → from_slice) byte-for-value; and `serde_json::from_slice` on a truncated/garbage byte slice
  returns `Err` (which the processor will later map to `Error::Dvc`, never panic — API-01, V5). No
  `unwrap`/`expect`/`panic` outside `#[cfg(test)]`.
  </action>
  <verify>
    <automated>cargo test -p rdpilot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p rdpilot` exits 0 and `cargo test -p rdpilot` exits 0
    - `crates/rdpilot/Cargo.toml` contains `serde = { version = "1", features = ["derive"] }` and `serde_json = "1"` under `[dependencies]`, and no `serde_json` line remains under `[dev-dependencies]`
    - `crates/rdpilot/src/error.rs` contains `Dvc(String)`, a `fn dvc(` constructor, and an `Error::Dvc(_) => "dvc"` category arm
    - `crates/rdpilot/src/lib.rs` contains `mod sensor;` and does NOT contain `pub use sensor`
    - `crates/rdpilot/src/sensor.rs` contains `const PROTOCOL_VERSION`, `struct Envelope`, `enum MsgType`, and variants `Version`, `Ping`, `Pong`
    - a unit test asserts an `Envelope` serializes with `"type":"Version"` and round-trips; a unit test asserts garbage bytes yield `Err` from `serde_json::from_slice`
    - `grep -nE 'unwrap\(\)|expect\(|panic!' crates/rdpilot/src/sensor.rs` returns no matches outside `#[cfg(test)]`
  </acceptance_criteria>
  <done>serde promoted; Error::Dvc mirrors CoordinateOutOfBounds; the Version/Ping/Pong envelope round-trips JSON offline; malformed JSON is a typed Err, never a panic.</done>
</task>

<task type="auto">
  <name>Task 2: RdpilotSensorProcessor + SensorShared + handshake state machine (SC#1 name/first-message, SC#3 mismatch)</name>
  <files>crates/rdpilot/src/sensor.rs</files>
  <read_first>
    - crates/rdpilot/src/sensor.rs (the Envelope/MsgType/PROTOCOL_VERSION from Task 1 this builds on)
    - crates/rdpilot/src/connect.rs (RDPILOT_SENSOR const L45 — the channel name the processor must return; the DVC seam comment L86-96 for the registration contract Plan 02 will satisfy)
    - .planning/phases/04-dvc-transport-channel/04-RESEARCH.md (Q1 SensorShared struct + pending-map/oneshot design ~L123-148; Q2 handshake process() logic ~L174-186; Code Example start() ~L463-473; DvcEncode wrapper ~L434-461; Pitfall 4 impl_as_any! requirement ~L425-430; Don't Hand-Roll "chunking" row — inbound payloads arrive fully reassembled)
    - .planning/phases/04-dvc-transport-channel/04-CONTEXT.md (D-4.3 reuse ironrdp-dvc chunking, do not hand-roll framing; SC#3 version handshake first + mismatch → clear error)
  </read_first>
  <action>
  Add to `sensor.rs` the correlation state and the processor. Prefer the crate's existing umbrella
  import style for ironrdp types (`connect.rs` uses `ironrdp::dvc::DrdynvcClient`; `session_loop.rs` uses
  `ironrdp::core::WriteBuf`): use `ironrdp::dvc::{DvcProcessor, DvcEncode, DvcMessage}` and
  `ironrdp::core::{Encode, EncodeResult, WriteCursor, ensure_size, impl_as_any}` — fall back to the direct
  `ironrdp_dvc::`/`ironrdp_core::` paths only if a symbol is not re-exported under the umbrella.
  Define `struct JsonDvcMessage(Vec<u8>)` with a `new<T: Serialize>(&T) -> Result<Self, serde_json::Error>`
  constructor (wrapping `serde_json::to_vec`), and implement `Encode` (`encode` writes the bytes via
  `ensure_size!` + `dst.write_slice`; `name()` returns a static str; `size()` returns the byte length) and
  the marker `DvcEncode` — copy the shape from RESEARCH Code Examples verbatim in intent (do NOT inline the
  code block here; read it there).
  Define `pub(crate) enum HandshakeState { Pending, Ok, Mismatched { local: u32, remote: u32 } }` and
  `pub(crate) struct SensorShared { pending: std::sync::Mutex<std::collections::HashMap<u64, tokio::sync::oneshot::Sender<()>>>, handshake: std::sync::Mutex<HandshakeState> }` with a `new()` that starts
  `Pending` and an empty map. `oneshot` is runtime-agnostic (crosses the session-loop OS-thread boundary —
  RESEARCH Q1); `std::sync::Mutex` is correct here because it is only ever locked for the brief synchronous
  map/state mutation, never held across an `.await`.
  Define `pub(crate) struct RdpilotSensorProcessor { shared: std::sync::Arc<SensorShared> }` with
  `pub(crate) fn new(shared: Arc<SensorShared>) -> Self`. Add `ironrdp` `impl_as_any!(RdpilotSensorProcessor);`
  IMMEDIATELY after the struct (Pitfall 4 — without AsAny, Plan 02's `get_dvc::<RdpilotSensorProcessor>()`
  TypeId lookup silently returns None). Implement `DvcProcessor`:
    - `channel_name(&self) -> &str` returns `crate::connect::RDPILOT_SENSOR` (do not hard-code a second copy
      of the literal — reference the existing const so SC#1's reserved name and the processor agree).
    - `start(&mut self, _channel_id) -> PduResult<Vec<DvcMessage>>` builds ONE `Envelope { version:
      PROTOCOL_VERSION, req_id: 0, msg_type: MsgType::Version, payload: None }`, wraps it in a
      `JsonDvcMessage`, and returns it boxed as the single element — this is the unconditional first
      outbound message (SC#3). Map any encode error via `ironrdp::core::other_err!` (never unwrap).
    - `process(&mut self, _channel_id, payload: &[u8]) -> PduResult<Vec<DvcMessage>>`: `serde_json::from_slice`
      the payload into `Envelope`; on decode error, drop it (return `Ok(Vec::new())`) — malformed JSON from
      the unauthenticated channel MUST NOT panic (API-01, V5, T-04-02). On `MsgType::Version`: lock
      `handshake` and set `Ok` if `env.version == PROTOCOL_VERSION` else `Mismatched { local: PROTOCOL_VERSION,
      remote: env.version }`; return `Ok(Vec::new())` (the handshake is a terminal 2-message exchange — no
      reply). On `MsgType::Pong`: lock `pending`, `remove(&env.req_id)`, and if a sender was present
      `.send(())` (ignore the `Result` — the receiver may have already timed out); return `Ok(Vec::new())`.
      On `MsgType::Ping`: the SDK is the client and never receives Pings — ignore (return empty).
    - `close(&mut self, _channel_id)`: default (no-op) is fine.
  Add `pub(crate) fn encode_ping(&self, req_id: u64) -> Result<Vec<DvcMessage>, serde_json::Error>` that
  builds a `Ping` envelope with the given `req_id` and returns it as a single boxed `JsonDvcMessage`
  (Plan 02's session-loop arm calls this, then hands the result to `ironrdp_dvc::encode_dvc_messages` for
  DataFirst/Data chunking — do NOT chunk here, D-4.3 / Don't Hand-Roll).
  Add `#[cfg(test)]` unit tests (all offline, no VM, construct the processor with a fresh
  `Arc<SensorShared>`):
    - `start()` returns exactly one message and, when its bytes are decoded back, they are a Version
      envelope with `req_id == 0` and `version == PROTOCOL_VERSION` (SC#3 first-message guard).
    - feeding `process()` a hand-built Version reply with `version == PROTOCOL_VERSION` leaves
      `HandshakeState::Ok`; feeding one with a different version leaves `HandshakeState::Mismatched { local:
      1, remote: <other> }` (SC#3 negative path — the only place this is testable, RESEARCH A3).
    - inserting a `oneshot` sender under `req_id = 7`, then feeding a `Pong` envelope with `req_id == 7`,
      fulfils the receiver (`rx.try_recv()`/await resolves to `Ok(())`) and removes the entry from `pending`.
    - feeding `process()` a truncated/garbage byte slice returns `Ok(empty)` and does NOT panic.
  No `unwrap`/`expect`/`panic` outside `#[cfg(test)]`.
  </action>
  <verify>
    <automated>cargo test -p rdpilot 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p rdpilot` exits 0 and `cargo test -p rdpilot` exits 0
    - `crates/rdpilot/src/sensor.rs` contains `impl DvcProcessor for RdpilotSensorProcessor`, `impl_as_any!(RdpilotSensorProcessor)`, `struct SensorShared`, `enum HandshakeState`, and `fn encode_ping`
    - `channel_name()` returns `crate::connect::RDPILOT_SENSOR` (references the existing const, not a duplicated literal)
    - a unit test proves `start()` emits a single Version envelope with req_id 0 (SC#3 first-message)
    - a unit test proves a mismatched inbound version sets `HandshakeState::Mismatched { local: 1, remote }` and a matching one sets `Ok` (SC#3 negative)
    - a unit test proves a Pong with a known req_id fulfils and removes the pending oneshot
    - a unit test proves malformed inbound bytes yield `Ok(empty)` with no panic (V5)
    - `grep -nE 'unwrap\(\)|expect\(|panic!' crates/rdpilot/src/sensor.rs` returns no matches outside `#[cfg(test)]`; `grep -nE 'DataFirst|DataPdu|MAX_DATA_SIZE' crates/rdpilot/src/sensor.rs` returns no matches (chunking is delegated, D-4.3)
  </acceptance_criteria>
  <done>RdpilotSensorProcessor implements DvcProcessor with AsAny; start() sends Version first; process() drives the handshake state machine and fulfils Pong oneshots; mismatch → Mismatched; malformed JSON never panics — all offline-tested, no VM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DVC channel → RdpilotSensorProcessor::process() | Inbound bytes from the (by-design unauthenticated, Pitfall m3) `RDPILOT_SENSOR` channel are untrusted; `serde_json::from_slice` is the input-validation point (ASVS V5) |
| Envelope wire shape → SDK internal state | A wrong/unknown `type` or version must resolve to a safe internal outcome, never memory unsafety or a panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | Malformed / oversized / unknown-type JSON on the DVC channel causing a panic in `process()` | high | mitigate | `serde_json::from_slice` returns `Result`; decode errors return `Ok(empty)` (drop the message); no `unwrap`/`expect`/`panic` outside tests — grep-guarded and unit-tested with garbage bytes |
| T-04-02 | Tampering | Silent version-skew corruption (SC#3) if a mismatch were ignored | high | mitigate | `process()` records `HandshakeState::Mismatched{local,remote}` on any version != PROTOCOL_VERSION; unit-tested. Plan 02 fails `ping()` fast with `Error::Dvc` on that state |
| T-04-SC | Tampering | cargo dependency supply chain | low | accept | No new external crates: serde/serde_json already resolved in Cargo.lock (dev-dep → runtime-dep promotion only). RESEARCH Package Legitimacy Audit: both Approved; no [SLOP]/[SUS]. No blocking-human package checkpoint required |
</threat_model>

<verification>
- `cargo build -p rdpilot` and `cargo test -p rdpilot` exit 0 offline (no VM)
- Envelope round-trips JSON for Version/Ping/Pong; `type` serializes as the bare string form (SC#3 wire shape)
- `start()` emits the Version handshake as the single first message (SC#3)
- Version mismatch → `HandshakeState::Mismatched`; match → `Ok` (SC#3 negative, unit-only per A3)
- Malformed JSON → typed drop, never panic (API-01, ASVS V5, T-04-01)
- `Error::Dvc(String)` present, mirroring `CoordinateOutOfBounds`; no ironrdp-dvc type re-exported (D-09)
</verification>

<success_criteria>
- The durable `{version, req_id, type, payload}` envelope and Version/Ping/Pong types exist and round-trip (D-4.3, D-4.4)
- `RdpilotSensorProcessor` is a complete `DvcProcessor` (with AsAny) whose `start()` sends Version first and whose `process()` drives the handshake + Pong correlation (SC#1 groundwork, SC#3)
- `SensorShared` (pending oneshot map + handshake state) is ready for Plan 02 to thread through connect/session
- `Error::Dvc` ready; serde promoted to runtime deps; all offline, no VM
</success_criteria>

<output>
Create `.planning/phases/04-dvc-transport-channel/04-01-SUMMARY.md` when done
</output>
