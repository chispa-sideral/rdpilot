---
phase: 01-test-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - infra/README.md
  - infra/tests/ManageEnv.Tests.ps1
  - infra/tests/Validate-Target.ps1
autonomous: true
# Scaffolding/Wave-0 plan. It does NOT deliver any ENV-* requirement — it lays the
# secrets-safe baseline, file layout, and Pester/assertion harness that every later
# plan consumes. ENV-01 is delivered by Plan 02 (infra) + Plan 03 (in-guest hardening);
# ENV-02/ENV-03 by Plan 04. requirements is intentionally empty so the verifier credits
# ENV-* delivery to 02/03/04, not here. (W1 revision 2026-06-04)
requirements: []
must_haves:
  truths:
    - "Repository has a .gitignore that excludes .secrets/ and .env before any provisioning runs"
    - "Pester is available and a test scaffold exists that runs green with no Azure cost"
    - "A post-deploy assertion script skeleton exists covering all ENV-01 criteria"
  artifacts:
    - path: ".gitignore"
      provides: "Secrets exclusion (.secrets/, .env)"
      contains: ".secrets/"
    - path: "infra/tests/ManageEnv.Tests.ps1"
      provides: "Pester unit tests for IP detection, password generation, param wiring (az mocked)"
    - path: "infra/tests/Validate-Target.ps1"
      provides: "Post-deploy WinRM assertion skeleton for ENV-01 criteria"
    - path: "infra/README.md"
      provides: "up/down usage documentation"
  key_links:
    - from: ".gitignore"
      to: ".secrets/connection.json"
      via: "ignore rule"
      pattern: "\\.secrets/"
---

<objective>
Establish the rdpilot infrastructure baseline: the project's first `.gitignore` (secrets-safe), the `infra/` directory layout, and the validation scaffolding (Pester) that every later Phase 1 plan and every future IaC artifact depends on. This is Wave 0 work mandated by RESEARCH.md — `.gitignore` and the test harness MUST exist before any Bicep is authored or any `up` is ever run.

This is a SCAFFOLDING plan. It delivers NO ENV-* requirement directly (hence `requirements: []`): it only creates the secrets-safe baseline, the file layout, and the test/assertion harness. ENV-01 is delivered by Plan 02 (cloud infra) + Plan 03 (in-guest hardening); ENV-02 and ENV-03 are delivered by Plan 04. The skeletons created here (the Pester tests and `Validate-Target.ps1`) are activated/run-for-real by those later plans.

Purpose: Pitfall 7 (RESEARCH.md) — the repo has NO `.gitignore`, so `.secrets/connection.json` (D-06) could be committed accidentally. This plan closes that gap first and lays down the conventions (file layout, secrets handling, Pester-as-test-framework, no-Azure-cost per-commit gate) that bind every later artifact.

Output: `.gitignore`, `infra/README.md`, `infra/tests/ManageEnv.Tests.ps1` (runnable, green), `infra/tests/Validate-Target.ps1` (skeleton with all ENV-01 assertion stubs).
</objective>

<execution_context>
@C:/Users/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@C:/Users/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-environment/01-CONTEXT.md
@.planning/phases/01-test-environment/01-RESEARCH.md
@.planning/phases/01-test-environment/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols/artifacts (exclude from drift verification — they do not exist yet):
- File `.gitignore` (repo root) — contains `.secrets/` and `.env`
- Directory `infra/` and subdirectories `infra/scripts/`, `infra/tests/`
- File `infra/README.md`
- File `infra/tests/ManageEnv.Tests.ps1` — Pester `Describe` blocks for password generation, dev-IP detection, and `az` param wiring (all with mocked `az`)
- File `infra/tests/Validate-Target.ps1` — post-deploy assertion skeleton (param `$ConnectionFile`/`$Host`), `Describe`/`It` stubs for: RDP 3389 reachable, WinRM 5986 reachable, NLA `UserAuthentication=1`, default-hive `LogPixels=96`/`Win8DpiScaling=1`, HKLM + default-hive `RemoteDesktop_SuppressWhenMinimized=2`, `7zFM.exe` present
- Convention: `infra/` is the IaC root (chosen over `tools/env/`); `.secrets/` is the only credential location
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore and infra/ layout</name>
  <files>.gitignore, infra/README.md</files>
  <read_first>
    - .planning/phases/01-test-environment/01-PATTERNS.md (`.gitignore` section; "Recommended Project Structure"; "Secrets handling" shared pattern)
    - .planning/phases/01-test-environment/01-RESEARCH.md (Pitfall 7 L294-296; project structure L163-178)
    - C:/Users/marc/.claude/rules/secrets-handling.md (never commit secrets; .secrets/ excluded)
  </read_first>
  <action>
    Create `.gitignore` at repo root containing at minimum `.secrets/` and `.env` (one per line). This is the first task of the phase per Pitfall 7 — it MUST be committed before any `up` runs so `.secrets/connection.json` (D-06) is never staged. Create the `infra/` root directory with subdirectories `infra/scripts/` and `infra/tests/` (use `.gitkeep` files where a directory would otherwise be empty so git tracks them). Write `infra/README.md` documenting: the `infra/` layout (main.bicep, scripts/, tests/, manage-env.ps1 — referencing files later plans create), the `manage-env.ps1 -Action up|down` entry point (ENV-02), the pre-flight requirement of an authenticated `az login` against the chosen subscription, the West Europe default region (Claude's discretion per D-05/A6 — note user may override), and the secrets convention (credentials live ONLY in gitignored `.secrets/connection.json`, never echoed, never committed). Do NOT author any Bicep or the actual scripts here — only the directory skeleton and docs.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "if ((Get-Content .gitignore -Raw) -notmatch '\.secrets/') { throw '.gitignore missing .secrets/' }; if (-not (Test-Path infra/tests) -or -not (Test-Path infra/scripts)) { throw 'infra layout missing' }; 'ok'"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` exists at repo root and contains a line `.secrets/` and a line `.env`
    - `git check-ignore .secrets/connection.json` exits 0 (the path is ignored)
    - Directories `infra/`, `infra/scripts/`, `infra/tests/` exist and are tracked by git
    - `infra/README.md` documents the up/down entry point, the `az login` pre-flight requirement, the West Europe default, and the `.secrets/` secrets convention
  </acceptance_criteria>
  <done>`.gitignore` excludes `.secrets/` and `.env`; `infra/` layout and README exist; `git check-ignore .secrets/connection.json` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pester scaffold for manage-env.ps1 logic (no Azure cost)</name>
  <files>infra/tests/ManageEnv.Tests.ps1</files>
  <behavior>
    - Test: a generated password is >= 24 characters and uses a cryptographic source (asserts length and character-class coverage, not the hand-rolled shuffler)
    - Test: dev-IP detection parses the `{ "ip": "x.x.x.x" }` shape from the ipify response (mock `Invoke-RestMethod`)
    - Test: `up` invokes `az deployment group create` with `--parameters adminPassword=...` and `allowedSourceIp=...` (mock `az`, assert it was called with those param names)
    - Test: the `-AllowedSourceIp` override param bypasses the ipify lookup when supplied
  </behavior>
  <read_first>
    - .planning/phases/01-test-environment/01-PATTERNS.md (`infra/tests/ManageEnv.Tests.ps1` section; Pattern 2; "Don't Hand-Roll" password rule)
    - .planning/phases/01-test-environment/01-RESEARCH.md (Validation Architecture L423-456; Pattern 2 L215-234; Don't Hand-Roll L251)
  </read_first>
  <action>
    Ensure Pester is available: check `Get-Module -ListAvailable Pester`; if absent, run `Install-Module Pester -Scope CurrentUser -Force -SkipPublisherCheck` pinned to a specific 5.x version (record the version in the test file header comment). Write `infra/tests/ManageEnv.Tests.ps1` as Pester 5 `Describe`/`It` blocks covering: (1) password generation — assert >= 24 chars and presence of multiple character classes, mocking the crypto RNG source so the assertion is deterministic; (2) dev-IP detection — `Mock Invoke-RestMethod` returning `@{ ip = '203.0.113.7' }` and assert the parsed IP; (3) `az deployment group create` invocation — `Mock az` and assert it is called with parameter tokens `adminPassword=` and `allowedSourceIp=`; (4) `-AllowedSourceIp` override skips the ipify call. Because `infra/manage-env.ps1` does not exist yet (Plan 04 creates it), structure the tests to dot-source it behind a guard: if the script is absent, the IP/password/`az`-wiring `It` blocks are marked `-Skip` with reason "manage-env.ps1 created in Plan 04" so the file runs green now and becomes active once Plan 04 lands. The password-entropy `It` block tests the standalone generation expression directly (inline in the test) so at least one assertion is live today. NEVER place the real password into test output.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Invoke-Pester -Path infra/tests/ManageEnv.Tests.ps1 -CI"</automated>
  </verify>
  <acceptance_criteria>
    - `Invoke-Pester -Path infra/tests/ManageEnv.Tests.ps1 -CI` exits 0 (green)
    - The file contains at least one live (non-skipped) assertion verifying generated password length >= 24
    - Tests that depend on `infra/manage-env.ps1` are `-Skip`ped with reason text referencing Plan 04, not failing
    - `Mock` is used for `az` and `Invoke-RestMethod` (no real Azure call, no network call in the test run)
    - Pester version is pinned and recorded in a header comment
  </acceptance_criteria>
  <done>Pester runs `ManageEnv.Tests.ps1` green with at least one live password-entropy assertion and Plan-04-dependent tests skipped; no Azure or network calls occur.</done>
</task>

<task type="auto">
  <name>Task 3: Post-deploy assertion skeleton (Validate-Target.ps1)</name>
  <files>infra/tests/Validate-Target.ps1</files>
  <read_first>
    - .planning/phases/01-test-environment/01-PATTERNS.md (`infra/tests/Validate-Target.ps1` section; "Least-exposure"/WinRM client convention)
    - .planning/phases/01-test-environment/01-RESEARCH.md (Phase Requirements → Test Map L436-445; Pitfall 2 NLA L266-269; Pitfall 3 WinRM client L272-275)
  </read_first>
  <action>
    Write `infra/tests/Validate-Target.ps1` as the phase-gate post-deploy assertion script. It takes a `-ConnectionFile` param defaulting to `.secrets/connection.json` and reads `host`, `user`, `password`, `rdpPort`, `winrmPort` from it. Structure as one assertion per ENV-01 criterion: (1) `Test-NetConnection $host -Port 3389` succeeds (RDP reachable); (2) `Test-NetConnection $host -Port 5986` succeeds (WinRM reachable); (3) over a WinRM session built with `New-PSSessionOption -SkipCACheck -SkipCNCheck` + `-UseSSL` (self-signed cert, Pitfall 3): assert `(Get-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp').UserAuthentication -eq 1` (NLA enforced — VERIFICATION not configuration, Pitfall 2); (4) read the default user hive (load `C:\Users\Default\NTUSER.DAT`) and assert `LogPixels -eq 96` and `Win8DpiScaling -eq 1`; (5) assert `RemoteDesktop_SuppressWhenMinimized -eq 2` at both `HKLM\Software\Microsoft\Terminal Server Client` and the default-hive user path; (6) assert `Test-Path 'C:\Program Files\7-Zip\7zFM.exe'`. Since no live VM exists during this plan, mark each assertion block so the script is runnable as a SKELETON: emit a clear "PENDING — requires live target from manage-env.ps1 up" line per check and exit 0 when no `-ConnectionFile` is present, but contain the real command text (as the active code path when the file IS present) so Plan 04's phase-gate run executes them for real. Do NOT echo the password.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "& { . infra/tests/Validate-Target.ps1 } ; if ($LASTEXITCODE -ne 0) { throw 'skeleton must exit 0 without a connection file' } ; if ((Get-Content infra/tests/Validate-Target.ps1 -Raw) -notmatch 'UserAuthentication') { throw 'NLA check missing' }; 'ok'"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/tests/Validate-Target.ps1` runs and exits 0 when no live `.secrets/connection.json` is present (skeleton mode), printing a PENDING line per ENV-01 check
    - The file's source contains the real assertion commands: `Test-NetConnection` for ports 3389 and 5986, `UserAuthentication -eq 1`, `LogPixels`/`Win8DpiScaling`, `RemoteDesktop_SuppressWhenMinimized`, and `7zFM.exe`
    - WinRM sessions are constructed with `-SkipCACheck -SkipCNCheck -UseSSL`
    - The password is never written to console/log output
  </acceptance_criteria>
  <done>`Validate-Target.ps1` skeleton runs green standalone, contains every ENV-01 assertion command, and uses the self-signed WinRM client options.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Secrets in `.secrets/` must never cross into committed history |
| test process → console/log | Generated/real passwords must never be emitted by tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.secrets/connection.json` committed | mitigate | `.gitignore` excludes `.secrets/` and `.env` created in Task 1 before any `up`; `git check-ignore` asserted (ASVS V7) |
| T-01-02 | Information Disclosure | Test output leaking password | mitigate | Tests assert on length/class only; real/generated values never written to output (ASVS V7) |
| T-01-03 | Tampering | Untrusted Pester from PSGallery | accept | Pester is first-party PowerShell test framework, pinned to a specific 5.x version; PSGallery is the canonical source. Low risk for a dev-only test dependency. |
</threat_model>

<verification>
- `git check-ignore .secrets/connection.json` exits 0
- `Invoke-Pester -Path infra/tests/ManageEnv.Tests.ps1 -CI` exits 0
- `Validate-Target.ps1` runs standalone and exits 0 in skeleton mode
- No real Azure or network calls occur in any test run (all mocked)
</verification>

<success_criteria>
- `.gitignore` excludes `.secrets/` and `.env` and is committed before any provisioning
- `infra/` layout (scripts/, tests/) and README exist
- Pester is available (pinned) and `ManageEnv.Tests.ps1` runs green with a live password-entropy assertion
- `Validate-Target.ps1` contains every ENV-01 assertion command and runs green in skeleton mode
</success_criteria>

<output>
Create `.planning/phases/01-test-environment/01-01-SUMMARY.md` when done.
</output>
