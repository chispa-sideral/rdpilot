---
phase: 07-uia-tree-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sensor/UiaInterop.cs
  - sensor/Program.cs
autonomous: true
requirements: [PERC-03]
must_haves:
  truths:
    - "The four hand-authored [GeneratedComInterface] UIA interfaces compile with GUID + complete in-order slot lists (no _VTblGap shortcuts)"
    - "GetRootAutomation() obtains IUIAutomation via a hand-rolled ole32.dll CoCreateInstance P/Invoke + StrategyBasedComWrappers (no CsWin32, no new PackageReference)"
    - "A --smoke-test-uia arg exists that exercises every risky marshalling path (CoCreateInstance/BSTR/BOOL/RECT/SAFEARRAY/FindAll) on the desktop root element"
    - "The sensor project still AOT-trims clean (source-generator + trimming warning-free as far as an offline publish can prove)"
  artifacts:
    - path: sensor/UiaInterop.cs
      provides: "Hand-authored [GeneratedComInterface] IUIAutomation/IUIAutomationElement/IUIAutomationElementArray/IUIAutomationCondition + CoCreateInstance P/Invoke + GetRootAutomation() + ReadRuntimeId SAFEARRAY decode + control-type friendly-string table is NOT here (mapping is Rust-side per 07-01)"
      contains: "GeneratedComInterface"
      min_lines: 120
    - path: sensor/Program.cs
      provides: "--smoke-test-uia arg + RunUiaAotSmokeTest() sibling to RunAotSmokeTest"
      contains: "smoke-test-uia"
  key_links:
    - from: sensor/Program.cs
      to: sensor/UiaInterop.cs
      via: "RunUiaAotSmokeTest calls UiaInterop.GetRootAutomation / ReadRuntimeId"
      pattern: "UiaInterop\\."
---

<objective>
Hand-author the highest-risk surface of this phase — the `[GeneratedComInterface]` UIA COM interop declarations (D-7.5) — and the `--smoke-test-uia` AOT gate that exercises every risky marshalling path, WITHOUT yet writing any real UIA handler. This mirrors the Phase 6 "AOT smoke test before real handler code" risk gate.

This is a Wave-1 offline plan: it produces the interop code and the smoke-test entry point, and proves as much as a Linux host can (compile + AOT trim/source-gen-warning check). The actual on-Windows smoke RUN — where the LOW-confidence SAFEARRAY/BSTR/BOOL marshalling assumptions (RESEARCH A1/A2/A3) are empirically resolved — is the dedicated Wave-2 risk gate (07-03). Runs in parallel with the Rust wire plan (07-01, zero file overlap).

Purpose: Concentrate all novel COM-under-NativeAOT risk into one throwaway-gated surface so that when it passes on Windows (07-03), the real handler (07-04) is mechanical. No official `[GeneratedComInterface]` UIA bindings exist (RESEARCH confirmed) — the vtable-shaped interfaces must be hand-ported from the GUID-verified slot tables in RESEARCH.
Output: `sensor/UiaInterop.cs` (four interfaces + CoCreateInstance + GetRootAutomation + ReadRuntimeId), `sensor/Program.cs` `--smoke-test-uia` arg + `RunUiaAotSmokeTest()`, and an offline AOT-trim publish check.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-uia-tree-module/07-RESEARCH.md
@.planning/phases/07-uia-tree-module/07-CONTEXT.md
@sensor/Program.cs
@sensor/WindowEnumeration.cs
@sensor/RdpilotSensor.csproj
</context>

<artifacts_produced>
Symbols this plan introduces (consumed by the 07-03 gate and 07-04 handler):
- `RdpilotSensor.UiaInterop` (static class) with:
  - `[GeneratedComInterface][Guid("30cbe57d-d9d0-452a-ab13-7ac5ac4825ee")] partial interface IUIAutomation` — 19 slots; only slot 4 `ElementFromHandle(nint hwnd) -> IUIAutomationElement` and slot 19 `CreateTrueCondition() -> IUIAutomationCondition` are real, slots 1-3/5-18 are never-invoked placeholders (see RESEARCH slot table).
  - `[Guid("d22108aa-8ac5-49a5-837b-37bbb3d7591e")] partial interface IUIAutomationElement` — 41 slots; real slots: 2 `GetRuntimeId` (SAFEARRAY, `[PreserveSig]`), 4 `FindAll(TreeScope, IUIAutomationCondition) -> IUIAutomationElementArray`, 19 `CurrentControlType`, 21 `CurrentName`, 24 `CurrentHasKeyboardFocus`, 25 `CurrentIsKeyboardFocusable`, 26 `CurrentIsEnabled`, 36 `CurrentIsOffscreen`, 41 `CurrentBoundingRectangle`; all other slots placeholders (see RESEARCH slot table).
  - `[Guid("14314595-b4bc-4055-95f2-58f2e42c9855")] partial interface IUIAutomationElementArray` — 2 real slots: 1 `Length`, 2 `GetElement(int)`.
  - `[Guid("352ffba8-0973-437c-a61f-f64cafd81df9")] partial interface IUIAutomationCondition` — empty marker (0 members).
  - `CoCreateInstance` `[LibraryImport("ole32.dll")]`, `GetRootAutomation() -> IUIAutomation` (CLSID_CUIAutomation `ff48dba4-60ef-4201-aa87-54103eef594e`, CLSCTX_INPROC_SERVER, StrategyBasedComWrappers).
  - `ReadRuntimeId(IUIAutomationElement) -> int[]` — the manual SAFEARRAY decode wrapping GetRuntimeId's `[PreserveSig]` + `out nint`, using `Marshal.SafeArrayGetLBound/GetUBound/GetElement` and a `finally` `Marshal.SafeArrayDestroy` (never leak — T-07-03).
  - `Rect32` struct — REUSE the exact `[StructLayout(LayoutKind.Sequential)]` `{int Left,Top,Right,Bottom}` shape already in WindowEnumeration.cs (RESEARCH Open Question #2 recommendation).
  - `TreeScope` `[Flags] enum { Children = 2 }` (RESEARCH Pitfall 5 — value MUST be 2).
- `RdpilotSensor.Program.RunUiaAotSmokeTest()` + the `--smoke-test-uia` arg constant, sibling to the existing `--smoke-test`/`RunAotSmokeTest`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author UiaInterop.cs — the four [GeneratedComInterface] UIA interfaces + CoCreateInstance + SAFEARRAY decode</name>
  <files>sensor/UiaInterop.cs</files>
  <read_first>
    .planning/phases/07-uia-tree-module/07-RESEARCH.md (Hand-Authoring Reference: GUID table + the exact ordered slot tables for all four interfaces; Pattern 2 [GeneratedComInterface] mechanics; Pattern 3 CoCreateInstance/GetRootAutomation; Don't Hand-Roll: SAFEARRAY via Marshal.SafeArrayGet*; Common Pitfalls 1-5),
    sensor/WindowEnumeration.cs (existing [LibraryImport] discipline, the Rect32 struct shape to reuse, StringMarshalling.Utf16 precedent, [UnmanagedCallersOnly]-free P/Invoke style),
    sensor/RdpilotSensor.csproj (AllowUnsafeBlocks already set; zero PackageReference must stay zero)
  </read_first>
  <action>
    Create `sensor/UiaInterop.cs` in namespace `RdpilotSensor`. Declare the four `[GeneratedComInterface]` + `[Guid(...)]` `internal partial interface`s using the EXACT GUIDs and COMPLETE in-order slot lists from the RESEARCH Hand-Authoring Reference. Critical rules from RESEARCH: (1) vtable slot = C# declaration order, so declare EVERY slot from 1 through the last real member — use real never-invoked placeholder methods (e.g. `void _reserved3();`) for unused slots, NEVER `_VTblGapN_M()` (Pitfall 1, dotnet/runtime#102421); (2) flat interfaces only, no C# inheritance / no `new`-shadowing (Pitfall 4); (3) `IUIAutomationCondition` is an empty marker interface (opaque pass-through). For `GetRuntimeId` (IUIAutomationElement slot 2) use `[PreserveSig]` returning `int` HRESULT plus `out nint` to the raw SAFEARRAY (Pitfall 2 — no confirmed SAFEARRAY marshaller). For the `BSTR` property `CurrentName` set `StringMarshalling = StringMarshalling.Utf16` at the interface attribute level (Pitfall 3, first attempt). For `BOOL` properties (`CurrentIsEnabled`/`CurrentIsKeyboardFocusable`/`CurrentHasKeyboardFocus`/`CurrentIsOffscreen`) try `[MarshalAs(UnmanagedType.Bool)]` on the return, and leave a code comment naming the `[PreserveSig]` + raw-int + `!= 0` fallback (RESEARCH A3) so 07-03 can swap if the gate shows a size mismatch. `CurrentBoundingRectangle` returns the reused `Rect32` struct by out-pointer. Add the `TreeScope` `[Flags] enum { None=0, Element=1, Children=2, Descendants=4 }`.

    Add the `CoCreateInstance` `[LibraryImport("ole32.dll")] private static partial int CoCreateInstance(in Guid rclsid, nint pUnkOuter, uint dwClsContext, in Guid riid, out nint ppv)`, the CLSID/IID `static readonly Guid` constants, and `internal static IUIAutomation GetRootAutomation()` exactly per RESEARCH Pattern 3 (CLSCTX_INPROC_SERVER, throw `COMException` on nonzero hr/null ppv, wrap with `StrategyBasedComWrappers.GetOrCreateObjectForComInstance`). Add `internal static int[] ReadRuntimeId(IUIAutomationElement el)` doing the manual SAFEARRAY decode: call GetRuntimeId, check hr, then `Marshal.SafeArrayGetLBound/GetUBound`, loop `Marshal.SafeArrayGetElement`, ALWAYS `Marshal.SafeArrayDestroy` in a `finally` (T-07-03 no-leak). No new `<PackageReference>` — `System.Runtime.InteropServices.Marshalling` is built into the .NET 8 SDK. Do NOT put the ControlType→role table here — that mapping is Rust-side (07-01); the sensor ships the raw `control_type` int.
  </action>
  <acceptance_criteria>
    - sensor/UiaInterop.cs contains four `[GeneratedComInterface]` interfaces with the five GUIDs from the RESEARCH GUID table.
    - `grep -c "GeneratedComInterface" sensor/UiaInterop.cs` returns 4.
    - `grep -c "_VTblGap" sensor/UiaInterop.cs` returns 0 (Pitfall 1 avoided).
    - `grep -n "PreserveSig" sensor/UiaInterop.cs` shows GetRuntimeId declared PreserveSig.
    - `grep -n "SafeArrayDestroy" sensor/UiaInterop.cs` shows the finally-block free (T-07-03).
    - `grep -c "PackageReference" sensor/RdpilotSensor.csproj` returns 0 (unchanged).
    - `dotnet build sensor/RdpilotSensor.csproj -c Release` compiles clean (no SYSLIB/CS errors).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build sensor/RdpilotSensor.csproj -c Release 2>&1 | tail -20</automated>
  </verify>
  <done>UiaInterop.cs compiles with the four GUID-verified GeneratedComInterface declarations, CoCreateInstance/GetRootAutomation, and the leak-safe SAFEARRAY runtime-id decode; csproj still has zero package references.</done>
</task>

<task type="auto">
  <name>Task 2: Add --smoke-test-uia + RunUiaAotSmokeTest() and prove the AOT publish trims clean offline</name>
  <files>sensor/Program.cs</files>
  <read_first>
    sensor/Program.cs (Main arg dispatch lines 43-59; RunAotSmokeTest lines 86-159 — the sibling to mirror; existing [LibraryImport] user32 helpers style),
    sensor/UiaInterop.cs (Task 1 — GetRootAutomation/ReadRuntimeId/interface members),
    .planning/phases/07-uia-tree-module/07-RESEARCH.md (Code Examples: AOT smoke-test gate shape)
  </read_first>
  <action>
    In Program.cs add a `private const string UiaSmokeTestArg = "--smoke-test-uia";` and, in `Main`, an `if (args.Length > 0 && args[0] == UiaSmokeTestArg) return RunUiaAotSmokeTest();` branch sibling to the existing `--smoke-test` branch. Implement `private static int RunUiaAotSmokeTest()` per the RESEARCH Code Example: `GetDesktopWindow()` (add a `[LibraryImport("user32.dll")] static partial nint GetDesktopWindow();` if not present), `UiaInterop.GetRootAutomation()`, `automation.ElementFromHandle(desktop)`, then EXERCISE EVERY risky path in isolation and in this order so a failure pinpoints the culprit: `UiaInterop.ReadRuntimeId(root)` FIRST (the single riskiest line, Pitfall 2/A1), then `root.CurrentName` (BSTR/A2), `root.CurrentIsEnabled` (BOOL/A3), `root.CurrentBoundingRectangle` (RECT-by-out-pointer, Open Q#2), `automation.CreateTrueCondition()`, `root.FindAll(TreeScope.Children, trueCond)`, `children.Length`. On success print a single line beginning `[smoke-test-uia] PASS:` including the runtimeId length, name, enabled, rect, and child count (so the 07-03 gate can grep for PASS). Wrap the whole body in try/catch printing `[smoke-test-uia] FAIL: {ex}` to stderr and `return 1;` on any exception. This is a throwaway diagnostic — it does NOT go through the Envelope/DVC path and needs no MsgType.Uia (that arrives in 07-04).

    Then prove the sensor still AOT-trims: run `dotnet publish sensor/RdpilotSensor.csproj -c Release -r linux-x64 -p:PublishAot=true --self-contained` (the linux-x64 surrogate — win-x64 cannot cross-compile from this Linux host, RESEARCH Pitfall 7 / Environment). The publish MUST complete with ZERO ILC/trimming/source-generator warnings for the new `[GeneratedComInterface]` code. (Runtime behavior on Linux is meaningless — `ole32.dll`/UIA do not exist there — so DO NOT run the produced binary; the surrogate publish only proves AOT-trim/source-gen cleanliness. The real behavioral smoke RUN is 07-03 on Windows.) If the linux-x64 AOT publish surfaces a `[GeneratedComInterface]`-specific trim/source-gen ERROR (not a runtime-only concern), fix it here before finishing.
  </action>
  <acceptance_criteria>
    - Program.cs contains `--smoke-test-uia` and `RunUiaAotSmokeTest`.
    - The smoke body references `GetRootAutomation`, `ReadRuntimeId`, `CurrentName`, `CurrentIsEnabled`, `CurrentBoundingRectangle`, `CreateTrueCondition`, `FindAll`, and prints a line containing `[smoke-test-uia] PASS`.
    - `dotnet publish sensor/RdpilotSensor.csproj -c Release -r linux-x64 -p:PublishAot=true --self-contained` exits 0 with no ILC/trim/source-gen warnings attributable to UiaInterop.cs.
  </acceptance_criteria>
  <verify>
    <automated>dotnet publish sensor/RdpilotSensor.csproj -c Release -r linux-x64 -p:PublishAot=true --self-contained 2>&1 | grep -iE "warning|error|IL[0-9]|SYSLIB" | grep -iv "0 Warning" | tail -30; echo "exit=$?"</automated>
  </verify>
  <done>--smoke-test-uia entry point exists and exercises every risky marshalling path on the desktop root; the sensor AOT-trims clean on the linux-x64 surrogate (compile/source-gen proven offline; Windows behavioral run deferred to 07-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sensor process → Windows UIA COM subsystem | Hand-authored vtable interop; a wrong slot/GUID causes memory corruption, not a clean error |
| Manual SAFEARRAY decode | Hand-rolled allocation the source generator does not manage — leak/UAF risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering (memory) | Hand-authored vtable slot layout | high | mitigate | Complete in-order slot lists from the GUID-verified RESEARCH tables; no `_VTblGap` shortcuts (Pitfall 1); GUID mismatch self-corrects as `E_NOINTERFACE` at the 07-03 gate (RESEARCH A4) BEFORE any handler depends on it. |
| T-07-03 | Denial of Service | ReadRuntimeId SAFEARRAY decode | medium | mitigate | Always pair GetRuntimeId with `Marshal.SafeArrayDestroy` in a `finally` — no leaked native allocation even on the exception path. |
| T-07-SC | Tampering | Dependency supply chain | low | accept | No new `<PackageReference>`; `[GeneratedComInterface]`/`StrategyBasedComWrappers` are built into the .NET 8 SDK (RESEARCH Package Legitimacy Audit: N/A, zero installs). |
</threat_model>

<verification>
- `dotnet build sensor/RdpilotSensor.csproj -c Release` compiles clean.
- `dotnet publish -r linux-x64 -p:PublishAot=true --self-contained` produces zero ILC/trim/source-gen warnings for the interop code (offline surrogate for AOT-trim cleanliness only — behavioral validation is 07-03 on Windows, RESEARCH Pitfall 7).
- csproj retains zero `<PackageReference>` (D-7.5).
</verification>

<success_criteria>
- The four UIA interfaces are hand-authored with GUID-verified, complete, in-order slot lists and compile + AOT-trim clean offline.
- `--smoke-test-uia` exists and exercises CoCreateInstance/BSTR/BOOL/RECT/SAFEARRAY/FindAll on the desktop root, ready for the 07-03 Windows gate.
- Zero new package dependencies (D-7.5 preserved).
</success_criteria>

<output>
Create `.planning/phases/07-uia-tree-module/07-02-SUMMARY.md` when done.
</output>
