---
phase: 07-uia-tree-module
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - sensor/UiaInterop.cs
  - sensor/Program.cs
autonomous: false
requirements: [PERC-03]
user_setup:
  - service: azure
    why: "The COM-under-NativeAOT smoke gate must AOT-publish and RUN the sensor on a genuine Windows host — CoCreateInstance/UIA have zero meaning on this Linux dev host (RESEARCH Pitfall 7)"
    dashboard_config:
      - task: "Provision + tear down the disposable Windows test VM"
        location: "infra/manage-env.ps1 up -VmSize Standard_B2s_v2 / infra/manage-env.ps1 down"
      - task: "AOT-publish + run the sensor on the VM (WinRM auth fails from this host)"
        location: "az vm run-command invoke (Phase 5/6 proven mechanism)"
must_haves:
  truths:
    - "rdpilot-sensor.exe --smoke-test-uia prints [smoke-test-uia] PASS on a real Windows desktop root element"
    - "The SAFEARRAY(GetRuntimeId) decode returns a non-empty int array under [GeneratedComInterface] + NativeAOT (resolves RESEARCH A1)"
    - "BSTR (CurrentName), BOOL (CurrentIsEnabled), and RECT (CurrentBoundingRectangle) marshal correctly under the source generator (resolves A2/A3, Open Q#2)"
    - "FindAll(TreeScope_Children, trueCondition) returns a child array on the desktop root"
  artifacts:
    - path: sensor/UiaInterop.cs
      provides: "The interop declarations, now empirically validated (fix-forward applied if the gate revealed a marshalling mismatch)"
      contains: "GeneratedComInterface"
  key_links:
    - from: sensor/Program.cs
      to: sensor/UiaInterop.cs
      via: "the win-x64 AOT-published --smoke-test-uia binary exercises the interop on the VM desktop root"
      pattern: "smoke-test-uia"
---

<objective>
Run the D-7.5 risk gate: win-x64 AOT-publish the sensor ON a real Windows VM and execute `rdpilot-sensor.exe --smoke-test-uia`, empirically resolving the phase's three LOW-confidence marshalling assumptions (RESEARCH A1 SAFEARRAY, A2 BSTR, A3 BOOL) plus the vtable-layout correctness (A4), on the always-available desktop root element — BEFORE the real UIA handler (07-04) is written.

This is THE risk gate. Everything downstream (07-04 handler, 07-05 live gate) depends on it passing. It is intentionally spike-first (D-7.5): the novel "IUIAutomation + [GeneratedComInterface] + NativeAOT" combination has no known prior-art worked example, so it is validated live before any code depends on it. Because AOT cannot cross-compile from Linux (RESEARCH Pitfall 7), the publish + run happen on the VM via `az vm run-command invoke` (the Phase 5/6 proven mechanism, since WinRM auth fails from this host). This plan is where marshalling fixes (e.g. swapping `[MarshalAs(UnmanagedType.Bool)]` for the `[PreserveSig]` int fallback) are applied and re-run until PASS.

Purpose: Convert the phase's single concentrated unknown into a proven fact (or a scoped fix) before mechanical handler work begins — exactly the Phase 6 AOT-smoke-test-before-real-code discipline.
Output: A captured `[smoke-test-uia] PASS` from a real Windows desktop root, any fix-forward edits to UiaInterop.cs the gate required, and the VM torn down.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-uia-tree-module/07-RESEARCH.md
@.planning/phases/07-uia-tree-module/07-CONTEXT.md
@.planning/phases/06-window-process-perception/06-05-SUMMARY.md
@sensor/UiaInterop.cs
@sensor/Program.cs
@infra/manage-env.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: AOT-publish the sensor on the VM and run --smoke-test-uia, iterating marshalling fixes until PASS</name>
  <files>sensor/UiaInterop.cs, sensor/Program.cs</files>
  <read_first>
    .planning/phases/06-window-process-perception/06-05-SUMMARY.md (the exact az vm run-command invoke publish + blob-SAS-relay mechanism proven in Phase 6),
    infra/manage-env.ps1 (up/down; -VmSize Standard_B2s_v2 — default Standard_B2ms is SkuNotAvailable in westeurope, STATE.md pending todo),
    sensor/UiaInterop.cs + sensor/Program.cs (the interop + smoke entry from 07-02),
    .planning/phases/07-uia-tree-module/07-RESEARCH.md (Common Pitfalls 1-3, Assumptions A1/A2/A3, Open Questions 1-2 — the exact fallbacks to apply if a path fails)
  </read_first>
  <action>
    Provision the disposable VM (`infra/manage-env.ps1 up -VmSize Standard_B2s_v2`). AOT-publish the sensor ON the VM: `dotnet publish sensor/RdpilotSensor.csproj -c Release -r win-x64 -p:PublishAot=true --self-contained`, driven via `az vm run-command invoke` (WinRM Negotiate/NTLM auth fails from this Linux host — use the Phase 6 pattern: run-command to build on the VM, short-lived Azure Storage blob SAS to relay artifacts if needed). Then run `rdpilot-sensor.exe --smoke-test-uia` on the VM (in the interactive session — UIA requires the session hosting the desktop) and capture stdout/stderr.

    If it prints `[smoke-test-uia] PASS`, the gate is met — proceed to teardown. If it prints `[smoke-test-uia] FAIL`, diagnose by the isolation order the smoke test uses (ReadRuntimeId first, then BSTR, BOOL, RECT, FindAll) and apply the RESEARCH-documented fallback for the failing path ONLY: SAFEARRAY (A1) — confirm the `[PreserveSig]` + `Marshal.SafeArrayGet*` decode; BSTR name garbage (A2) — swap `StringMarshalling.Utf16` for explicit `[MarshalAs(UnmanagedType.BStr)]` per-property or a raw `nint` + `Marshal.PtrToStringBSTR`; BOOL always-true/false (A3) — swap `[MarshalAs(UnmanagedType.Bool)]` for `[PreserveSig]` + raw `int` + `!= 0`; a crash right after a placeholder slot (Pitfall 1) — re-check the slot count/order against the RESEARCH table; `E_NOINTERFACE` (A4) — re-verify the GUID. Re-publish + re-run until PASS. Keep every edit confined to UiaInterop.cs / Program.cs (no handler code, no wire/DTO changes). Record the exact PASS line and which assumptions (A1/A2/A3) resolved which way in the SUMMARY.

    Do NOT tear down the VM until the human-verify checkpoint (Task 2) has captured the PASS.
  </action>
  <acceptance_criteria>
    - The VM-run `rdpilot-sensor.exe --smoke-test-uia` stdout contains `[smoke-test-uia] PASS`.
    - The captured PASS line reports a non-empty runtimeId length and a nonzero (or explicitly zero-but-valid) child count for the desktop root.
    - Any marshalling fix-forward is confined to sensor/UiaInterop.cs and/or sensor/Program.cs (no other files touched).
    - The win-x64 AOT publish completed on the VM with zero ILC/trim/source-gen warnings.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — behavioral gate is a Windows-only VM run captured in Task 2's checkpoint; no Linux-runnable automated command exists (RESEARCH Pitfall 7). Offline proxy already ran in 07-02 (linux-x64 AOT-trim publish).</automated>
    <human-check>The az vm run-command output for `--smoke-test-uia` shows `[smoke-test-uia] PASS`.</human-check>
  </verify>
  <done>The win-x64 AOT-published sensor prints [smoke-test-uia] PASS on a real Windows desktop root, resolving A1/A2/A3/A4 empirically; any required marshalling fallback is applied to the interop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture the --smoke-test-uia PASS on a real Windows VM and tear the VM down</name>
  <action>Pause for the operator to review the `az vm run-command invoke` output for the win-x64 AOT-published `rdpilot-sensor.exe --smoke-test-uia`, confirm the `[smoke-test-uia] PASS` line, record which marshalling assumptions (A1 SAFEARRAY / A2 BSTR / A3 BOOL) resolved which way and any fix-forward applied, then tear the VM down and confirm its resource group is absent.</action>
  <what-built>The hand-authored [GeneratedComInterface] UIA interop, AOT-published win-x64 on a real Windows VM and exercised end-to-end by --smoke-test-uia against the live desktop root element — validating the CoCreateInstance / SAFEARRAY(GetRuntimeId) / BSTR / BOOL / RECT / FindAll(TreeScope_Children) marshalling paths that have no known prior-art worked example (D-7.5 spike gate).</what-built>
  <how-to-verify>
    1. Confirm the `az vm run-command invoke` output for `rdpilot-sensor.exe --smoke-test-uia` contains a line beginning `[smoke-test-uia] PASS:` with a non-empty `runtimeId.len` and a `children=` count.
    2. Confirm the win-x64 AOT publish reported zero ILC/trimming/source-generator warnings.
    3. Confirm any marshalling fix-forward is limited to UiaInterop.cs / Program.cs (no handler or wire changes leaked into the gate).
    4. Tear the VM down: `infra/manage-env.ps1 down`, then confirm `az group exists -n rdpilot-test` returns false.
  </how-to-verify>
  <resume-signal>Type "approved" once the PASS is captured and the VM is torn down, or describe the failing marshalling path to iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sensor → Windows UIA COM (live) | The hand-authored vtable now executes against the real COM subsystem — the point where a wrong slot/GUID would surface |
| Dev host → disposable Azure VM | Ephemeral credentials + short-lived blob SAS relay for the AOT artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering (memory) | Live vtable dispatch | high | mitigate | The gate itself IS the mitigation — a bad slot/GUID crashes or returns garbage HERE (on a throwaway binary, desktop root) rather than in the real handler; GUID mismatch surfaces as `E_NOINTERFACE` (RESEARCH A4). |
| T-07-03 | Denial of Service | SAFEARRAY decode | medium | mitigate | `finally` `SafeArrayDestroy` confirmed exercised on the live path during the gate. |
| T-07-04 | Information Disclosure | Cost/credential leak from a left-up VM | medium | mitigate | Blocking human-verify Step 4 requires `manage-env.ps1 down` + `az group exists` false before approval (every prior phase's teardown discipline). |
| T-07-SC | Tampering | Dependency supply chain | low | accept | No new `<PackageReference>`; built-in .NET 8 SDK interop only. |
</threat_model>

<verification>
- `[smoke-test-uia] PASS` captured from a real Windows desktop root (the D-7.5 gate).
- win-x64 AOT publish warning-free on the VM.
- VM torn down and confirmed absent before the phase proceeds.
</verification>

<success_criteria>
- The novel COM-under-NativeAOT combination is proven working (or fixed) on real Windows — the phase's single concentrated risk is retired before mechanical handler work.
- A1/A2/A3 assumptions are empirically resolved and recorded.
</success_criteria>

<output>
Create `.planning/phases/07-uia-tree-module/07-03-SUMMARY.md` when done (record the PASS line and how A1/A2/A3 resolved).
</output>
