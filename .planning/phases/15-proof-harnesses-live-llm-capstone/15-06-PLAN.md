---
phase: 15-proof-harnesses-live-llm-capstone
plan: 06
type: execute
wave: 4
depends_on: [15-05, 15-01, 15-02, 15-03]
files_modified: []
autonomous: true
requirements: [DAEMON-04, SESSION-01, SESSION-03, SESSION-04, CLI-02, CLI-03, MCP-04]
must_haves:
  truths:
    - "From the Linux host against the live VM: kill-9 mid-session + restart surfaces the remote session as Orphaned and reconciles it explicitly; connect/list/disconnect work end-to-end"
    - "CLI-02 real screenshot pixels + click landing + UIA shape and CLI-03 multi-MB put/get checksum pass through the rdpilot CLI surface"
    - "MCP-04 real click landing near screen edges/corners passes through the computer tool"
  artifacts: []
  key_links:
    - from: "the Linux orchestration host"
      to: "the live Azure VM (7-Zip::FM)"
      via: "RDPILOT_LIVE=1 cargo test against the real daemon (fake connector UNSET)"
      pattern: "RDPILOT_LIVE"
---

<objective>
LIVE-RUN (single VM session, wave 2 of the batched gate — Linux host against the SAME VM from Plan 15-05). Run the cross-platform live gates that need NO Windows build host (binding direction 2): the daemon orphan-liveness + e2e session verify (the Linux-hostable half of 12-07, authored in 15-01), the CLI-02/03 live re-exercise (authored in 15-02), and the MCP-04 live half (authored in 15-03). All drive the REAL daemon over the Unix IPC path against the real remote VM with the Phase 13/14 fake connector UNSET.

Purpose: This closes DAEMON-04 (live half), SESSION-01/03/04, the Phase-13-deferred CLI-02/03 live items, and the Phase-14-deferred MCP-04 live half — the remaining batched gates before the phase's own PROOF harnesses run. Sequenced after the DACL gate and before the PROOF harnesses to fail fast on any real-target regression while the VM is already up. The VM stays UP for Plans 15-07/15-08.

Output: PASS records for orphan-liveness + e2e (DAEMON-04/SESSION-01/03/04), CLI-02/03 live, and MCP-04 live.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/rdpilot-daemon/tests/live_daemon_e2e.rs
@crates/rdpilot-cli/tests/live_cli_verbs.rs
@crates/rdpilot-mcp/tests/live_mcp_computer.rs
@crates/rdpilot/tests/common/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orphan-liveness + e2e session verify (DAEMON-04, SESSION-01/03/04)</name>
  <files>crates/rdpilot-daemon/tests/live_daemon_e2e.rs</files>
  <read_first>
    crates/rdpilot-daemon/tests/live_daemon_e2e.rs (the Linux-hostable gate authored in Plan 15-01)
    crates/rdpilot/tests/common/mod.rs (RDPILOT_LIVE gating + connection.json loader)
  </read_first>
  <action>
    Build the workspace, then run `RDPILOT_LIVE=1 cargo test -p rdpilot-daemon --test live_daemon_e2e -- --ignored` from the Linux host against the live VM (connection from `.secrets/connection.json` written by Plan 15-05; fake connector UNSET so the daemon drives a real RDP session). Confirm the D-9.4 trace shows: after a `kill -9` of the daemon mid-session and a restart, `list` surfaces the prior possibly-still-live remote session as `Orphaned` (NOT an empty list, NOT auto-closed) and an explicit disconnect reconciles/tears it down (DAEMON-04 live; D-30/D-31); and connect/list/disconnect succeed end-to-end with correct name/id/status/timestamps (SESSION-01/03/04). Never print the password.
  </action>
  <acceptance_criteria>
    - `RDPILOT_LIVE=1 cargo test -p rdpilot-daemon --test live_daemon_e2e -- --ignored` runs against the real VM (fake connector unset)
    - The orphan-liveness assertion shows `Orphaned` after kill-9/restart, then explicit reconciliation
    - connect/list/disconnect succeed e2e with correct name/id/status/timestamps
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot-daemon --test live_daemon_e2e -- --ignored</automated>
  </verify>
  <done>DAEMON-04 live half + SESSION-01/03/04 pass end-to-end against the real VM.</done>
</task>

<task type="auto">
  <name>Task 2: CLI-02/03 live re-exercise through the rdpilot CLI surface</name>
  <files>crates/rdpilot-cli/tests/live_cli_verbs.rs</files>
  <read_first>
    crates/rdpilot-cli/tests/live_cli_verbs.rs (the CLI-02/03 live gate authored in Plan 15-02)
  </read_first>
  <action>
    Run `RDPILOT_LIVE=1 cargo test -p rdpilot-cli --test live_cli_verbs -- --ignored` from the Linux host against the live VM (fake connector UNSET). Confirm: (CLI-02) a real `perceive screenshot` decodes to non-blank pixels and an `input click` lands where expected, verified via `perceive uia`; (CLI-03) a multi-MB `put`/`get` reports `bytes_transferred` == file size and a matching round-tripped checksum. This re-exercises the SAME semantics through the CLI surface; it does not re-prove the Phase-10 SDK transfer internals. Every command uses an explicit `--session` (D-29). Never print the password.
  </action>
  <acceptance_criteria>
    - The CLI-02 real screenshot / click-landing / UIA assertions pass against the VM
    - The CLI-03 multi-MB put/get bytes_transferred + checksum assertions pass
    - The command ran with the fake connector unset (real daemon, real target)
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot-cli --test live_cli_verbs -- --ignored</automated>
  </verify>
  <done>CLI-02/03 live items pass through the real rdpilot CLI surface against the VM.</done>
</task>

<task type="auto">
  <name>Task 3: MCP-04 live half through the computer tool</name>
  <files>crates/rdpilot-mcp/tests/live_mcp_computer.rs</files>
  <read_first>
    crates/rdpilot-mcp/tests/live_mcp_computer.rs (the MCP-04 live-half gate authored in Plan 15-03)
  </read_first>
  <action>
    Run `RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_mcp_computer -- --ignored` from the Linux host against the live VM (real rdpilot-mcp binary via rmcp client, real daemon, fake connector UNSET). Confirm the `computer` tool's `left_click` actions near screen edges/corners (not just center — Pitfall 6) land on the expected 7-Zip elements, verified via `rdpilot_uia` (Pitfall 3 — UIA verifies, does not compute the click). Every call passes an explicit `session` (D-29). Never print the password.
  </action>
  <acceptance_criteria>
    - The MCP-04 live edge/corner click-landing assertions pass against the VM
    - Landing is verified via UIA element name/role, not native-space math
    - The command ran against the real daemon (fake connector unset)
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_mcp_computer -- --ignored</automated>
  </verify>
  <done>MCP-04 live half passes: real scaled coordinates land correctly near edges/corners through the computer tool.</done>
</task>

</tasks>

## Artifacts this plan produces

- PASS records (in the SUMMARY) for: DAEMON-04 live + SESSION-01/03/04 (orphan-liveness + e2e), CLI-02/03 live, MCP-04 live half
- No source changes — this plan runs the gates authored in Plans 15-01/15-02/15-03 against the VM from Plan 15-05

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linux host → live remote Windows target | Real credentials cross into a real RDP session over the daemon's Unix IPC path |
| crashed daemon → live remote session | A possibly-still-live remote session must surface as Orphaned, not be forgotten or blindly killed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-14 | Repudiation / Destruction of work | Live remote session forgotten or auto-killed on crash-restart | high | mitigate | live_daemon_e2e asserts `Orphaned` surfacing + explicit reconciliation against the real VM |
| T-15-15 | Information Disclosure | Live target password in test traces | high | mitigate | Never print the password; reuse ConnectionConfig Debug redaction across all three gates |
| T-15-SC | Tampering | package installs | low | accept | No new packages — runs already-authored gated tests only |
</threat_model>

<verification>
- All three `--ignored` live commands pass against the VM from Plan 15-05 (fake connector unset throughout).
- The VM remains UP for Plans 15-07/15-08.
</verification>

<success_criteria>
- DAEMON-04 live + SESSION-01/03/04 pass (orphan-liveness + e2e).
- CLI-02/03 live pass through the CLI surface.
- MCP-04 live half passes through the computer tool.
</success_criteria>

<output>
Create `.planning/phases/15-proof-harnesses-live-llm-capstone/15-06-SUMMARY.md` when done.
</output>
