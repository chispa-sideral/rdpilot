---
phase: 15-proof-harnesses-live-llm-capstone
plan: 07
type: execute
wave: 5
depends_on: [15-06, 15-02, 15-03]
files_modified: []
autonomous: true
requirements: [PROOF-02, PROOF-03]
must_haves:
  truths:
    - "PROOF-02 passes: the scripted CLI harness drives the real rdpilot binary end-to-end against 7-Zip::FM on the live VM with no live LLM, asserting distinct D-28 exit codes"
    - "PROOF-03 passes: the scripted MCP harness drives the real rdpilot-mcp binary programmatically (connect/list/disconnect + perception read + put/get) against the live VM with no live LLM"
  artifacts: []
  key_links:
    - from: "the Linux orchestration host"
      to: "the live Azure VM (7-Zip::FM)"
      via: "RDPILOT_LIVE=1 cargo test of the PROOF harnesses against the real binaries"
      pattern: "RDPILOT_LIVE"
---

<objective>
LIVE-RUN (single VM session, wave 3 of the batched gate — Linux host against the SAME VM). Run the phase's two scripted, no-live-LLM PROOF harnesses authored offline: PROOF-02 (CLI end-to-end, Plan 15-02) and PROOF-03 (MCP end-to-end, Plan 15-03), both against the real 7-Zip File Manager on the live VM (D-15.1 full fidelity, D-25 dual-finish-line — scripted proof per surface).

Purpose: These are two of the milestone's three finish-line deliverables. They must pass against the same live VM before the capstone (Plan 15-08). Running them after the batched-gate re-exercises (Plan 15-06) means any surface regression already surfaced; here the composed end-to-end proofs run clean.

Output: PASS records for PROOF-02 and PROOF-03.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/rdpilot-cli/tests/live_proof.rs
@crates/rdpilot-mcp/tests/live_proof.rs
@crates/rdpilot/tests/common/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run PROOF-02 (scripted CLI end-to-end, no live LLM)</name>
  <files>crates/rdpilot-cli/tests/live_proof.rs</files>
  <read_first>
    crates/rdpilot-cli/tests/live_proof.rs (the PROOF-02 harness authored in Plan 15-02)
  </read_first>
  <action>
    Run `RDPILOT_LIVE=1 cargo test -p rdpilot-cli --test live_proof -- --ignored` from the Linux host against the live VM (fake connector UNSET — real daemon, real target). Confirm the D-9.4 PASS/FAIL trace shows every step green: connect, screenshot (nonzero PNG), launch 7-Zip, window list asserting `7-Zip::FM`, put, get-checksum-match, disconnect, AND the deliberately-broken call asserting a DISTINCT non-zero exit code (D-28). Never print the password.
  </action>
  <acceptance_criteria>
    - `RDPILOT_LIVE=1 cargo test -p rdpilot-cli --test live_proof -- --ignored` passes against the VM
    - The trace shows the full connect→...→disconnect sequence green plus the D-28 distinct-exit-code assertion
    - The run used the real daemon (fake connector unset)
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot-cli --test live_proof -- --ignored</automated>
  </verify>
  <done>PROOF-02 passes: the CLI surface is proven end-to-end against a real remote-only Windows program, no live LLM.</done>
</task>

<task type="auto">
  <name>Task 2: Run PROOF-03 (scripted MCP end-to-end, no live LLM)</name>
  <files>crates/rdpilot-mcp/tests/live_proof.rs</files>
  <read_first>
    crates/rdpilot-mcp/tests/live_proof.rs (the PROOF-03 harness authored in Plan 15-03)
  </read_first>
  <action>
    Run `RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_proof -- --ignored` from the Linux host against the live VM (real rdpilot-mcp binary via rmcp client, real daemon, fake connector UNSET). Confirm the D-9.4 trace shows: list_tools discovers the rdpilot_* tools; connect (explicit session, D-29); list shows a D-30 status; one perception read (world_state/uia) with is_error false; put then get with matching `{bytes_transferred, checksum}` metadata (MCP-05 proven live); disconnect. Every CallToolResult.is_error is false. Never print the password.
  </action>
  <acceptance_criteria>
    - `RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_proof -- --ignored` passes against the VM
    - The trace covers list_tools + connect + list + perception read + put/get metadata match + disconnect
    - Every tool call passed an explicit session and every CallToolResult.is_error is false
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_proof -- --ignored</automated>
  </verify>
  <done>PROOF-03 passes: the MCP surface is proven end-to-end programmatically, no live LLM.</done>
</task>

</tasks>

## Artifacts this plan produces

- PASS records (in the SUMMARY) for PROOF-02 (CLI) and PROOF-03 (MCP), both against the live VM with no live LLM
- No source changes — runs the harnesses authored in Plans 15-02/15-03

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real CLI / MCP binaries | The harnesses treat both surfaces as untrusted black boxes over their real transports |
| surfaces/daemon → live remote Windows target | Real credentials cross into a real RDP session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-16 | Information Disclosure | Live target password in the PROOF traces / capture files | high | mitigate | Never print the password; capture files hold only surface stdout/stderr |
| T-15-17 | Tampering | A surface regression masked as a pass | medium | mitigate | PROOF-02 asserts distinct D-28 exit codes; PROOF-03 asserts is_error==false + metadata match, not prose |
| T-15-SC | Tampering | package installs | low | accept | No new packages — runs already-authored gated harnesses only |
</threat_model>

<verification>
- Both `--ignored` PROOF commands pass against the VM from Plan 15-05 (fake connector unset).
- The VM remains UP for Plan 15-08 (the capstone + teardown).
</verification>

<success_criteria>
- PROOF-02 passes (scripted CLI end-to-end, no live LLM, D-28 exit codes).
- PROOF-03 passes (scripted MCP end-to-end, no live LLM, MCP-05 metadata match).
</success_criteria>

<output>
Create `.planning/phases/15-proof-harnesses-live-llm-capstone/15-07-SUMMARY.md` when done.
</output>
