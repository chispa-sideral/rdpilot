---
phase: 15-proof-harnesses-live-llm-capstone
plan: 08
type: execute
wave: 6
depends_on: [15-07, 15-04]
files_modified: []
autonomous: false
requirements: [PROOF-04]
must_haves:
  truths:
    - "The local authenticated claude CLI is confirmed reachable before spending model tokens"
    - "PROOF-04 passes: claude -p drives a read/inspect + file-transfer task through the real MCP server against 7-Zip::FM, with success asserted by transcript AND an independent side-effect checksum"
    - "The Azure VM is torn down only after explicit developer authorization, then confirmed destroyed"
  artifacts: []
  key_links:
    - from: "the local claude CLI"
      to: "the real rdpilot-mcp server → daemon → live RDP session"
      via: "claude -p --mcp-config, then independent rmcp side-effect verification"
      pattern: "mcp-config"
---

<objective>
LIVE-RUN (single VM session, final wave of the batched gate — Linux host against the SAME VM). Run the PROOF-04 live-LLM capstone via `claude -p` (Plan 15-04, binding direction 1 — the local authenticated Claude Code CLI, no Anthropic API key/SDK), then tear the VM down under the ONE kept blocking human checkpoint (binding direction 4).

Purpose: PROOF-04 is the milestone's capstone finish line (D-25) — a live LLM driving a real read/inspect + file-transfer task through the MCP surface (D-15.1). It runs last because it depends on every prior surface being green and it is the most expensive/novel step. Teardown authorization is the single blocking human checkpoint the developer explicitly kept (all provisioning/second-account/cost decisions were pre-authorized autonomous). This plan closes the phase and the v1.1 milestone.

Output: a PROOF-04 PASS, then a developer-authorized teardown confirmed by `az group exists` => false.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/rdpilot-mcp/tests/live_capstone.rs
@crates/rdpilot-mcp/tests/fixtures/capstone-mcp-config.json
@crates/rdpilot/tests/common/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm the local claude CLI is authenticated and reachable</name>
  <files>crates/rdpilot-mcp/tests/live_capstone.rs</files>
  <read_first>
    crates/rdpilot-mcp/tests/live_capstone.rs (the capstone gate authored in Plan 15-04 — gates on RDPILOT_LIVE + the claude binary, never ANTHROPIC_API_KEY)
  </read_first>
  <action>
    Confirm the local Claude Code CLI is present and authenticated before running the capstone (it spends real model tokens via the CLI's own auth — binding direction 1: no ANTHROPIC_API_KEY in this process). Run a trivial probe such as `claude --version` and a minimal `claude -p "reply OK"` to confirm the CLI responds; if the CLI is not authenticated or absent, STOP and surface it (the capstone cannot run) rather than silently skipping. Developer authorized the token spend autonomously — this is a fail-fast verification, not a spend-approval gate.
  </action>
  <acceptance_criteria>
    - `claude` resolves on PATH and `claude --version` succeeds
    - A minimal `claude -p` probe returns a response (CLI authenticated)
    - If unauthenticated/absent, the plan stops and surfaces it (no silent skip)
  </acceptance_criteria>
  <verify>
    <automated>command -v claude &amp;&amp; claude --version</automated>
  </verify>
  <done>The local claude CLI is confirmed authenticated and reachable; the capstone can drive it.</done>
</task>

<task type="auto">
  <name>Task 2: Run the PROOF-04 capstone (claude -p through the real MCP server)</name>
  <files>crates/rdpilot-mcp/tests/live_capstone.rs</files>
  <read_first>
    crates/rdpilot-mcp/tests/live_capstone.rs (the capstone authored in Plan 15-04)
    crates/rdpilot-mcp/tests/fixtures/capstone-mcp-config.json (the --mcp-config template it renders)
  </read_first>
  <action>
    Run `RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_capstone -- --ignored` from the Linux host against the live VM (real rdpilot-mcp server launched by `claude -p --mcp-config`, real daemon, fake connector UNSET). At execution time, supply whatever the local claude CLI requires to auto-permit the `rdpilot` MCP tools headlessly (allowed-tools / permission-mode flag scoping to the `rdpilot` server) — this is the one flag detail deferred from Plan 15-04; determine and record the exact flag used. Confirm the capstone's D-9.4 trace shows SUCCESS asserted BOTH ways (binding direction 1): the transcript evidences at least one read/inspect tool call (rdpilot_uia or computer/screenshot) AND a file put/get, AND the independent rmcp side-effect check confirms the uploaded file landed on the target with a matching checksum — never model self-report alone. Never print the password or the rendered config's secret values.
  </action>
  <acceptance_criteria>
    - `RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_capstone -- --ignored` passes against the VM
    - The transcript shows both a read/inspect tool call and a file put/get through MCP
    - The independent side-effect check confirms the file landed with a matching checksum (not self-report)
    - The exact claude auto-permit flag used is recorded; no credential is printed
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_capstone -- --ignored</automated>
  </verify>
  <done>PROOF-04 passes: a live LLM (via claude -p) drove a read/inspect + file-transfer task through the MCP surface, verified by transcript + side-effect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Teardown authorization + confirmed destroy</name>
  <what-built>
    All batched live gates + PROOF-02/03/04 have run against the single provisioned Azure VM (Plans 15-05 through this plan). The VM is still UP. Per binding direction 4, teardown authorization is the ONE kept blocking human checkpoint for this phase — no other live step was gated (provisioning, second-account, cost, and claude token spend were all pre-authorized autonomous).
  </what-built>
  <how-to-verify>
    1. Review the relayed/observed live results across Plans 15-05..15-08: DAEMON-02 Windows DACL PASS, DAEMON-04/SESSION-01/03/04 PASS, CLI-02/03 live PASS, MCP-04 live PASS, PROOF-02 PASS, PROOF-03 PASS, PROOF-04 capstone PASS.
    2. If all green and no further live investigation is needed, authorize teardown. The executor will run `infra/manage-env.ps1 down` and then confirm `az group exists -n rdpilot-test` => `false`.
    3. Reply "approved: teardown" to proceed, or describe what to investigate first (VM stays up).
  </how-to-verify>
  <resume-signal>Type "approved: teardown" to destroy the VM, or name what to investigate while it stays up.</resume-signal>
</task>

</tasks>

## Artifacts this plan produces

- A PROOF-04 PASS record (live-LLM capstone via claude -p, transcript + side-effect verified)
- A confirmed VM teardown (`manage-env.ps1 down` + `az group exists` => false) closing the single-VM session and the v1.1 milestone

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local claude CLI → rdpilot-mcp server | The LLM autonomously issues tool calls; its arguments flow into the existing FILE-03 path validators |
| remote UI element text → model context | Potential prompt-injection surface (low, disposable credential-free VM) |
| rendered --mcp-config temp file → disk/logs | Holds real connection secrets; gitignored temp only, never committed/logged |
| provisioned VM → Azure cost | Must be torn down after authorization to stop billing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-18 | Information Disclosure | Connection secrets in the rendered --mcp-config temp / capstone trace | high | mitigate | Render to gitignored temp; never echo; never print the password; delete after run |
| T-15-19 | Tampering / Elevation | Prompt injection via remote UI text steering a transfer path | low | accept | Trusted-operator model; disposable credential-free VM with no attacker content in this proof (documented in the test) |
| T-15-20 | Denial of Service (local) | Hung claude subprocess / malformed transcript stalling the run | medium | mitigate | Bounded subprocess timeout + turn budget; missing transcript evidence is an immediate FAIL |
| T-15-21 | Repudiation / Cost | VM left running after the session (ongoing Azure billing) | high | mitigate | Blocking teardown-authorization checkpoint + `az group exists` => false confirmation + armed auto-destroy backstop |
| T-15-SC | Tampering | package installs | low | accept | No new packages — runs the already-authored capstone; `claude` is a pre-installed local tool |
</threat_model>

<verification>
- `command -v claude && claude --version` succeeds (CLI authenticated).
- `RDPILOT_LIVE=1 cargo test -p rdpilot-mcp --test live_capstone -- --ignored` passes (transcript + side-effect).
- After the blocking teardown checkpoint: `manage-env.ps1 down` run, then `az group exists -n rdpilot-test` => `false`.
</verification>

<success_criteria>
- PROOF-04 passes: a live LLM drove read/inspect + file-transfer through the MCP surface (D-25, D-15.1), success verified by transcript + independent side-effect.
- The VM is torn down under explicit developer authorization and confirmed destroyed.
- The v1.1 milestone dual finish line (PROOF-02/03/04) is complete.
</success_criteria>

<output>
Create `.planning/phases/15-proof-harnesses-live-llm-capstone/15-08-SUMMARY.md` when done.
</output>
