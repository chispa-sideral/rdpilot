---
phase: 15-proof-harnesses-live-llm-capstone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot-cli/tests/live_proof.rs
  - crates/rdpilot-cli/tests/live_cli_verbs.rs
autonomous: true
requirements: [PROOF-02, CLI-02, CLI-03]
must_haves:
  truths:
    - "A gated CLI harness drives the REAL rdpilot binary end-to-end (connect -> perceive -> launch -> window list -> put/get -> disconnect) against a real remote-only Windows program"
    - "The harness asserts distinct non-zero exit codes for at least one deliberately-broken call (D-28 taxonomy)"
    - "A gated CLI test re-exercises real screenshot pixels / click landing / UIA tree (CLI-02) and a real multi-MB put/get checksum (CLI-03) through the CLI surface"
  artifacts:
    - path: "crates/rdpilot-cli/tests/live_proof.rs"
      provides: "PROOF-02 scripted CLI end-to-end harness (real binary subprocess, no live LLM)"
      contains: "CARGO_BIN_EXE_rdpilot"
    - path: "crates/rdpilot-cli/tests/live_cli_verbs.rs"
      provides: "CLI-02/03 live-deferred re-exercise against the real target"
      contains: "RDPILOT_LIVE"
  key_links:
    - from: "crates/rdpilot-cli/tests/live_proof.rs"
      to: "the compiled rdpilot + rdpilot-daemon binaries"
      via: "env!(CARGO_BIN_EXE_rdpilot) subprocess spawn with the fake connector UNSET"
      pattern: "CARGO_BIN_EXE_rdpilot"
---

<objective>
OFFLINE-AUTHOR. Author the PROOF-02 scripted CLI end-to-end proof harness and the CLI-02/03 live-deferred re-exercise, both as gated `tests/*.rs` in `rdpilot-cli` (research CARGO_BIN_EXE finding, binding direction 3b — NOT `--example` binaries, whose targets never get `CARGO_BIN_EXE_<name>`). Both spawn the REAL compiled `rdpilot` CLI + `rdpilot-daemon` binaries as subprocesses against the real remote VM (D-15.1 full fidelity), with the Phase 13 fake connector (`RDPILOT_DAEMON_TEST_CONNECTOR`) UNSET so the daemon drives a real RDP session. Target the v1.0 7-Zip File Manager window (`class_name` `"7-Zip::FM"`) for continuity with the Phase 9 proof.

Purpose: PROOF-02 is a permanent, re-runnable artifact proving the CLI surface as real consumers see it — args in, exit codes + stdout out, over live IPC to a live target. The CLI-02/03 live re-exercise closes the items Phase 13 explicitly deferred to this batched gate (real screenshot pixels, real click/UIA shape, real multi-MB transfer checksum) WITHOUT re-litigating the underlying transfer semantics (FILE-01/02/04 were already live-verified in Phase 10). All code is authored + compile-checked offline here (binding direction 5); the live runs happen in Plans 15-06 (CLI-02/03) and 15-07 (PROOF-02).

Output: `tests/live_proof.rs` (PROOF-02) and `tests/live_cli_verbs.rs` (CLI-02/03 live).
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/rdpilot-cli/tests/cli_lifecycle.rs
@crates/rdpilot-cli/tests/cli_verbs.rs
@crates/rdpilot-cli/tests/cli_errors.rs
@crates/rdpilot-cli/src/main.rs
@crates/rdpilot-cli/src/cli.rs
@crates/rdpilot/tests/common/mod.rs
@crates/rdpilot/examples/proof_harness.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the PROOF-02 scripted CLI end-to-end harness</name>
  <files>crates/rdpilot-cli/tests/live_proof.rs</files>
  <read_first>
    crates/rdpilot-cli/tests/cli_lifecycle.rs (the CARGO_BIN_EXE_rdpilot resolution + run_cli subprocess helper + stdio-pitfall file-capture pattern to copy verbatim)
    crates/rdpilot-cli/src/main.rs (the exact verb -> handler mapping: Connect, Perceive Screenshot/WorldState/Uia/Window List, Input Launch, Put, Get, Disconnect, List)
    crates/rdpilot-cli/src/cli.rs (arg shapes: ConnectArgs, --session, --output, --json, PutArgs/GetArgs)
    crates/rdpilot/tests/common/mod.rs (RDPILOT_LIVE gating + .secrets/connection.json loader convention)
  </read_first>
  <action>
    Create `crates/rdpilot-cli/tests/live_proof.rs`, a `#[ignore = "requires RDPILOT_LIVE=1 and a live Azure VM (.secrets/connection.json)"]` test that early-returns cleanly when `RDPILOT_LIVE` is unset (D-18). Reuse `cli_lifecycle.rs`'s `run_cli` subprocess helper + `env!("CARGO_BIN_EXE_rdpilot")` + sibling-`rdpilot-daemon` resolution VERBATIM, but with `RDPILOT_DAEMON_TEST_CONNECTOR` UNSET (the opposite of `cli_lifecycle.rs`'s offline mode) so the auto-started daemon drives a REAL RDP session from `.secrets/connection.json` (host/username/password wired via the D-27 config layer — `--host`/`--username`/`--password` flags or `RDPILOT_*` env). Drive the full CLI surface as a black box, printing a D-9.4-style per-step PASS/FAIL trace: (1) `connect --name proof-cli` (D-29 explicit name); (2) `perceive screenshot --session proof-cli --output <tmp>.png` -> assert the PNG is nonzero size; (3) `input launch --session proof-cli "C:\\Program Files\\7-Zip\\7zFM.exe"` (open the remote-only target program); (4) `perceive window list --session proof-cli --json` -> assert a `"7-Zip::FM"` class_name entry appears; (5) `put --session proof-cli <local_file> <remote_name>` -> assert exit 0, parse `--json` `{bytes_transferred, checksum}`; (6) `get --session proof-cli <remote_name> <local_dest>` -> assert the round-tripped checksum matches step 5; (7) `disconnect proof-cli`. Then assert D-28's distinct exit-code taxonomy end-to-end: run one deliberately-broken call (e.g. `get --session does-not-exist ...`) and assert the process exits with the DISTINCT non-zero code mapped from `session-not-found` (per `cli_errors.rs`'s code map), not a generic 1. Never print the password.
  </action>
  <acceptance_criteria>
    - live_proof.rs contains `env!("CARGO_BIN_EXE_rdpilot")` and resolves the sibling `rdpilot-daemon` binary
    - The test early-returns when `RDPILOT_LIVE` is unset and does NOT set `RDPILOT_DAEMON_TEST_CONNECTOR`
    - The step sequence covers connect, screenshot, launch, window-list assertion of `7-Zip::FM`, put, get-checksum-match, disconnect
    - A deliberately-broken call asserts a DISTINCT non-zero exit code (D-28), not generic 1
    - Prints a per-step PASS/FAIL trace; never prints the password
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot-cli --test live_proof -- --list</automated>
  </verify>
  <done>The PROOF-02 harness compiles, lists as an ignored live test, and is ready to run against the VM in Plan 15-07.</done>
</task>

<task type="auto">
  <name>Task 2: Author the CLI-02/03 live-deferred re-exercise</name>
  <files>crates/rdpilot-cli/tests/live_cli_verbs.rs</files>
  <read_first>
    crates/rdpilot-cli/tests/cli_verbs.rs (the offline perception/input verb assertions this extends to a live target)
    crates/rdpilot-cli/tests/cli_lifecycle.rs (the CARGO_BIN_EXE + run_cli subprocess helper to reuse)
    crates/rdpilot-cli/src/main.rs (the perceive/input/put/get verb mapping)
    crates/rdpilot/tests/common/mod.rs (RDPILOT_LIVE gating convention)
  </read_first>
  <action>
    Create `crates/rdpilot-cli/tests/live_cli_verbs.rs`, gated identically (`#[ignore]` + `RDPILOT_LIVE`, fake connector UNSET, real daemon + real VM). This file closes the Phase-13-deferred CLI-02/03 live items — re-exercising the SAME semantics through the CLI surface, NOT re-proving the underlying SDK transfer (Phase 10 already live-verified FILE-01/02/04). Author two focused live assertions with a D-9.4 PASS/FAIL trace: (CLI-02) after connecting + launching 7-Zip, take a real `perceive screenshot` and assert the PNG decodes to non-blank pixels (real screenshot content), run one `input click` at a known window coordinate and assert via `perceive uia --json` (or a follow-up `window list`) that the expected element/state changed (real click landing + real UIA tree shape); (CLI-03) run a `put` of a multi-MB local file then a `get` back and assert the reported `bytes_transferred` equals the file size AND the round-tripped checksum matches (real multi-MB transfer through the CLI). Every command uses an explicit `--session` (D-29). Never print the password. Keep this file distinct from `live_proof.rs` so the two live-run steps (CLI-02/03 in 15-06, PROOF-02 in 15-07) remain independently runnable.
  </action>
  <acceptance_criteria>
    - live_cli_verbs.rs is `#[ignore]` + `RDPILOT_LIVE`-gated with the fake connector UNSET
    - Contains a CLI-02 assertion: real screenshot non-blank pixels + click-landing verified via UIA
    - Contains a CLI-03 assertion: multi-MB put/get with bytes_transferred == file size AND checksum match
    - Every command passes an explicit `--session`; the password is never printed
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot-cli --test live_cli_verbs -- --list</automated>
  </verify>
  <done>The CLI-02/03 live re-exercise compiles, lists as an ignored live test, and is ready to run against the VM in Plan 15-06.</done>
</task>

</tasks>

## Artifacts this plan produces

- `crates/rdpilot-cli/tests/live_proof.rs` — PROOF-02 scripted CLI end-to-end harness (real binaries, no LLM, `CARGO_BIN_EXE_rdpilot`)
- `crates/rdpilot-cli/tests/live_cli_verbs.rs` — CLI-02/03 live-deferred re-exercise through the CLI surface

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real CLI binary | The harness treats the CLI as an untrusted black box, observing only args/exit-codes/stdout |
| CLI/daemon → live remote Windows target | Real credentials from `.secrets/connection.json` cross into a real RDP session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Information Disclosure | Live target password leaking into the PASS/FAIL trace or capture files | high | mitigate | Never print the password; reuse ConnectionConfig's Debug-redaction discipline; capture files hold only CLI stdout/stderr, never the secrets file |
| T-15-05 | Tampering | Exit-code taxonomy regression masking a real failure as success | medium | mitigate | Assert DISTINCT non-zero codes (D-28) for a deliberately-broken call, not just exit != 0 |
| T-15-SC | Tampering | package installs | low | accept | No new packages this plan — reuses existing rdpilot-cli test deps only |
</threat_model>

<verification>
- Both new test files list under `cargo test -p rdpilot-cli -- --list` as ignored live tests.
- `cargo build --workspace --tests` remains green (the harnesses compile offline).
- The live runs are deferred: CLI-02/03 to Plan 15-06, PROOF-02 to Plan 15-07, both against the VM.
</verification>

<success_criteria>
- PROOF-02 harness authored: real `rdpilot` binary, real daemon, real VM, D-28 distinct exit codes, 7-Zip::FM target.
- CLI-02/03 live re-exercise authored: real screenshot pixels, click landing, UIA shape, multi-MB checksum.
- Both compile offline and are ready-to-run gated live tests.
</success_criteria>

<output>
Create `.planning/phases/15-proof-harnesses-live-llm-capstone/15-02-SUMMARY.md` when done.
</output>
