---
phase: 15-proof-harnesses-live-llm-capstone
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot-mcp/Cargo.toml
  - crates/rdpilot-mcp/tests/live_proof.rs
  - crates/rdpilot-mcp/tests/live_mcp_computer.rs
autonomous: true
requirements: [PROOF-03, MCP-04]
must_haves:
  truths:
    - "A gated MCP harness drives the REAL rdpilot-mcp binary as an rmcp client subprocess, exercising connect/list/disconnect + one perception read + file put/get programmatically (no live LLM)"
    - "A gated MCP test re-exercises real click landing near screen edges/corners through the computer tool (MCP-04 live half)"
    - "rmcp client + transport-child-process features are added as dev-dependencies without violating the production thin-client cargo-tree gate"
  artifacts:
    - path: "crates/rdpilot-mcp/Cargo.toml"
      provides: "dev-dependency rmcp client + transport-child-process features for driving rdpilot-mcp programmatically"
      contains: "transport-child-process"
    - path: "crates/rdpilot-mcp/tests/live_proof.rs"
      provides: "PROOF-03 scripted MCP end-to-end harness (rmcp TokioChildProcess, no LLM)"
      contains: "CARGO_BIN_EXE_rdpilot-mcp"
    - path: "crates/rdpilot-mcp/tests/live_mcp_computer.rs"
      provides: "MCP-04 live half: real click landing near edges/corners through the computer tool"
      contains: "RDPILOT_LIVE"
  key_links:
    - from: "crates/rdpilot-mcp/tests/live_proof.rs"
      to: "the compiled rdpilot-mcp binary"
      via: "rmcp TokioChildProcess + serve_client over stdio (env!(CARGO_BIN_EXE_rdpilot-mcp))"
      pattern: "TokioChildProcess"
---

<objective>
OFFLINE-AUTHOR. Add the rmcp CLIENT-side dev-dependencies and author two gated `tests/*.rs` in `rdpilot-mcp`: the PROOF-03 scripted MCP end-to-end harness (binding direction 3b — drives the REAL `rdpilot-mcp` binary as an rmcp `TokioChildProcess` subprocess, exercising tool calls programmatically with NO live LLM) and the MCP-04 live half (real click landing near screen edges/corners through the `computer` tool — the pure `scale_to_native` math was offline-proven in Phase 14). Target the 7-Zip File Manager window (`"7-Zip::FM"`) for continuity.

Purpose: PROOF-03 proves the MCP surface as any MCP host sees it — JSON-RPC tool calls over stdio to the real server, round-tripping to the real daemon and a real RDP session (D-15.1 full fidelity). It uses `rmcp`'s own `client` + `transport-child-process` features (D-26-locked crate, additive features) rather than a hand-rolled JSON-RPC client, guaranteeing wire-compatibility with the production server by construction. The MCP-04 live half closes what Phase 14 deferred: proving the scaled coordinates the bridge produces actually land on real Windows near edges/corners. All code is authored + compile-checked offline (binding direction 5); live runs happen in Plans 15-06 (MCP-04) and 15-07 (PROOF-03). NOTE (binding direction 1): the capstone (Plan 15-04) uses `claude -p`, NOT the Anthropic HTTP API — so `reqwest`/`base64` dev-deps that the research recommended are DELIBERATELY OMITTED; only the rmcp client features are added here.

Output: MCP `Cargo.toml` dev-deps, `tests/live_proof.rs` (PROOF-03), `tests/live_mcp_computer.rs` (MCP-04 live).
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/rdpilot-mcp/Cargo.toml
@crates/rdpilot-mcp/src/handler.rs
@crates/rdpilot-mcp/src/native_tools.rs
@crates/rdpilot-mcp/src/computer
@crates/rdpilot-mcp/tests/tool_schema.rs
@crates/rdpilot-mcp/tests/non_blocking.rs
@crates/rdpilot/tests/common/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rmcp client dev-dependencies (client + transport-child-process)</name>
  <files>crates/rdpilot-mcp/Cargo.toml</files>
  <read_first>
    crates/rdpilot-mcp/Cargo.toml (the existing rmcp [dependencies] entry with server/macros/transport-io features — the client features must be ADDITIVE dev-deps, never merged into the production entry)
    crates/rdpilot-mcp/tests/non_blocking.rs (the existing MCP integration-test structure)
  </read_first>
  <action>
    Add a `[dev-dependencies]` entry to `crates/rdpilot-mcp/Cargo.toml`: `rmcp = { version = "2.2.0", features = ["client", "transport-child-process"] }`. This is the SAME already-D-26-approved crate/version — only additional feature flags, so NO new package-legitimacy checkpoint (rmcp is already approved; features are additive). Do NOT add `reqwest` or `base64` (binding direction 1: the capstone drives `claude -p`, not the Anthropic HTTP API — those deps are not needed). Leave the production `[dependencies] rmcp` entry (server/macros/transport-io) untouched so the thin-client `cargo tree` gate (D-17) is unaffected — dev-dependencies are excluded from a production `cargo tree` invocation. Confirm the workspace still resolves.
  </action>
  <acceptance_criteria>
    - Cargo.toml `[dev-dependencies]` contains `rmcp` with `client` and `transport-child-process` features
    - The production `[dependencies] rmcp` entry is unchanged (server/macros/transport-io)
    - No `reqwest` or `base64` dev-dependency is added
    - `cargo metadata` / `cargo build -p rdpilot-mcp --tests` resolves cleanly
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p rdpilot-mcp --tests</automated>
  </verify>
  <done>The rmcp client dev-dependencies resolve; the production thin-client dependency surface is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author the PROOF-03 scripted MCP end-to-end harness</name>
  <files>crates/rdpilot-mcp/tests/live_proof.rs</files>
  <read_first>
    crates/rdpilot-mcp/src/native_tools.rs (the exact rdpilot_* tool names + argument schemas: rdpilot_connect/list/disconnect, rdpilot_world_state, rdpilot_window_list, rdpilot_uia, rdpilot_put, rdpilot_get)
    crates/rdpilot-mcp/src/handler.rs (tool dispatch + CallToolResult.is_error semantics)
    crates/rdpilot-mcp/tests/tool_schema.rs (the tools/list schema assertions to reuse for tool discovery)
    crates/rdpilot/tests/common/mod.rs (RDPILOT_LIVE gating + .secrets/connection.json loader convention)
  </read_first>
  <action>
    Create `crates/rdpilot-mcp/tests/live_proof.rs` as an `async` `#[tokio::test]` with `#[ignore = "requires RDPILOT_LIVE=1 and a live Azure VM"]`, early-returning clean when `RDPILOT_LIVE` is unset. Spawn the REAL `rdpilot-mcp` binary via `rmcp`'s client transport: `().serve(TokioChildProcess::new(Command::new(env!("CARGO_BIN_EXE_rdpilot-mcp")).configure(|cmd| { /* inherit RDPILOT_* host/creds from .secrets/connection.json loading */ })))` then `serve_client`/`ServiceExt` per rmcp 2.2.0. Drive the MCP surface programmatically (NO LLM) with a D-9.4 PASS/FAIL trace, covering at minimum (CONTEXT.md's floor): call `list_tools` and assert the expected rdpilot_* tools are present; `rdpilot_connect` (explicit `session` param, D-29) against the live VM; `rdpilot_list` shows the session with a D-30 status; one perception read (`rdpilot_world_state` with screenshot, or `rdpilot_uia`) asserting `CallToolResult.is_error == false` and non-empty content; `rdpilot_put` then `rdpilot_get` asserting the rendered `{path, bytes_transferred, checksum}` metadata matches (MCP-05 metadata-only contract, proven live); `rdpilot_disconnect`. Every tool call passes an explicit `session` (D-29). Assert on the returned CallToolResults, never on prose. Never print the password.
  </action>
  <acceptance_criteria>
    - live_proof.rs uses `env!("CARGO_BIN_EXE_rdpilot-mcp")` and rmcp `TokioChildProcess`
    - Early-returns when `RDPILOT_LIVE` is unset
    - Exercises list_tools + connect + list + one perception read + put + get + disconnect, each with an explicit `session`
    - Asserts put/get `{bytes_transferred, checksum}` metadata matches and every CallToolResult.is_error is false
    - Prints a per-step PASS/FAIL trace; never prints the password
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot-mcp --test live_proof -- --list</automated>
  </verify>
  <done>The PROOF-03 harness compiles against the rmcp client dev-deps, lists as an ignored live test, and is ready to run against the VM in Plan 15-07.</done>
</task>

<task type="auto">
  <name>Task 3: Author the MCP-04 live half (real click landing near edges/corners)</name>
  <files>crates/rdpilot-mcp/tests/live_mcp_computer.rs</files>
  <read_first>
    crates/rdpilot-mcp/src/computer (the computer tool: scale_to_native, action-discriminated screenshot/left_click, the fixed 1280x800 advertised space)
    crates/rdpilot-mcp/tests/scale_to_native.rs (the offline-proven pure coordinate math this live test complements)
    crates/rdpilot/tests/common/mod.rs (RDPILOT_LIVE gating convention)
  </read_first>
  <action>
    Create `crates/rdpilot-mcp/tests/live_mcp_computer.rs`, gated identically (`async` `#[tokio::test]`, `#[ignore]` + `RDPILOT_LIVE`, rmcp client against the real `rdpilot-mcp` binary + real daemon + real VM). This closes the MCP-04 LIVE half Phase 14 deferred (the pure `scale_to_native` is already offline-proven — this proves the scaled coordinates actually LAND correctly on real Windows). With a D-9.4 PASS/FAIL trace: connect, launch/foreground the 7-Zip File Manager, take a `computer` `screenshot` (in the fixed 1280x800 advertised space), then issue `computer` `left_click` actions near the SCREEN EDGES/CORNERS (not just center — Pitfall 6) at coordinates whose expected target element is known, and VERIFY the click landed by reading `rdpilot_uia` for the element name/role that should now be focused/activated (Pitfall 3: use UIA only to VERIFY the click, never to compute a click target in native space — keep the coordinate reasoning in the advertised 1280x800 space). Every call passes an explicit `session` (D-29). Never print the password.
  </action>
  <acceptance_criteria>
    - live_mcp_computer.rs is `#[ignore]` + `RDPILOT_LIVE`-gated, driving the real rdpilot-mcp binary via rmcp client
    - Issues `computer` left_click actions near edges/corners (not only center) and verifies landing via `rdpilot_uia`
    - Coordinate reasoning stays in the advertised 1280x800 space; UIA is used only to verify, not to compute the click
    - Every call passes an explicit `session`; the password is never printed
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot-mcp --test live_mcp_computer -- --list</automated>
  </verify>
  <done>The MCP-04 live-half test compiles, lists as an ignored live test, and is ready to run against the VM in Plan 15-06.</done>
</task>

</tasks>

## Artifacts this plan produces

- `crates/rdpilot-mcp/Cargo.toml` — `[dev-dependencies] rmcp` client + transport-child-process (additive, no legitimacy checkpoint; no reqwest/base64 per claude -p decision)
- `crates/rdpilot-mcp/tests/live_proof.rs` — PROOF-03 scripted MCP end-to-end harness (rmcp TokioChildProcess, no LLM)
- `crates/rdpilot-mcp/tests/live_mcp_computer.rs` — MCP-04 live half (real click landing near edges/corners)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real rdpilot-mcp binary | The harness speaks MCP JSON-RPC to the server as an untrusted external client |
| MCP server/daemon → live remote Windows target | Real credentials cross into a real RDP session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Information Disclosure | Live target password leaking via the harness trace or child-process env echo | high | mitigate | Never print the password; RDPILOT_* env inherited into the child is not echoed; ConnectionConfig Debug redaction |
| T-15-07 | Tampering | Coordinate-space confusion causing a click to land on the wrong element while unit tests pass | medium | mitigate | Keep click reasoning in the advertised 1280x800 space; verify landing via UIA element name/role, not native-space math (Pitfall 3/6) |
| T-15-SC | Tampering | dev-dependency addition | low | accept | rmcp already D-26-approved; client/transport-child-process are additive features on the same version — no new package, no legitimacy checkpoint |
</threat_model>

<verification>
- `cargo build -p rdpilot-mcp --tests` resolves the rmcp client dev-deps and compiles all three artifacts.
- Both new test files list under `cargo test -p rdpilot-mcp -- --list` as ignored live tests.
- The production `cargo tree` thin-client surface is unchanged (dev-deps excluded).
- Live runs deferred: MCP-04 to Plan 15-06, PROOF-03 to Plan 15-07.
</verification>

<success_criteria>
- PROOF-03 harness authored: real rdpilot-mcp binary via rmcp client, connect/list/one-read/put/get/disconnect, MCP-05 metadata match, no LLM.
- MCP-04 live half authored: real edge/corner click landing verified via UIA.
- rmcp client dev-deps added without touching the production thin-client dependency surface; no reqwest/base64.
</success_criteria>

<output>
Create `.planning/phases/15-proof-harnesses-live-llm-capstone/15-03-SUMMARY.md` when done.
</output>
