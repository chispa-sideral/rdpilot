---
phase: 15-proof-harnesses-live-llm-capstone
plan: 05
type: execute
wave: 3
depends_on: [15-01]
files_modified:
  - .secrets/connection.json
autonomous: true
requirements: [DAEMON-02]
must_haves:
  truths:
    - "A single Azure WS2022 VM is provisioned (Standard_B2s_v2, westeurope) with 7-Zip pre-installed and .secrets/connection.json written"
    - "The C# sensor is built ON the VM and relayed back with SHA-256 byte-identity confirmed"
    - "The #[cfg(windows)] DACL code compiles on the VM and the Windows-only live gate passes: owner-only DACL, anti-squatting fails loudly, a genuinely different Windows account is rejected"
  artifacts:
    - path: ".secrets/connection.json"
      provides: "live VM connection details consumed by every subsequent live wave"
      contains: "host"
  key_links:
    - from: "az vm run-command invoke (on the VM)"
      to: "cargo test -p rdpilot-daemon --test live_daemon_windows_dacl"
      via: "on-VM Rust build + second-Windows-account provisioning"
      pattern: "live_daemon_windows_dacl"
---

<objective>
LIVE-RUN (single VM session, wave 1 of the batched gate). Provision the ONE Azure VM this whole phase shares, build the C# sensor on it, and run the Windows-only DACL live gate ON THE VM (binding direction 2 — no separate local Windows host: the `#[cfg(windows)]` named-pipe DACL / anti-squatting / cross-account-rejection test compiles and runs on the Azure Windows VM itself). This opens the single-VM session that Plans 15-06/15-07/15-08 continue against the SAME VM (torn down only at the end of 15-08). Ordered to fail fast and minimize billable VM time.

Purpose: This closes the DAEMON-02 Windows half authored in Plan 15-01. The DACL/squatting/cross-account semantics are genuinely Windows-only (`tokio::net::windows::named_pipe` is `#[cfg(windows)]`) and, per the developer decision, run on the Azure VM rather than a separate pinned Windows host. The sensor build is also done here so it is ready for the perception-dependent waves (15-06 onward). Developer has AUTHORIZED autonomous drive for provisioning + second-account creation + Azure cost (binding direction 4) — so these proceed WITHOUT a blocking human checkpoint; the ONE kept end-checkpoint is teardown authorization, which lives in Plan 15-08.

Output: a live VM + fresh `.secrets/connection.json`, a byte-verified sensor exe, and a PASS on the Windows-DACL live gate.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-proof-harnesses-live-llm-capstone/15-RESEARCH.md
@crates/rdpilot-daemon/src/ipc/windows.rs
@crates/rdpilot-daemon/tests/live_daemon_windows_dacl.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provision the single Azure VM (autonomous per binding direction 4)</name>
  <files>.secrets/connection.json</files>
  <read_first>
    .planning/STATE.md (the Accumulated Context live-gate lore: VM size Standard_B2s_v2 in westeurope — Standard_B2ms is SkuNotAvailable; manage-env.ps1 up/down; .secrets/connection.json convention; auto-destroy runbook)
    .planning/phases/15-proof-harnesses-live-llm-capstone/15-RESEARCH.md (Batched Live-Gate Procedure Wave 1)
  </read_first>
  <action>
    Run `infra/manage-env.ps1 up -VmSize Standard_B2s_v2 -Location westeurope` (ALWAYS override the size — the Standard_B2ms default is SkuNotAvailable in westeurope per STATE.md). This provisions a WS2022 VM with 7-Zip 26.01 pre-installed by `Configure-Target.ps1` and writes fresh `.secrets/connection.json` (overwriting the stale prior copy — expected). Confirm the VM is reachable and `.secrets/connection.json` parses with host/username/password. Per binding direction 4 the developer has authorized this autonomously — do NOT gate provisioning behind a blocking human checkpoint; note the extended-uptime cost awareness in the SUMMARY (this session spans 4 live waves, longer than prior single-plan gates). Record the auto-destroy backstop is armed.
  </action>
  <acceptance_criteria>
    - `manage-env.ps1 up` completes with `-VmSize Standard_B2s_v2 -Location westeurope`
    - `.secrets/connection.json` exists, parses, and has host/username/password
    - The VM is confirmed reachable (RDP/az)
    - The auto-destroy backstop is confirmed armed
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.secrets/connection.json')); assert d.get('host'); print('connection.json OK')"</automated>
  </verify>
  <done>A single live Azure VM is provisioned with a fresh, parseable connection.json; cost awareness noted.</done>
</task>

<task type="auto">
  <name>Task 2: Build the C# sensor ON the VM and relay it back (SHA-256 byte-identity)</name>
  <files>.secrets/connection.json</files>
  <read_first>
    .planning/STATE.md (the 10-05 finding: fresh WS2022 has NEITHER .NET 8 SDK NOR VS Build Tools preinstalled — install both first; the Phase 8 finding: az vm run-command --parameters has an undocumented size limit that silently fails — embed the base64 source tarball IN the script body; the Storage blob SAS relay pattern)
    .planning/phases/15-proof-harnesses-live-llm-capstone/15-RESEARCH.md (Batched Live-Gate Procedure Wave 1; Pitfall 4)
  </read_first>
  <action>
    Build the C# sensor on the VM via `az vm run-command invoke`, following the established pattern: FIRST install `.NET 8 SDK` + `VS Build Tools` (VCTools workload) since a fresh WS2022 Datacenter image has neither (10-05 finding); embed the sensor source tarball as base64 DIRECTLY in the run-command script body (NEVER via `--parameters` — undocumented size limit silently fails, Phase 8 finding, Pitfall 4); build it on the VM; relay the built exe back to the Linux host via a short-lived Storage blob SAS. Confirm SHA-256 byte-identity between the VM-built and locally-relayed copies (mandatory relay-integrity pattern from every prior live gate). This makes the sensor available for the perception-dependent waves (15-06/15-07/15-08).
  </action>
  <acceptance_criteria>
    - .NET 8 SDK + VS Build Tools installed on the VM before the sensor build
    - The sensor source is embedded as base64 in the run-command script body, not passed via --parameters
    - The built sensor exe is relayed back via a short-lived Storage blob SAS
    - SHA-256 of the VM-built and relayed copies match
  </acceptance_criteria>
  <verify>
    <automated>test -f .secrets/connection.json &amp;&amp; echo "sensor-relay step recorded in SUMMARY with matching SHA-256"</automated>
  </verify>
  <done>The C# sensor is built on the VM and relayed back with confirmed SHA-256 byte-identity, ready for the perception waves.</done>
</task>

<task type="auto">
  <name>Task 3: Windows-DACL live gate ON the VM (owner-only DACL, anti-squatting, cross-account rejection)</name>
  <files>.secrets/connection.json</files>
  <read_first>
    crates/rdpilot-daemon/src/ipc/windows.rs (the DACL code from Plan 15-01 that must compile on real Windows for the first time)
    crates/rdpilot-daemon/tests/live_daemon_windows_dacl.rs (the Windows-only live gate authored in 15-01, consumes RDPILOT_SECOND_WINDOWS_ACCOUNT)
    crates/rdpilot-daemon/tests/ipc_security.rs (the second-account cross-account-rejection intent mirrored on Windows)
  </read_first>
  <action>
    On the Azure Windows VM (via `az vm run-command invoke`, same embed-base64 pattern), per binding direction 2: (a) provision a SECOND Windows local account autonomously (create a local user via PowerShell `New-LocalUser`) for the cross-account rejection assertion, exporting its name as `RDPILOT_SECOND_WINDOWS_ACCOUNT` — developer authorized this autonomously (binding direction 4), no human checkpoint; (b) build `rdpilot-daemon` (or just its Windows IPC test target) ON the VM — this is the FIRST real compile of the `#[cfg(windows)]` DACL code from Plan 15-01; if `windows-sys` cannot cleanly express the owner-only protected DACL, that is the documented fallback trigger to `windows-permissions` which would require the 12-07 Task-1 blocking-human legitimacy checkpoint before its manifest entry (contingency — flag to the developer if it fires, do not silently switch); (c) run `set RDPILOT_LIVE=1 && cargo test -p rdpilot-daemon --test live_daemon_windows_dacl -- --ignored` on the VM and relay the results back. Confirm: a connection attempt from the genuinely different Windows account is rejected by the pipe DACL; a second `first_pipe_instance(true)` creation of the same pipe name FAILS LOUDLY; the DACL is present and non-null. Never relay any SID or credential into logs.
  </action>
  <acceptance_criteria>
    - A second Windows local account is provisioned on the VM and its name exported as RDPILOT_SECOND_WINDOWS_ACCOUNT
    - `ipc/windows.rs` compiles on the VM's Windows target (or the windows-permissions fallback is flagged, not silently taken)
    - `live_daemon_windows_dacl` passes: cross-account rejection, anti-squatting fail-loud, non-null DACL
    - Results relayed back to the Linux host; no SID/credential leaked into logs
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/rdpilot-daemon/tests/live_daemon_windows_dacl.rs &amp;&amp; echo "windows-dacl PASS relayed and recorded in SUMMARY"</automated>
  </verify>
  <done>DAEMON-02 Windows half is proven on the VM: explicit owner-only DACL, anti-squatting fails loudly, a different Windows account is rejected.</done>
</task>

</tasks>

## Artifacts this plan produces

- A single live Azure VM (Standard_B2s_v2, westeurope) + fresh `.secrets/connection.json`, held UP through Plan 15-08
- A byte-verified C# sensor exe relayed from the VM (available to all perception waves)
- A PASS record for the Windows-DACL live gate (DAEMON-02 Windows half)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| other Windows account → named pipe | The provisioned second account must be denied by the pipe DACL |
| VM build output → Linux host | The relayed sensor exe must be byte-identical (SHA-256), not tampered in transit |
| connection.json → disk/logs | Real VM credentials must not leak into run-command output or the SUMMARY |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-11 | Tampering | Sensor exe altered during VM→host relay | high | mitigate | SHA-256 byte-identity confirmation of VM-built vs relayed copy (mandatory relay pattern) |
| T-15-12 | Elevation of Privilege | Cross-account access not actually rejected by the DACL | critical | mitigate | Live second-Windows-account rejection assertion on the real VM (owner-only protected DACL) |
| T-15-13 | Information Disclosure | VM credentials / SIDs in run-command output or SUMMARY | high | mitigate | Never echo secrets/SIDs; connection.json stays gitignored; SUMMARY records outcomes not credentials |
| T-15-SC | Tampering | windows-sys→windows-permissions fallback introducing an [ASSUMED] crate | high | mitigate | Contingency only; if the fallback fires, the 12-07 Task-1 blocking-human legitimacy checkpoint gates the manifest entry — do not silently switch |
</threat_model>

<verification>
- `.secrets/connection.json` parses with a reachable host.
- Sensor VM-built vs relayed SHA-256 match recorded.
- `live_daemon_windows_dacl` PASS relayed from the VM (cross-account rejection, anti-squatting, non-null DACL).
- The VM remains UP for Plans 15-06/15-07/15-08 (teardown only in 15-08).
</verification>

<success_criteria>
- Single VM provisioned autonomously (binding direction 4), 7-Zip present, connection.json fresh.
- Sensor built on VM and byte-verified.
- DAEMON-02 Windows half PASSES on the VM; no legitimacy checkpoint fired (windows-sys held).
</success_criteria>

<output>
Create `.planning/phases/15-proof-harnesses-live-llm-capstone/15-05-SUMMARY.md` when done.
</output>
