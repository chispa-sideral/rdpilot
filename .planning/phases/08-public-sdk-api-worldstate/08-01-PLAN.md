---
phase: 08-public-sdk-api-worldstate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot/src/lib.rs
  - crates/rdpilot/src/screenshot.rs
  - crates/rdpilot/src/perception.rs
autonomous: true
requirements: [API-01, API-02]

must_haves:
  truths:
    - "Library code fails clippy/compile if it introduces `unsafe`, `unwrap`, or `expect` (SC#4, compiler-enforced not convention-enforced)."
    - "Every owned public perception/screenshot type serializes to JSON via serde_json without error (D-8.4)."
    - "A serialized `Screenshot` carries `width`/`height` but never the `rgba` byte buffer (D-8.4 context-bloat guard, threat T-08-02)."
    - "`WindowState` serializes as the lowercase strings `normal`/`minimized`/`maximized`, matching the existing wire convention."
  artifacts:
    - path: "crates/rdpilot/src/lib.rs"
      provides: "Crate-level strict lint gates as inner attributes"
      contains: "#![deny(unsafe_code)]"
    - path: "crates/rdpilot/src/screenshot.rs"
      provides: "Serialize on Rect and dims-only Serialize on Screenshot"
      contains: "serde::Serialize"
    - path: "crates/rdpilot/src/perception.rs"
      provides: "Serialize on WindowInfo, WindowState, ProcessInfo, UiaElement"
      contains: "serde::Serialize"
  key_links:
    - from: "crates/rdpilot/src/screenshot.rs Screenshot"
      to: "serde output"
      via: "#[serde(skip)] on rgba"
      pattern: "serde\\(skip\\)"
    - from: "crates/rdpilot/src/lib.rs"
      to: "cargo clippy"
      via: "inner #![deny(clippy::unwrap_used)] / expect_used / unsafe_code"
      pattern: "deny\\(clippy::unwrap_used\\)"
---

<objective>
Turn the "strict by convention" API-01 guarantee into a compiler-enforced one and make every owned
public SDK type JSON-serializable — the two mechanical, crate-wide prerequisites the new `WorldState`
(Plan 02) builds on.

Purpose: SC#4 ("compiles clean under strict Rust settings, no `unsafe`/`unwrap` in library code")
becomes permanently, structurally true via `lib.rs` inner attributes; D-8.4's `Serialize` derives
unlock the JSON `WorldState` output the seeded v2 CLI/MCP consumer needs — without pulling any
third-party type (`image`/`ironrdp`) into the public surface (D-09).
Output: `lib.rs` gated with `#![deny(...)]`; `Rect`, `WindowInfo`, `WindowState`, `ProcessInfo`,
`UiaElement` derive `Serialize`; `Screenshot` serializes dims-only; serde round-trip unit tests.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-public-sdk-api-worldstate/08-CONTEXT.md
@.planning/phases/08-public-sdk-api-worldstate/08-RESEARCH.md
@crates/rdpilot/src/lib.rs
@crates/rdpilot/src/screenshot.rs
@crates/rdpilot/src/perception.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add crate-level strict lint gates as lib.rs inner attributes</name>
  <files>crates/rdpilot/src/lib.rs</files>
  <read_first>
    crates/rdpilot/src/lib.rs (file modified — current inner doc-comment header + mod list)
  </read_first>
  <action>
    Add three inner attributes at the very top of `lib.rs`, immediately after the `//!` module
    doc-comment block and before the first `mod` declaration: `#![deny(unsafe_code)]`,
    `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]` (per D-8.4). These MUST be
    `lib.rs` inner attributes, NOT a `Cargo.toml [lints]` table — RESEARCH Pitfall 1 verified that a
    `[lints]` table is package-scoped and breaks the 116 legitimate `.expect()` calls in
    `tests/live_session.rs` with 116 clippy errors, whereas inner attributes scope to the library
    crate compilation unit only and leave the separate `tests/*.rs` crate roots untouched. RESEARCH
    verified (against the current tree) that these exact three gates produce ZERO new violations in
    library code — no latent unwrap/expect/unsafe cleanup is needed. Do NOT add `panic`/`missing_docs`
    gates (out of the D-8.4 discretion scope chosen here — keep the set to exactly the three named).
    Add a short comment above the attributes stating they enforce API-01/SC#4 at compile/lint time and
    are deliberately library-scoped (cite Pitfall 1) so a future maintainer does not "helpfully"
    migrate them to `Cargo.toml` and break the live-test suite.
  </action>
  <acceptance_criteria>
    - lib.rs contains the literal line `#![deny(unsafe_code)]`
    - lib.rs contains `#![deny(clippy::unwrap_used)]` and `#![deny(clippy::expect_used)]`
    - The three attributes appear before the first `mod` declaration
    - No `[lints]` table is added to crates/rdpilot/Cargo.toml
    - `RUSTUP_TOOLCHAIN=stable cargo clippy -p rdpilot --lib --target x86_64-unknown-linux-gnu` exits 0 (library target lints clean under the new gates)
  </acceptance_criteria>
  <verify>
    <automated>RUSTUP_TOOLCHAIN=stable cargo clippy -p rdpilot --lib --target x86_64-unknown-linux-gnu 2>&1 | grep -qi "error" && exit 1 || exit 0</automated>
  </verify>
  <done>lib.rs carries the three inner `#![deny(...)]` attributes; library clippy passes clean; Cargo.toml has no `[lints]` table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive Serialize on the owned types with dims-only Screenshot, plus serde round-trip tests</name>
  <files>crates/rdpilot/src/screenshot.rs, crates/rdpilot/src/perception.rs</files>
  <read_first>
    crates/rdpilot/src/screenshot.rs (file modified — Rect/Screenshot definitions + existing derives)
    crates/rdpilot/src/perception.rs (file modified — WindowInfo/WindowState/ProcessInfo/UiaElement owned types + existing WindowStateWire `#[serde(rename_all = "lowercase")]` convention)
  </read_first>
  <behavior>
    - Test: `serde_json::to_value(&Screenshot{width,height,rgba})` yields an object with `width` and `height` keys and NO `rgba` key (dims-only, D-8.4).
    - Test: `serde_json::to_value(&Rect{x,y,w,h})` yields the four numeric fields.
    - Test: `serde_json::to_value(&WindowState::Minimized)` yields the JSON string `"minimized"` (lowercase convention parity).
    - Test: a fully-populated `WindowInfo`, `ProcessInfo`, and `UiaElement` each serialize without error and expose their documented fields (e.g. `UiaElement` exposes `id`/`role`/`bbox`).
  </behavior>
  <action>
    In `screenshot.rs`: add `serde::Serialize` to `Rect`'s existing derive list (it already derives
    Clone/Copy/Debug/PartialEq/Eq). Add `serde::Serialize` to `Screenshot`'s derive list and annotate
    the `rgba` field with `#[serde(skip)]` so the raw RGBA buffer is excluded from serde output while
    `width`/`height` remain (D-8.4, RESEARCH Pattern 3). Do NOT add `Deserialize` to `Screenshot` —
    RESEARCH Pattern 3 verified that `#[serde(skip)]` on a Serialize-only derive needs no `Default`
    bound, whereas adding `Deserialize` would require a `#[serde(default)]`/`Default` for the skipped
    field; the existing `to_png()` remains the only path bytes leave the type.
    In `perception.rs`: add `serde::Serialize` to the owned types `WindowInfo`, `ProcessInfo`, and
    `UiaElement`. Add `serde::Serialize` to `WindowState` AND annotate it with
    `#[serde(rename_all = "lowercase")]` so its serialized form (`"normal"`/`"minimized"`/`"maximized"`)
    matches the crate's existing `WindowStateWire` deserialization convention exactly (RESEARCH Code
    Examples). Derive `Serialize` on the OWNED types only — never on the crate-internal `*Wire` structs
    (they stay `Deserialize`-only) and never on `image`/`ironrdp` types (D-09/D-8.4). Because `bbox`
    and `rect` are `crate::Rect` (now Serialize) and `Screenshot` is Serialize, no field type blocks
    these derives.
    Add a `#[cfg(test)] mod` (or extend the existing test module) in each file with the round-trip
    assertions from the behavior block, using `serde_json::to_value(...)` and asserting on the presence
    /absence of specific keys and the lowercase enum rendering. Use `.expect(...)` freely in these test
    functions — the `lib.rs` deny gates do not apply to `#[cfg(test)]`... note: they DO apply to
    in-crate `#[cfg(test)] mod tests` since those compile as part of the library crate; the existing
    library unit tests already use `.expect(...)`, so confirm the gate does not fire on test code by
    running clippy after — RESEARCH verified library clippy stays clean, and `unwrap_used`/`expect_used`
    are allowed-by-default in `#[cfg(test)]` when the lints are set at crate level via `deny` only if
    clippy's test-context allowance applies; if clippy flags `.expect()` in the new in-crate tests,
    scope them with an inner `#![allow(clippy::expect_used)]` at the top of the test module (mirroring
    how the existing test modules already compile clean).
  </action>
  <acceptance_criteria>
    - screenshot.rs `Rect` derive list includes `serde::Serialize`
    - screenshot.rs `Screenshot` derives `serde::Serialize` and the `rgba` field carries `#[serde(skip)]`
    - perception.rs `WindowInfo`, `ProcessInfo`, `UiaElement` each derive `serde::Serialize`
    - perception.rs `WindowState` derives `serde::Serialize` and carries `#[serde(rename_all = "lowercase")]`
    - No `*Wire` struct derives `Serialize` (they remain `Deserialize`-only)
    - A test asserts `serde_json::to_value(&screenshot)` has no `"rgba"` key and has `"width"`/`"height"`
    - A test asserts `WindowState::Minimized` serializes to the string `"minimized"`
    - `RUSTUP_TOOLCHAIN=stable cargo test -p rdpilot --target x86_64-unknown-linux-gnu --lib` passes (all lib unit tests green, including the new serde tests)
  </acceptance_criteria>
  <verify>
    <automated>RUSTUP_TOOLCHAIN=stable cargo test -p rdpilot --target x86_64-unknown-linux-gnu --lib 2>&1 | tail -5</automated>
  </verify>
  <done>All five owned types derive `Serialize`; `Screenshot` is dims-only; `WindowState` renders lowercase; new serde round-trip tests pass; `*Wire` types untouched; D-09 boundary preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer ↔ SDK public API | Owned SDK types cross here; third-party (`image`/`ironrdp`) types must never leak (D-09) |
| serialized WorldState/Screenshot ↔ downstream sink | JSON output may later be forwarded to a v2 CLI/MCP consumer; payload size and contents matter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Denial of Service | `Screenshot` serde output | medium | mitigate | `#[serde(skip)]` on `Screenshot::rgba` keeps serialized payloads lean regardless of desktop resolution (D-8.4 context-bloat guard) — verified by a test asserting no `rgba` key |
| T-08-03 | Tampering (silent API-01 regression) | library code (`lib.rs`) | medium | mitigate | `#![deny(unsafe_code)]`/`clippy::unwrap_used`/`clippy::expect_used` inner attributes make any future `unwrap`/`unsafe` in library code a lint/compile failure rather than a silent convention breach |

No npm/pip/cargo install tasks exist in this plan (RESEARCH confirmed zero new external packages; `serde` `derive` is already enabled in Cargo.toml) — no supply-chain (`T-08-SC`) checkpoint is required.
</threat_model>

<verification>
- `RUSTUP_TOOLCHAIN=stable cargo clippy -p rdpilot --lib --target x86_64-unknown-linux-gnu` exits 0 under the new deny gates (SC#4).
- `RUSTUP_TOOLCHAIN=stable cargo test -p rdpilot --target x86_64-unknown-linux-gnu --lib` passes (serde round-trip tests).
- Offline substitution target `x86_64-unknown-linux-gnu` is valid: the crate has no `cfg(windows)`/`cfg(target_os)` code (RESEARCH Environment Availability, re-verified 0 matches).
</verification>

<success_criteria>
- SC#4 is compiler-enforced: library code with `unsafe`/`unwrap`/`expect` fails to lint.
- All owned public types (`Rect`, `Screenshot`, `WindowInfo`, `WindowState`, `ProcessInfo`, `UiaElement`) serialize via serde; `Screenshot` is dims-only.
- No third-party or `*Wire` type gained a `Serialize` impl (D-09/D-8.4 boundary intact).
</success_criteria>

<output>
Create `.planning/phases/08-public-sdk-api-worldstate/08-01-SUMMARY.md` when done.
</output>
