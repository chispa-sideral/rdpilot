---
phase: 08-public-sdk-api-worldstate
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - crates/rdpilot/tests/live_session.rs
autonomous: true
requirements: [API-01, API-02]

must_haves:
  truths:
    - "A gated live test calls `Session::world_state()` against a real target and records the measured `capture_span` for the record (SC#2)."
    - "The live test uses only the public `Session`/`WorldState` API — no `ironrdp`/`image`/sensor-protocol type appears (SC#1)."
    - "The default `cargo test -p rdpilot` run stays green with no target present (test is `#[ignore]`'d and early-returns when the live config is absent)."
  artifacts:
    - path: "crates/rdpilot/tests/live_session.rs"
      provides: "Gated live world_state capture-span test(s)"
      contains: "world_state"
  key_links:
    - from: "crates/rdpilot/tests/live_session.rs"
      to: "rdpilot::Session::world_state"
      via: "public API only, gated by RDPILOT_LIVE"
      pattern: "world_state\\("
---

<objective>
Add the thin end-of-phase live check for SC#2: exercise `Session::world_state()` against a real
disposable Azure VM and record the measured `capture_span`, reusing the existing gated live-test
fixture pattern in `tests/live_session.rs`. Per D-8.2 the 500ms bound is BEST-EFFORT — the test
reports the measured span for the record (as Phase 7 SC#3 reported "measured 30.36ms") and does not
hard-fail on the timing number; it DOES assert the call returns `Ok` with the requested components
populated.

Purpose: SC#2 is the one criterion that benefits from a real measurement against a real target
(matching the project's consistent "reason offline, live-tune empirically" methodology). SC#1/SC#3/SC#4
are already closed offline by Plans 01–02, so this plan is a small, low-risk addition to the existing
live-test file — not a new harness.
Output: gated `world_state` live test(s) in `tests/live_session.rs`, run at the end-of-phase live gate.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-public-sdk-api-worldstate/08-CONTEXT.md
@.planning/phases/08-public-sdk-api-worldstate/08-RESEARCH.md
@crates/rdpilot/tests/live_session.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gated live world_state capture-span test(s) and run the end-of-phase live gate</name>
  <files>crates/rdpilot/tests/live_session.rs</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (file modified — the `require_target!` skip macro, the `#[ignore = "live: ..."]` convention, the `launch_notepad_and_find_window` helper, the existing UIA live tests `uia_tree_walk_within_500ms`/`uia_tree_round_trips_live`, and the `min_titled_z_order` pattern used for the foreground heuristic)
  </read_first>
  <action>
    Add one (optionally two) gated live test(s) to `tests/live_session.rs`, mirroring the existing
    live tests exactly: annotate with `#[ignore = "live: requires a provisioned RDP target + published
    rdpilot-sensor.exe (RDPILOT_LIVE=1)"]`, open with `let Some(cfg) = require_target!("<test_name>")
    else { return };` so the default `cargo test` stays green with no target, connect via the public
    `rdpilot::Session`, and `deploy_and_launch()` the sensor before any sensor-backed request (same
    preamble as `uia_tree_round_trips_live`).
    First test — default options: launch Notepad via `launch_notepad_and_find_window` (so a titled app
    window exists), call `session.world_state(WorldStateOptions::default())`, `.expect(...)` an `Ok`,
    then assert `screenshot.is_some()`, `window_list.is_some()`, `uia.is_none()` (SC#2 default shape),
    and `eprintln!` the measured `capture_span` (as milliseconds) FOR THE RECORD. Do NOT `assert!` the
    span is `< 500ms` — D-8.2 makes the bound best-effort; the recorded value is the SC#2 evidence, not
    a hard gate. Optionally also assert the span is a plausible non-zero, finite `Duration` (e.g.
    `> Duration::ZERO`) to catch a broken measurement, but never gate on the 500ms figure.
    Second test (recommended) — foreground UIA: call `world_state` with `WorldStateOptions { screenshot:
    true, window_list: true, uia: UiaMode::Foreground, ..Default::default() }` against the launched
    Notepad, assert `uia` is `Some` with exactly one `(hwnd, elements)` group whose `hwnd` matches the
    focused Notepad window (use the same titled-min-`z_order` reasoning the SDK uses), assert the group's
    element list is non-empty, and `eprintln!` the `capture_span`. This exercises the Pitfall-3 internal
    window-list fetch AND the Foreground heuristic against a real target.
    Import `WorldStateOptions`, `UiaMode`, and `WorldState` from `rdpilot` in the test file's use list
    (public API only — no `ironrdp`/`image`/`rustls` type, matching the file's existing public-only
    convention). Keep `.expect(...)` usage consistent with the rest of the file (test crate is a
    separate compilation unit, unaffected by the library's `#![deny]` gates — RESEARCH Pitfall 1).
    Then run the end-of-phase live gate: provision the target per the project's established pattern
    (`infra/manage-env.ps1 up`; VM size `Standard_B2s_v2` in westeurope), export `RDPILOT_LIVE=1`, run
    `cargo test -p rdpilot --test live_session -- --include-ignored --test-threads=1 world_state`,
    record the measured `capture_span`(s) in the SUMMARY, and tear the VM down (`manage-env.ps1 down`).
    If no VM is available at execution time, the offline SC#1/SC#3/SC#4 already close cleanly (Plans
    01–02); note in the SUMMARY that the SC#2 span measurement is deferred to a follow-up live run,
    exactly as prior phases handled an unavailable target.
  </action>
  <acceptance_criteria>
    - tests/live_session.rs contains a `#[ignore = "live: ..."]` test that calls `session.world_state(WorldStateOptions::default())`
    - The test asserts `screenshot.is_some()`, `window_list.is_some()`, `uia.is_none()` for default options
    - The test `eprintln!`s the measured `capture_span` and does NOT `assert!` a `< 500ms` bound (D-8.2 best-effort)
    - The test opens with `require_target!` so `cargo test -p rdpilot` (no target) stays green
    - Only public `rdpilot` types are imported (no `ironrdp`/`image`/`rustls`)
    - `RUSTUP_TOOLCHAIN=stable cargo test -p rdpilot --target x86_64-unknown-linux-gnu --test live_session` compiles and passes with the tests skipped (no target present)
    - `<human-check>`: end-of-phase live gate run recorded a measured `capture_span` against a real VM (or SUMMARY documents deferral if no VM was available)
  </acceptance_criteria>
  <verify>
    <automated>RUSTUP_TOOLCHAIN=stable cargo test -p rdpilot --target x86_64-unknown-linux-gnu --test live_session 2>&1 | tail -8</automated>
    <human-check>With a live VM provisioned (`infra/manage-env.ps1 up`, `RDPILOT_LIVE=1`), `cargo test -p rdpilot --test live_session -- --include-ignored --test-threads=1 world_state` returns Ok for the default-options and Foreground world_state tests and prints a plausible measured `capture_span`; record the value; tear the VM down. Deferral is acceptable if no target is available (SC#1/SC#3/SC#4 already close offline).</human-check>
  </verify>
  <done>Gated `world_state` live test(s) exist and compile; the default `cargo test` run stays green; the end-of-phase live gate recorded a measured `capture_span` for SC#2 (or the SUMMARY documents deferral to a follow-up live run).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ live remote target | Gated live test drives a real disposable Azure VM over RDP via the public `Session` API only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Denial of Service (runaway cloud cost) | live test VM lifecycle | low | mitigate | Reuse the established disposable-VM pattern: `manage-env.ps1 up`/`down` plus the Phase 1 scheduled auto-destroy runbook as a backstop; tear down after the run (per prior phases' live gates) |
| T-08-04 | Input Validation | `world_state` hwnd values against the real sensor | low | accept | Reuses the unchanged Phase 6/7 `get_uia_tree` contract; an invalid hwnd surfaces as `Error::SensorRejected`/`Error::Dvc` (ASVS V5, D-6.4) |

No package installs in this plan (zero new external packages — RESEARCH); no `T-08-SC` supply-chain checkpoint required.
</threat_model>

<verification>
- Offline: `RUSTUP_TOOLCHAIN=stable cargo test -p rdpilot --target x86_64-unknown-linux-gnu --test live_session` compiles and passes with the live tests skipped (no target).
- Live (end-of-phase gate): `RDPILOT_LIVE=1 cargo test -p rdpilot --test live_session -- --include-ignored --test-threads=1 world_state` returns Ok and records the measured `capture_span` (SC#2), then the VM is torn down.
- SC#2 is best-effort per D-8.2 — the recorded span is evidence, not a hard pass/fail on 500ms.
</verification>

<success_criteria>
- SC#2: `world_state()` returns a screenshot + window list (+ optional UIA) with a measured `capture_span` recorded against a real target; components correlated in one call.
- The default (no-target) test run stays green; the live suite is opt-in via `RDPILOT_LIVE=1`.
- Live test uses only the public API (SC#1 reinforced end-to-end).
</success_criteria>

<output>
Create `.planning/phases/08-public-sdk-api-worldstate/08-03-SUMMARY.md` when done.
</output>
