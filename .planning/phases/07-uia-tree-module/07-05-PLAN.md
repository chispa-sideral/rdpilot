---
phase: 07-uia-tree-module
plan: 05
type: execute
wave: 4
depends_on: [07-01, 07-04]
files_modified:
  - crates/rdpilot/tests/live_session.rs
autonomous: false
requirements: [PERC-03]
user_setup:
  - service: azure
    why: "The end-of-phase live gate provisions a disposable Windows VM to validate all four Phase 7 success criteria against a real Notepad window over RDP+DVC"
    dashboard_config:
      - task: "Provision + tear down the disposable Windows test VM"
        location: "infra/manage-env.ps1 up -VmSize Standard_B2s_v2 / infra/manage-env.ps1 down"
      - task: "AOT-publish the sensor on the VM (WinRM auth fails from this host)"
        location: "az vm run-command invoke (Phase 5/6 proven mechanism)"
must_haves:
  truths:
    - "get_uia_tree(hwnd) for a live Notepad window returns a flat UiaElement[] with id/role/name/bbox/enabled/visible/focusable/focused/depth populated (SC#1)"
    - "UIA bbox coordinates fall in the same physical virtual-desktop pixel space as get_window_list's rect for the same window (SC#2)"
    - "The TreeScope_Children walk completes within 500 ms for the Notepad window (SC#3)"
    - "The returned UiaElement[] round-trips through serde_json without loss (SC#4)"
  artifacts:
    - path: crates/rdpilot/tests/live_session.rs
      provides: "Gated live tests, one per Phase 7 success criterion, mirroring the existing gated-suite pattern"
      contains: "fn uia_tree_returns_populated_elements"
  key_links:
    - from: crates/rdpilot/tests/live_session.rs
      to: crates/rdpilot/src/session.rs
      via: "exercises the public get_uia_tree API against a launched Notepad window"
      pattern: "get_uia_tree"
---

<objective>
Add the four gated live integration tests — one per Phase 7 success criterion — mirroring the existing `live_session.rs` gated-suite pattern, then run the end-of-phase live gate against a real disposable Azure Windows VM to prove all four criteria hold end-to-end (Rust SDK → DVC → C# UIA handler → real Notepad).

This is the phase gate. SC#1 (field population), SC#2 (coordinate alignment vs the window list), and SC#3 (<500ms walk) are live-only, cross-call assertions that cannot be verified offline; SC#4 (round-trip) is already proven offline in 07-01 but is re-asserted live for defense in depth. If the live gate shows the SC#3 500ms budget at risk, this is the ONLY point D-7.7 authorizes reaching for `IUIAutomation::CreateCacheRequest` bulk-cached retrieval.

Purpose: Prove the phase's user-visible outcome — a local caller retrieves the accessibility tree of a program reachable only via RDP — on real hardware, and retire PERC-03.
Output: Four `#[ignore]`'d + `require_target!`-gated tests, and a captured live-gate run (measurements + pass/fail) via the blocking human-verify checkpoint, VM torn down after.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-uia-tree-module/07-RESEARCH.md
@.planning/phases/07-uia-tree-module/07-CONTEXT.md
@.planning/phases/06-window-process-perception/06-05-SUMMARY.md
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/src/session.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add four gated live tests (one per Phase 7 success criterion)</name>
  <files>crates/rdpilot/tests/live_session.rs</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (require_target! macro lines 25-30; the window_list_returns_visible_windows test lines 881-970 and launch_process_appears_in_followup_process_tree ~1165 — the exact deploy_and_launch + get_window_list + launch_process live-test shape to mirror; common::sensor_exe_path / sensor_binary_path usage),
    crates/rdpilot/src/session.rs (get_uia_tree from 07-01; launch_process; get_window_list; desktop_size),
    .planning/phases/07-uia-tree-module/07-RESEARCH.md (Validation Architecture: Phase Requirements → Test Map with the four test names + commands)
  </read_first>
  <action>
    Add four `#[test] #[ignore = "live: ..."]` `require_target!`-gated tests to live_session.rs, each mirroring `window_list_returns_visible_windows`'s setup (assert `sensor_exe_path().exists()`, `Session::connect` with `sensor_binary_path`, `deploy_and_launch().await`). Each test first `launch_process`es Notepad (mirror `launch_process_appears_in_followup_process_tree`), then `get_window_list()`s to find the Notepad window's hwnd + rect (find by non-empty title/class matching Notepad, as the existing tests do), then calls `get_uia_tree(hwnd)`:

    - `uia_tree_returns_populated_elements` (SC#1): assert the returned `Vec<UiaElement>` is non-empty and at least one element has a non-empty `id`, a `role` that is not `"Unknown"`, and a `bbox` with nonzero `w`/`h`; assert `enabled`/`visible`/`focusable`/`focused` are booleans by construction and `depth` includes both 0 (root) and 1 (a child) values (D-7.4).
    - `uia_bbox_shares_window_pixel_space` (SC#2): cross-check that every element's `bbox` lies within (or at) the Notepad window rect from `get_window_list` (same physical virtual-desktop pixel space) and within `desktop_size()` — no element bbox exceeds the desktop bounds; this proves coordinate alignment, not a remap.
    - `uia_tree_walk_within_500ms` (SC#3): wrap the `get_uia_tree(hwnd)` call in `std::time::Instant::now()`/`.elapsed()` and assert the elapsed is `< Duration::from_millis(500)` for the Notepad window.
    - `uia_tree_round_trips_live` (SC#4): `serde_json::to_string` then `from_str::<Vec<UiaElement>>` (or assert via the public shape) the live result and assert equality — defense-in-depth over 07-01's offline round-trip.

    Reuse the existing gated-suite helpers (`common::sensor_exe_path`, `block_on`, `settle`) and the `#[ignore]` + `require_target!` early-return convention verbatim. Do NOT weaken any existing test. These compile offline (behind `#[ignore]`) but only PASS against a live VM.
  </action>
  <acceptance_criteria>
    - live_session.rs contains `fn uia_tree_returns_populated_elements`, `fn uia_bbox_shares_window_pixel_space`, `fn uia_tree_walk_within_500ms`, `fn uia_tree_round_trips_live`, each `#[ignore]`'d + `require_target!`-gated.
    - The SC#3 test wraps `get_uia_tree` in an `Instant`/`elapsed()` timer asserting `< 500ms`.
    - The SC#2 test cross-references the Notepad rect from `get_window_list` against each UIA `bbox`.
    - `cargo test -p rdpilot --test live_session -- --list` shows the four new tests (compiles).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --test live_session -- --list 2>&1 | grep -E "uia_tree_returns_populated_elements|uia_bbox_shares_window_pixel_space|uia_tree_walk_within_500ms|uia_tree_round_trips_live"</automated>
  </verify>
  <done>Four gated live tests exist and compile (offline), one per Phase 7 success criterion, mirroring the established gated-suite pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the end-of-phase live gate against a real Azure VM and tear it down</name>
  <action>Pause for the operator to provision the disposable Azure VM, AOT-publish the sensor win-x64 on it, run the armed Phase 7 gated suite (all four success-criterion tests) against a launched Notepad window, capture per-criterion results and the SC#3 measured walk time, apply D-7.7's CreateCacheRequest optimization and re-run only if SC#3 exceeds 500ms, then tear the VM down.</action>
  <what-built>Four gated live integration tests exercising the full Phase 7 stack (Rust get_uia_tree → DVC → C# UIA handler → FindAll(TreeScope_Children) → real Notepad), plus the end-of-phase live gate: the sensor AOT-published win-x64 on a real disposable Azure VM and run against a launched Notepad window to validate all four success criteria on real hardware.</what-built>
  <how-to-verify>
    1. Provision the VM: `infra/manage-env.ps1 up -VmSize Standard_B2s_v2` (default Standard_B2ms is SkuNotAvailable in westeurope).
    2. AOT-publish the sensor win-x64 ON the VM via `az vm run-command invoke` (WinRM auth fails from this Linux host — Phase 5/6 pattern) and retrieve/point `common::sensor_exe_path()` at the published `rdpilot-sensor.exe`.
    3. Run the gated suite: `RDPILOT_LIVE=1 cargo test -p rdpilot --test live_session -- --ignored --test-threads=1 uia_tree_returns_populated_elements uia_bbox_shares_window_pixel_space uia_tree_walk_within_500ms uia_tree_round_trips_live`.
    4. Confirm all four PASS. Record the SC#3 measured walk time. If SC#3 exceeds 500ms, apply D-7.7's authorized `IUIAutomation::CreateCacheRequest` bulk-cached optimization in sensor/UiaTree.cs, re-publish, re-run (this is the ONLY point that optimization is warranted).
    5. Tear the VM down: `infra/manage-env.ps1 down`, then confirm `az group exists -n rdpilot-test` returns false.
  </how-to-verify>
  <resume-signal>Type "approved" once all four SC pass and the VM is torn down, or describe the failing criterion (and the measured SC#3 time if the walk was slow).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev host → disposable Azure VM | Ephemeral credentials, short-lived blob SAS relay for the AOT artifact |
| Live RDP+DVC session → sensor | The full untrusted transport exercised end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information Disclosure / cost | A left-up VM | medium | mitigate | Blocking human-verify Step 5 requires `manage-env.ps1 down` + `az group exists` false before approval (every prior phase's teardown discipline). |
| T-07-01 | DoS | get_uia_tree round trip under real load | low | mitigate | The transport timeout (ENUMERATION_TIMEOUT_MS) and D-6.4 error branching (from 07-01) surface a slow/failed sensor as a typed error, not a hang; SC#3 explicitly measures the walk budget. |
| T-07-SC | Tampering | Dependency supply chain | low | accept | No new dependency in this plan (test-only Rust code). |
</threat_model>

<verification>
- All four gated live tests PASS against a real disposable Azure VM with a launched Notepad window.
- SC#3 walk time recorded (< 500ms, or CreateCacheRequest applied per D-7.7 and re-verified).
- VM torn down and confirmed absent (`az group exists -n rdpilot-test` == false).
</verification>

<success_criteria>
- SC#1: `get_uia_tree(hwnd)` for Notepad returns a field-complete flat `UiaElement[]`.
- SC#2: UIA bboxes share the framebuffer/window-list physical-pixel space.
- SC#3: the `TreeScope_Children` walk completes within 500ms.
- SC#4: the response round-trips through serde_json without loss (live-confirmed).
- PERC-03 retired.
</success_criteria>

<output>
Create `.planning/phases/07-uia-tree-module/07-05-SUMMARY.md` when done (record the four SC results + the SC#3 measured time).
</output>
