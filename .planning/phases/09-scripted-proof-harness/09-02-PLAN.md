---
phase: 09-scripted-proof-harness
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - crates/rdpilot/tests/support/proof_harness.rs
  - crates/rdpilot/examples/proof_harness.rs
  - crates/rdpilot/tests/live_session.rs
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "run_proof_harness runs the full loop connect->screenshot->launch->get_uia_tree->navigate->verify and returns rdpilot::Result<ProofReport> carrying a step trace + passed flag (D-9.3/D-9.4)"
    - "examples/proof_harness.rs is a main() -> ExitCode binary that shares the harness via #[path] and maps report.passed to a non-zero exit on failure (D-9.4)"
    - "proof_harness_end_to_end is a gated #[ignore] + require_target! test in live_session.rs sharing the SAME harness via #[path]"
    - "The decisive navigate->verify wait is a bounded poll-with-timeout with eprintln progress, never a single fixed sleep (D-9.5)"
  artifacts:
    - path: crates/rdpilot/tests/support/proof_harness.rs
      provides: "The single shared async harness function + ProofReport, consumed by both callers via #[path]"
      contains: "pub async fn run_proof_harness"
    - path: crates/rdpilot/examples/proof_harness.rs
      provides: "The human-facing v1 demo binary: connect, deploy, run harness, print trace, exit code"
      contains: "fn main"
    - path: crates/rdpilot/tests/live_session.rs
      provides: "The gated live end-to-end test calling the shared harness"
      contains: "fn proof_harness_end_to_end"
  key_links:
    - from: crates/rdpilot/examples/proof_harness.rs
      to: crates/rdpilot/tests/support/proof_harness.rs
      via: "#[path = \"../tests/support/proof_harness.rs\"] mod proof_harness;"
      pattern: "path.*support/proof_harness.rs"
    - from: crates/rdpilot/tests/support/proof_harness.rs
      to: crates/rdpilot/src/session.rs
      via: "consumes the public Session API (screenshot/launch_process/get_window_list/get_uia_tree/send_mouse) via rdpilot::"
      pattern: "get_uia_tree"
---

<objective>
Build the phase's only genuinely new code surface: one shared async harness function (D-9.3) that runs the full read/inspect loop and returns a `ProofReport`, called by BOTH a human-facing `examples/proof_harness.rs` binary AND a gated `#[ignore]` test in `tests/live_session.rs`, wired via `#[path]` module declarations. The exact SC#2 element assertions, the D-9.2 navigation target, the D-9.6 seeding decision, and the 7-Zip window match predicate are taken from the recorded 09-01 spike findings — NOT guessed here.

Purpose: Deliver the composition + assertion layer that turns the already-live-verified Session API into a scripted, no-LLM proof of the Core Value narrative ("read/inspect a program only reachable via RDP"). This is pure composition of existing public types — no new SDK API surface (unless 09-01 explicitly flagged a scoped deeper-walk, in which case honor that flag, do not silently expand).
Output: `tests/support/proof_harness.rs` (shared fn + ProofReport), `examples/proof_harness.rs` (main -> ExitCode), and one new gated test in `tests/live_session.rs` — all compiling offline behind `#[ignore]`, ready for the 09-03 live gate.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scripted-proof-harness/09-RESEARCH.md
@.planning/phases/09-scripted-proof-harness/09-CONTEXT.md
@.planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md
@crates/rdpilot/examples/screenshot.rs
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/tests/common/mod.rs
@crates/rdpilot/src/session.rs
@crates/rdpilot/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared harness module (run_proof_harness + ProofReport) with the spike-recorded assertions</name>
  <files>crates/rdpilot/tests/support/proof_harness.rs</files>
  <read_first>
    .planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md (the recorded spike findings — the exact SC#2 elements to assert, the D-9.2 navigation target + verify signal, the D-9.6 seeding decision, the 7-Zip window class/title predicate, and any flagged deeper-walk),
    crates/rdpilot/tests/live_session.rs (launch_notepad_and_find_window ~1222-1242 — the poll idiom to adapt as launch_7zip_and_find_window; settle ~269-273; region_changed/changed_fraction/clamped_rect ~279-325 — the navigate-verify screenshot-diff template; mouse_click_activates_menu ~333-383),
    crates/rdpilot/src/session.rs (screenshot, launch_process, get_window_list, get_uia_tree(hwnd), send_mouse, send_key, desktop_size),
    crates/rdpilot/src/lib.rs (public re-exports: Session, UiaElement, WindowInfo, Rect, MouseAction, Button, KeyAction, Key, Result, Error — the ONLY names available; UiaElementWire is NOT re-exported),
    crates/rdpilot/src/perception.rs (UiaElement field set: id/role/name/bbox/enabled/visible/focusable/focused/depth)
  </read_first>
  <behavior>
    - run_proof_harness executes six ordered steps and records each as a (step_name, pass, detail) entry: (1) screenshot succeeds and has nonzero dims [SC#1]; (2) launch_7zip_and_find_window returns the 7zFM window within the poll budget; (3) get_uia_tree(hwnd) returns a flat UiaElement[] and the spike-recorded named elements are present with nonzero-area bbox [SC#2]; (4) the D-9.2 navigation action (send_mouse/send_key) round-trips [SC#3]; (5) a bounded poll-with-timeout verifies the post-navigation state (screenshot region_changed OR a follow-up get_uia_tree shows the expected new element) [SC#3 verify, D-9.5]; (6) ProofReport assembled with passed = all-steps-passed.
    - A step failure is recorded as pass=false with a detail string and makes ProofReport.passed=false; a hard transport error propagates as Err(rdpilot::Error) (the SDK's own Result — no bespoke error enum, Pitfall 5).
    - The decisive step-5 wait is a bounded attempt loop with eprintln progress (never a lone fixed sleep). An inter-step settle throttle is fine; the CONFIRMATION of the transition is the poll, not a sleep.
  </behavior>
  <action>
    Create `crates/rdpilot/tests/support/proof_harness.rs` as a module reachable by both crate roots via `#[path]` (RESEARCH-recommended location, mirroring the tests/common/ precedent). Its top doc comment MUST warn: this file is compiled TWICE (once in the examples/proof_harness binary crate, once in the live_session test crate); `crate::` does NOT mean the rdpilot library from either site — always import via the external crate name `use rdpilot::{Session, UiaElement, WindowInfo, Rect, MouseAction, Button, ...};` (Pitfall 5, exactly as live_session.rs:21-24 already does).

    Define `pub struct ProofReport { steps: Vec<(&'static str, bool, String)>, passed: bool }` (fields pub or with pub accessors as the callers need). Define `pub async fn run_proof_harness(session: &rdpilot::Session) -> rdpilot::Result<ProofReport>` implementing the six-step behavior above. Also add an internal `async fn launch_7zip_and_find_window(session: &rdpilot::Session) -> ...` adapted from `launch_notepad_and_find_window`: `launch_process("7zFM.exe", <seeding arg per 09-01 decision>, None)` then bounded poll of `get_window_list()` using the SPIKE-RECORDED match predicate (real class_name/title from 09-01-SUMMARY, not a guess — Pitfall 1). For SC#2, assert the specific named elements the 09-01 checkpoint recorded (by name/role) each have a nonzero-area bbox within the window rect; do NOT invent element names not in the spike SUMMARY. For SC#3 default to the mouse-click-on-UIA-bbox navigation the spike chose (click the recorded target element's bbox center via `MouseAction::Click`), and verify via `region_changed` over a `clamped_rect` around the click, OR a follow-up `get_uia_tree` re-query — whichever verify signal the spike recorded — inside a bounded poll (D-9.5). Assemble the stdout-oriented step trace + PASS/FAIL into ProofReport (D-9.4: no JSON schema, no report file). This module is a test/example-support crate root member, OUTSIDE the library's `#![deny(expect_used)]` scope, so `.expect()` is permitted; keep the RETURNED error type the SDK's own `rdpilot::Result`. If 09-01 flagged a scoped deeper-walk as required, implement ONLY that flagged addition and note it explicitly in the SUMMARY.
  </action>
  <acceptance_criteria>
    - crates/rdpilot/tests/support/proof_harness.rs exists and contains `pub async fn run_proof_harness` returning `rdpilot::Result<ProofReport>` and a `pub struct ProofReport`.
    - The module imports the library via `rdpilot::` (grep shows `use rdpilot::`), never `crate::Session`.
    - The SC#2 assertions reference the exact element names/roles recorded in 09-01-SUMMARY.md (not placeholder names).
    - The navigate->verify step is a bounded poll loop (grep shows an attempt-bounded loop with eprintln progress), not a lone `tokio::time::sleep` used as the sole transition confirmation.
    - `cargo build -p rdpilot --examples --tests` compiles once Tasks 2 and 3 wire it (the module has no independent target).
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/rdpilot/tests/support/proof_harness.rs && grep -q 'pub async fn run_proof_harness' crates/rdpilot/tests/support/proof_harness.rs && grep -q 'use rdpilot::' crates/rdpilot/tests/support/proof_harness.rs && ! grep -q 'crate::Session' crates/rdpilot/tests/support/proof_harness.rs && echo OK</automated>
  </verify>
  <done>The shared harness module exists with run_proof_harness + ProofReport, imports via rdpilot::, asserts the spike-recorded SC#2 elements, and confirms the navigation with a bounded poll (D-9.5).</done>
</task>

<task type="auto">
  <name>Task 2: Create examples/proof_harness.rs — the human-facing v1 demo binary</name>
  <files>crates/rdpilot/examples/proof_harness.rs</files>
  <read_first>
    crates/rdpilot/examples/screenshot.rs (the exact template: main() -> ExitCode, current-thread tokio runtime, local load_config() reading .secrets/connection.json, password-never-printed doc discipline, String-mapped errors),
    crates/rdpilot/tests/support/proof_harness.rs (the run_proof_harness signature + ProofReport shape from Task 1),
    crates/rdpilot/tests/common/mod.rs (sensor_exe_path ~line 127 — reimplement its default/RDPILOT_SENSOR_EXE logic locally, since examples/ cannot `mod common;`)
  </read_first>
  <action>
    Create `crates/rdpilot/examples/proof_harness.rs` modeled on examples/screenshot.rs, reading like a narrated walkthrough of the Core Value narrative (it is the v1 "proof it works" deliverable). Declare the shared module with `#[path = "../tests/support/proof_harness.rs"] mod proof_harness;`. In `main() -> ExitCode`: build a current-thread tokio runtime (copy screenshot.rs), `block_on` an async `run()` that: loads config via a local `load_config()` (copied from screenshot.rs), resolves the sensor exe path locally (mirror `common::sensor_exe_path`'s default + `RDPILOT_SENSOR_EXE` override, since `mod common;` is unreachable from an example crate root — Pitfall 6), sets `sensor_binary_path`, `Session::connect`, `deploy_and_launch`, then `proof_harness::run_proof_harness(&session).await`. Print the ProofReport's step trace to stdout as a step-by-step PASS/FAIL narration ending in a single clear `PROOF: PASS`/`PROOF: FAIL` summary line (D-9.4), `session.close()`, and map `report.passed` to `ExitCode::SUCCESS`/`ExitCode::FAILURE`. Map any `rdpilot::Error` to a `String` for a clean `eprintln!` (screenshot.rs's `Result<(), String>` idiom) — and NEVER print the password (carry screenshot.rs's Security doc comment verbatim).
  </action>
  <acceptance_criteria>
    - crates/rdpilot/examples/proof_harness.rs exists with `fn main() -> ExitCode` and `#[path = "../tests/support/proof_harness.rs"] mod proof_harness;`.
    - It calls `proof_harness::run_proof_harness` and maps `report.passed` to a non-zero ExitCode on failure.
    - It prints a step trace ending in a single PASS/FAIL summary line and never prints the password.
    - `cargo build -p rdpilot --example proof_harness` compiles clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p rdpilot --example proof_harness 2>&1 | tail -n 5</automated>
  </verify>
  <done>The example binary compiles, shares the harness via #[path], runs the full loop, prints a stdout PASS/FAIL report, and returns a non-zero exit on failure (D-9.4).</done>
</task>

<task type="auto">
  <name>Task 3: Append the gated proof_harness_end_to_end live test to live_session.rs</name>
  <files>crates/rdpilot/tests/live_session.rs</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (require_target! macro ~25-42; the uia_tree_returns_populated_elements preamble ~1252-1262 — sensor_exe_path().exists() precondition + sensor_binary_path + connect + deploy_and_launch + block_on),
    crates/rdpilot/tests/support/proof_harness.rs (run_proof_harness signature + ProofReport from Task 1),
    crates/rdpilot/tests/common/mod.rs (sensor_exe_path, load_config gating)
  </read_first>
  <action>
    Append ONE new gated test `proof_harness_end_to_end` to `crates/rdpilot/tests/live_session.rs`, and add the shared-module decl `#[path = "support/proof_harness.rs"] mod proof_harness;` near the file's existing `mod common;` (the `support/` relative path resolves from the tests/ crate root). Mirror the `uia_tree_returns_populated_elements` preamble verbatim: `#[test] #[ignore = "live: requires a provisioned RDP target + published rdpilot-sensor.exe (RDPILOT_LIVE=1)"]`, `let Some(cfg) = require_target!("proof_harness_end_to_end") else { return };`, assert `common::sensor_exe_path().exists()` with the same publish-hint message, `cfg.sensor_binary_path(sensor_exe)`, then `block_on(async { Session::connect -> deploy_and_launch -> let report = proof_harness::run_proof_harness(&session).await.expect("harness"); assert!(report.passed, "proof harness failed: {:?}", report.steps); session.close().await.expect("close"); })`. Do NOT weaken any existing test. This compiles offline behind `#[ignore]` and only PASSes against a live VM.
  </action>
  <acceptance_criteria>
    - live_session.rs contains `fn proof_harness_end_to_end`, `#[ignore]`'d + `require_target!`-gated, and `#[path = "support/proof_harness.rs"] mod proof_harness;`.
    - The test asserts `report.passed` on the shared harness's returned ProofReport.
    - No existing test is modified or weakened.
    - `cargo test -p rdpilot --test live_session -- --list` shows `proof_harness_end_to_end` (compiles).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --test live_session -- --list 2>&1 | grep -q 'proof_harness_end_to_end' && echo OK</automated>
  </verify>
  <done>A single gated #[ignore] live test exists, shares the harness via #[path], asserts report.passed, and compiles offline — ready for the 09-03 live gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK consumer (harness) -> public Session API | The harness composes only owned public types; it never touches the DVC wire or ironrdp internals (D-09) |
| examples/proof_harness.rs -> .secrets/connection.json | The example reads gitignored credentials and must never echo the password |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Credential leak via the example's stdout trace | high | mitigate | The example carries screenshot.rs's Security doc discipline verbatim; ConnectionConfig Debug redacts the password (config.rs); acceptance criteria assert the password is never printed. |
| T-09-05 | Tampering (of scope) | Harness silently expanding the locked public API (D-09) | medium | mitigate | The shared fn lives in tests/support/ (not src/) and consumes only re-exported owned types; any new capability is limited to a 09-01-flagged scoped deeper-walk, never a silent addition. |
| T-09-06 | Denial of Service | A blind fixed sleep masking a never-completing navigate->verify | low | mitigate | D-9.5 bounded poll-with-timeout with eprintln progress is required and grep-checked; no lone fixed sleep confirms the transition. |
| T-09-SC | Tampering | Dependency supply chain | low | accept | Zero new packages — reuses tokio/serial_test dev-deps and the existing crate graph (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo build -p rdpilot --examples --tests` compiles clean (the shared module, the example, and the gated test all build offline).
- `cargo test -p rdpilot --test live_session -- --list` lists `proof_harness_end_to_end`.
- The shared module imports via `rdpilot::` (not `crate::`) and the navigate-verify step is a bounded poll (D-9.5).
</verification>

<success_criteria>
- One shared `run_proof_harness` function is called by both the example binary and the gated test via `#[path]` (D-9.3).
- The harness asserts the spike-recorded SC#2 elements and performs the spike-chosen SC#3 navigation + bounded-poll verify (D-9.2/D-9.5).
- The example emits a stdout-only PASS/FAIL report with a non-zero exit on failure (D-9.4); all offline compilation is green.
</success_criteria>

<output>
Create `.planning/phases/09-scripted-proof-harness/09-02-SUMMARY.md` when done (record the chosen module location, the SC#2 elements asserted, the SC#3 navigation + verify signal, and any 09-01-flagged deeper-walk implemented).
</output>
