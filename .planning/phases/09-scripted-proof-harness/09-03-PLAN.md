---
phase: 09-scripted-proof-harness
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - crates/rdpilot/tests/support/proof_harness.rs
  - crates/rdpilot/examples/proof_harness.rs
  - crates/rdpilot/tests/live_session.rs
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "run_proof_harness runs the full loop connect->screenshot->launch->find-7zip-window->get_uia_tree(deeper)->navigate->verify and returns rdpilot::Result<ProofReport> with a step trace + passed flag (D-9.3/D-9.4)"
    - "SC#2 asserts REAL deeper 7-Zip elements now reachable via the 09-02 UiaScope::Subtree walk (menu items from File/Edit/View/Favorites/Tools/Help, and/or the Add/Extract/Test/Copy/Move/Delete/Info toolbar buttons, and/or listview rows from the seeded C:\\Program Files listing) — each with a nonzero-area bbox; NOT just the 5 depth-1 containers"
    - "The 7-Zip window is matched by exact class_name == \"7-Zip::FM\" (never a title substring); launch uses the full quoted exe path + quoted seed arg per the 09-01 decision (D-9.6)"
    - "examples/proof_harness.rs is a main() -> ExitCode binary sharing the harness via #[path], printing a stdout PASS/FAIL trace and mapping report.passed to a non-zero exit (D-9.4)"
    - "proof_harness_end_to_end is a gated #[ignore] + require_target! test in live_session.rs sharing the SAME harness via #[path]; the navigate->verify wait is a bounded poll-with-timeout, never a lone fixed sleep (D-9.5)"
  artifacts:
    - path: crates/rdpilot/tests/support/proof_harness.rs
      provides: "The single shared async harness fn + ProofReport, consumed by both callers via #[path]"
      contains: "pub async fn run_proof_harness"
    - path: crates/rdpilot/examples/proof_harness.rs
      provides: "The human-facing v1 demo binary: connect, deploy, run harness, print trace, exit code"
      contains: "fn main"
    - path: crates/rdpilot/tests/live_session.rs
      provides: "The gated live end-to-end test calling the shared harness"
      contains: "fn proof_harness_end_to_end"
  key_links:
    - from: crates/rdpilot/examples/proof_harness.rs
      to: crates/rdpilot/tests/support/proof_harness.rs
      via: "#[path = \"../tests/support/proof_harness.rs\"] mod proof_harness;"
      pattern: "path.*support/proof_harness.rs"
    - from: crates/rdpilot/tests/support/proof_harness.rs
      to: crates/rdpilot/src/session.rs
      via: "consumes get_uia_tree(hwnd, UiaScope::Subtree{..}) + screenshot/launch_process/get_window_list/send_mouse via rdpilot::"
      pattern: "UiaScope"
---

<objective>
Build the phase's composition + assertion layer: ONE shared async harness function (D-9.3) that runs the full read/inspect loop and returns a `ProofReport`, called by BOTH a human-facing `examples/proof_harness.rs` binary AND a gated `#[ignore]` test in `tests/live_session.rs`, wired via `#[path]`. Unlike the original composition-only plan, this now consumes the 09-02 deeper-walk capability: SC#2 asserts the REAL 7-Zip elements that were one level below `TreeScope_Children` (the menu items, toolbar buttons, and listview rows the 09-01 spike proved absent from the depth-1 dump), and SC#3 navigates one of those deeper elements. The 7-Zip window predicate (`class_name == "7-Zip::FM"`) and the D-9.6 seeding form (full quoted `7zFM.exe` path + quoted seed arg) are taken verbatim from the recorded 09-01 findings — NOT guessed here.

Purpose: Deliver the scripted, no-LLM proof of the Core Value narrative ("read/inspect a program only reachable via RDP") using the now-capable SDK. This is composition + assertion over public types plus the 09-02 `UiaScope` control — no NEW SDK API surface is added here.
Output: `tests/support/proof_harness.rs` (shared fn + ProofReport), `examples/proof_harness.rs` (main -> ExitCode), and one new gated test in `tests/live_session.rs` — all compiling offline behind `#[ignore]`, ready for the 09-04 live gate.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scripted-proof-harness/09-RESEARCH.md
@.planning/phases/09-scripted-proof-harness/09-CONTEXT.md
@.planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md
@.planning/phases/09-scripted-proof-harness/09-02-SUMMARY.md
@crates/rdpilot/examples/screenshot.rs
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/tests/common/mod.rs
@crates/rdpilot/src/session.rs
@crates/rdpilot/src/lib.rs
@crates/rdpilot/src/perception.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared harness module (run_proof_harness + ProofReport) asserting the real deeper 7-Zip elements</name>
  <files>crates/rdpilot/tests/support/proof_harness.rs</files>
  <read_first>
    .planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md (the recorded spike findings: the 5 depth-1 containers and the specific deeper elements that were ABSENT — menu items File/Edit/View/Favorites/Tools/Help, toolbar buttons Add/Extract/Test/Copy/Move/Delete/Info, the 17-row C:\Program Files listview; the exact class_name == "7-Zip::FM" predicate; the D-9.6 seeding form exe="\"C:\\Program Files\\7-Zip\\7zFM.exe\"" args=Some("\"C:\\Program Files\"")),
    .planning/phases/09-scripted-proof-harness/09-02-SUMMARY.md (the final UiaScope shape + the chosen max_depth needed to reach the deeper elements + the UIA_MAX_WALK_DEPTH cap),
    crates/rdpilot/tests/live_session.rs (launch_notepad_and_find_window ~1222-1242 — the poll idiom to adapt as launch_7zip_and_find_window; SETTLE ~269-273; region_changed/changed_fraction/clamped_rect ~279-325 — the navigate-verify screenshot-diff template; mouse_click_activates_menu ~333-383),
    crates/rdpilot/src/session.rs (screenshot, launch_process, get_window_list, get_uia_tree(hwnd, scope), send_mouse, send_key, desktop_size),
    crates/rdpilot/src/lib.rs (public re-exports incl. the new UiaScope — the ONLY names available; UiaElementWire is NOT re-exported),
    crates/rdpilot/src/perception.rs (UiaElement field set: id/role/name/bbox/enabled/visible/focusable/focused/depth/parent_id; UiaScope::Subtree{max_depth})
  </read_first>
  <behavior>
    - run_proof_harness executes six ordered steps recorded as (step_name, pass, detail) entries: (1) screenshot succeeds with nonzero dims [SC#1]; (2) launch_7zip_and_find_window returns the "7-Zip::FM" window within the poll budget; (3) get_uia_tree(hwnd, UiaScope::Subtree { max_depth: <from 09-02 SUMMARY> }) returns a flat UiaElement[] and the spike-recorded DEEPER named elements are present with nonzero-area bbox inside the window rect [SC#2]; (4) the D-9.2 navigation action round-trips [SC#3]; (5) a bounded poll-with-timeout verifies the post-navigation state (screenshot region_changed over a clamped_rect around the click, OR a follow-up get_uia_tree showing the expected new element) [SC#3 verify, D-9.5]; (6) ProofReport assembled with passed = all-steps-passed.
    - A step failure is recorded pass=false with a detail string, making ProofReport.passed=false; a hard transport error propagates as Err(rdpilot::Error) (the SDK's own Result — no bespoke error enum, Pitfall 5).
    - The step-5 wait is a bounded attempt loop with eprintln progress (never a lone fixed sleep); an inter-step SETTLE throttle is fine, but the CONFIRMATION of the transition is the poll.
  </behavior>
  <action>
    Create `crates/rdpilot/tests/support/proof_harness.rs` reachable by both crate roots via `#[path]`. Its top doc comment MUST warn: this file is compiled TWICE (in the examples/proof_harness binary crate and the live_session test crate); `crate::` does NOT mean the rdpilot library from either site — always import via `use rdpilot::{Session, UiaElement, UiaScope, WindowInfo, Rect, MouseAction, Button, ...};` (Pitfall 5, exactly as live_session.rs already does).

    Define `pub struct ProofReport { steps: Vec<(&'static str, bool, String)>, passed: bool }` (fields pub or with pub accessors). Define `pub async fn run_proof_harness(session: &rdpilot::Session) -> rdpilot::Result<ProofReport>` implementing the six-step behavior. Add an internal `async fn launch_7zip_and_find_window(session) -> WindowInfo` adapted from launch_notepad_and_find_window: call `launch_process` with the EXACT 09-01 seeding form (full quoted exe path `"\"C:\\Program Files\\7-Zip\\7zFM.exe\""` + `Some("\"C:\\Program Files\"")` arg + None cwd), then bounded-poll get_window_list matching EXACTLY `w.class_name == "7-Zip::FM"` (exact match, NOT a title substring — the title reflects the navigated folder per 09-01). For SC#2, call get_uia_tree with `UiaScope::Subtree { max_depth: <the value 09-02-SUMMARY records as sufficient to reach the deeper elements> }` and assert the spike-recorded DEEPER elements are present by name/role with nonzero-area bbox within the window rect — assert on elements that were ABSENT from the depth-1 dump (e.g. a File/Edit/View MenuItem, and/or an Add/Extract toolbar Button, and/or a listview row), NOT the depth-1 ToolBar/Pane/MenuBar containers. Do NOT invent element names not observable in the seeded 7-Zip. For SC#3 default to a mouse-click on a chosen deeper element's bbox center via MouseAction::Click, and verify via region_changed over a clamped_rect around the click OR a follow-up get_uia_tree re-query, inside a bounded poll (D-9.5). Assemble the stdout-oriented step trace + PASS/FAIL into ProofReport (D-9.4: no JSON schema, no report file). This module is a test/example-support crate root member, OUTSIDE the library's expect_used deny scope, so `.expect()` is permitted; keep the RETURNED error type the SDK's own `rdpilot::Result`.
  </action>
  <acceptance_criteria>
    - crates/rdpilot/tests/support/proof_harness.rs exists with `pub async fn run_proof_harness` returning `rdpilot::Result<ProofReport>` and a `pub struct ProofReport`.
    - It imports the library via `rdpilot::` (grep shows `use rdpilot::`), never `crate::Session`.
    - It calls get_uia_tree with `UiaScope::Subtree` (grep shows `UiaScope::Subtree`) — the deeper walk, not children-only.
    - The 7-Zip window match is the exact `class_name == "7-Zip::FM"` predicate (grep shows `"7-Zip::FM"`), and the launch uses the full quoted 7zFM.exe path (grep shows `7-Zip\\7zFM.exe`).
    - The SC#2 assertions reference deeper element names/roles recorded in 09-01/09-02 SUMMARYs (menu item / toolbar button / listview row), not the depth-1 containers or placeholder names.
    - The navigate->verify step is a bounded poll loop (grep shows an attempt-bounded loop with eprintln progress), not a lone `tokio::time::sleep` as the sole transition confirmation.
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/rdpilot/tests/support/proof_harness.rs && grep -q 'pub async fn run_proof_harness' crates/rdpilot/tests/support/proof_harness.rs && grep -q 'use rdpilot::' crates/rdpilot/tests/support/proof_harness.rs && ! grep -q 'crate::Session' crates/rdpilot/tests/support/proof_harness.rs && grep -q 'UiaScope::Subtree' crates/rdpilot/tests/support/proof_harness.rs && grep -q '7-Zip::FM' crates/rdpilot/tests/support/proof_harness.rs && echo OK</automated>
  </verify>
  <done>The shared harness module exists with run_proof_harness + ProofReport, imports via rdpilot::, asserts the spike-recorded DEEPER 7-Zip elements via UiaScope::Subtree, matches the exact "7-Zip::FM" window with the full quoted seeding form, and confirms navigation with a bounded poll (D-9.5).</done>
</task>

<task type="auto">
  <name>Task 2: Create examples/proof_harness.rs — the human-facing v1 demo binary</name>
  <files>crates/rdpilot/examples/proof_harness.rs</files>
  <read_first>
    crates/rdpilot/examples/screenshot.rs (the exact template: main() -> ExitCode, current-thread tokio runtime, local load_config() reading .secrets/connection.json, password-never-printed doc discipline, String-mapped errors),
    crates/rdpilot/tests/support/proof_harness.rs (the run_proof_harness signature + ProofReport shape from Task 1),
    crates/rdpilot/tests/common/mod.rs (sensor_exe_path ~line 127 — reimplement its default/RDPILOT_SENSOR_EXE logic locally, since examples/ cannot `mod common;`)
  </read_first>
  <action>
    Create `crates/rdpilot/examples/proof_harness.rs` modeled on examples/screenshot.rs, reading like a narrated walkthrough of the Core Value narrative (the v1 "proof it works" deliverable). Declare the shared module with `#[path = "../tests/support/proof_harness.rs"] mod proof_harness;`. In `main() -> ExitCode`: build a current-thread tokio runtime (copy screenshot.rs), `block_on` an async `run()` that loads config via a local `load_config()` (copied from screenshot.rs), resolves the sensor exe path locally (mirror `common::sensor_exe_path`'s default + `RDPILOT_SENSOR_EXE` override, since `mod common;` is unreachable from an example crate root — Pitfall 6), sets `sensor_binary_path`, `Session::connect`, `deploy_and_launch`, then `proof_harness::run_proof_harness(&session).await`. Print the ProofReport step trace to stdout as a step-by-step PASS/FAIL narration ending in a single clear `PROOF: PASS`/`PROOF: FAIL` summary line (D-9.4), `session.close()`, and map `report.passed` to `ExitCode::SUCCESS`/`ExitCode::FAILURE`. Map any `rdpilot::Error` to a `String` for a clean `eprintln!` (screenshot.rs's `Result<(), String>` idiom) — and NEVER print the password (carry screenshot.rs's Security doc comment verbatim).
  </action>
  <acceptance_criteria>
    - crates/rdpilot/examples/proof_harness.rs exists with `fn main() -> ExitCode` and `#[path = "../tests/support/proof_harness.rs"] mod proof_harness;`.
    - It calls `proof_harness::run_proof_harness` and maps `report.passed` to a non-zero ExitCode on failure.
    - It prints a step trace ending in a single PASS/FAIL summary line and never prints the password.
    - `cargo build -p rdpilot --example proof_harness` compiles clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p rdpilot --example proof_harness 2>&1 | tail -n 5</automated>
  </verify>
  <done>The example binary compiles, shares the harness via #[path], runs the full loop, prints a stdout PASS/FAIL report, and returns a non-zero exit on failure (D-9.4).</done>
</task>

<task type="auto">
  <name>Task 3: Append the gated proof_harness_end_to_end live test to live_session.rs</name>
  <files>crates/rdpilot/tests/live_session.rs</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (require_target! macro ~25-42; the uia_tree_returns_populated_elements preamble ~1252-1262 — sensor_exe_path().exists() precondition + sensor_binary_path + connect + deploy_and_launch + block_on; note these callers were migrated to UiaScope::Children in 09-02 — do not disturb them),
    crates/rdpilot/tests/support/proof_harness.rs (run_proof_harness signature + ProofReport from Task 1),
    crates/rdpilot/tests/common/mod.rs (sensor_exe_path, load_config gating)
  </read_first>
  <action>
    Append ONE new gated test `proof_harness_end_to_end` to `crates/rdpilot/tests/live_session.rs`, and add the shared-module decl `#[path = "support/proof_harness.rs"] mod proof_harness;` near the file's existing `mod common;`. Mirror the `uia_tree_returns_populated_elements` preamble verbatim: `#[test] #[ignore = "live: requires a provisioned RDP target + published rdpilot-sensor.exe (RDPILOT_LIVE=1)"]`, `let Some(cfg) = require_target!("proof_harness_end_to_end") else { return };`, assert `common::sensor_exe_path().exists()` with the same publish-hint message, `cfg.sensor_binary_path(sensor_exe)`, then `block_on(async { Session::connect -> deploy_and_launch -> let report = proof_harness::run_proof_harness(&session).await.expect("harness"); assert!(report.passed, "proof harness failed: {:?}", report.steps); session.close().await.expect("close"); })`. Do NOT weaken any existing test. This compiles offline behind `#[ignore]` and only PASSes against a live VM running a sensor that has the 09-02 deeper walk.
  </action>
  <acceptance_criteria>
    - live_session.rs contains `fn proof_harness_end_to_end`, `#[ignore]`'d + `require_target!`-gated, and `#[path = "support/proof_harness.rs"] mod proof_harness;`.
    - The test asserts `report.passed` on the shared harness's returned ProofReport.
    - No existing test is modified or weakened (the 09-02 UiaScope::Children migrations stay intact).
    - `cargo test -p rdpilot --test live_session -- --list` shows `proof_harness_end_to_end` (compiles).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --test live_session -- --list 2>&1 | grep -q 'proof_harness_end_to_end' && echo OK</automated>
  </verify>
  <done>A single gated #[ignore] live test exists, shares the harness via #[path], asserts report.passed, and compiles offline — ready for the 09-04 live gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK consumer (harness) -> public Session API | The harness composes only owned public types incl. the 09-02 UiaScope; it never touches the DVC wire or ironrdp internals (D-09) |
| examples/proof_harness.rs -> .secrets/connection.json | The example reads gitignored credentials and must never echo the password |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Credential leak via the example's stdout trace | high | mitigate | The example carries screenshot.rs's Security doc discipline verbatim; ConnectionConfig Debug redacts the password (config.rs); acceptance criteria assert the password is never printed. |
| T-09-05 | Tampering (of scope) | Harness silently expanding the public API | medium | mitigate | The shared fn lives in tests/support/ (not src/) and consumes only re-exported owned types incl. UiaScope; no NEW SDK surface is added here (the deeper-walk capability was the flagged 09-02 addition). |
| T-09-06 | Denial of Service | A blind fixed sleep masking a never-completing navigate->verify | low | mitigate | D-9.5 bounded poll-with-timeout with eprintln progress is required and grep-checked; no lone fixed sleep confirms the transition. |
| T-09-SC | Tampering | Dependency supply chain | low | accept | Zero new packages — reuses tokio/serial_test dev-deps and the existing crate graph (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo build -p rdpilot --examples --tests` compiles clean (shared module + example + gated test all build offline).
- `cargo test -p rdpilot --test live_session -- --list` lists `proof_harness_end_to_end`.
- The shared module imports via `rdpilot::` (not `crate::`), asserts deeper elements via `UiaScope::Subtree`, matches `"7-Zip::FM"` exactly, and the navigate-verify step is a bounded poll (D-9.5).
</verification>

<success_criteria>
- One shared `run_proof_harness` function is called by both the example binary and the gated test via `#[path]` (D-9.3).
- The harness asserts the spike-recorded DEEPER SC#2 elements via the 09-02 UiaScope::Subtree walk and performs the spike-chosen SC#3 navigation + bounded-poll verify (D-9.2/D-9.5), using the exact "7-Zip::FM" predicate + D-9.6 seeding.
- The example emits a stdout-only PASS/FAIL report with a non-zero exit on failure (D-9.4); all offline compilation is green.
</success_criteria>

<output>
Create `.planning/phases/09-scripted-proof-harness/09-03-SUMMARY.md` when done (record the module location, the exact deeper SC#2 elements asserted + the max_depth used, the SC#3 navigation + verify signal, and the confirmed "7-Zip::FM" predicate + seeding form).
</output>
