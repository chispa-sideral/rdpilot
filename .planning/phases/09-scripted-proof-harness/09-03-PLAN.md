---
phase: 09-scripted-proof-harness
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: []
autonomous: false
requirements: [PROOF-01]
user_setup:
  - service: azure
    why: "The terminal v1 live gate must prove all four PROOF-01 success criteria against a REAL remote-only Windows program (7-Zip) over RDP on a genuine disposable Azure VM — parity with every Phase 2-8 live gate; there is no offline substitute"
    dashboard_config:
      - task: "Provision + tear down the disposable Windows test VM"
        location: "infra/manage-env.ps1 up -VmSize Standard_B2s_v2 / infra/manage-env.ps1 down"
      - task: "AOT-publish the sensor on the VM (WinRM auth fails from this host)"
        location: "az vm run-command invoke (Phase 5/6/7 proven mechanism)"
must_haves:
  truths:
    - "SC#1: the harness connects, authenticates, and produces a screenshot of the REAL 7-Zip File Manager (a remote-only program, not a toy/localhost)"
    - "SC#2: the harness reads 7-Zip's UIA tree and asserts specific named elements are present with valid bounding boxes"
    - "SC#3: the harness injects a navigation action and verifies the result via a follow-up screenshot or UIA query"
    - "SC#4: the full loop completes and exits with a documented stdout PASS/FAIL report + a correct exit code"
  artifacts:
    - path: .planning/phases/09-scripted-proof-harness/09-03-SUMMARY.md
      provides: "The captured SC#1-4 live-gate results retiring PROOF-01 and closing the v1 milestone"
      contains: "PROOF-01"
  key_links:
    - from: crates/rdpilot/tests/live_session.rs
      to: crates/rdpilot/tests/support/proof_harness.rs
      via: "the armed proof_harness_end_to_end test drives run_proof_harness against a live 7-Zip window on a real VM"
      pattern: "proof_harness_end_to_end"
---

<objective>
Run the TERMINAL v1 live gate: provision a real disposable Azure VM, AOT-publish the sensor win-x64 on it, and prove all four PROOF-01 success criteria end-to-end against the REAL 7-Zip File Manager over RDP — running BOTH the armed gated test (`proof_harness_end_to_end`) and the human-facing `examples/proof_harness.rs` binary (to capture SC#4's stdout report + exit code). This is the final plan of the final v1 phase; its PASS closes the milestone.

This gate is inherently live — every one of PROOF-01's four criteria is a cross-call assertion against a real remote Windows program that cannot be verified offline (RESEARCH Pitfall 7; this dev host is Linux). The code is complete from 09-02; this plan is purely the live proof + teardown, in parity with the Phase 2-8 end-of-phase live gates.

Purpose: Produce the SC#1-4 pass/fail proof that rdpilot's full read/inspect loop works against a real remote-only Windows program with no live LLM — the v1 done signal — and retire PROOF-01.
Output: A captured live-gate run (both the gated test result and the example binary's stdout report + exit code), recorded per-criterion results in the SUMMARY, and the VM torn down.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scripted-proof-harness/09-RESEARCH.md
@.planning/phases/09-scripted-proof-harness/09-CONTEXT.md
@.planning/phases/09-scripted-proof-harness/09-01-SUMMARY.md
@.planning/phases/09-scripted-proof-harness/09-02-SUMMARY.md
@.planning/phases/07-uia-tree-module/07-05-PLAN.md
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/examples/proof_harness.rs
@infra/manage-env.ps1
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the terminal PROOF-01 live gate against a real Azure VM (test + example), then tear it down</name>
  <action>Pause for the operator to provision the disposable Azure VM, AOT-publish the sensor win-x64 on it, run the armed `proof_harness_end_to_end` gated test AND the `examples/proof_harness` binary against the real 7-Zip File Manager, capture the four success-criterion results plus the example's stdout report + exit code, then tear the VM down before the phase is considered done.</action>
  <what-built>The complete scripted, no-LLM proof harness (09-02): one shared run_proof_harness driving the full loop connect -> screenshot -> get_window_list -> get_uia_tree -> navigate -> verify against the REAL 7-Zip File Manager over RDP, exposed as both a gated live test and a human-facing example binary with a stdout PASS/FAIL report + exit code. This gate publishes the sensor win-x64 on a real disposable Azure VM and runs the harness end-to-end to prove all four PROOF-01 criteria on real hardware — the v1 milestone done signal.</what-built>
  <how-to-verify>
    1. Provision the VM: `infra/manage-env.ps1 up -VmSize Standard_B2s_v2` (default Standard_B2ms is SkuNotAvailable in westeurope).
    2. AOT-publish the sensor win-x64 ON the VM via `az vm run-command invoke` (WinRM auth fails from this Linux host — Phase 5/6/7 pattern), retrieve the byte-identical binary, and point `common::sensor_exe_path()` (or `RDPILOT_SENSOR_EXE`) at it.
    3. Run the armed gated test: `RDPILOT_LIVE=1 cargo test -p rdpilot --test live_session -- --ignored --test-threads=1 proof_harness_end_to_end`. Confirm it PASSes.
    4. Run the human-facing binary: `RDPILOT_LIVE=1 cargo run -p rdpilot --example proof_harness`. Read the stdout step trace, confirm the final `PROOF: PASS` summary line, and confirm the process exit code is 0 (and would be non-zero on failure) — this is SC#4's documented report + exit code.
    5. Record per-criterion results in the SUMMARY: SC#1 (7-Zip screenshot produced), SC#2 (named elements + valid bboxes asserted — name which elements), SC#3 (navigation action + verify signal observed), SC#4 (full loop + PASS/FAIL report + exit code). If any criterion fails, capture the failing step's detail from ProofReport.steps and iterate (a first-RDP-login `deploy_and_launch` transient is a known, documented condition that self-resolves on retry — Phases 7/8).
    6. Tear the VM down: `infra/manage-env.ps1 down`, then confirm `az group exists -n rdpilot-test` returns false.
  </how-to-verify>
  <resume-signal>Type "approved" once all four PROOF-01 criteria pass (gated test PASS + example `PROOF: PASS` with exit code 0) and the VM is torn down — or describe the failing criterion and the ProofReport step detail.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev host -> disposable Azure VM | Ephemeral credentials + short-lived blob SAS relay for the AOT sensor artifact |
| Live RDP+DVC session -> sensor -> real 7-Zip | The full untrusted transport exercised end-to-end against a real remote-only program |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Information Disclosure / cost | A left-up disposable VM after the terminal gate | medium | mitigate | Blocking human-verify Step 6 requires `manage-env.ps1 down` + `az group exists -n rdpilot-test` false before approval (every prior phase's teardown discipline). |
| T-09-08 | Information Disclosure | Credential leak in the example/test live output | high | mitigate | Both callers inherit the password-redaction discipline from 09-02 (ConnectionConfig Debug redacts); the live run's captured output must contain no plaintext password. |
| T-09-09 | Tampering | Byte-integrity of the VM-built sensor relayed back | low | mitigate | The Phase 7 SHA256-verified byte-identical relay pattern is reused; the published binary is confirmed identical VM-built vs retrieved before the gate run. |
| T-09-SC | Tampering | Dependency supply chain | low | accept | Zero new packages introduced in this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- The armed `proof_harness_end_to_end` gated test PASSes against a real disposable Azure VM running the real 7-Zip File Manager.
- The `examples/proof_harness` binary prints a `PROOF: PASS` stdout report and exits 0 (SC#4).
- All four PROOF-01 success criteria are recorded as passing in 09-03-SUMMARY.md; PROOF-01 retired.
- VM torn down and confirmed absent (`az group exists -n rdpilot-test` == false).
</verification>

<success_criteria>
- SC#1: connect + authenticate + screenshot of the real 7-Zip File Manager (remote-only program).
- SC#2: 7-Zip's UIA tree read with specific named elements asserted present + valid bounding boxes.
- SC#3: a navigation action injected and its result verified via follow-up screenshot or UIA query.
- SC#4: the full loop completes and exits with a documented stdout PASS/FAIL report + correct exit code.
- PROOF-01 retired; the v1 milestone is proven end-to-end with no live LLM.
</success_criteria>

<output>
Create `.planning/phases/09-scripted-proof-harness/09-03-SUMMARY.md` when done (record the four SC results, which SC#2 elements were asserted, the SC#3 navigation + verify observed, the example's exit code, and the VM teardown confirmation).
</output>
