---
phase: 01-test-environment
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - infra/main.bicep
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "A single main.bicep declares network + VM + NSG + a CustomScriptExtension and compiles clean"
    - "The VM uses WS2022 Datacenter Gen2 Desktop, Standard_B2ms (param-overridable), Standard/Static public IP"
    - "RDP 3389 and WinRM 5986 are open ONLY from the developer's IP param; never 0.0.0.0/0"
    - "adminPassword is a @secure() param with no default; the CSE invokes Configure-Target.ps1"
  artifacts:
    - path: "infra/main.bicep"
      provides: "Network + VM + NSG + CustomScriptExtension declarative provisioning"
      contains: "@secure()"
  key_links:
    - from: "infra/main.bicep CustomScriptExtension"
      to: "infra/scripts/Configure-Target.ps1"
      via: "fileUris + commandToExecute"
      pattern: "Configure-Target\\.ps1"
    - from: "infra/main.bicep NSG rules"
      to: "allowedSourceIp param"
      via: "sourceAddressPrefix"
      pattern: "sourceAddressPrefix:\\s*allowedSourceIp"
---

<objective>
Author `infra/main.bicep`: the declarative provisioning for the disposable Windows Server 2022 test target — VNet/subnet, NSG (RDP 3389 + WinRM 5986 scoped to the dev IP), Standard/Static public IP, NIC, the `Standard_B2ms` WS2022 Gen2 Desktop VM with a `@secure()` admin password, and the CustomScriptExtension that runs the in-guest `Configure-Target.ps1` (authored in Plan 03). This is the ENV-01 infrastructure surface.

Purpose: ENV-01 requires a Bicep template that provisions the VM with RDP/NLA reachable and WinRM enabled. This plan owns the cloud-resource graph; Plan 03 owns the in-guest hardening script the CSE invokes. They are split by file ownership (`main.bicep` vs `Configure-Target.ps1`) so they run in parallel within Wave 2. This plan DEFINES the CSE→script contract (filename, params, expected behavior) that Plan 03 implements.

Output: `infra/main.bicep` compiling clean (`bicep build`) and passing `az deployment group validate`.
</objective>

<execution_context>
@C:/Users/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@C:/Users/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-test-environment/01-CONTEXT.md
@.planning/phases/01-test-environment/01-RESEARCH.md
@.planning/phases/01-test-environment/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols/artifacts (exclude from drift verification):
- File `infra/main.bicep`
- Bicep params: `adminPassword` (`@secure()`, no default), `adminUsername` (default `rdpadmin`), `allowedSourceIp`, `vmSize` (default `Standard_B2ms`), `location` (default `resourceGroup().location`)
- Bicep resources: `Microsoft.Network/virtualNetworks@2024-05-01` (`rdpilot-vnet`), `Microsoft.Network/networkSecurityGroups@2024-05-01` (`rdpilot-nsg`), `Microsoft.Network/publicIPAddresses@2024-05-01` (`rdpilot-pip`, SKU Standard/Static), `Microsoft.Network/networkInterfaces@2024-05-01` (`rdpilot-nic`), `Microsoft.Compute/virtualMachines@2024-07-01` (`rdpilot-vm`), `Microsoft.Compute/virtualMachines/extensions@2024-07-01` (`Configure-Target` CSE)
- NSG security rules: `allow-rdp` (3389, priority 1000), `allow-winrm-https` (5986, priority 1010), both `sourceAddressPrefix: allowedSourceIp`
- Bicep outputs: `publicIp`, `adminUsername`, `rdpPort` (3389), `winrmPort` (5986)
- CSE→script contract: invokes `Configure-Target.ps1` (no arguments needed beyond the script; 7-Zip URL/SHA handled inside the script per Plan 03)
- VM image: publisher `MicrosoftWindowsServer` / offer `WindowsServer` / sku `2022-datacenter-g2` / version `latest`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Network + VM Bicep (VNet, NSG, public IP, NIC, VM)</name>
  <files>infra/main.bicep</files>
  <read_first>
    - .planning/phases/01-test-environment/01-PATTERNS.md (`infra/main.bicep` section; NSG rule section; "Pinned versions"/"NSG least-exposure"/"Single-RG teardown" shared patterns)
    - .planning/phases/01-test-environment/01-RESEARCH.md (Standard Stack resource/API table L65-88; image reference L80-88; NSG code example L300-328; Pitfall 2 NLA L266-269; Pitfall 6 vmSize param L289-292)
    - .planning/phases/01-test-environment/01-CONTEXT.md (D-01 image, D-02 size, D-05 network, D-06 secure password)
  </read_first>
  <action>
    Create `infra/main.bicep` at scope `resourceGroup`. Declare params: `@secure() param adminPassword string` (NO default — passed by manage-env.ps1, never logged, never committed: D-06); `param adminUsername string = 'rdpadmin'`; `@description('Developer public IP, detected at up-time by manage-env.ps1') param allowedSourceIp string`; `param vmSize string = 'Standard_B2ms'` (Pitfall 6 — one-flag switch to `Standard_D2s_v5`); `param location string = resourceGroup().location`. Declare resources with the EXACT pinned API versions from RESEARCH.md: `virtualNetworks@2024-05-01` (`rdpilot-vnet`, a single subnet), `networkSecurityGroups@2024-05-01` (`rdpilot-nsg`) with two inbound rules — `allow-rdp` (priority 1000, TCP, destinationPortRange 3389) and `allow-winrm-https` (priority 1010, TCP, destinationPortRange 5986), both with `sourceAddressPrefix: allowedSourceIp` and `destinationAddressPrefix: '*'` (NEVER `0.0.0.0/0`); `publicIPAddresses@2024-05-01` (`rdpilot-pip`, SKU `Standard`, allocation `Static`); `networkInterfaces@2024-05-01` (`rdpilot-nic`) binding the public IP and subnet, with the NSG associated at the subnet or NIC; `virtualMachines@2024-07-01` (`rdpilot-vm`, `hardwareProfile.vmSize: vmSize`) with `imageReference` publisher `MicrosoftWindowsServer` / offer `WindowsServer` / sku `2022-datacenter-g2` / version `latest` (D-01: Gen2 Desktop — NOT `-core`, NOT `-azure-edition`), `osProfile.adminUsername`/`adminPassword`, and `osProfile.windowsConfiguration` (NLA is the Azure default — do NOT add NLA-disabling config; it is verified later, Pitfall 2). Emit outputs `publicIp` (the pip address), `adminUsername`, `rdpPort = 3389`, `winrmPort = 5986`. Do NOT author `Configure-Target.ps1` here (Plan 03 owns it). Do NOT add the auto-destroy resources here (Plan 04 owns them).
  </action>
  <verify>
    <automated>az bicep build --file infra/main.bicep && pwsh -NoProfile -Command "$c = Get-Content infra/main.bicep -Raw; if ($c -notmatch '@secure\(\)') { throw 'adminPassword not @secure()' }; if ($c -match '0\.0\.0\.0/0') { throw 'NSG too open' }; if ($c -notmatch '2022-datacenter-g2') { throw 'wrong image sku' }; 'ok'"</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build --file infra/main.bicep` compiles with no errors
    - `main.bicep` declares `param adminPassword string` decorated `@secure()` with no default value
    - NSG has exactly two inbound allow rules (3389, 5986), both with `sourceAddressPrefix: allowedSourceIp`; the file contains NO `0.0.0.0/0`
    - VM `imageReference.sku` is `2022-datacenter-g2`; `hardwareProfile.vmSize` references the `vmSize` param defaulting to `Standard_B2ms`
    - Public IP resource declares SKU `Standard` and allocation `Static`
    - Outputs `publicIp`, `adminUsername`, `rdpPort`, `winrmPort` are present
  </acceptance_criteria>
  <done>`bicep build` is clean; secure password param, IP-scoped NSG, WS2022-g2 image, Standard/Static public IP, and connection outputs are all present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the CustomScriptExtension to Configure-Target.ps1</name>
  <files>infra/main.bicep</files>
  <read_first>
    - .planning/phases/01-test-environment/01-PATTERNS.md (CustomScriptExtension section; "Idempotency" shared pattern)
    - .planning/phases/01-test-environment/01-RESEARCH.md (CSE code example L330-350; Architectural Responsibility Map L45-54; anti-pattern "no deploymentScripts"/"no reboot" L236-243)
  </read_first>
  <action>
    Add to `infra/main.bicep` a `Microsoft.Compute/virtualMachines/extensions@2024-07-01` resource named `Configure-Target`, `parent: <vm resource>`, with `publisher: 'Microsoft.Compute'`, `type: 'CustomScriptExtension'`, `typeHandlerVersion: '1.10'`, `autoUpgradeMinorVersion: true`. Set `settings.fileUris` to a parameterized `scriptUri` (add `param scriptUri string` — the raw URL or storage-blob URL where `Configure-Target.ps1` is published at deploy time; manage-env.ps1 in Plan 04 supplies it) and `protectedSettings.commandToExecute` to `powershell -ExecutionPolicy Unrestricted -File Configure-Target.ps1`. This DEFINES the contract Plan 03 implements: the CSE expects a script literally named `Configure-Target.ps1` that is idempotent and performs ALL in-guest hardening in one pass (WinRM HTTPS listener + firewall, default-user-hive DPI + SuppressWhenMinimized, HKLM SuppressWhenMinimized, SHA-verified 7-Zip install) with no reboot. Use `Microsoft.Compute` CustomScriptExtension — do NOT use `deploymentScripts` (runs in a container, cannot touch the guest, RESEARCH.md L93). Document in a Bicep comment that the script body lives in `infra/scripts/Configure-Target.ps1` (Plan 03) and the `scriptUri` is set at deploy time.
  </action>
  <verify>
    <automated>az bicep build --file infra/main.bicep && pwsh -NoProfile -Command "$c = Get-Content infra/main.bicep -Raw; if ($c -notmatch 'CustomScriptExtension') { throw 'no CSE' }; if ($c -notmatch 'Configure-Target\.ps1') { throw 'CSE not wired to script' }; if ($c -match 'deploymentScripts') { throw 'must not use deploymentScripts' }; 'ok'"</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build --file infra/main.bicep` remains clean after adding the CSE
    - The CSE resource is `Microsoft.Compute/virtualMachines/extensions@2024-07-01`, type `CustomScriptExtension`, handler `1.10`, parented to the VM
    - `protectedSettings.commandToExecute` references `Configure-Target.ps1`
    - The template contains NO `deploymentScripts` resource
    - A `scriptUri` param exists and feeds `settings.fileUris`
  </acceptance_criteria>
  <done>main.bicep wires one CustomScriptExtension to `Configure-Target.ps1` via `scriptUri`/`commandToExecute`, with no `deploymentScripts` and no in-extension reboot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → VM (RDP/WinRM) | Untrusted inbound traffic crosses the NSG into the VM |
| manage-env.ps1 → ARM (deployment params) | The admin password crosses into the control plane as a @secure() param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing / Elevation | Exposed RDP 3389 brute-force | mitigate | NSG `allow-rdp` scoped to `allowedSourceIp` only; NLA is Azure default (verified in Validate-Target.ps1); strong random password (Plan 04). ASVS V2/V4 |
| T-01-05 | Information Disclosure | adminPassword in deployment history | mitigate | `@secure()` param keeps the value out of ARM deployment history and logs (ASVS V7). HIGH — blocks if param is not `@secure()`. |
| T-01-06 | Tampering / Spoofing | Over-broad NSG (0.0.0.0/0) | mitigate | Both inbound rules use `sourceAddressPrefix: allowedSourceIp`; build gate fails the file if it contains `0.0.0.0/0` (ASVS V14). HIGH severity for global RDP exposure. |
| T-01-07 | Tampering | CSE wrong tool (deploymentScripts can't reach guest) / unguarded script | mitigate | Use `CustomScriptExtension`; contract requires the idempotent guarded script from Plan 03 (no reboot). |
</threat_model>

<verification>
- `az bicep build --file infra/main.bicep` compiles clean
- `adminPassword` is `@secure()` with no default (HIGH — blocks otherwise)
- No `0.0.0.0/0` anywhere in the template (HIGH — blocks otherwise)
- VM image sku is `2022-datacenter-g2`; public IP is Standard/Static
- CSE wired to `Configure-Target.ps1`; no `deploymentScripts`
- (At Plan 04 phase-gate) `az deployment group validate` / `what-if` passes against a scratch RG with sample params
</verification>

<success_criteria>
- `infra/main.bicep` provisions VNet + NSG (IP-scoped 3389/5986) + Standard/Static public IP + NIC + WS2022-g2 Standard_B2ms VM with a `@secure()` admin password
- A single CustomScriptExtension invokes `Configure-Target.ps1`
- Outputs expose connection details (publicIp, adminUsername, rdpPort, winrmPort)
- Template compiles clean with all pinned API versions
</success_criteria>

<output>
Create `.planning/phases/01-test-environment/01-02-SUMMARY.md` when done.
</output>
