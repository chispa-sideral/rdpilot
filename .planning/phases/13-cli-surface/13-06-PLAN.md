---
phase: 13-cli-surface
plan: 06
type: execute
wave: 5
depends_on: [13-05, 13-04]
files_modified:
  - crates/rdpilot-cli/Cargo.toml
  - crates/rdpilot-cli/src/cli.rs
  - crates/rdpilot-cli/src/main.rs
  - crates/rdpilot-cli/src/verbs/perceive.rs
  - crates/rdpilot-cli/src/verbs/input.rs
  - crates/rdpilot-cli/src/render/table.rs
  - crates/rdpilot-cli/tests/cli_verbs.rs
autonomous: false
requirements: [CLI-02]
must_haves:
  truths:
    - "The full perception+input+launch verb set (screenshot, world-state, uia, window list, process list, click/type/key/scroll/drag, launch, foreground) runs against an explicit required --session."
    - "screenshot writes decoded PNG bytes to a REQUIRED --output path; binary bytes never hit stdout."
    - "Window/process/uia results render as human tables by default and machine JSON under --json."
  artifacts:
    - path: "crates/rdpilot-cli/src/verbs/perceive.rs"
      provides: "screenshot/world-state/uia/window-list/process-list handlers"
      contains: "output"
    - path: "crates/rdpilot-cli/src/verbs/input.rs"
      provides: "click/type/key/scroll/drag/launch/foreground handlers"
      contains: "WireMouseAction"
  key_links:
    - from: "crates/rdpilot-cli/src/verbs/perceive.rs"
      to: "Request::Screenshot / WireResponse::Screenshot png_base64"
      via: "base64-decode then std::fs::write to --output"
      pattern: "STANDARD.decode"
---

<objective>
Deliver CLI-02: the full perception + input + launch verb set — `screenshot`, `world-state`, `uia`, `window list`, `process list` (perception); `click`, `type`, `key`, `scroll`, `drag` (input); `launch`, `foreground` — each targeting an explicit required `--session` (D-29), reusing the wire DTOs (Plan 13-02) and the now-live dispatch (Plan 13-04). `screenshot` writes decoded PNG bytes to a required `--output <path>` (D-13.1: binary bytes never on stdout), which requires the `base64` crate.

Purpose: CLI-02 is the perception/control surface the operator and Phase 15 PROOF-02 use to read and drive a remote-only program. The offline proof exercises wire encode/decode + dispatch routing + CLI arg parsing + table/`--json` rendering against the fake connector; REAL sensor semantics (coordinate correctness, UIA tree shape) are the Phase 15 batched live gate.
Output: The perception/input/launch verbs wired into the clap tree, the `base64` dependency added behind a legitimacy checkpoint, and an offline `cli_verbs` integration test proving each verb round-trips a renderable result against the real daemon with the canned fake session.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-cli-surface/13-RESEARCH.md
@.planning/phases/13-cli-surface/13-CONTEXT.md
@crates/rdpilot-cli/src/cli.rs
@crates/rdpilot-cli/src/connect.rs
@crates/rdpilot-cli/src/render/table.rs
@crates/rdpilot-ipc/src/request.rs
@crates/rdpilot-ipc/src/response.rs
@crates/rdpilot-ipc/src/perception.rs
@crates/rdpilot-ipc/src/input.rs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: base64 package legitimacy verification (blocking, non-auto-approvable)</name>
  <what-built>Nothing yet — this gate precedes adding the `base64` crate to rdpilot-cli. Research flagged `base64` as [ASSUMED — not scanned this session] in the Package Legitimacy Audit; it must clear the gate before it enters the dependency graph.</what-built>
  <how-to-verify>
    1. Run `slopcheck install base64` (installed at /home/marc/.local/bin/slopcheck) and confirm an `[OK]` verdict for `base64` on crates.io.
    2. Confirm the package identity at https://crates.io/crates/base64 — long-established (multi-year), high download count, source repo github.com/marshallpierce/rust-base64.
    3. Run `cargo add base64 --dry-run -p rdpilot-cli` (or `cargo search base64`) to note the actual current version to pin (research guessed 0.22.x — verify, do not trust the guess).
  </how-to-verify>
  <resume-signal>Type "approved" with the confirmed current base64 version, or describe the issue if slopcheck flags it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Perception verbs — screenshot (--output), world-state, uia, window list, process list</name>
  <files>crates/rdpilot-cli/Cargo.toml, crates/rdpilot-cli/src/cli.rs, crates/rdpilot-cli/src/main.rs, crates/rdpilot-cli/src/verbs/perceive.rs, crates/rdpilot-cli/src/render/table.rs</files>
  <read_first>
    crates/rdpilot-cli/src/cli.rs (Command enum + SessionArg pattern to extend with a grouped Perceive family),
    crates/rdpilot-cli/src/connect.rs (round_trip helper),
    crates/rdpilot-cli/src/render/table.rs (the hand-rolled column printer to reuse),
    crates/rdpilot-ipc/src/request.rs (Request::Screenshot/WindowList/ProcessList/Uia/WorldState field shapes),
    crates/rdpilot-ipc/src/response.rs (WireResponse::Screenshot/WindowList/ProcessList/Uia/WorldState shapes),
    crates/rdpilot-ipc/src/perception.rs (WireWindowInfo/WireProcessInfo/WireUiaElement/WireUiaScope/WireUiaMode/WireWorldStateOptions)
  </read_first>
  <action>
    Add the approved-version `base64` dependency to `crates/rdpilot-cli/Cargo.toml` (use the version confirmed in Task 1). Extend the clap tree in `cli.rs` with a grouped `#[command(subcommand)] Perceive(PerceiveCmd)` family (research D-13.1: perception verbs live under their noun group), each leaf carrying a required `#[arg(long)] session: String` (D-29). Leaves:
    - `screenshot`: `--session`, required `--output PathBuf` (D-13.1 — no default). Sends `Request::Screenshot`, expects `WireResponse::Screenshot { png_base64 }`, `base64::engine::general_purpose::STANDARD.decode(...)` then `std::fs::write(output, bytes)`. Never write image bytes to stdout. Print a one-line "wrote <output> (<n> bytes)" (table) / `{"output": "...", "bytes": n}` (--json).
    - `world-state`: `--session`, flags `--screenshot`/`--window-list`/`--uia-mode <none|foreground|all|hwnd>` (+ `--hwnd` repeatable for the hwnd mode) mapping to `WireWorldStateOptions`; optional `--output` for the embedded screenshot (decode+write when present, per research). Renders the structured components as tables (or --json the whole WorldState).
    - `uia`: `--session`, `--hwnd u64`, `--scope <children|subtree>` (+ `--max-depth` when subtree) → `WireUiaScope`. Renders the `Vec<WireUiaElement>` as a table (id/role/name/bbox/depth) or --json.
    - `window list` (grouped `perceive window list` / consider a flat convenience is optional): `--session` → `Request::WindowList` → table (hwnd/title/rect/z_order/state/class_name/pid).
    - `process list`: `--session` → `Request::ProcessList` → table (pid/parent_pid/name/path/command_line/owner).
    Wire all handlers into `main.rs`'s dispatch. Each handler destructures ITS expected WireResponse variant; an unexpected variant or a `WireResponse::Error` maps to the appropriate CliError/exit code (reuse Plan 13-05's exit_codes). Add table-rendering helpers for window/process/uia rows in `render/table.rs`.
  </action>
  <acceptance_criteria>
    - `rdpilot perceive screenshot --session S --output /path.png` writes a PNG file (base64-decoded); no image bytes appear on stdout; omitting `--output` is a clap parse error.
    - `rdpilot perceive window list --session S` and `process list --session S` render tables; `--json` emits machine-readable arrays.
    - `rdpilot perceive uia --session S --hwnd N --scope children` renders UIA elements.
    - `rdpilot perceive world-state --session S` renders the requested components.
    - Every perception leaf requires `--session` (clap rejects its absence).
    - crates/rdpilot-cli/Cargo.toml lists `base64` at the Task-1-approved version.
    - `cargo build -p rdpilot-cli` succeeds; `cargo tree -p rdpilot-cli` still free of ironrdp/rustls/rdpilot.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p rdpilot-cli && test -z "$(cargo tree -p rdpilot-cli 2>/dev/null | grep -E 'ironrdp|rustls|^rdpilot-daemon')"</automated>
  </verify>
  <done>All perception verbs parse, target a required --session, round-trip, and render tables/JSON; screenshot writes bytes to --output only.</done>
</task>

<task type="auto">
  <name>Task 3: Input + launch verbs (click/type/key/scroll/drag, launch, foreground) and the offline CLI-02 integration proof</name>
  <files>crates/rdpilot-cli/src/cli.rs, crates/rdpilot-cli/src/main.rs, crates/rdpilot-cli/src/verbs/input.rs, crates/rdpilot-cli/tests/cli_verbs.rs</files>
  <read_first>
    crates/rdpilot-cli/src/cli.rs (Command enum to extend with a grouped Input family),
    crates/rdpilot-ipc/src/input.rs (WireMouseAction/WireButton/WireKey/WireKeyAction variant shapes),
    crates/rdpilot-ipc/src/request.rs (Request::Mouse/Key/LaunchProcess/SetForeground field shapes),
    crates/rdpilot-ipc/src/response.rs (WireResponse::Ack/Pid),
    crates/rdpilot-daemon/tests/autostart_lifecycle.rs (the RDPILOT_DAEMON_TEST_CONNECTOR + XDG_RUNTIME_DIR spawn pattern),
    crates/rdpilot-cli/tests/cli_lifecycle.rs (the CLI subprocess harness from Plan 13-05 to reuse)
  </read_first>
  <action>
    Add a grouped `#[command(subcommand)] Input(InputCmd)` family (each leaf with required `--session`, D-29):
    - `click`: `--session`, `--x u16`, `--y u16`, `--button <left|right|middle>` (default left) → `WireMouseAction::Click`; add `--double` to select `DoubleClick`, or a separate `double-click` leaf — pick one and document it. → `Request::Mouse` → expects `Ack`.
    - `scroll`: `--session`, `--x`, `--y`, `--dy i16` → `WireMouseAction::Scroll`.
    - `drag`: `--session`, `--from-x`, `--from-y`, `--to-x`, `--to-y`, `--button` → `WireMouseAction::Drag`.
    - `type` (clap rename to avoid the reserved word if needed, e.g. subcommand name "type" via `#[command(name = "type")]`): `--session`, positional/`--text String` → `WireKeyAction::Type` → `Request::Key`.
    - `key`: `--session`, `--combo <comma-separated key names>` parsed into `Vec<WireKey>` (map the CLI key names to WireKey variants; reject unknown names with a legible CliError) → `WireKeyAction::Combo`.
    - `launch`: `--session`, `--exe String`, `--args Option<String>`, `--cwd Option<String>` → `Request::LaunchProcess` → expects `WireResponse::Pid { pid }`, prints the pid.
    - `foreground`: `--session`, `--hwnd u64` → `Request::SetForeground` → expects `Ack`.
    Wire all into `main.rs`. Add `crates/rdpilot-cli/tests/cli_verbs.rs` (CLI-02 offline proof): reuse Plan 13-05's subprocess harness (XDG_RUNTIME_DIR temp + RDPILOT_DAEMON_TEST_CONNECTOR=1 so the daemon's canned fake session answers). connect a session, then for each verb assert exit 0 and a renderable result: `perceive window list --json` contains the fake's canned window; `perceive process list --json` contains the canned process; `perceive screenshot --session S --output <tmp>` writes a non-empty file that starts with the PNG magic bytes; `input click --session S --x 10 --y 10` exits 0 (Ack); `input launch --session S --exe notepad.exe` prints the canned pid; `input foreground --session S --hwnd 1` exits 0. Any assertion needing REAL Windows semantics (actual pixels, real UIA shape, real click landing) is `#[ignore]`-gated with a comment deferring it to the Phase 15 live gate (research Offline-vs-Batched-Live SC#2 row).
  </action>
  <acceptance_criteria>
    - `rdpilot input click/scroll/drag/type/key --session S ...` each build the correct WireMouseAction/WireKeyAction and expect Ack.
    - `rdpilot input launch --session S --exe X` prints the returned pid; `rdpilot input foreground --session S --hwnd N` exits 0.
    - Unknown `--combo` key names produce a legible CliError, not a panic.
    - tests/cli_verbs.rs passes offline against the real daemon + canned fake session: window/process list render the canned rows, screenshot writes PNG-magic bytes, click/launch/foreground succeed.
    - `cargo test -p rdpilot-cli` passes; live-only assertions are `#[ignore]`-gated with a Phase-15 deferral comment.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot-cli</automated>
  </verify>
  <done>The full input+launch verb set targets a required --session, round-trips, and the offline CLI-02 integration proof renders/acks every verb against the canned fake session; real-Windows semantics are explicitly deferred to Phase 15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI verb args → wire Request → SDK | User-supplied coordinates/handles/paths cross into the daemon/SDK |
| package registry → build | A new crate (`base64`) enters the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-SC | Tampering | `base64` cargo install ([ASSUMED] in research) | high | mitigate | Blocking, non-auto-approvable `checkpoint:human-verify` (Task 1) runs slopcheck + crates.io identity check BEFORE the dependency is added; version pinned to the verified current release |
| T-13-16 | Information Disclosure | binary image bytes on stdout corrupting a pipe | medium | mitigate | `screenshot` requires `--output` and writes bytes to the file only (D-13.1); stdout carries only a text/JSON summary |
| T-13-17 | Tampering | unknown `--combo` key names silently ignored | low | mitigate | Unrecognized key names produce a legible CliError (no panic, no silent drop), so a mistyped combo is visible not lost |
| T-13-18 | Elevation of Privilege | coordinate/handle bounds | low | accept | Bounds-checking is SDK-side (`Session::send_mouse` vs desktop_size); the CLI passes raw args — no weaker duplicate check introduced |
</threat_model>

<artifacts_produced>
- `base64` dependency (legitimacy-gated) on rdpilot-cli.
- `cli.rs` grouped `Perceive` + `Input` command families.
- `verbs/perceive.rs`: screenshot/world-state/uia/window-list/process-list handlers.
- `verbs/input.rs`: click/scroll/drag/type/key/launch/foreground handlers.
- `render/table.rs` window/process/uia table helpers.
- `tests/cli_verbs.rs` CLI-02 offline integration proof.
</artifacts_produced>

<verification>
- `cargo test -p rdpilot-cli` green (unit + cli_verbs integration against the canned fake session).
- `cargo tree -p rdpilot-cli` still free of the RDP stack after adding base64.
- Live-only semantics `#[ignore]`-gated for the Phase 15 batched gate.
</verification>

<success_criteria>
CLI-02 satisfied: the full perception + input + launch verb set runs against an explicit `--session`, with screenshot writing to `--output` and structured results rendering as tables/JSON — proven offline via the fake-connector integration test (real Windows behavior deferred to Phase 15).
</success_criteria>

<output>
Create `.planning/phases/13-cli-surface/13-06-SUMMARY.md` when done.
</output>
