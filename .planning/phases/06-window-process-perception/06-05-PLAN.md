---
phase: 06-window-process-perception
plan: 05
type: execute
wave: 4
depends_on: [06-02, 06-04]
files_modified:
  - crates/rdpilot/tests/live_session.rs
autonomous: false
requirements: [PERC-01, PERC-02, PERC-04, PROC-01, CAP-02]
user_setup:
  - service: azure
    why: "The gated live gate provisions a disposable Windows VM to validate the four hard success criteria against a real remote desktop"
    dashboard_config:
      - task: "Provision + tear down the disposable test VM"
        location: "infra/manage-env.ps1 up -VmSize Standard_B2s_v2 / infra/manage-env.ps1 down (default Standard_B2ms is SkuNotAvailable in westeurope)"
must_haves:
  truths:
    - "get_window_list returns HWND/title/rect/z-order/state for visible windows on the live remote desktop"
    - "get_process_tree returns PID/parent-PID/name/path for live processes"
    - "set_foreground_window brings a window forward, confirmed by a follow-up window-list query (SC#3)"
    - "launch_process starts notepad.exe and the returned PID appears in a follow-up process-tree query (SC#4)"
  artifacts:
    - path: crates/rdpilot/tests/live_session.rs
      provides: "Four gated live tests, one per Phase 6 success criterion, mirroring the existing gated-suite pattern"
      contains: "fn launch_process_appears_in_followup_process_tree"
  key_links:
    - from: crates/rdpilot/tests/live_session.rs
      to: crates/rdpilot/src/session.rs
      via: "exercises the public get_window_list/get_process_tree/set_foreground_window/launch_process API"
      pattern: "get_window_list"
---

<objective>
Add the four gated live integration tests — one per Phase 6 success criterion — mirroring the existing `live_session.rs` gated-suite pattern, then run the end-of-phase live gate against a real disposable Azure Windows VM to prove all four criteria hold end-to-end (Rust SDK to DVC to C# sensor to real Win32).

Purpose: The four hard success criteria (ROADMAP Phase 6 SC#1-4) are cross-call, live-only assertions (e.g. "confirms the focus change in a subsequent window list query") that cannot be verified offline. This plan is the phase gate.
Output: Four `#[ignore]`'d + `require_target!`-gated tests, and a captured live-gate run (measurements + pass/fail) via the blocking human-verify checkpoint.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-window-process-perception/06-RESEARCH.md
@.planning/phases/06-window-process-perception/06-CONTEXT.md
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/tests/common/mod.rs
</context>

<interface_context>
From Plans 06-02 / 06-04 (already merged): the public `Session::get_window_list() -> Result<Vec<WindowInfo>>`, `get_process_tree() -> Result<Vec<ProcessInfo>>`, `set_foreground_window(u64) -> Result<()>`, `launch_process(&str, Option<&str>, Option<&str>) -> Result<u32>`, `screenshot_window(&WindowInfo) -> Result<Screenshot>`; owned `WindowInfo`/`WindowState`/`ProcessInfo`.

Existing `live_session.rs` helpers to REUSE (do not reinvent): `require_target!("name")`, `block_on(fut)`, `capture_when_ready(&session)`, `settle()`/`SETTLE`, `common::sensor_exe_path()`, `cfg.sensor_binary_path(exe)` + `session.deploy_and_launch()` to bring the sensor up in-band (the proven Phase 5 path, `sensor_rdpdr_deploy_and_ping_within_1s` at :757). Each new test must connect WITH `sensor_binary_path` set and call `deploy_and_launch()` first so the sensor is answering before issuing window/process requests.

Gating (D-18): every test is `#[ignore]`'d and early-returns via `require_target!` when the suite is not armed, so default `cargo test -p rdpilot` stays green with no VM. Canonical armed run: `RDPILOT_LIVE=1 RDPILOT_IDLE_SECS=600 cargo test -p rdpilot -- --include-ignored --test-threads=1`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Add the four gated live tests (one per success criterion) + optional per-window screenshot smoke</name>
  <files>crates/rdpilot/tests/live_session.rs</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (require_target! at :25, block_on at :90, settle/SETTLE at :266-270, capture_when_ready at :871, sensor_rdpdr_deploy_and_ping_within_1s at :757 for the deploy_and_launch bring-up + common::sensor_exe_path usage)
    crates/rdpilot/tests/common/mod.rs (load_config gating, sensor_exe_path)
    .planning/phases/06-window-process-perception/06-RESEARCH.md (Test Map — one test per success criterion; Pitfall 5 — confirm focus via follow-up query, not the call's own return; Open Question 1 — enumeration timeout may need live tuning)
  </read_first>
  <action>
    Append four `#[test]` functions to live_session.rs, each `#[ignore = "live: requires a provisioned RDP target + published rdpilot-sensor.exe (RDPILOT_LIVE=1)"]`, following the established shape (require_target! guard then block_on then connect with `cfg.sensor_binary_path(common::sensor_exe_path())` then `deploy_and_launch().await` to bring up the sensor then issue requests then assert then `session.close().await`):
    1. `window_list_returns_visible_windows` (SC#1, PERC-02): assert `get_window_list()` returns a non-empty Vec; at least one window has a non-empty title, a rect fully within `session.desktop_size()` (proving framebuffer pixel space — the Phase 7 dependency), a nonzero pid, and a valid `WindowState`. Optional CAP-02 smoke: call `session.screenshot_window(&that_window)` and assert the crop dimensions equal the window rect w by h — keep this a soft smoke (CAP-02 is otherwise covered by the offline crop tests in 06-02), not a hard gate.
    2. `process_tree_returns_pid_parent_name_path` (SC#2, PERC-01): assert `get_process_tree()` returns a non-empty Vec; at least one record has a nonzero pid and non-empty name, and at least one has a non-empty path (best-effort path resolution succeeds for ordinary processes). Do NOT assert on command_line/owner (D-6.3 optional extras).
    3. `set_foreground_window_confirmed_by_followup_query` (SC#3, PERC-04): `launch_process("notepad.exe", None, None)`, settle, `get_window_list()` to find a window whose pid matches the returned PID, call `set_foreground_window(hwnd)`, settle, then `get_window_list()` AGAIN and assert that hwnd is now the foreground/top-z-order window — the confirmation is the follow-up query, NOT the call's own return (Pitfall 5). If the foreground-lock timeout makes this flaky on the VM, leave a DECISION POINT comment (like the existing DoubleClick/Scroll ones) for live tuning.
    4. `launch_process_appears_in_followup_process_tree` (SC#4, PROC-01): `launch_process("notepad.exe", None, None)` returns a PID; settle; `get_process_tree()` and assert a record with that exact PID exists and its name is notepad-like. Fire-and-forget (D-6.2) — no wait/poll beyond a settle. The VM is torn down after the gate, so no explicit process cleanup is required (matches the existing suite convention).
    Reuse the file's `settle()` between an action and its follow-up query. Never log secrets. These are additive — do not modify existing Phase 2-5 tests.
  </action>
  <acceptance_criteria>
    - live_session.rs contains fn window_list_returns_visible_windows, fn process_tree_returns_pid_parent_name_path, fn set_foreground_window_confirmed_by_followup_query, fn launch_process_appears_in_followup_process_tree
    - Every new test is #[ignore]'d and guarded by require_target!
    - The SC#3 test asserts foreground via a SECOND get_window_list call, not the set_foreground_window return value
    - The SC#4 test asserts the launch_process PID appears in a follow-up get_process_tree
    - Unarmed `cargo test -p rdpilot` stays green and compiles the new tests
    - `cargo test -p rdpilot -- --list` lists all four new test names
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot 2>&1 | tail -15 && cargo test -p rdpilot -- --list 2>/dev/null | grep -cE "window_list_returns_visible_windows|process_tree_returns_pid_parent_name_path|set_foreground_window_confirmed_by_followup_query|launch_process_appears_in_followup_process_tree"</automated>
  </verify>
  <done>Four gated live tests exist, one per success criterion, each bringing the sensor up via deploy_and_launch and asserting cross-call behavior; the unarmed suite stays green and compiles them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the end-of-phase live gate against a real Azure VM</name>
  <action>Pause for the operator to publish the sensor on a Windows host, provision the disposable Azure VM, run the armed Phase 6 gated suite (all four success-criterion tests), capture per-criterion results and any timeout tuning, then tear the VM down.</action>
  <what-built>
    Four gated live tests (Task 1) covering ROADMAP Phase 6 SC#1-4, plus the full Rust SDK (Plans 01/02) and C# sensor (Plans 03/04) implementing window list, process tree, focus, and fire-and-forget launch over the RDPILOT_SENSOR DVC channel.
  </what-built>
  <how-to-verify>
    Run the end-of-phase live gate against a real disposable Azure Windows VM (mirrors the Phase 4/5 live-gate procedure):
    1. Publish the sensor on a Windows host with the .NET 8 SDK (NativeAOT does not cross-compile): `dotnet publish sensor/RdpilotSensor.csproj -r win-x64 -p:PublishAot=true --self-contained`. Confirm the published `rdpilot-sensor.exe` is where `common::sensor_exe_path()` expects it.
    2. Provision the VM: `infra/manage-env.ps1 up -VmSize Standard_B2s_v2` (the default Standard_B2ms is SkuNotAvailable in westeurope — STATE.md). This writes `.secrets/connection.json`.
    3. Arm and run the Phase 6 gate: `RDPILOT_LIVE=1 cargo test -p rdpilot -- --include-ignored --test-threads=1 window_list_returns_visible_windows process_tree_returns_pid_parent_name_path set_foreground_window_confirmed_by_followup_query launch_process_appears_in_followup_process_tree` (single-threaded — one VM).
    4. Expected: all four pass. SC#1 window list non-empty with valid rects in desktop bounds; SC#2 process tree with real PID/name/path; SC#3 focus confirmed by the follow-up query; SC#4 the launched notepad PID appears in the follow-up process tree. If the enumeration calls time out, widen the 2000ms enumeration timeout (06-02) and note the measured value; if SC#3 is flaky, tune per its DECISION POINT comment.
    5. Tear down: `infra/manage-env.ps1 down` and confirm the test resource group is deleted.
    Report: pass/fail per criterion, any measured timeout adjustments, and any live-run bugs found+fixed (Phase 4/5 each surfaced several).
  </how-to-verify>
  <resume-signal>Type "approved" with the per-criterion results, or describe failures to close as gaps.</resume-signal>
</task>

</tasks>

<artifacts_produced>
New symbols this plan produces:
- `crates/rdpilot/tests/live_session.rs`: `window_list_returns_visible_windows`, `process_tree_returns_pid_parent_name_path`, `set_foreground_window_confirmed_by_followup_query`, `launch_process_appears_in_followup_process_tree`
- A captured live-gate result validating PERC-01/PERC-02/PERC-04/PROC-01 (and CAP-02 smoke) against a real VM
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live VM | The gated suite drives a real remote desktop; `launch_process("notepad.exe")` exercises the intended RCE primitive within the authenticated RDP session |
| `.secrets/connection.json` → test | Live credentials for the disposable VM; never logged (existing common::mod.rs redaction discipline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Information Disclosure | live credentials in test output | low | mitigate | Reuse common::mod.rs's existing password-redaction; the new tests never print connection secrets, only PIDs/window metadata (operational, non-secret) |
| T-06-03 | Elevation of Privilege | launch_process("notepad.exe") | low | accept | Intentional PROC-01 capability on a disposable VM torn down after the gate; launches only benign notepad.exe |
| T-06-SC | Tampering | dependency supply chain | low | accept | No new dependency; test-only additions (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Unarmed `cargo test -p rdpilot` green; the four new tests compile and are listed
- Armed live gate: all four success-criterion tests pass against a real Azure VM; results captured at the checkpoint
- VM torn down (`manage-env.ps1 down`) after the run — no lingering resource group
</verification>

<success_criteria>
- SC#1: get_window_list returns HWND/title/rect/z-order/state for visible windows (rects in framebuffer pixel space)
- SC#2: get_process_tree returns PID/parent-PID/name/path for running processes
- SC#3: set_foreground_window brings a window forward, confirmed by a follow-up window-list query
- SC#4: launch_process starts notepad.exe and the returned PID appears in a follow-up process-tree query
</success_criteria>

<output>
Create `.planning/phases/06-window-process-perception/06-05-SUMMARY.md` when done.
</output>
