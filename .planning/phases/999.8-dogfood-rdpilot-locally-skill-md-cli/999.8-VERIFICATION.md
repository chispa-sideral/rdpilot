---
phase: 999.8-dogfood-rdpilot-locally-skill-md-cli
verified: 2026-07-12T14:30:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 999.8: Dogfood rdpilot locally — SKILL.md on the CLI + install for other agents in this box — Verification Report

**Phase Goal:** Make `rdpilot` available in this box for other agents so we can dogfood it — author a `SKILL.md` documenting the `rdpilot` CLI and install the CLI (and its companion daemon) so other agents on this machine can discover and invoke it.
**Verified:** 2026-07-12T14:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from PLAN frontmatter must_haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Any agent on this box discovers the rdpilot skill at BOTH `~/.claude/skills/rdpilot/SKILL.md` and `~/.agents/skills/rdpilot/SKILL.md` (D-1) | VERIFIED | Both paths exist on disk; `~/.claude/skills/rdpilot` confirmed via `test -L` to be a symlink to `/home/marc/.agents/skills/rdpilot` (`readlink` output). `~/.agents/skills/rdpilot/SKILL.md` is a real 8268-byte file. |
| 2 | `rdpilot` and `rdpilot-daemon` are both installed to the SAME directory so the CLI's sibling-file daemon autostart resolves (D-2) | VERIFIED | `command -v rdpilot` → `/home/marc/.cargo/bin/rdpilot`; `command -v rdpilot-daemon` → `/home/marc/.cargo/bin/rdpilot-daemon`. `dirname` of both is identical. Matches `crates/rdpilot-cli/src/connect.rs`'s `current_exe().with_file_name("rdpilot-daemon")` sibling-lookup requirement. |
| 3 | `rdpilot list` auto-starts the daemon and returns exit 0, proving the CLI→daemon IPC path works end-to-end (D-8) | VERIFIED | Live-executed: `rdpilot --version` → exit 0 (`rdpilot 0.1.0`); `rdpilot list` → exit 0 (empty table); `rdpilot list --json` → exit 0 (`[]`). |
| 4 | `~/.config/rdpilot/config.toml` exists, is mode 0600, and contains ONLY commented placeholder keys — no real credentials (D-4) | VERIFIED | `stat -c '%a'` → `600`. Full file content inspected: every key (`host`, `port`, `username`, `password`, `domain`, `accept_invalid_certs`, `share_root`) is commented out, copied verbatim from `crates/rdpilot-config/assets/config.toml.template`. `grep -Eq '^(host\|username\|password) *='` returns false (no uncommented credential line). |
| 5 | SKILL.md documents the full CLI verb surface quickstart-first, with read/inspect safety guidance, session-naming convention, and daemon-disconnect etiquette (D-3/D-4/D-6/D-7) | VERIFIED | 176-line SKILL.md read in full; section order is exactly what-it-is → quickstart → read/inspect workflows → safety & etiquette → config reference → per-verb CLI reference + exit-code table. Every one of the 17 required verb tokens, `DaemonUnreachable`, `DuplicateSession`, `RDPILOT_`, and `sensor_binary_path` is present (checked programmatically, no `MISSING` hits). Cross-checked against `crates/rdpilot-cli/src/cli.rs` and `exit_codes.rs` — every documented verb, flag, and exit code matches the real compiled CLI exactly (no invented or stale surface). |
| 6 | Re-running the installer never clobbers an existing populated config.toml (idempotent seed guard) | VERIFIED (independently re-proven live, not trusting SUMMARY) | I personally injected a `host = "sentinel-verify-test"` line into the live seeded config, re-ran `bash scripts/install-local.sh` end-to-end (full rebuild+reinstall), and confirmed the sentinel line survived the re-run (`grep -q sentinel-verify-test` succeeded post-run). Reverted the sentinel afterward; config confirmed clean (no uncommented credential keys) again. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.claude/skills/rdpilot/SKILL.md` | Version-controlled skill doc, `name: rdpilot`, ≥90 lines | VERIFIED | 176 lines. Frontmatter: `name: rdpilot` / `description:` mentions both "RDP" and "remote", explicitly excludes non-RDP local GUI automation (D-5). No debt markers (TODO/FIXME/TBD/XXX/HACK) found. |
| `scripts/install-local.sh` | Idempotent installer, contains `cargo +stable install`, ≥40 lines | VERIFIED | 84 lines. `bash -n` syntax-clean. Contains host-triple detection, both `cargo +stable install --path ... --target ... --root` invocations (cli + daemon, same root), guarded 0600 config seed, dual skill-surface install (copy + `ln -sf`), D-8 smoke test with explicit exit-code checks. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `scripts/install-local.sh` | `.claude/skills/rdpilot/SKILL.md` | `cp` to `~/.agents/skills/rdpilot/`, then `ln -sf` to `~/.claude/skills/rdpilot` | WIRED | Regex `\.claude/skills/rdpilot/SKILL\.md` matches line 63 of the script (the `cp` source path); the subsequent `ln -sf "$AGENTS_SKILL_DIR" "$CLAUDE_SKILL_LINK"` produces the symlink, confirmed live via `test -L` + `readlink`. |
| `scripts/install-local.sh` | `~/.cargo/bin/{rdpilot,rdpilot-daemon}` | `cargo +stable install --path ... --root` (same root, both crates) | WIRED | Regex `cargo \+stable install --path.*rdpilot-daemon` matches line 47. Both binaries confirmed present in `~/.cargo/bin/` via live `ls -la`. |
| `~/.cargo/bin/rdpilot` | `~/.cargo/bin/rdpilot-daemon` | `current_exe().with_file_name("rdpilot-daemon")` sibling lookup at connect time | WIRED | Live-exercised: `rdpilot list` (with no daemon pre-running) successfully auto-spawned `rdpilot-daemon` from the same directory and completed exit 0, proving the sibling lookup resolves in practice, not just in source. |

### D-1..D-8 Decision Coverage

| Decision | Requirement | Status | Evidence |
|----------|-------------|--------|----------|
| D-1 | Skill discoverable at both `~/.claude/skills/rdpilot` and `~/.agents/` | VERIFIED | Both present; `~/.claude` hop is a symlink (crabbox precedent, matches D-1/RESEARCH Pattern 3). |
| D-2 | Repeatable install script builds+installs BOTH binaries to a shared root, seeds config, installs skill to both surfaces | VERIFIED | `scripts/install-local.sh` does all four; script re-run confirmed idempotent live. |
| D-3 | SKILL.md documents full CLI verb surface, quickstart-first | VERIFIED | Section order matches; all 17 verbs present and accurate against `cli.rs`. |
| D-4 | Placeholder-only config; documents schema/env/layering; read/inspect-only safety guidance | VERIFIED | Seeded config all-commented; SKILL.md "Config reference" section documents all 7 template fields + `RDPILOT_` env prefix + file→env→flag precedence; "Safety & etiquette" section explicitly frames input verbs as mutating and instructs perceive-first. |
| D-5 | RDP-scoped frontmatter description, not over-triggering on generic local GUI automation | VERIFIED | Description text keys on "remote Windows desktop... reachable only over RDP" and explicitly states "Not for local/non-RDP GUI automation." |
| D-6 | Self-owned session-name convention + pre-connect `rdpilot list` collision check | VERIFIED | SKILL.md Safety section documents `<agent>-<purpose>` naming, `rdpilot list` before `connect`, `DuplicateSession`/exit 7 on collision. |
| D-7 | Daemon lifecycle etiquette: don't manage daemon manually, always disconnect | VERIFIED | SKILL.md Safety section: "Do not manage the daemon manually" + "Always disconnect explicitly when finished," idle-reap/self-shutdown explicitly framed as backstop not primary cleanup. |
| D-8 | Install script self-verifies via `rdpilot --version` + `rdpilot list`, prints config path + next steps | VERIFIED | Script step [5/5] runs both, checks exit codes explicitly, prints config path and next-steps text. Live-confirmed exit 0 on both, twice (fresh install + idempotency re-run). |

All eight locked decisions are honored by the delivered artifacts — none silently dropped.

### Requirements Coverage

Phase 999.8 declares no formal `REQ-*` IDs (ROADMAP.md: "Requirements: D-1..D-8 — CONTEXT.md locked decisions serve as de facto acceptance criteria; no formal REQ IDs"). The D-1..D-8 coverage table above serves as the requirements-coverage equivalent; all 8 are SATISFIED.

### Anti-Patterns Found

None. Both files scanned for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/stub patterns/empty implementations — the only match ("placeholder-only config" in an echo string) is a legitimate feature description, not a debt marker or stub.

### Minor Note (non-blocking, pre-existing, outside this phase's scope)

`crates/rdpilot-config/assets/config.toml.template` (a Phase 11/13-era artifact this phase seeds verbatim, not something 999.8 authored) does not include a commented `sensor_binary_path` line, even though `sensor_binary_path` was added to `ResolvedConfig` later (per its own doc comment, "Live-diagnosed (Plan 15-06)"). SKILL.md correctly documents `sensor_binary_path` as a real, functionally-critical config field (its own config table + the safety text), but a user who edits the seeded `config.toml` will not see a ready-to-uncomment `# sensor_binary_path = "..."` line there — they would need to add the key by hand despite reading about it in SKILL.md. This is a template completeness gap in a crate that predates this phase (`template_documents_every_d27_key`'s own test only checks the 7 D-27 keys, not `sensor_binary_path`), not a regression introduced by 999.8, and not one of the D-1..D-8 acceptance criteria. Flagged as informational only — does not block phase goal achievement (a user can still set `RDPILOT_SENSOR_BINARY_PATH` or add the key manually, both of which SKILL.md documents).

### Human Verification Required

None. All must-haves are independently machine/live-verifiable and were live re-exercised in this verification pass (not merely trusted from SUMMARY.md): binary presence/co-location, config mode/content, SKILL.md content accuracy against source, symlink assertion, and the idempotency guard (sentinel-inject → re-run → survive → revert) were all re-run by the verifier directly against this box's live filesystem state, not read from the executor's claims.

### Gaps Summary

No gaps. All 6 must-have truths, both required artifacts, all 3 key links, and all 8 locked decisions (D-1..D-8) are verified present, substantive, and functionally wired — live-exercised on this exact box, not merely inferred from SUMMARY.md or static file presence. `rdpilot` is genuinely dogfoodable on this box today: both binaries installed and functional, the daemon autostart path works end-to-end, the seeded config is safe (0600, placeholder-only) and idempotently protected against re-install clobbering, and the skill is discoverable at both required global surfaces with the symlink structure intact.

---

_Verified: 2026-07-12T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
