---
phase: 05-sensor-bootstrap-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot/Cargo.toml
  - crates/rdpilot/src/input.rs
  - crates/rdpilot/src/rdpdr_backend.rs
  - crates/rdpilot/src/lib.rs
autonomous: false
requirements: [SENSOR-02]

must_haves:
  truths:
    - "The input vocabulary can express the Windows/GUI key (Key::Win), so a Win+R launch sequence is constructible (D-5.1)"
    - "A minimal RdpdrBackend serves exactly one read-only file and rejects any other path (access-control boundary of the whole feature)"
    - "The offline unit suite (cargo test -p rdpilot) stays green with the new ironrdp-rdpdr dependency and the backend module compiled in"
  artifacts:
    - path: "crates/rdpilot/src/input.rs"
      provides: "Key::Win variant + Set-1 extended scancode 0x5B mapping"
      contains: "Win"
    - path: "crates/rdpilot/src/rdpdr_backend.rs"
      provides: "RdpilotDriveBackend (std::fs-based, Create/Close/Read/QueryDirectory) implementing ironrdp_rdpdr::RdpdrBackend"
      contains: "RdpilotDriveBackend"
    - path: "crates/rdpilot/Cargo.toml"
      provides: "ironrdp-rdpdr 0.6 dependency"
      contains: "ironrdp-rdpdr"
  key_links:
    - from: "crates/rdpilot/src/rdpdr_backend.rs"
      to: "ironrdp_rdpdr::backend::RdpdrBackend"
      via: "impl RdpdrBackend for RdpilotDriveBackend"
      pattern: "impl RdpdrBackend"
    - from: "crates/rdpilot/src/input.rs"
      to: "Set-1 extended scancode 0x5B"
      via: "Key::Win => (true, 0x5B) in scancode()"
      pattern: "Key::Win"
---

<objective>
Lay the offline Rust foundation for the RDPDR drive-redirection primary path (SENSOR-02, feeds SC2): add the `ironrdp-rdpdr` dependency, the missing `Key::Win` input variant (needed to express D-5.1's Win+R launch), and a minimal, portable `RdpilotDriveBackend` that serves exactly one read-only file over MS-RDPEFS.

Purpose: `ironrdp-rdpdr` ships ZERO Windows filesystem backend (`ironrdp-rdpdr-native` is *nix-cfg-gated and empty on the pinned `x86_64-pc-windows-gnu` target — RESEARCH Pitfall 2). The SDK must supply its own `std::fs`-based backend. This plan produces the two building blocks (`Key::Win`, `RdpilotDriveBackend`) that Plan 03 wires into the connect path and the launch bootstrap.

Output: `ironrdp-rdpdr` in `Cargo.toml`, `Key::Win` in `input.rs`, and a new `rdpdr_backend.rs` module — all offline-unit-tested; no VM required.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sensor-bootstrap-deployment/05-RESEARCH.md
@.planning/phases/05-sensor-bootstrap-deployment/05-CONTEXT.md
@crates/rdpilot/src/input.rs
@crates/rdpilot/Cargo.toml
</context>

<artifacts_produced>
New symbols this plan creates (consumed by Plan 03):
- `Key::Win` variant in `crates/rdpilot/src/input.rs` (extended Set-1 scancode `0x5B`), routed through the existing `KeyAction::Combo` machinery unchanged.
- `crate::rdpdr_backend::RdpilotDriveBackend` — `pub(crate)` struct implementing `ironrdp_rdpdr::backend::RdpdrBackend`; constructor takes the local served-file path + the name shown under the redirected drive.
- `mod rdpdr_backend;` declaration in `crates/rdpilot/src/lib.rs`.
- `ironrdp-rdpdr = "0.6"` dependency line in `crates/rdpilot/Cargo.toml`.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for ironrdp-rdpdr (blocking, non-auto-approvable)</name>
  <action>Pause for the operator to verify `ironrdp-rdpdr` legitimacy on crates.io (Devolutions/IronRDP publisher, 0.6.x) before the Cargo.toml dependency edit in Task 2.</action>
  <what-built>Nothing yet — this gate precedes adding a new crate to Cargo.toml. `ironrdp-rdpdr` is flagged [ASSUMED] in the RESEARCH Package Legitimacy Audit (no cargo/registry tool was available in the research sandbox to confirm it), so per the package legitimacy gate it must be human-verified before the dependency edit.</what-built>
  <how-to-verify>
    1. Open https://crates.io/crates/ironrdp-rdpdr and confirm: publisher is Devolutions (same org as every other `ironrdp-*` crate already in `Cargo.lock`), the repository is `github.com/Devolutions/IronRDP`, and version `0.6.x` exists.
    2. Confirm it is the same release train as the already-pinned `ironrdp-dvc`/`ironrdp-input`/`ironrdp-pdu` (RESEARCH: sibling first-party crate, not an independent publisher).
    3. This checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates).
  </how-to-verify>
  <resume-signal>Type "approved" once crates.io confirms Devolutions/IronRDP publisher + 0.6.x, or "reject" if the publisher/repo does not match.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ironrdp-rdpdr dependency + Key::Win variant and scancode</name>
  <files>crates/rdpilot/Cargo.toml, crates/rdpilot/src/input.rs, crates/rdpilot/src/lib.rs</files>
  <read_first>
    crates/rdpilot/Cargo.toml (the existing ironrdp-dvc/ironrdp-input/ironrdp-pdu pin lines to add alongside),
    crates/rdpilot/src/input.rs (the Key enum + the scancode() match — Key::Win follows the exact pattern of Key::Delete => (true, 0x53), and the is_modifier() predicate stays false for Win),
    crates/rdpilot/src/lib.rs (the mod declaration list)
  </read_first>
  <behavior>
    - Test: `scancode(Key::Win)` returns `(extended: true, code: 0x5B)` (Set-1 left Windows/GUI key, RESEARCH Pitfall 3).
    - Test: `key_operations(&KeyAction::Combo(vec![Key::Win, Key::R]))` yields a non-empty ordered press/release sequence (Win pressed then R, released R then Win) — Win is a terminal key, not a modifier, so both keys press in order and release in reverse (no partition surprise since is_modifier(Win) is false).
  </behavior>
  <action>Add `ironrdp-rdpdr = "0.6"` to `crates/rdpilot/Cargo.toml` `[dependencies]` alongside the existing `ironrdp-dvc`/`ironrdp-input` lines (RESEARCH Standard Stack). Add the `Win` variant to the `Key` enum in `input.rs` and its arm `Key::Win => (true, 0x5B)` to the `scancode()` match, following the exact pattern of the existing extended keys (e.g. `Key::Delete => (true, 0x53)`). Do NOT add Win to `is_modifier()` (it is a terminal key here). Add `mod rdpdr_backend;` to `lib.rs` (the module file is created in Task 3 — declare it now so the crate builds after Task 3; if Task 3 lands in the same session, order the module file first). Run `cargo build -p rdpilot` to confirm `ironrdp-rdpdr` resolves cleanly against the already-pinned `ironrdp-svc`/`ironrdp-pdu` versions (RESEARCH Assumption A1 — treat any version conflict as a normal, cheaply-fixed planning surprise, not a blocker). Do NOT add `ironrdp-rdpdr-native` (RESEARCH Pitfall 2 — empty on the Windows target).</action>
  <acceptance_criteria>
    - `crates/rdpilot/Cargo.toml` contains `ironrdp-rdpdr` and does NOT contain `ironrdp-rdpdr-native`.
    - `input.rs` `scancode()` maps `Key::Win` to `(true, 0x5B)` (asserted by a unit test).
    - A `Combo([Win, R])` produces a non-empty operation sequence (asserted by a unit test).
    - `cargo build -p rdpilot` and `cargo test -p rdpilot input::` succeed.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot input:: 2>&1 | grep -q 'test result: ok' && grep -q 'ironrdp-rdpdr' crates/rdpilot/Cargo.toml && test $(grep -c 'ironrdp-rdpdr-native' crates/rdpilot/Cargo.toml) -eq 0</automated>
  </verify>
  <done>`ironrdp-rdpdr` resolves and builds; `Key::Win` maps to the correct extended Set-1 scancode and is Combo-expressible; the *-native crate is not depended on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the minimal RdpilotDriveBackend (Create/Close/Read/QueryDirectory)</name>
  <files>crates/rdpilot/src/rdpdr_backend.rs, crates/rdpilot/src/lib.rs</files>
  <read_first>
    ~/.cargo/registry/src/*/ironrdp-rdpdr-0.6.0/src/pdu/efs.rs (READ THIS FIRST at execution time — RESEARCH Open Question #1: confirm the exact ServerDriveIoRequest variant names, the RdpdrBackend trait signature, and the field-level construction of DeviceCreateResponse/DeviceReadResponse/ClientDriveQueryDirectoryResponse plus the NtStatus values for success vs OBJECT_NAME_NOT_FOUND before writing the four match arms — Phase 4 grounded RdpilotSensorProcessor in pinned crate source the same way),
    ~/.cargo/registry/src/*/ironrdp-rdpdr-0.6.0/src/backend/mod.rs (the RdpdrBackend trait + handle_server_device_announce_response / handle_scard_call signatures),
    crates/rdpilot/src/sensor.rs (the crate's pub(crate)-only, no-unwrap/expect/panic conventions — D-09/API-01 apply identically here),
    crates/rdpilot/src/error.rs (the owned Error type — no ironrdp-rdpdr type may leak into public API)
  </read_first>
  <behavior>
    - Test: constructing each of the four `ServerDriveIoRequest` variants (Create/Close/Read/QueryDirectory) and passing it to `handle_drive_io_request` returns `Ok(_)` and never panics (mirrors Phase 4's `malformed_payload_is_dropped_without_panic` style).
    - Test: a `Create` request for the one served filename (or the drive root) succeeds; a `Create`/`Read` request for any OTHER path is rejected with a not-found NtStatus and does NOT open a std::fs handle to it (access-control boundary — the redirected drive must never become a general local-file-read primitive, ASVS V4 / T-05-04).
    - Test: a `Read` at a given offset/length returns exactly those bytes from the served file.
  </behavior>
  <action>Create `crates/rdpilot/src/rdpdr_backend.rs` defining `pub(crate) struct RdpilotDriveBackend` holding the local served-file `PathBuf`, the served filename `String` (the name shown under `\\tsclient\RDPILOT\`), and an open-handle map keyed by the RDPDR file id. Implement `ironrdp_rdpdr::backend::RdpdrBackend` for it with the four `handle_drive_io_request` arms dispatched from the `ServerDriveIoRequest` enum's exactly-four variants (RESEARCH Pattern 2):
- Create: succeed for the drive root and for the one served filename; for any other path return the not-found NtStatus (exact value read from efs.rs) WITHOUT touching the filesystem — this hard path allow-list IS the security posture of the minimal backend (RESEARCH Security Domain / T-05-04). Normalize/compare the server-supplied path string against the single allowed filename; never trust it raw.
- QueryDirectory: on the root, return the one served file's metadata (size/name) so the remote `copy`/Explorer enumeration sees it; otherwise empty/no-more-files.
- Read: stream bytes from the served file at the requested offset/length via `std::fs` (portable across the windows-gnu target and any Linux test host — NOT ironrdp-rdpdr-native).
- Close: drop the handle.
Implement the trait's other required methods (`handle_server_device_announce_response`, `handle_scard_call`) as `Ok(())` no-ops (RESEARCH Code Examples). No `unwrap`/`expect`/`panic` outside `#[cfg(test)]` (API-01); map any `std::fs`/IO failure to the appropriate NtStatus response, never a panic. Keep `mod rdpdr_backend;` in `lib.rs` (not re-exported — D-09, no ironrdp-rdpdr type in the public surface). Add offline unit tests per the behavior block using a temp file as the served binary.</action>
  <acceptance_criteria>
    - `rdpdr_backend.rs` contains `impl RdpdrBackend for RdpilotDriveBackend` with all four IRP arms present.
    - A `Create`/`Read` for a non-served path is rejected with a not-found NtStatus and opens no std::fs handle (asserted by a unit test).
    - A `Read` returns the exact served-file bytes at the requested offset (asserted by a unit test).
    - No `unwrap(`/`expect(`/`panic!(` outside `#[cfg(test)]` in `rdpdr_backend.rs` (grep, comments excluded).
    - `cargo test -p rdpilot rdpdr_backend::` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot rdpdr_backend:: 2>&1 | grep -q 'test result: ok'</automated>
  </verify>
  <done>`RdpilotDriveBackend` implements the four-variant RdpdrBackend, serves exactly one read-only file, hard-rejects any other path, and passes offline unit tests without leaking an ironrdp-rdpdr type into the public API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RDP server IRPs → RdpilotDriveBackend | The remote session issues MS-RDPEFS Create/Read/QueryDirectory IRPs with server-supplied path strings — untrusted input crossing into local `std::fs`. |
| Cargo supply chain | New first-party dependency `ironrdp-rdpdr`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | RdpilotDriveBackend Create/Read/QueryDirectory | high | mitigate | Hard path allow-list: only the one served filename (or drive root) resolves; any other path returns a not-found NtStatus and opens no `std::fs` handle. This is the entire security posture of the minimal backend (ASVS V4, RESEARCH Security Domain) — the redirected drive must not become a general local-filesystem read primitive. |
| T-05-05 | Denial of Service | backend IO error handling | low | mitigate | Every `std::fs`/IO failure maps to an NtStatus response, never an `unwrap`/`panic` (API-01). |
| T-05-SC | Tampering | `cargo add ironrdp-rdpdr` (crates.io) | high | mitigate | Blocking, non-auto-approvable human legitimacy checkpoint (Task 1) verifying Devolutions/IronRDP publisher + 0.6.x on crates.io before the Cargo.toml edit; `[ASSUMED]` per RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- `cargo build -p rdpilot` resolves `ironrdp-rdpdr` cleanly (Assumption A1).
- `cargo test -p rdpilot` green: `Key::Win` scancode + Combo tests, and the four RdpdrBackend routing/access-control/read tests.
- `ironrdp-rdpdr-native` is absent from `Cargo.toml` (Pitfall 2).
</verification>

<success_criteria>
- SENSOR-02 foundation in place: the input vocabulary can express Win+R (D-5.1) and a minimal, access-controlled, portable drive backend serves exactly one read-only file — the two building blocks Plan 03 wires into the connect path and launch bootstrap.
</success_criteria>

<output>
Create `.planning/phases/05-sensor-bootstrap-deployment/05-02-SUMMARY.md` when done — record the exact NtStatus values and response-PDU shapes read from `efs.rs` (resolving RESEARCH Open Question #1) so Plan 03 and any future backend extension can reference them.
</output>
