---
phase: 05-sensor-bootstrap-deployment
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - crates/rdpilot/tests/fixtures/deploy-winrm.ps1
  - crates/rdpilot/tests/live_session.rs
  - crates/rdpilot/tests/fixtures/sensor-responder.ps1
  - crates/rdpilot/tests/fixtures/deploy-responder.ps1
autonomous: false
requirements: [SENSOR-01, SENSOR-02]

must_haves:
  truths:
    - "The RDPDR primary path deploys the real sensor exe and gets a pong within 1 second of a successful launch (SC2 mandatory + SC4)"
    - "The WinRM fallback path also deploys + launches the real sensor exe and gets a pong within 1 second of launch (SC3 + SC4)"
    - "The throwaway Phase-4 responder fixtures are deleted once the C# sensor supersedes them (D-5.7)"
  artifacts:
    - path: "crates/rdpilot/tests/fixtures/deploy-winrm.ps1"
      provides: "WinRM deploy/launch/teardown helper copying the real rdpilot-sensor.exe (adapted from deploy-responder.ps1)"
      contains: "Copy-Item"
    - path: "crates/rdpilot/tests/live_session.rs"
      provides: "sensor_rdpdr_deploy_and_ping_within_1s + sensor_winrm_deploy_and_ping_within_1s gated tests"
      contains: "sensor_rdpdr_deploy_and_ping_within_1s"
  key_links:
    - from: "crates/rdpilot/tests/live_session.rs"
      to: "Session::deploy_and_launch (RDPDR) + Session::ping (WinRM)"
      via: "gated live tests asserting elapsed < 1s from launch (SC4)"
      pattern: "deploy_and_launch"
    - from: "crates/rdpilot/tests/fixtures/deploy-winrm.ps1"
      to: "rdpilot-sensor.exe on the target"
      via: "Copy-Item -ToSession + Register-ScheduledTask AtLogOn Interactive"
      pattern: "Register-ScheduledTask"
---

<objective>
Prove both deployment paths live and close out the phase: author the WinRM fallback fixture (SC3), add both gated live tests (SC2/SC3/SC4), delete the throwaway Phase-4 responder fixtures (D-5.7), and run the live gate. RDPDR success is MANDATORY (D-5.6); WinRM is independently required (SC3).

Purpose: This is the phase's live verification wave. It consumes Plan 01's `rdpilot-sensor.exe`, Plan 03's `Session::deploy_and_launch` + RDPDR registration, and Phase 4's proven `Session::connect`/`ping()`. The live gate also empirically resolves the phase's open unknowns: AV/EDR (D-5.5), drive-redirection GPO (D-5.6), and binary size (D-5.3).

Output: `deploy-winrm.ps1`, two gated live tests in `live_session.rs`, deletion of the two throwaway fixtures, and a passing live gate.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sensor-bootstrap-deployment/05-RESEARCH.md
@.planning/phases/05-sensor-bootstrap-deployment/05-CONTEXT.md
@.planning/phases/04-dvc-transport-channel/04-03-SUMMARY.md
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/tests/common/mod.rs
@crates/rdpilot/tests/fixtures/deploy-responder.ps1
</context>

<artifacts_produced>
New symbols/paths this plan creates:
- `crates/rdpilot/tests/fixtures/deploy-winrm.ps1` — WinRM deploy/arm/teardown helper (adapted from `deploy-responder.ps1`) copying the real `rdpilot-sensor.exe` and launching it via an AtLogOn Interactive-principal Scheduled Task; `-Remove` teardown switch.
- `live_session.rs::sensor_rdpdr_deploy_and_ping_within_1s` — gated test (RDPDR primary; SC2 mandatory + SC4).
- `live_session.rs::sensor_winrm_deploy_and_ping_within_1s` — gated test (WinRM fallback; SC3 + SC4).
Deletions: `tests/fixtures/sensor-responder.ps1`, `tests/fixtures/deploy-responder.ps1` (throwaway Phase-4 assets, D-5.7).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: WinRM fallback deploy fixture (deploy-winrm.ps1, SC3)</name>
  <files>crates/rdpilot/tests/fixtures/deploy-winrm.ps1</files>
  <read_first>
    crates/rdpilot/tests/fixtures/deploy-responder.ps1 (the proven Phase-4 shape to adapt near-verbatim: .secrets/connection.json read + PSCredential without echoing the password, New-PSSession with -SkipCACheck/-SkipCNCheck/-UseSSL/-Authentication Negotiate, Copy-Item -ToSession, Register-ScheduledTask AtLogOn + Interactive principal, immediate Start-ScheduledTask kick, and the -Remove teardown),
    .planning/phases/04-dvc-transport-channel/04-03-SUMMARY.md (the AtLogOn ~20-30s latency + no-refire-on-reconnect gotcha — RESEARCH Pitfall 5 — carried forward unchanged)
  </read_first>
  <action>Create `crates/rdpilot/tests/fixtures/deploy-winrm.ps1` by adapting `deploy-responder.ps1`: change the payload from the throwaway `.ps1` responder to the REAL published `rdpilot-sensor.exe` (default `-SensorExe` param pointing at `sensor/bin/Release/net8.0/win-x64/publish/rdpilot-sensor.exe`), `Copy-Item -ToSession` it to a target path (e.g. `C:\rdpilot\rdpilot-sensor.exe`), and change the Scheduled-Task action to launch that exe directly (`-Execute <copied exe path>`) instead of `pwsh -File`. Keep everything else identical to the proven Phase-4 mechanism: the same self-signed-lab-cert WinRM options, the AtLogOn trigger + Interactive principal (so the sensor lands in the RDP session, Session > 0 — not WinRM's Session 0), the immediate `Start-ScheduledTask` kick (handles already-logged-in), the never-echo-password discipline (T-04-06 / Security V5), and the `-Remove` teardown. Retain the header NOTE documenting the session-connect-event trigger (IDs 4778/25) as the empirical fallback if the sensor lands in Session 0. Include a D-5.5 hook: an optional `-AddAvExclusion` switch that, when set, adds a Defender exclusion for the sensor path in the same Invoke-Command (only used if the live gate surfaces an actual AV/EDR block — do NOT enable it proactively).</action>
  <acceptance_criteria>
    - `deploy-winrm.ps1` contains `Copy-Item` of a `.exe` (not a `.ps1`) and `Register-ScheduledTask` with an AtLogOn trigger + Interactive principal.
    - The password is never written to a `Write-Host`/log line (grep: no `$conn.password` or `$cred` interpolated into output).
    - Contains a `-Remove` teardown branch and the Session>0 header NOTE.
    - The default sensor-exe path points at the Plan 01 NativeAOT publish output.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Copy-Item' crates/rdpilot/tests/fixtures/deploy-winrm.ps1 && grep -q 'Register-ScheduledTask' crates/rdpilot/tests/fixtures/deploy-winrm.ps1 && grep -q 'rdpilot-sensor.exe' crates/rdpilot/tests/fixtures/deploy-winrm.ps1 && test $(grep -c 'Write-Host.*password' crates/rdpilot/tests/fixtures/deploy-winrm.ps1) -eq 0</automated>
  </verify>
  <done>`deploy-winrm.ps1` copies and launches the real sensor exe via the proven AtLogOn/Interactive Scheduled-Task mechanism, never echoes the password, and has a teardown switch — the SC3 deployment mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Add both gated live tests + delete the throwaway Phase-4 fixtures (D-5.7)</name>
  <files>crates/rdpilot/tests/live_session.rs, crates/rdpilot/tests/fixtures/sensor-responder.ps1, crates/rdpilot/tests/fixtures/deploy-responder.ps1</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (the exact patterns to mirror: require_target!/block_on harness, the existing sensor_ping_pong_under_500ms bounded-retry structure and its 60s RETRY_BUDGET, and the SC4-style elapsed assertion — the new tests reuse this shape but assert the 1s bound from a successful launch),
    crates/rdpilot/tests/common/mod.rs (load_config gating; the config now needs sensor_binary_path set for the RDPDR test — add it via ConnectionConfig::sensor_binary_path from Plan 03),
    crates/rdpilot/src/session.rs (Session::deploy_and_launch from Plan 03; Session::ping from Phase 4)
  </read_first>
  <action>Add two gated (`#[ignore]` + `RDPILOT_LIVE` via require_target!) tests to `live_session.rs`, mirroring `sensor_ping_pong_under_500ms`:
1. `sensor_rdpdr_deploy_and_ping_within_1s` (SC2 mandatory + SC4): build the config with `ConnectionConfig::sensor_binary_path(<published exe>)` set, `Session::connect` (RDPDR channel registers automatically), then call `Session::deploy_and_launch()` and assert the returned elapsed (first successful pong after a successful launch, per D-5.2) is `< Duration::from_secs(1)` (SC4). This path does NOT use WinRM — it is the pure in-band primary path (D-5.1). On failure, panic naming the RDPDR/drive-redirection-GPO risk (D-5.6) and the last error.
2. `sensor_winrm_deploy_and_ping_within_1s` (SC3 + SC4): after `deploy-winrm.ps1` has deployed+armed the sensor (a human/host step, invoked before the test — documented in the test doc-comment like Phase 4's deploy-responder.ps1), `Session::connect` (no sensor path needed here), then bounded poll-retry `ping()` (reuse the 60s outer setup budget for AtLogOn latency, RESEARCH Pitfall 5) and assert the first successful `ping()` elapsed is `< Duration::from_secs(1)` (SC4). `println!` the measured round trip for the human-check record.
Then DELETE `tests/fixtures/sensor-responder.ps1` and `tests/fixtures/deploy-responder.ps1` — the C# sensor + deploy-winrm.ps1 supersede them (D-5.7). Update the existing `sensor_ping_pong_under_500ms` test doc-comment (which references the deleted fixtures) or remove that Phase-4 test if it is fully superseded by the two new tests — confirm the two new tests cover its SC#2/SC#3 positive path before removing it; if kept, repoint its comment to the C# sensor + deploy-winrm.ps1.
Confirm the whole test file still compiles offline with `cargo test -p rdpilot --no-run` (the live tests stay `#[ignore]`; the default suite stays green).</action>
  <acceptance_criteria>
    - `live_session.rs` contains `sensor_rdpdr_deploy_and_ping_within_1s` and `sensor_winrm_deploy_and_ping_within_1s`, both `#[ignore]`-gated, both asserting `< Duration::from_secs(1)` (SC4).
    - The RDPDR test sets `ConnectionConfig::sensor_binary_path` and calls `Session::deploy_and_launch`.
    - `tests/fixtures/sensor-responder.ps1` and `tests/fixtures/deploy-responder.ps1` no longer exist.
    - `cargo test -p rdpilot --no-run` compiles the suite; `cargo test -p rdpilot` (offline) stays green.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'sensor_rdpdr_deploy_and_ping_within_1s' crates/rdpilot/tests/live_session.rs && grep -q 'sensor_winrm_deploy_and_ping_within_1s' crates/rdpilot/tests/live_session.rs && test ! -f crates/rdpilot/tests/fixtures/sensor-responder.ps1 && test ! -f crates/rdpilot/tests/fixtures/deploy-responder.ps1 && cargo test -p rdpilot --no-run 2>&1 | grep -qiE 'Compiling|Finished'</automated>
  </verify>
  <done>Both gated live tests exist and compile, asserting the SC4 1s bound for the RDPDR (SC2) and WinRM (SC3) paths; the throwaway Phase-4 fixtures are deleted (D-5.7); the offline suite stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the live gate on a Windows host + Azure VM (SC2 mandatory, SC3, SC4)</name>
  <action>Run the live gate on a Windows host + Azure VM: RDPDR primary path (SC2 mandatory) and WinRM fallback (SC3), both asserting ping/pong within 1s of launch (SC4); resolve AV/EDR + GPO empirically and tear down.</action>
  <what-built>The full phase: the NativeAOT sensor (Plan 01), the RDPDR primary path + deploy_and_launch (Plans 02/03), and the WinRM fallback fixture + gated tests (this plan). This checkpoint runs the actual live gate — it cannot run in the Linux authoring sandbox (no pwsh/WSMan, no route to the Azure VM; same constraint as Phase 4's live gate, 04-03-SUMMARY).</what-built>
  <how-to-verify>
    On a Windows host with pwsh + Azure access, with the sensor already published (Plan 01 Task 3):
    1. Provision the target: `pwsh infra/manage-env.ps1 up` (writes `.secrets/connection.json`). A fresh `up` + first connect produces a fresh logon (needed for the AtLogOn WinRM trigger to fire — RESEARCH Pitfall 5).
    2. RDPDR PRIMARY (SC2 MANDATORY, D-5.6 — no conditional pass): `RDPILOT_LIVE=1 cargo test -p rdpilot sensor_rdpdr_deploy_and_ping_within_1s -- --ignored --test-threads=1`. Assert PASS and record the measured elapsed (`< 1s`, SC4). If drive-redirection is blocked by GPO (STATE Blocker), fix it on the target via a Configure-Target.ps1 policy change (D-5.5 empirical pattern) — RDPDR success is required, not waivable.
    3. WinRM FALLBACK (SC3): `pwsh crates/rdpilot/tests/fixtures/deploy-winrm.ps1` to deploy+arm the real exe, then `RDPILOT_LIVE=1 cargo test -p rdpilot sensor_winrm_deploy_and_ping_within_1s -- --ignored --test-threads=1`. Assert PASS + record elapsed. Tear down with `deploy-winrm.ps1 -Remove`.
    4. Empirically resolve and record: AV/EDR behavior (D-5.5 — if the sensor exe is flagged/quarantined, add the AV exclusion via `deploy-winrm.ps1 -AddAvExclusion` / the deploy step, only as needed); the binary size (D-5.3); whether the AtLogOn trigger needed a manual `Start-ScheduledTask` kick on reconnect.
    5. Tear down: `pwsh infra/manage-env.ps1 -Action down` and confirm the test RG is deleted.
  </how-to-verify>
  <resume-signal>Type "approved" with both measured elapsed times (RDPDR + WinRM, each < 1s), the binary size, and any AV/EDR or GPO remediation applied — or describe the failure (which path, which SC).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WinRM control channel → target | `deploy-winrm.ps1` opens a WinRM session with lab-cert options and copies/launches the sensor exe; credentials come from `.secrets/connection.json`. |
| Live target environment | AV/EDR + drive-redirection GPO on the Azure VM (empirical, D-5.5/D-5.6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Information Disclosure | deploy-winrm.ps1 credential handling | high | mitigate | Password read once into a PSCredential, never echoed to any Write-Host/log line (T-04-06 / Security V5); `.secrets/connection.json` is the only credential source (unchanged). Grep-gate forbids password interpolation into output. |
| T-05-10 | Denial of Service (self-inflicted) | AV/EDR quarantining rdpilot-sensor.exe | medium | mitigate | D-5.5 empirical handling — an AV exclusion is added (via `-AddAvExclusion`) ONLY if the live gate surfaces an actual block; no proactive code-signing (deferred out of v1). |
| T-05-11 | Spoofing | Sensor launched in the wrong session (Session 0) | medium | mitigate | AtLogOn trigger + Interactive principal lands the sensor in the RDP session (Session > 0); the header NOTE documents the session-connect-event trigger as the empirical fallback (proven Phase-4 mechanism, T-04-08). |
</threat_model>

<verification>
- Offline: `deploy-winrm.ps1` static checks (copies the .exe, AtLogOn/Interactive task, no password echo, -Remove); `cargo test -p rdpilot --no-run` compiles both gated tests; offline suite green; throwaway fixtures deleted.
- Live gate (phase gate): `sensor_rdpdr_deploy_and_ping_within_1s` PASS (SC2 mandatory, elapsed < 1s SC4) AND `sensor_winrm_deploy_and_ping_within_1s` PASS (SC3, elapsed < 1s SC4).
</verification>

<success_criteria>
- SC2 met (mandatory, D-5.6): the SDK deploys the sensor via drive-redirection copy and launches it in the RDP session; ping/pong within 1s of a successful launch.
- SC3 met: the WinRM bootstrap path also deploys + launches the sensor; ping/pong within 1s.
- SC4 met on both paths: first successful ping after launch is < 1 second.
- D-5.7 satisfied: the throwaway Phase-4 responder fixtures are deleted.
- Empirical unknowns resolved and recorded: AV/EDR (D-5.5), drive-redirection GPO (D-5.6), binary size (D-5.3).
</success_criteria>

<output>
Create `.planning/phases/05-sensor-bootstrap-deployment/05-04-SUMMARY.md` when done — record both measured round-trip times, the binary size, and the AV/EDR + GPO empirical outcomes.
</output>
