---
phase: 05-sensor-bootstrap-deployment
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - crates/rdpilot/src/connect.rs
  - crates/rdpilot/src/config.rs
  - crates/rdpilot/src/session.rs
  - crates/rdpilot/src/error.rs
autonomous: true
requirements: [SENSOR-02]

must_haves:
  truths:
    - "At connect time the SDK registers the RDPDR static channel alongside DrdynvcClient, announcing a RDPILOT drive backed by the built sensor exe (D-5.1, SC2)"
    - "Session::deploy_and_launch injects the Win+R copy-and-start sequence then poll-retries pings until a pong, re-injecting up to N times (D-5.2)"
    - "SC4's 1-second budget is measured from a successful launch / first pong, not from the first keystroke (D-5.2)"
  artifacts:
    - path: "crates/rdpilot/src/connect.rs"
      provides: "Rdpdr static-channel registration on the connector before connect_begin"
      contains: "Rdpdr"
    - path: "crates/rdpilot/src/session.rs"
      provides: "Session::deploy_and_launch bootstrap (Win+R inject + poll-and-retry)"
      contains: "deploy_and_launch"
    - path: "crates/rdpilot/src/config.rs"
      provides: "ConnectionConfig::sensor_binary_path builder + getter (owned PathBuf)"
      contains: "sensor_binary_path"
  key_links:
    - from: "crates/rdpilot/src/connect.rs"
      to: "ironrdp_rdpdr::Rdpdr"
      via: "connector.with_static_channel(rdpdr_channel) alongside the existing DrdynvcClient registration"
      pattern: "with_static_channel"
    - from: "crates/rdpilot/src/session.rs"
      to: "Session::send_key(Combo([Win, R])) + Session::ping()"
      via: "deploy_and_launch orchestrates inject-then-poll (D-5.2)"
      pattern: "deploy_and_launch"
---

<objective>
Wire the RDPDR drive-redirection PRIMARY deployment path (D-5.1, SENSOR-02, SC2 + the SC4 launch-timing contract): register the `ironrdp-rdpdr` static channel at connect time with a `RdpilotDriveBackend` serving the built sensor exe, and add `Session::deploy_and_launch` — the in-band Win+R copy-and-start injection with D-5.2 poll-and-retry.

Purpose: This is the SDK-side of the primary, WinRM-independent deployment mechanism the phase mandates (D-5.6 makes RDPDR success mandatory). It reuses Plan 02's `Key::Win` and `RdpilotDriveBackend`, the existing Phase-3 `Session::send_key`, and the Phase-4 `Session::ping()`.

Output: `connect.rs` registers the RDPDR static channel; `ConnectionConfig` carries the served sensor-exe path; `Session::deploy_and_launch` performs the injected launch + poll-retry; `Error::Bootstrap` names launch failures.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sensor-bootstrap-deployment/05-RESEARCH.md
@.planning/phases/05-sensor-bootstrap-deployment/05-CONTEXT.md
@crates/rdpilot/src/connect.rs
@crates/rdpilot/src/session.rs
@crates/rdpilot/src/config.rs
@crates/rdpilot/src/error.rs
@crates/rdpilot/src/rdpdr_backend.rs
</context>

<artifacts_produced>
New symbols this plan creates (consumed by Plan 04's live tests):
- `ConnectionConfig::sensor_binary_path(impl Into<PathBuf>)` builder + `get_sensor_binary_path() -> Option<&Path>` getter (owned type, D-09).
- RDPDR static-channel registration in `connect::connect` (before `connect_begin`, SC#1-style seam) via `ironrdp_rdpdr::Rdpdr::new(Box::new(RdpilotDriveBackend::new(path)), "rdpilot").with_drives(Some(vec![(0, "RDPILOT")]))` + `connector.with_static_channel(...)`, registered only when a sensor path is configured.
- `Session::deploy_and_launch(&self) -> Result<Duration>` — injects Win+R + copy/start command, poll-retries `ping()` per D-5.2, returns the first successful round-trip elapsed.
- `Error::Bootstrap(String)` variant + `Error::bootstrap(impl Into<String>)` constructor + `"bootstrap"` category.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Register the RDPDR static channel at connect time + carry the sensor-exe path in config</name>
  <files>crates/rdpilot/src/config.rs, crates/rdpilot/src/connect.rs</files>
  <read_first>
    crates/rdpilot/src/connect.rs (the exact seam: the DrdynvcClient registration + connector.with_static_channel at connect.rs ~line 99-101, BEFORE connect_begin — the Rdpdr channel registers at the identical seam as a SIBLING static channel, NOT routed through DrdynvcClient; RESEARCH Pattern 1),
    crates/rdpilot/src/config.rs (the owned builder pattern: port()/domain()/accept_invalid_certs() and their get_* getters — sensor_binary_path follows the same shape),
    crates/rdpilot/src/rdpdr_backend.rs (RdpilotDriveBackend::new signature from Plan 02),
    ~/.cargo/registry/src/*/ironrdp-client-*/src/rdp.rs OR the ironrdp-rdpdr docs (confirm the exact Rdpdr::new / with_drives signature and that with_static_channel accepts it — RESEARCH Sources)
  </read_first>
  <action>Add an owned `sensor_binary_path: Option<PathBuf>` field to `ConnectionConfig` with a `sensor_binary_path(impl Into<PathBuf>)` builder method and a `get_sensor_binary_path(&self) -> Option<&std::path::Path>` getter, mirroring the existing `port()`/`get_port()` owned-builder shape (D-09 — no third-party type in the signature).

In `connect::connect`, AFTER the existing DrdynvcClient registration and BEFORE `connect_begin` (the same connect-time seam — registration is impossible once the session is active, SC#1 hard constraint), conditionally register the RDPDR static channel WHEN `cfg.get_sensor_binary_path()` is `Some(path)`: construct `ironrdp_rdpdr::Rdpdr::new(Box::new(crate::rdpdr_backend::RdpilotDriveBackend::new(path, "rdpilot-sensor.exe")), "rdpilot".to_owned()).with_drives(Some(vec![(0, "RDPILOT".to_owned())]))` and pass it to `connector.with_static_channel(...)` as a sibling to the drdynvc channel (RESEARCH Pattern 1). Confirm at execution time (read_first) that the registered static channel's inbound IRPs are dispatched automatically by `ActiveStage::process` (the same automatic SVC dispatch the drdynvc static channel already relies on) — if so, NO `session_loop.rs` change is needed for RDPDR request/response; the backend responds reactively. If the crate requires a manual dispatch arm, note it and add the minimal wiring, but do not hand-roll MS-RDPEFS framing (RESEARCH Don't Hand-Roll). When no sensor path is configured, the connect path is byte-for-byte the current behavior (no RDPDR channel) so all existing Phase 2-4 tests stay green.</action>
  <acceptance_criteria>
    - `config.rs` contains `sensor_binary_path` (builder) and `get_sensor_binary_path` (getter) with an owned `PathBuf`/`Path` signature (no ironrdp type).
    - `connect.rs` contains `Rdpdr` and a second `with_static_channel` call gated on a configured sensor path, placed before `connect_begin`.
    - `cargo build -p rdpilot` succeeds and `cargo test -p rdpilot` stays green (existing connect tests unaffected when no sensor path is set).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'sensor_binary_path' crates/rdpilot/src/config.rs && grep -q 'Rdpdr' crates/rdpilot/src/connect.rs && cargo test -p rdpilot connect:: 2>&1 | grep -q 'test result: ok'</automated>
  </verify>
  <done>The RDPDR static channel is registered at the connect-time seam alongside DrdynvcClient when a sensor exe path is configured; the served file path flows from ConnectionConfig into RdpilotDriveBackend; existing tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Session::deploy_and_launch — Win+R inject + poll-and-retry (D-5.1/D-5.2)</name>
  <files>crates/rdpilot/src/session.rs, crates/rdpilot/src/error.rs</files>
  <read_first>
    crates/rdpilot/src/session.rs (the existing send_key(Combo)/send_key(Type) and ping() methods this orchestrates; the offline test harness test_session_with_channel/test_session_with_sensor patterns to add unit tests against),
    crates/rdpilot/src/input.rs (Key::Win from Plan 02, KeyAction::Combo/Type),
    crates/rdpilot/src/error.rs (the owned Error enum + category() + the dvc()/coordinate_out_of_bounds() constructor pattern to mirror for Bootstrap),
    .planning/phases/04-dvc-transport-channel/04-03-SUMMARY.md (the empirically-measured launch latency: AtLogOn ~20-30s is WinRM-specific, but the RDPDR path still needs a generous retry budget for the responder's own WTSVirtualChannelOpenEx race)
  </read_first>
  <action>Add `Error::Bootstrap(String)` to `error.rs` with a `bootstrap(impl Into<String>)` constructor and a `"bootstrap"` category arm, mirroring the existing `Error::dvc` shape (owned String, no third-party type — D-09).

Add `pub async fn deploy_and_launch(&self) -> Result<Duration>` to `Session`. It performs the D-5.1 in-band launch, then the D-5.2 poll-and-retry:
1. Inject `send_key(KeyAction::Combo(vec![Key::Win, Key::R]))` to open the Run dialog, a brief settle, then `send_key(KeyAction::Type(<launch command>))`, then `send_key(KeyAction::Combo(vec![Key::Enter]))`. The launch command copies the sensor from the redirected drive and starts it, e.g. `cmd /c copy \\tsclient\RDPILOT\rdpilot-sensor.exe %TEMP%\rdpilot-sensor.exe && start "" %TEMP%\rdpilot-sensor.exe` (construct the exact string in a small pure helper so it is offline-unit-testable; the served filename must match the name announced by the RDPDR backend in Task 1).
2. Poll `self.ping()` on a bounded interval; on the FIRST `Ok(elapsed)`, return that elapsed (this is the SC4 measurement — from a successful launch/first pong, NOT from the first keystroke, D-5.2). If the ping window elapses with no pong, RE-INJECT the Win+R launch sequence and continue, up to N launch attempts, before returning `Error::Bootstrap` naming the interactive-session/launch-failure cause (parallel to the live test's diagnostic in Phase 4). Choose N and the interval so the total outer budget comfortably exceeds the responder's own open-retry window; the per-ping hard bound stays `Session::ping()`'s own 500ms (unchanged). Do this on the caller's async context (never inside the session loop; Pitfall 3).
Add offline unit tests for the pure launch-command-string helper (asserts the `\\tsclient\RDPILOT\` source, the `%TEMP%` destination, and the `start` invocation) and for `Error::Bootstrap` category/message rendering. The full inject-then-poll loop is exercised live in Plan 04.</action>
  <acceptance_criteria>
    - `error.rs` contains an `Error::Bootstrap` variant and a `"bootstrap"` category arm (asserted by a unit test on category()).
    - `session.rs` contains `pub async fn deploy_and_launch` that calls `send_key(Combo([Win, R]))`, types a launch command containing `\\tsclient\RDPILOT\` and `start`, and poll-retries `ping()`.
    - The launch-command helper is a pure function with a unit test asserting the source/destination/start substrings.
    - `cargo test -p rdpilot` passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'deploy_and_launch' crates/rdpilot/src/session.rs && grep -q 'Bootstrap' crates/rdpilot/src/error.rs && cargo test -p rdpilot 2>&1 | grep -q 'test result: ok'</automated>
  </verify>
  <done>`Session::deploy_and_launch` injects the Win+R copy-and-start sequence and poll-retries pings per D-5.2, returning the first successful round-trip elapsed (SC4 measured from launch, not first keystroke); the launch-command builder and Error::Bootstrap are offline-unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → remote interactive session | The SDK injects keystrokes (Win+R + a shell command) and reads pongs; the command runs at the interactive user's integrity level. |
| Connect-time channel registration | The RDPDR static channel is announced before connect_begin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | Injected Win+R launch command | low | accept | The launch runs at the interactive logon user's own integrity level (never elevated); the command only copies + starts the SDK's own sensor from the SDK-served drive — no privilege boundary is crossed. Single-tenant lab VM (carried-forward Phase-4 disposition). |
| T-05-07 | Tampering | Launch-command string construction | medium | mitigate | The command is built in a pure, unit-tested helper with a fixed served filename that matches the RDPDR backend's announced name; the served file is access-controlled by the backend (T-05-04, Plan 02). |
| T-05-08 | Denial of Service | deploy_and_launch poll loop | low | mitigate | Bounded launch-attempt count + bounded per-ping timeout (Session::ping's own 500ms); on exhaustion returns a typed `Error::Bootstrap`, never hangs or panics (API-01). |
</threat_model>

<verification>
- `cargo test -p rdpilot` green: connect-path tests unaffected when no sensor path is set; launch-command-builder + Error::Bootstrap unit tests pass.
- RDPDR channel registers only when a sensor path is configured (existing Phase 2-4 behavior preserved otherwise).
- Live proof (SC2/SC4) is exercised in Plan 04's `sensor_rdpdr_deploy_and_ping_within_1s`.
</verification>

<success_criteria>
- SENSOR-02 primary path implemented: the SDK announces the RDPILOT redirected drive at connect and can deploy+launch the sensor in-band via Win+R with D-5.2 poll-and-retry — ready for the live gate to prove SC2 (mandatory, D-5.6) and SC4.
</success_criteria>

<output>
Create `.planning/phases/05-sensor-bootstrap-deployment/05-03-SUMMARY.md` when done — record whether ActiveStage auto-dispatches the RDPDR static channel (or whether a session_loop arm was needed), and the chosen launch-attempt count N / interval for deploy_and_launch.
</output>
