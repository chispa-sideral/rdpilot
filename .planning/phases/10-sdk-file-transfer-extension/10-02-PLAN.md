---
phase: 10-sdk-file-transfer-extension
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - crates/rdpilot/src/rdpdr_backend.rs
autonomous: true
requirements: [FILE-01, FILE-04]
must_haves:
  truths:
    - "DeviceWriteRequest writes offset-keyed bytes into a per-transfer staged .part file under <share_root>/.rdpilot-staging/ (D-10.3)"
    - "ServerDriveSetInformationRequest (FILE_END_OF_FILE_INFORMATION) records the authoritative expected final size"
    - "A clean Close after all bytes are accounted for atomically renames the .part to the final destination; an interrupted transfer leaves a stale .part and NEVER a final-named file (FILE-04 detectable failure)"
    - "A synthetic multi-IRP write sequence (3+ sequential Write IRPs) reassembles correctly — the chunked write loop actually loops, with no hardcoded chunk-size constant (FILE-04)"
  artifacts:
    - path: "crates/rdpilot/src/rdpdr_backend.rs"
      provides: "handle_write, handle_set_information, staged-temp-file write path + atomic rename on Close"
      contains: "handle_write"
  key_links:
    - from: "rdpdr_backend::handle_write"
      to: "<share_root>/.rdpilot-staging/<uuid>.part"
      via: "seek(offset)+write bounded per-IRP, never buffering the whole file"
      pattern: "staging"
    - from: "rdpdr_backend::handle_close"
      to: "fs::rename (atomic)"
      via: "rename staged .part to the resolved destination only after end_of_file bytes are accounted for"
      pattern: "rename"
---

<objective>
Implement the two currently-`NOT_SUPPORTED` MS-RDPEFS IRPs — `DeviceWriteRequest` and `ServerDriveSetInformationRequest` — as a staged-temp-file + atomic-rename write path (D-10.3), giving FILE-04's "chunked loop actually loops" and "interrupted transfer surfaces a clean, detectable failure" behaviors. This is the download-direction leg on the Rust side: the C# sensor writing to the redirected `\\tsclient\RDPILOT\<name>` UNC path drives these Write/SetInformation IRPs.

Purpose: Builds on Plan 10-01's `OpenEntry::File(PathBuf)` resolved-path contract. Owns `rdpdr_backend.rs` in Wave 2 (after 10-01). FILE-04 is BLOCKING; its offline synthetic-multi-IRP proxy lands here (the real per-IRP chunk size is only observable at the live gate, Plan 10-05).

Output: Working staged-write path with atomic rename, plus offline unit tests proving multi-IRP reassembly and interrupted-transfer detection.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sdk-file-transfer-extension/10-CONTEXT.md
@.planning/phases/10-sdk-file-transfer-extension/10-RESEARCH.md
@crates/rdpilot/src/rdpdr_backend.rs
@.planning/phases/10-sdk-file-transfer-extension/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Staged-write path — DeviceWriteRequest into <uuid>.part</name>
  <files>crates/rdpilot/src/rdpdr_backend.rs</files>
  <read_first>
    crates/rdpilot/src/rdpdr_backend.rs (post-10-01 state: OpenEntry::File(PathBuf), handle_read/read_served_bytes bounded-IO pattern, handle_drive_io_request dispatch, reject_unsupported line ~472, the two arms at lines ~507-509)
    10-RESEARCH.md Code Examples (DeviceWriteRequest { offset, write_data }, DeviceWriteResponse { length } — must ECHO bytes written; efs.rs:3526-3570)
    10-01-SUMMARY.md (final new() signature + OpenEntry contract)
  </read_first>
  <behavior>
    - A Create for a new (nonexistent) destination name under the share root is granted a handle and opens/creates a fresh <uuid>.part in <share_root>/.rdpilot-staging/ associated with that handle
    - Three sequential DeviceWriteRequest IRPs at offsets 0, N, 2N reassemble into one staged file with the concatenated bytes in order (loop actually loops)
    - Each DeviceWriteResponse echoes `length` = number of bytes actually written for that IRP
    - A Write against a read-only handle (the sensor exe) or an unknown file_id is rejected with a typed NtStatus, never a panic, never a std::fs write
    - A server-supplied enormous write is bounded to the actual write_data length (no unbounded preallocation, T-05-05)
  </behavior>
  <action>
    Extend `OpenEntry` so a writable file handle carries its staged `.part` path and the resolved final destination (e.g. `OpenEntry::WriteFile { staging: PathBuf, dest: PathBuf, expected_len: Option<u64> }`), keeping `OpenEntry::File(PathBuf)` for read handles and `Root` unchanged. In `handle_create`, when the create disposition indicates a create/overwrite intent (FILE_CREATE/FILE_OPEN_IF/FILE_OVERWRITE_IF/FILE_SUPERSEDE — inspect `create_disposition`) for a share-root path (NOT the read-only sensor exe), allocate a fresh staging path `<share_root>/.rdpilot-staging/<uuid>.part` (uuid: reuse the existing nanos+pid scheme from the test helper, or a monotonic counter — no new crate) and record a WriteFile handle; the resolved `dest` still comes from `resolve_under_root` (Plan 10-01) so the destination is ancestry-validated BEFORE any file is created.
    Implement `handle_write(DeviceWriteRequest)`: look up the WriteFile handle (reject unknown/read-only ids with NtStatus::ACCESS_DENIED, no fs touch); open the staging file, `seek(SeekFrom::Start(offset))`, write exactly `write_data`, and reply `DeviceWriteResponse { length: write_data.len() }`. Never buffer the whole transfer in memory — one seek+write per IRP (T-05-05, D-10.3). Any IO error maps to NtStatus::UNSUCCESSFUL, never a panic (API-01). Wire the `DeviceWriteRequest` arm in `handle_drive_io_request` to `handle_write` (replacing `reject_unsupported` at line ~507). Do NOT hardcode or assume any per-IRP chunk size — the offset/length come from the IRP (10-RESEARCH Item 1 / Open Question 1).
    Add an offline test feeding 3+ sequential Write IRPs at increasing offsets after a create, reading the staged file back, and asserting exact ordered reassembly + that each response echoes the right length.
  </action>
  <acceptance_criteria>
    - rdpdr_backend.rs contains `fn handle_write` and a WriteFile-style OpenEntry variant carrying staging + dest paths
    - handle_drive_io_request dispatches `DeviceWriteRequest` to handle_write (no longer reject_unsupported)
    - DeviceWriteResponse echoes the per-IRP bytes-written length
    - A test drives >=3 sequential Write IRPs and asserts exact ordered reassembly (chunked loop loops)
    - No chunk-size numeric constant is hardcoded anywhere (grep: no magic byte-count gate)
    - No unwrap/expect/panic outside #[cfg(test)]
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib rdpdr_backend::</automated>
  </verify>
  <done>Multi-IRP offset-keyed writes reassemble into a staged .part; each response echoes bytes written; unbounded-alloc and access-control disciplines preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetInformation (end-of-file) + atomic rename on clean Close (FILE-04 interrupted-transfer detection)</name>
  <files>crates/rdpilot/src/rdpdr_backend.rs</files>
  <read_first>
    crates/rdpilot/src/rdpdr_backend.rs (handle_close line ~149, handle_write from Task 1, the ServerDriveSetInformationRequest arm line ~508)
    10-RESEARCH.md Code Examples (ServerDriveSetInformationRequest { set_buffer: FileInformationClass }, FileEndOfFileInformation { end_of_file: i64 } efs.rs:3639-3654, ClientDriveSetInformationResponse::new(&req, io_status) helper efs.rs:3747-3761)
  </read_first>
  <behavior>
    - A SetInformation carrying FILE_END_OF_FILE_INFORMATION records the expected total size on the WriteFile handle and replies ClientDriveSetInformationResponse with the request's Length echoed and SUCCESS
    - A SetInformation on an unknown/read-only handle is rejected with a typed NtStatus, never a panic
    - A Close on a WriteFile handle whose staged bytes account for the expected end_of_file atomically renames <uuid>.part to the resolved destination; the final-named file exists with the full content
    - A Close on a WriteFile handle that is INCOMPLETE (fewer bytes than expected_len, i.e. interrupted) does NOT rename — the destination name never appears; the stale .part remains for detection (FILE-04)
    - Any of the 5 decodable SetInformation sub-variants returns a well-formed ClientDriveSetInformationResponse (never NOT_SUPPORTED — Windows' write sequence issues these)
  </behavior>
  <action>
    Implement `handle_set_information(ServerDriveSetInformationRequest)`: for a WriteFile handle, when `set_buffer` decodes to `FileEndOfFileInformation`, store `end_of_file` as the handle's `expected_len` (the authoritative total-size signal, 10-RESEARCH). For the other four decodable sub-variants (Basic/Disposition/Rename/Allocation) reply success without side effects (this minimal backend does not model rename/disposition semantics — it only needs end-of-file). Build the reply via `ClientDriveSetInformationResponse::new(&req, io_status)` so `length` echoes the request Length per spec. Reject unknown/read-only handles with ACCESS_DENIED. Wire the `ServerDriveSetInformationRequest` arm in `handle_drive_io_request` to this handler (replacing reject_unsupported at line ~508).
    Extend `handle_close`: when closing a WriteFile handle, flush/close the staging file, then check completeness — if `expected_len` is Some and the staged file length equals it (or, when end_of_file was never sent, treat any bytes-written-then-clean-close as complete only if the design decision below allows), atomically `fs::rename(staging, dest)` (single syscall, no partially-written destination ever observable — Don't Hand-Roll table). If incomplete, DO NOT rename: leave the `.part` in staging (FILE-04 "clean, detectable failure — staged-and-renamed"). A rename failure maps to a typed NtStatus, never a panic. Adopt the CONTEXT-discretionary staging cleanup policy: best-effort removal of stale `.part` files at connect/first-use time (implement the sweep helper here or note it for connect.rs; do not block on it). Record in the SUMMARY the exact completeness rule chosen (expected_len match vs bytes-accounted) since the live gate confirms real Windows Close/SetInformation ordering.
    Add offline tests: (a) create → 3 writes → SetInformation(end_of_file=total) → Close ⇒ destination exists with full content, staging empty; (b) create → partial writes (fewer than expected_len) → Close ⇒ destination does NOT exist, a `.part` remains (interrupted-transfer detection).
  </action>
  <acceptance_criteria>
    - rdpdr_backend.rs contains `fn handle_set_information` and dispatches ServerDriveSetInformationRequest to it (not reject_unsupported)
    - end_of_file value from FileEndOfFileInformation is stored as the handle's expected total size
    - handle_close performs `fs::rename` of the staged .part to the resolved dest ONLY when complete
    - Test (a): clean transfer renames to destination with exact full content
    - Test (b): interrupted transfer leaves no destination file and a stale .part (grep for a test asserting the destination path does NOT exist)
    - No unwrap/expect/panic outside #[cfg(test)]
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib rdpdr_backend::</automated>
  </verify>
  <done>SetInformation records the authoritative size; a clean Close atomically renames; an interrupted transfer is detectable (no destination file, stale .part) — FILE-04 offline proxy proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote Windows OS → Rust RdpilotDriveBackend (write path) | Write/SetInformation IRP offsets, lengths, and end_of_file are untrusted; the destination path was already ancestry-validated in Plan 10-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Denial of Service | rdpdr_backend::handle_write | high | mitigate | One bounded seek+write per IRP into the staged .part; never buffer the whole transfer in memory (D-10.3, T-05-05) |
| T-10-04 | Tampering (integrity) | rdpdr_backend::handle_close atomic rename | high | mitigate | Staged temp file + single-syscall atomic rename only on a complete Close; an interrupted transfer never produces a final-named file, only a detectable stale .part (D-10.3, FILE-04) |
| T-10-01 | Tampering / Elevation | write destination path | critical | mitigate | Destination resolved via Plan 10-01 resolve_under_root BEFORE any staging file is created — no write path bypasses canonicalized ancestry validation (D-10.2) |
</threat_model>

<verification>
- `cargo test -p rdpilot --lib rdpdr_backend::` green (write + set-information + close reassembly and interrupted-transfer tests)
- No hardcoded per-IRP chunk-size constant (FILE-04 design correctness — 10-RESEARCH Item 1)
- No new clippy::unwrap_used/expect_used in non-test code
</verification>

<success_criteria>
The staged-write path (Write + SetInformation + atomic-rename-on-Close) is implemented and offline-proven: multi-IRP sequences reassemble (chunked loop loops), and interrupted transfers are cleanly detectable (no destination file, stale .part) — satisfying FILE-04's offline-verifiable half. Real per-IRP chunk size and Windows Close/SetInformation ordering are deferred to the live gate (Plan 10-05).
</success_criteria>

<output>
Create `.planning/phases/10-sdk-file-transfer-extension/10-02-SUMMARY.md` when done, recording: the WriteFile OpenEntry shape, the exact transfer-completeness rule (expected_len match vs bytes-accounted), the staging-cleanup approach, and that the real chunk size + Close/SetInformation ordering remain live-gate items for Plan 10-05.
</output>
