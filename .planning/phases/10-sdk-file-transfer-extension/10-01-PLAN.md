---
phase: 10-sdk-file-transfer-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rdpilot/Cargo.toml
  - crates/rdpilot/src/error.rs
  - crates/rdpilot/src/config.rs
  - crates/rdpilot/src/rdpdr_backend.rs
  - crates/rdpilot/src/connect.rs
autonomous: true
requirements: [FILE-01, FILE-03]
must_haves:
  truths:
    - "A share-root path is configurable on ConnectionConfig and threaded into the RDPDR drive backend at connect time"
    - "Every RDPDR-supplied path is canonicalized and ancestry-checked under the share root before any std::fs call (D-10.2, Rust side)"
    - "The BLOCKING FILE-03 adversarial matrix (trailing `..` no separator, mixed `/`+`\\`, absolute-as-relative, sibling-prefix) is REJECTED with Error::PathTraversal-shaped NtStatus by the Rust validator"
    - "A Read against a validated share-root file returns its bytes; the sensor exe read-only special case still works (bootstrap unbroken)"
  artifacts:
    - path: "crates/rdpilot/src/error.rs"
      provides: "Error::PathTraversal(String) and Error::ChecksumMismatch { expected, actual } variants + constructors + category strings"
      contains: "PathTraversal"
    - path: "crates/rdpilot/src/config.rs"
      provides: "share_root builder + get_share_root getter on ConnectionConfig"
      contains: "share_root"
    - path: "crates/rdpilot/src/rdpdr_backend.rs"
      provides: "share-root generalization: OpenEntry::File(PathBuf), resolve_under_root validator, generalized handle_create/handle_read"
      contains: "resolve_under_root"
  key_links:
    - from: "crates/rdpilot/src/connect.rs"
      to: "RdpilotDriveBackend::new"
      via: "passes cfg.get_share_root() alongside the sensor exe path"
      pattern: "RdpilotDriveBackend::new"
    - from: "crates/rdpilot/src/rdpdr_backend.rs handle_create"
      to: "resolve_under_root"
      via: "every non-sensor-exe path routed through the canonicalizing validator"
      pattern: "resolve_under_root"
---

<objective>
Lay the Rust-side foundation for bidirectional file transfer: the new error taxonomy (D-10.4), the configurable share root (D-10.1), the canonicalizing path validator that is the entire FILE-03 security posture on the Rust side (D-10.2), and the generalization of `RdpilotDriveBackend` from a single read-only served file to an allow-listed share root — WITHOUT breaking the existing sensor-exe bootstrap read path.

Purpose: This plan owns `rdpdr_backend.rs` first (Wave 1); Plan 10-02 builds the staged-write IRPs on top of the `OpenEntry::File(PathBuf)` contract defined here. FILE-03 is BLOCKING; its Rust adversarial suite lands here against the validator that enforces it.

Output: Compiling crate with new `Error` variants, `share_root` config, the `resolve_under_root` validator, generalized Create/Read, and a passing FILE-03 adversarial test module.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sdk-file-transfer-extension/10-CONTEXT.md
@.planning/phases/10-sdk-file-transfer-extension/10-RESEARCH.md
@crates/rdpilot/src/error.rs
@crates/rdpilot/src/config.rs
@crates/rdpilot/src/rdpdr_backend.rs
@crates/rdpilot/src/connect.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: New error taxonomy + configurable share root + promote sha2</name>
  <files>crates/rdpilot/src/error.rs, crates/rdpilot/src/config.rs, crates/rdpilot/Cargo.toml</files>
  <read_first>
    crates/rdpilot/src/error.rs (variant/constructor/category pattern — CoordinateOutOfBounds line 76, Dvc line 91, SensorRejected line 106, category strings lines 187-190, constructors lines 118-170)
    crates/rdpilot/src/config.rs (builder/getter pattern — sensor_binary_path line 129, get_sensor_binary_path line 182, Debug impl lines 188-199)
    crates/rdpilot/Cargo.toml (dependency block; sha2 is already resolved transitively per 10-RESEARCH — promoting to a direct dep introduces no new supply-chain node)
  </read_first>
  <action>
    In error.rs add two narrow variants per D-10.4, DISTINCT from Dvc/SensorRejected so a path rejection is never conflated with a transport failure: `Error::PathTraversal(String)` (Display: "path rejected: {0}") and `Error::ChecksumMismatch { expected: String, actual: String }` (Display naming both hex digests). Add `#[non_exhaustive]`-compatible arms to `category()` returning "path_traversal" and "checksum_mismatch". Add `pub(crate)` constructors `Error::path_traversal(impl Into<String>)` and `Error::checksum_mismatch(expected, actual)` mirroring the existing `dvc()`/`sensor_rejected()` constructor style. Add unit tests mirroring `sensor_rejected_category_and_message_render` for both variants (category string + Display contains the reason/digests). The Send+Sync+'static assertion at the bottom must still hold (both variants own their String data — no borrowed/third-party types, D-09).
    In config.rs add a `share_root: Option<PathBuf>` field, a `#[must_use] pub fn share_root(mut self, path: impl Into<PathBuf>) -> Self` builder and a `pub fn get_share_root(&self) -> Option<&Path>` getter, mirroring the `sensor_binary_path`/`get_sensor_binary_path` pair exactly. Add `share_root` to the redacted `Debug` impl (it is a path, not a secret — print it normally).
    In Cargo.toml add `sha2 = "0.11"` to `[dependencies]` (10-RESEARCH: version already pinned in Cargo.lock, slopcheck `[OK]`, RustCrypto reference impl). Do NOT add any C# dependency here. sha2 is consumed by Plan 10-04's checksum verification (interface-first: the dep is available before its consumer).
  </action>
  <acceptance_criteria>
    - error.rs contains `PathTraversal(String)` and `ChecksumMismatch {` and `fn path_traversal(` and `fn checksum_mismatch(`
    - error.rs `category()` match returns "path_traversal" and "checksum_mismatch"
    - config.rs contains `fn share_root(` and `fn get_share_root(` returning `Option<&Path>`
    - Cargo.toml `[dependencies]` contains a `sha2` line
    - No use of unwrap/expect/panic in non-test code (API-01; lib.rs clippy gates apply)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib error:: config::</automated>
  </verify>
  <done>Both new Error variants render and categorize; share_root builder/getter present; sha2 is a direct dependency; existing tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize RdpilotDriveBackend to a canonicalized share root (validator + Create/Read)</name>
  <files>crates/rdpilot/src/rdpdr_backend.rs, crates/rdpilot/src/connect.rs</files>
  <read_first>
    crates/rdpilot/src/rdpdr_backend.rs (OpenEntry enum line 55, struct fields lines 68-73, new() line 81, normalize() line 93, handle_create line 113, handle_read line 168, read_served_bytes line 204, handle_drive_io_request dispatch line 493)
    crates/rdpilot/src/connect.rs (RdpilotDriveBackend::new call site line 128-129, SENSOR_EXE_NAME line 64, gating on get_sensor_binary_path lines 127-145)
    crates/rdpilot/src/config.rs (get_share_root getter added in Task 1)
    10-RESEARCH.md Pitfall 1 (separator normalization) + Pitfall 2 (canonicalize-of-nonexistent-parent) + Code Example resolve_under_root
  </read_first>
  <behavior>
    - resolve_under_root(root_canonical, "sub/dir/file.bin") returns a path under the canonical root (happy path, nested legitimate file)
    - resolve_under_root rejects "..\\..\\Windows\\System32" (mixed/backslash separators) — normalized to `/` BEFORE any Path op so the case is faithful on the Linux test host (Pitfall 1)
    - resolve_under_root rejects a trailing `..` with NO following separator (FreeRDP off-by-one class, CVE-2025-48817)
    - resolve_under_root rejects an absolute-path-as-relative input (leading `/` stripped, or a rooted input)
    - handle_create maps SENSOR_EXE_NAME to the read-only sensor exe path (bootstrap unchanged); any other name is resolved under the share root, and an escaping path yields NtStatus::NO_SUCH_FILE with file_id 0 and no std::fs touch
    - handle_read streams bytes for a validated share-root OpenEntry::File(path) AND for the sensor exe, bounded by Read::take (T-05-05)
  </behavior>
  <action>
    Change `OpenEntry::File` to `OpenEntry::File(PathBuf)` carrying the RESOLVED absolute path (D-10.1). Restructure `RdpilotDriveBackend` to hold: the sensor exe path + SENSOR_EXE_NAME (the existing read-only special case), and a NEW `share_root: PathBuf` plus its canonicalized form. Change `new()` to accept the share root as an additional argument (per 10-CONTEXT Integration Points — connect.rs call site updates to pass `cfg.get_share_root()`, defaulting to a per-connection share root when unset; if `get_share_root()` is None the backend still serves ONLY the sensor exe, preserving pre-Phase-10 behavior byte-for-byte).
    Add `fn resolve_under_root(&self, untrusted: &str) -> Result<PathBuf, ()>` implementing D-10.2 + Pitfalls 1/2: FIRST `untrusted.replace('\\', "/")` UNCONDITIONALLY (regardless of host OS — this is the Pitfall 1 fix that makes the offline Linux test faithful), trim leading `/`, join onto the canonicalized share root, canonicalize the PARENT dir (which always exists — the share root, pre-created at connect/first-use) NOT the full leaf, ancestry-check the canonical parent with `Path::starts_with(canonical_root)`, then re-join the leaf component-wise onto the canonical parent. Any failure is a rejection (the caller maps it to NtStatus::NO_SUCH_FILE, never a panic, API-01). Reject rooted inputs and any resolution whose parent escapes the root.
    Rewrite `handle_create`: SENSOR_EXE_NAME (case-insensitive) → OpenEntry::File(sensor_exe_path); empty → OpenEntry::Root; otherwise `resolve_under_root` → OpenEntry::File(resolved) on success, NtStatus::NO_SUCH_FILE on rejection. Rewrite `handle_read` and `read_served_bytes` to read from the per-handle resolved `OpenEntry::File(path)` rather than a single `served_path`. Update `handle_query_directory`/`handle_query_information` metadata to stat the per-handle path (keep the defensive zero-size fallback). Keep the DeviceWriteRequest/ServerDriveSetInformationRequest arms as `reject_unsupported` for now — Plan 10-02 implements them. Do NOT normalize using host-conditional `std::path` separator logic anywhere (Pitfall 1).
    Update connect.rs to pass the share root into `RdpilotDriveBackend::new`. Pre-create the share root directory (and its `.rdpilot-staging/` subdir — Plan 10-02 relies on it existing) at connect time when a share root is configured; a create failure maps to Error::Config, never a panic.
  </action>
  <acceptance_criteria>
    - rdpdr_backend.rs contains `OpenEntry::File(` carrying a PathBuf and `fn resolve_under_root`
    - resolve_under_root normalizes `\\`→`/` before any Path/PathBuf construction (grep: `replace('\\\\', "/")` present)
    - handle_create routes non-sensor-exe names through resolve_under_root; sensor exe path still granted read-only
    - connect.rs call to `RdpilotDriveBackend::new` passes the share root argument
    - Existing rdpdr_backend tests (create_accepts_root_and_served_file..., read_returns_exact_served_bytes_at_offset, query_* ) still pass after adaptation to the new signature
    - No unwrap/expect/panic outside #[cfg(test)]
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib rdpdr_backend::</automated>
  </verify>
  <done>Backend serves the sensor exe (bootstrap) AND canonicalized share-root files; every untrusted path passes through resolve_under_root before std::fs; connect wiring compiles.</done>
</task>

<task type="auto">
  <name>Task 3: FILE-03 BLOCKING adversarial path-traversal suite (Rust validator)</name>
  <files>crates/rdpilot/src/rdpdr_backend.rs</files>
  <read_first>
    crates/rdpilot/src/rdpdr_backend.rs (#[cfg(test)] mod tests — write_temp_file helper line 536, create_req line 592, create_response_fields line 566, the existing create_accepts_root_and_served_file... test line 646 as the shape to extend)
    10-RESEARCH.md "Phase Requirements -> Test Map" FILE-03 row + "State of the Art" (FreeRDP off-by-one, trailing `..` no separator) + Pitfall 3 (sibling-prefix)
  </read_first>
  <action>
    Add a `path_traversal` test submodule (or clearly-named `traversal_*` test fns) inside the existing `#[cfg(test)] mod tests` block, driving `handle_drive_io_request(ServerCreateDriveRequest(create_req(id, path)))` for each adversarial input and asserting NtStatus::NO_SUCH_FILE + file_id 0 (never granted a handle), against a real temp share root created via `write_temp_file`'s directory. Cover EXACTLY the D-10.2 / FILE-03 matrix, one case per assertion, so a failure names the offending case: (1) trailing `..` with NO following separator (`\\subdir\\..` and bare `..`); (2) mixed `/`+`\\` separators (`..\\../Windows\\System32`, `foo/..\\..\\bar`); (3) absolute-path-as-relative (`\\C:\\Windows`, `/etc/passwd`, `C:\\Windows\\System32`); (4) sibling-directory prefix regression guard (share root name is a strict prefix of a sibling dir, e.g. root `.../share` vs candidate resolving to `.../share-evil/secret` — Rust's Path::starts_with is component-aware and MUST reject this; the case exists as a cross-language mirror of Pitfall 3). Add ONE positive control: a legitimate nested path (`sub/dir/ok.bin`) is ACCEPTED (granted a nonzero file_id) so the suite proves it rejects attacks WITHOUT over-rejecting valid nested writes (Pitfall 2 warning sign). Each test removes its temp dir/files. Do NOT hardcode any host-specific separator assumption — the Task 2 normalization makes every `\\`-bearing case faithful offline.
  </action>
  <acceptance_criteria>
    - A test named with `traversal` (e.g. `rejects_traversal_*`/`mod path_traversal`) exists asserting NtStatus::NO_SUCH_FILE for every adversarial case above
    - The trailing-`..`-no-separator case and the sibling-prefix case are each present as distinct assertions
    - A positive-control test asserts a legitimate nested path is granted a nonzero file_id
    - `cargo test -p rdpilot --lib rdpdr_backend::` runs the new cases and all pass
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib rdpdr_backend::</automated>
  </verify>
  <done>FILE-03 (Rust side) is proven offline: the full adversarial matrix is rejected by canonicalized ancestry validation, and a legitimate nested path is accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote Windows OS → Rust RdpilotDriveBackend | Every RDPDR IRP path string is attacker/bug-controllable untrusted input crossing into local std::fs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / Elevation | rdpdr_backend::resolve_under_root | critical | mitigate | Canonicalize parent + component-wise ancestry check under the share root; unconditional `\`→`/` normalization before any Path op; NEVER substring/Contains matching (D-10.2, Pitfall 1/2, FILE-03 BLOCKING suite in Task 3) |
| T-10-03 | Denial of Service | rdpdr_backend::read_served_bytes | medium | mitigate | Read::take bounds every per-IRP read to the requested length; never preallocate a length-sized buffer (T-05-05, carried from Phase 5) |
| T-10-SC | Tampering | sha2 crate promotion | low | accept | Already-resolved transitive dep (Cargo.lock 0.11.0), slopcheck `[OK]`, RustCrypto reference impl — no new supply-chain node (10-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cargo test -p rdpilot --lib` green (error, config, rdpdr_backend modules)
- FILE-03 Rust adversarial matrix passes offline (Task 3)
- No new clippy::unwrap_used / expect_used violations in non-test code (API-01, 08-01 gates)
</verification>

<success_criteria>
Error taxonomy (PathTraversal, ChecksumMismatch), share_root config, the resolve_under_root validator, and the generalized Create/Read compile and pass offline tests; the FILE-03 BLOCKING adversarial suite passes on the Rust validator; the sensor-exe bootstrap read path is unbroken.
</success_criteria>

<output>
Create `.planning/phases/10-sdk-file-transfer-extension/10-01-SUMMARY.md` when done, recording: the final `RdpilotDriveBackend::new` signature, the `OpenEntry::File(PathBuf)` contract for Plan 10-02, and any deviation from the resolve_under_root shape in 10-RESEARCH's Code Example.
</output>
