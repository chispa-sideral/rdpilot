---
phase: 10-sdk-file-transfer-extension
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - crates/rdpilot/src/session.rs
  - crates/rdpilot/src/lib.rs
autonomous: true
requirements: [FILE-01, FILE-02]
must_haves:
  truths:
    - "Session::upload_file(&self, local, remote_name) moves a local file to a named remote destination and verifies the sensor-reported SHA-256 against the locally-computed one (FILE-01, D-10.4/D-10.5)"
    - "Session::download_file(&self, remote_name, local) returns a TransferOutcome { bytes_transferred, checksum } with matching size/checksum (FILE-02)"
    - "Both methods ride the existing async sensor_request() extension point with a new TransferOutcome return — no tokio::spawn, threading model untouched"
    - "A SHA-256 mismatch surfaces as Error::ChecksumMismatch, distinct from Error::SensorRejected/Error::Dvc"
  artifacts:
    - path: "crates/rdpilot/src/session.rs"
      provides: "upload_file, download_file, TransferOutcome, sha2-based local checksum, TRANSFER_TIMEOUT_MS"
      contains: "pub async fn upload_file"
    - path: "crates/rdpilot/src/lib.rs"
      provides: "pub use session::TransferOutcome (owned public type, D-09)"
      contains: "TransferOutcome"
  key_links:
    - from: "session::upload_file / download_file"
      to: "sensor_request(MsgType::FileTransfer, payload, TRANSFER_TIMEOUT_MS)"
      via: "the existing round-trip helper, mirroring launch_process"
      pattern: "MsgType::FileTransfer"
    - from: "session::download_file"
      to: "Error::checksum_mismatch"
      via: "compare sha2 of the local file against the sensor-reported sha256"
      pattern: "checksum_mismatch"
---

<objective>
Expose the public SDK surface (D-10.4): `Session::upload_file` and `Session::download_file`, returning a `TransferOutcome { bytes_transferred, checksum }`, wired over the existing async `sensor_request()` extension point exactly like `launch_process` (Phase 7). Verify integrity with SHA-256 on the Rust side (D-10.5), surfacing a mismatch as the distinct `Error::ChecksumMismatch`.

Purpose: The consumer-facing capstone of the phase. Depends on the Rust backend read/write path (10-01, 10-02) and the sensor trigger (10-03). Owns `session.rs` + `lib.rs` in Wave 3. The threading model (dedicated OS thread + current-thread Tokio, session.rs ~206-210) is UNTOUCHABLE — no `tokio::spawn`.

Output: Two public methods + one public owned type, offline-tested for wiring/error-branching, with the real end-to-end transfer proven at the live gate (Plan 10-05).
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sdk-file-transfer-extension/10-CONTEXT.md
@.planning/phases/10-sdk-file-transfer-extension/10-RESEARCH.md
@crates/rdpilot/src/session.rs
@crates/rdpilot/src/lib.rs
@.planning/phases/10-sdk-file-transfer-extension/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TransferOutcome type + upload_file/download_file over sensor_request</name>
  <files>crates/rdpilot/src/session.rs, crates/rdpilot/src/lib.rs</files>
  <read_first>
    crates/rdpilot/src/session.rs (sensor_request helper lines 508-567, launch_process lines 818-831 as the round-trip + reply-field-extraction template, ACTION_TIMEOUT_MS/ENUMERATION_TIMEOUT_MS lines 128/136, threading-model note ~206-210)
    crates/rdpilot/src/lib.rs (pub use block lines 47-53 — TransferOutcome joins as an owned public type, D-09)
    crates/rdpilot/src/error.rs (Error::checksum_mismatch / Error::path_traversal constructors from Plan 10-01)
    10-CONTEXT.md D-10.4 (signatures, keep upload symmetric with download) + 10-RESEARCH architecture diagram ({op, remote_name, local_share_name} request / {success, bytes, sha256} reply)
  </read_first>
  <behavior>
    - upload_file sends a FileTransfer request (op=upload) and returns a TransferOutcome (symmetric with download per CONTEXT discretion) on success
    - download_file sends a FileTransfer request (op=download) and returns TransferOutcome { bytes_transferred, checksum }
    - A sensor success:false reply surfaces as Error::SensorRejected (existing sensor_request branch), a timeout/handshake failure as Error::Dvc — both distinct from a checksum failure
    - A malformed reply (missing bytes/sha256) surfaces as Error::Dvc with a clear message (mirrors launch_process's missing-"pid" handling)
    - TransferOutcome is an owned public struct exposing bytes_transferred + checksum; no ironrdp/third-party type leaks (D-09)
  </behavior>
  <action>
    Add a `TRANSFER_TIMEOUT_MS` constant to session.rs, generous for multi-MB transfers over RDP (start high, e.g. 30_000; note it as live-tunable at the 10-05 gate, mirroring how Phase 6/7 tuned their timeouts). Define `pub struct TransferOutcome { pub bytes_transferred: u64, pub checksum: String }` (owned, Serialize per the existing public-type conventions — check how WindowInfo/ProcessInfo derive; keep it credential-free). Implement `pub async fn upload_file(&self, local: &Path, remote_name: &str) -> Result<TransferOutcome>` and `pub async fn download_file(&self, remote_name: &str, local: &Path) -> Result<TransferOutcome>`, each mirroring `launch_process`'s shape: build the `{op, remote_name, ...}` payload, `self.sensor_request(crate::sensor::MsgType::FileTransfer, Some(payload), TRANSFER_TIMEOUT_MS).await?`, then extract `bytes` (u64) and `sha256` (str) from the reply `data`, mapping missing/invalid fields to Error::dvc (like the "pid" extraction). Do NOT add any tokio::spawn or new thread — these are ordinary async methods on the existing session loop (threading model UNTOUCHABLE). Add `pub use session::TransferOutcome;` to lib.rs. Add offline unit tests using the existing session.rs test scaffolding (the `RdpInputEvent::Request` drain harness around lines 1384-1600) asserting: upload_file/download_file emit a `MsgType::FileTransfer` Request with the correct op/remote_name payload and the right timeout, exactly like the existing WindowList/Uia/LaunchProcess request-emission tests.
  </action>
  <acceptance_criteria>
    - session.rs contains `pub async fn upload_file` and `pub async fn download_file` and `pub struct TransferOutcome`
    - Both call `sensor_request(crate::sensor::MsgType::FileTransfer, ...)` with TRANSFER_TIMEOUT_MS
    - lib.rs contains `pub use session::TransferOutcome`
    - No `tokio::spawn` added anywhere in session.rs (grep confirms none introduced by this plan)
    - Offline tests assert the FileTransfer Request payload op/remote_name and timeout
    - No unwrap/expect/panic outside #[cfg(test)]
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib session::</automated>
  </verify>
  <done>Public upload_file/download_file exist, ride sensor_request with no threading change, and their request emission is offline-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SHA-256 integrity verification + ChecksumMismatch branch</name>
  <files>crates/rdpilot/src/session.rs</files>
  <read_first>
    crates/rdpilot/src/session.rs (upload_file/download_file from Task 1)
    crates/rdpilot/src/error.rs (Error::checksum_mismatch { expected, actual } from Plan 10-01)
    10-CONTEXT.md D-10.5 (SHA-256 both sides) + 10-RESEARCH Don't Hand-Roll (use sha2::Sha256, never a custom loop)
  </read_first>
  <behavior>
    - For download_file: after the transfer completes, the Rust side computes sha2::Sha256 of the final local file (streamed, bounded read — never load the whole file into memory) and compares hex-lowercase against the sensor-reported sha256; a mismatch returns Error::ChecksumMismatch { expected: sensor_hash, actual: local_hash }
    - For upload_file: the Rust side computes sha2 of the LOCAL source file and compares against the sensor-reported sha256 of what it wrote remotely; a mismatch returns Error::ChecksumMismatch
    - On match, the returned TransferOutcome.checksum is the verified lowercase hex digest and bytes_transferred is the sensor-reported byte count (cross-checked against local file length where available)
    - Hashing uses sha2::Sha256 via a bounded streaming read (no custom hash, no unbounded buffer)
  </behavior>
  <action>
    Add a private helper `fn sha256_file(path: &Path) -> Result<String>` (or async wrapper) that streams the file through `sha2::Sha256` with a bounded buffer (e.g. 64KiB read loop) and returns lowercase hex — never a custom hash loop (Don't Hand-Roll), never loading the whole file (T-05-05 discipline). Wire it into upload_file (hash the local source) and download_file (hash the written local destination), comparing case-insensitively against the sensor-reported `sha256` and returning `Error::checksum_mismatch(expected, actual)` on divergence — a DISTINCT error from SensorRejected/Dvc so CLI-03 (next phase) gets legible errors. Populate TransferOutcome.checksum with the verified digest and bytes_transferred with the byte count. Ensure the hex form (lowercase, no separators) matches the C# `Convert.ToHexString` output casing — normalize both sides to lowercase before comparing (Convert.ToHexString yields uppercase; normalize). Add offline unit tests for sha256_file against known vectors (e.g. the SHA-256 of an empty input and of `b"abc"`) and a test that a deliberately mismatched expected-vs-actual pair yields Error::ChecksumMismatch.
  </action>
  <acceptance_criteria>
    - session.rs contains a sha2::Sha256-based file-hash helper using a bounded streaming read (no whole-file buffer)
    - download_file and upload_file compare the local digest against the sensor sha256 and return Error::checksum_mismatch on divergence
    - Digest comparison is case-insensitive / both normalized to lowercase (matches C# Convert.ToHexString casing)
    - A unit test asserts sha256_file matches a known vector (e.g. SHA-256 of "abc")
    - A unit test asserts a mismatch produces Error::ChecksumMismatch (distinct from SensorRejected/Dvc)
    - No unwrap/expect/panic outside #[cfg(test)]
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot --lib session::</automated>
  </verify>
  <done>SHA-256 integrity is verified on the Rust side against the sensor-reported digest; a mismatch surfaces as the distinct Error::ChecksumMismatch; TransferOutcome carries the verified checksum + byte count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK caller → Session public API | remote_name and local path are caller-supplied; remote_name is re-validated by BOTH validators (Rust backend + C# sensor) downstream |
| C# sensor reply → Rust SDK | The sensor-reported sha256/bytes are untrusted claims; the Rust side independently recomputes SHA-256 rather than trusting the reported hash (D-10.5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering (integrity) | session::download_file / upload_file | high | mitigate | Independent Rust-side sha2::Sha256 recomputation compared against the sensor-reported digest; divergence → Error::ChecksumMismatch (D-10.5) — neither side trusts the other's reported hash |
| T-10-07 | Denial of Service | session::sha256_file | medium | mitigate | Bounded streaming hash read, never load the whole file into memory (T-05-05) |
| T-10-08 | Information Disclosure | TransferOutcome | low | mitigate | Owned, credential-free public type (checksum + byte count only); no path/secret leakage, no ironrdp type leak (D-09, closing toward CONFIG-03 posture) |
</threat_model>

<verification>
- `cargo test -p rdpilot --lib session::` green (request emission + checksum vector + mismatch branch)
- No `tokio::spawn` introduced (threading model untouched)
- No new clippy::unwrap_used/expect_used in non-test code
- The full offline suite `cargo test -p rdpilot` is green at wave merge
</verification>

<success_criteria>
`Session::upload_file` and `Session::download_file` are public, return `TransferOutcome`, ride the existing async sensor_request path with no threading change, and verify SHA-256 integrity with a distinct `Error::ChecksumMismatch`. End-to-end real-target behavior is proven at the live gate (Plan 10-05).
</success_criteria>

<output>
Create `.planning/phases/10-sdk-file-transfer-extension/10-04-SUMMARY.md` when done, recording: the final method signatures + TransferOutcome shape, the chosen TRANSFER_TIMEOUT_MS (flagged live-tunable), the hex-casing normalization decision, and confirmation no tokio::spawn was added.
</output>
