---
phase: 10-sdk-file-transfer-extension
plan: 05
type: execute
wave: 4
depends_on: [10-04]
files_modified:
  - crates/rdpilot/tests/live_session.rs
  - crates/rdpilot/tests/common.rs
autonomous: false
requirements: [FILE-01, FILE-02, FILE-03, FILE-04]
user_setup:
  - service: azure
    why: "Disposable Windows VM live gate for SC#1/#2 (and FILE-04 real chunk size, FILE-03 Windows-drive-absolute re-confirmation) — same pattern as every prior phase's terminal live gate"
    dashboard_config:
      - task: "An authenticated `az` CLI session with a subscription that can create/destroy the rdpilot-test resource group (management RG rdpilot-mgmt persists)"
        location: "az login / az account set"
must_haves:
  truths:
    - "FILE-01: a real local file uploads to a named remote destination and is verified present on the target"
    - "FILE-02: a real remote file downloads to a named local destination with matching size + SHA-256 checksum"
    - "FILE-04: a file larger than one real per-IRP chunk transfers correctly (observed IRP/offset progression proves the loop looped) AND an interrupted transfer is cleanly detectable"
    - "FILE-03 live re-confirm: the `\\`-separator and Windows-drive-absolute adversarial cases are rejected on the REAL Windows target as Error::PathTraversal specifically (Pitfall 1 warning-sign closure + end-to-end distinct-error producer)"
    - "The real per-IRP chunk size is measured and recorded in STATE.md (10-RESEARCH Open Question 1) — never hardcoded"
  artifacts:
    - path: "crates/rdpilot/tests/live_session.rs"
      provides: "gated #[ignore] live tests for upload/download/large-file/interrupted/traversal-live"
      contains: "upload_file"
  key_links:
    - from: "tests/live_session.rs"
      to: "Session::upload_file / download_file"
      via: "public API exercised against a real provisioned RDP target (RDPILOT_LIVE=1)"
      pattern: "download_file"
---

<objective>
Close the phase against a real disposable Azure Windows VM — the terminal live gate mirroring every prior phase (Phase 5-9). Prove FILE-01 (upload verified present) and FILE-02 (download size/checksum) on real hardware; observe the REAL per-IRP chunk size to satisfy FILE-04's "loop actually loops" (10-RESEARCH Open Question 1, never hardcoded); confirm interrupted-transfer detection; and re-confirm the FILE-03 adversarial cases that are NOT faithful offline (the `\`-separator case per Pitfall 1, and the Windows-drive-absolute case per Plan 10-03) on genuine Windows path semantics.

Purpose: FILE-01/02/03/04 all have a live component; FILE-03 and FILE-04 are BLOCKING. This plan carries the blocking live re-confirmations and the human-verified live-gate checkpoint. Depends on the complete public API (10-04).

Output: Authored gated live tests + a recorded, human-confirmed live-gate run (measured chunk size, checksums, teardown).
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sdk-file-transfer-extension/10-CONTEXT.md
@.planning/phases/10-sdk-file-transfer-extension/10-RESEARCH.md
@crates/rdpilot/tests/live_session.rs
@crates/rdpilot/tests/common.rs
@.planning/phases/10-sdk-file-transfer-extension/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the gated live transfer tests (offline-compiling)</name>
  <files>crates/rdpilot/tests/live_session.rs, crates/rdpilot/tests/common.rs</files>
  <read_first>
    crates/rdpilot/tests/live_session.rs (the #[ignore] + early-return-on-no-config pattern lines 1-45, require_target! macro, block_on helper line 102, an existing live test e.g. mouse_click_activates_menu line 344 for the settle/screenshot-diff shape)
    crates/rdpilot/tests/common.rs (load_config — extend to supply a share_root for the connection if needed)
    10-RESEARCH.md Validation Architecture (Sampling Rate: live gate) + Open Question 1 (assert IRP/offset progression, not a byte constant) + Assumption A1 (few-hundred-KB-to-few-MB test file)
    STATE.md Phase 5-9 live-gate entries (az vm run-command build-on-VM, blob SAS relay, teardown discipline)
  </read_first>
  <action>
    Add gated `#[test] #[ignore = "live: ..."]` tests to live_session.rs, each early-returning when `common::load_config()` is None (so `cargo test -p rdpilot` stays green with no target), exercising the PUBLIC API only (Session::upload_file/download_file/TransferOutcome — no ironrdp type). Author: (1) `file_upload_roundtrip` — upload a generated local file to a remote name, then download it back to a fresh local path and assert byte-identical content + matching TransferOutcome.checksum (FILE-01/FILE-02). (2) `file_download_checksum_matches` — assert download_file's returned checksum equals an independently computed SHA-256 of the local result and bytes_transferred equals the file length (FILE-02). (3) `large_file_transfers_chunked` — transfer a file comfortably larger than any plausible single IRP (size it in the low-MB range per Assumption A1; make the size a named const, live-tunable), and assert correctness PLUS log the observed per-IRP offset progression / IRP count so the run RECORDS the real chunk size (FILE-04 — never assert a hardcoded chunk byte count). (4) `interrupted_transfer_is_detectable` — drive a transfer that is deliberately aborted mid-flight (e.g. drop/close before completion) and assert no final-named destination appears and the failure is surfaced as a clean Error (FILE-04 interrupted half). (5) `traversal_rejected_live` — issue upload/download requests with the `\`-separator adversarial path and a Windows-drive-absolute path (`C:\Windows\...`) against the real target and assert the returned error is specifically `Error::PathTraversal` (NOT a generic SensorRejected — this exercises the end-to-end distinct-error producer from the C# validator's `error_kind` sentinel through Plan 10-04's mapping; matches Error via `matches!(err, Error::PathTraversal(_))`, not a category-string hedge) (FILE-03 live re-confirm of the two offline-unfaithful cases — Pitfall 1 warning-sign closure). Extend common.rs's config loader with a share_root if the live connection needs one. Everything must COMPILE offline (the tests are ignored/early-return without a target).
  </action>
  <acceptance_criteria>
    - live_session.rs contains the five gated tests above, each `#[ignore]` + early-return on no config
    - The large-file test sizes the payload via a named const and logs observed offset/IRP progression (no hardcoded chunk-size assertion)
    - The traversal-live test covers BOTH the `\`-separator and Windows-drive-absolute cases and asserts `Error::PathTraversal` specifically (via matches!, not SensorRejected)
    - `cargo test -p rdpilot` (no target) stays green and the new tests compile
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p rdpilot</automated>
  </verify>
  <done>Gated live transfer + traversal tests are authored and compile; the default offline suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the live gate against a disposable Azure VM</name>
  <files>crates/rdpilot/tests/live_session.rs</files>
  <read_first>
    STATE.md Phase 8/9 live-gate entries (build win-x64 NativeAOT ON the VM via `az vm run-command invoke` — embed the base64 sensor source in the script body, NOT --parameters, per the Phase 8 CLI size-limit finding; relay the exe back via a short-lived Storage blob SAS; SHA-256 byte-identical VM-built vs relayed; teardown + confirm `az group exists -n rdpilot-test` => false)
    10-RESEARCH.md Pitfall 4 (device-list-mismatch silent IRP drop — if a transfer HANGS, suspect a dropped IRP before the new write logic)
  </read_first>
  <action>
    Provision a disposable Azure Windows VM (reuse the Phase 2 canonical infra scripts / Standard_B2s_v2 per STATE Pending Todos), build the win-x64 NativeAOT sensor ON the VM (embedded base64 source, `az vm run-command invoke`), relay the exe back via a short-lived blob SAS, confirm the SHA-256 is byte-identical VM-built vs relayed. Populate the live config (target host/creds/share_root) and run `RDPILOT_LIVE=1 cargo test -p rdpilot -- --include-ignored --test-threads=1` for the five new tests. RECORD: the measured real per-IRP chunk size / observed IRP count for the large-file test (FILE-04, Open Question 1); measured upload/download bytes + checksums; FILE-03 live-rejection confirmation (assert Error::PathTraversal specifically); the observed real transfer latency for the large-file test and whether TRANSFER_TIMEOUT_MS (10-04's 30s initial guess) needs tuning up/down (WARNING-2 — do not leave it silently unvalidated); and any live-diagnosed fix (mirroring how Phases 5-9 each found and fixed 1-3 live bugs — expect the same and fix in-place, noting each in the SUMMARY). If a transfer hangs rather than fails fast, apply the Pitfall 4 device-list-mismatch diagnosis first. Any auth failure from the `az`/WinRM path surfaces a dynamic auth gate (authenticate + retry — do not pre-plan it). Do NOT tear down until Task 3's checkpoint confirms the results are captured.
  </action>
  <acceptance_criteria>
    - The sensor is built on the VM and relayed back with a byte-identical SHA-256 (recorded)
    - All five gated tests run live and pass (or every failure is live-diagnosed, fixed in-place, and re-run to green)
    - The real per-IRP chunk size / observed IRP count is measured and recorded (FILE-04, not hardcoded)
    - FILE-03 `\`-separator and drive-absolute cases are confirmed rejected on the real Windows target
  </acceptance_criteria>
  <verify>
    <automated>RDPILOT_LIVE=1 cargo test -p rdpilot -- --include-ignored --test-threads=1</automated>
  </verify>
  <done>All FILE-01/02/03/04 live criteria pass on a real Azure VM; the real chunk size and checksums are recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-gate human verification + authorized teardown</name>
  <action>Pause for the developer to review the captured live-gate results (measured per-IRP chunk size, upload/download byte counts + checksums, sensor VM-build SHA-256, FILE-03 live rejections, any live-diagnosed fixes) and to authorize disposable-VM teardown. This is a blocking checkpoint — do not tear down the VM or mark the phase complete until the developer approves.</action>
  <what-built>The live gate ran the five gated transfer/traversal tests against a real disposable Azure Windows VM: FILE-01 upload verified present, FILE-02 download size+SHA-256 match, FILE-04 large-file chunked loop (real chunk size measured) + interrupted-transfer detection, FILE-03 `\`-separator and Windows-drive-absolute rejection on real Windows.</what-built>
  <how-to-verify>
    1. Review the recorded live-run output: the measured per-IRP chunk size / IRP count, the upload/download byte counts and matching checksums, and the FILE-03 live rejections.
    2. Confirm the sensor SHA-256 was byte-identical VM-built vs relayed.
    3. Confirm any live-diagnosed fixes are captured in 10-05-SUMMARY.md and STATE.md.
    4. Authorize teardown: `az group delete -n rdpilot-test` and confirm `az group exists -n rdpilot-test` => false (management RG rdpilot-mgmt persists).
  </how-to-verify>
  <resume-signal>Type "approved" once the results are captured and the VM is torn down, or describe issues to re-run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local test host → disposable Azure VM | Live gate exercises the full two-boundary hybrid path (remote Windows → Rust backend, Rust SDK → C# sensor) on real hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / Elevation | live FILE-03 re-confirm | critical | mitigate | The `\`-separator (Pitfall 1) and Windows-drive-absolute cases — offline-unfaithful — are rejected on the REAL Windows target before phase close (traversal_rejected_live) |
| T-10-04 | Tampering (integrity) | live FILE-04 | high | mitigate | Real large-file transfer proves the chunked loop loops (observed IRP progression) and interrupted transfer leaves no final-named file |
| T-10-09 | Information Disclosure | Azure teardown | medium | mitigate | Disposable VM torn down under a blocking human-verify checkpoint; management RG persists, test RG destroyed and confirmed absent |
</threat_model>

<verification>
- `cargo test -p rdpilot` (no target) green — gated tests compile and skip cleanly
- `RDPILOT_LIVE=1 cargo test -p rdpilot -- --include-ignored --test-threads=1` — all five gated tests pass on the real VM
- Real per-IRP chunk size recorded in STATE.md (FILE-04, Open Question 1)
- Teardown confirmed (`az group exists -n rdpilot-test` => false)
</verification>

<success_criteria>
All four FILE-* requirements are proven end-to-end on a real disposable Azure Windows VM: upload verified present (FILE-01), download size/checksum match (FILE-02), large-file chunked loop + interrupted-transfer detection with the real chunk size measured (FILE-04, BLOCKING), and the offline-unfaithful FILE-03 adversarial cases rejected on genuine Windows semantics (FILE-03, BLOCKING). Results human-verified; VM torn down.
</success_criteria>

<output>
Create `.planning/phases/10-sdk-file-transfer-extension/10-05-SUMMARY.md` when done, recording: the measured real per-IRP chunk size, upload/download checksums + byte counts, the sensor VM-build SHA-256, every live-diagnosed fix, and teardown confirmation. Update STATE.md with the live-gate findings (mirroring the Phase 5-9 live-gate entries).
</output>
