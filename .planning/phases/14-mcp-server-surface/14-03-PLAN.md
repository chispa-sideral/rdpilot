---
phase: 14-mcp-server-surface
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - crates/rdpilot-mcp/src/computer/mod.rs
  - crates/rdpilot-mcp/src/computer/scale.rs
  - crates/rdpilot-mcp/src/computer/dispatch.rs
  - crates/rdpilot-mcp/src/handler.rs
  - crates/rdpilot-mcp/tests/scale_to_native.rs
autonomous: true
requirements: [MCP-02, MCP-04]
must_haves:
  truths:
    - "A model can call a single `computer` tool whose action-discriminated schema mirrors Anthropic computer_20250124 field names verbatim"
    - "Every model-supplied 1280x800 coordinate is bridged to native 96-DPI pixels by ONE pure scale_to_native, never landing out of bounds even at the exclusive edge"
    - "The three unsupported computer-use action gaps are EXPLICIT tool-error rejections, never silent no-ops"
  artifacts:
    - path: "crates/rdpilot-mcp/src/computer/scale.rs"
      provides: "pure scale_to_native(x,y,native_w,native_h)->(u16,u16) + ADVERTISED_WIDTH/HEIGHT (1280x800)"
      contains: "scale_to_native"
    - path: "crates/rdpilot-mcp/src/computer/mod.rs"
      provides: "ComputerAction serde-tagged enum + required session field (D-29)"
      contains: "ComputerAction"
    - path: "crates/rdpilot-mcp/src/computer/dispatch.rs"
      provides: "action→Request mapping + 3 explicit gap rejections + wait/hold_key duration caps"
      contains: "left_mouse_down"
    - path: "crates/rdpilot-mcp/tests/scale_to_native.rs"
      provides: "MCP-04 BLOCKING edge/corner precision test"
      contains: "scale_to_native"
  key_links:
    - from: "crates/rdpilot-mcp/src/computer/dispatch.rs"
      to: "Request::DesktopSize + scale_to_native"
      via: "source native dims then scale every coordinate"
      pattern: "DesktopSize"
    - from: "crates/rdpilot-mcp/src/handler.rs"
      to: "computer tool method"
      via: "#[tool] on RdpilotMcpHandler"
      pattern: "computer"
---

<objective>
Implement the single Anthropic computer-use-compatible `computer` mega-tool (D-21/D-14.2): a schema-discriminated `computer_20250124` action enum (screenshot + mouse/keyboard/scroll actions) whose field names mirror Anthropic's verbatim (Pitfall 3), plus the BLOCKING coordinate bridge `scale_to_native` (MCP-04) that converts every model-supplied 1280x800 coordinate to rdpilot's native 96-DPI physical pixels via ONE pure, unit-tested function, plus the action→`Request` dispatch that maps each action onto the existing ipc input/capture verbs — and turns the three genuine SDK gaps into EXPLICIT tool-error rejections rather than silent no-ops.

Purpose: MCP-04 is BLOCKING and its acceptance test is a precision click near screen EDGES/CORNERS (not just center) — the bridge must be independently unit-testable and must never emit a coordinate `Session::check_bounds` would reject (round-then-clamp, half-open convention). MCP-02 requires one discriminated tool, not 25 one-per-action tools.
Output: `scale_to_native`; `ComputerAction`; the computer dispatch table + rejections; the `computer` tool method registered on the handler; the BLOCKING edge/corner test.
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-mcp-server-surface/14-RESEARCH.md
@.planning/phases/14-mcp-server-surface/14-CONTEXT.md
@crates/rdpilot-mcp/src/connect.rs
@crates/rdpilot-mcp/src/timeouts.rs
@crates/rdpilot-mcp/src/error.rs
@crates/rdpilot-mcp/src/handler.rs
@crates/rdpilot-ipc/src/input.rs
@crates/rdpilot-ipc/src/request.rs
@crates/rdpilot-ipc/src/response.rs
</context>

<artifacts_produced>
New symbols this plan introduces:
- `computer::scale::{ADVERTISED_WIDTH=1280, ADVERTISED_HEIGHT=800, scale_to_native(x,y,native_w,native_h)->(u16,u16)}`
- `computer::mod::{ComputerAction (serde tag="action"), ScrollDirection, ComputerArgs (session + action)}`
- `computer::dispatch::dispatch_computer(...)` mapping ComputerAction → one-or-more `Request`
- `RdpilotMcpHandler::computer` — the registered `#[tool]` method
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scale_to_native pure bridge + BLOCKING edge/corner precision test (MCP-04)</name>
  <files>crates/rdpilot-mcp/src/computer/scale.rs, crates/rdpilot-mcp/src/computer/mod.rs, crates/rdpilot-mcp/tests/scale_to_native.rs</files>
  <read_first>.planning/phases/14-mcp-server-surface/14-RESEARCH.md (§Coordinate Bridge: the exact round-then-clamp math + the recommended test vectors + Pitfall 2 boundary discipline), crates/rdpilot-ipc/src/input.rs (WireMouseAction coordinates are u16 physical pixels)</read_first>
  <behavior>
    - `scale_to_native(0, 0, 1920, 1080)` == `(0, 0)`.
    - `scale_to_native(1279, 799, 1920, 1080)` == `(1919, 1079)` (near-corner: x = 1279 × 1.5 = 1918.5 is an EXACT tie → `.round()` rounds half away from zero → 1919; y = 799 × 1.35 = 1078.65 → 1079; 1919 < 1920 so still inside bounds, no off-by-one past the last valid pixel).
    - `scale_to_native(640, 400, 1920, 1080)` == `(960, 540)` (center sanity).
    - `scale_to_native(1280, 800, 1920, 1080)` returns `(nx, ny)` with `nx < 1920 && ny < 1080` — the exclusive edge a model may still emit must clamp INSIDE bounds, never equal width/height (would be rejected by Session::check_bounds).
    - A square-scale case (e.g. native 1280x800) returns the identity mapping for interior points.
  </behavior>
  <action>Create `computer/scale.rs` with `pub const ADVERTISED_WIDTH: u32 = 1280;` and `pub const ADVERTISED_HEIGHT: u32 = 800;` (the FIXED advertised resolution, D-14.2 LOCKED) and the pure `pub fn scale_to_native(x: u32, y: u32, native_w: u32, native_h: u32) -> (u16, u16)` exactly per the research math: floating-point scale factors `native_dim / ADVERTISED_dim`, round-to-nearest via a plain idiomatic `f64::round()` call — which rounds half AWAY FROM ZERO (this is the required tie-break: NOT truncation, NOT banker's/round-half-to-even; an exact `.5` tie such as `1279 × 1.5 = 1918.5` MUST round UP to 1919, matching the test vector) — then `.clamp(0.0, native_dim - 1.0)` as the LAST step before the `u16` cast (Pitfall 2 — the bridge itself must never emit an out-of-bounds value; do not rely on Session::check_bounds as a safety net). The clamp is what keeps a rounded value that reaches `native_dim` (e.g. `1280 × 1.5 = 1920.0`) inside `[0, native_dim - 1]`. Keep it a single function with NO dependency on a live session or the handler, so the test is a pure unit test. Create `computer/mod.rs` declaring the `scale`/`dispatch` submodules and re-exporting the scale symbols. Create `crates/rdpilot-mcp/tests/scale_to_native.rs` as the MCP-04 BLOCKING integration test asserting ALL the behavior vectors above — corners, near-corners, the exclusive-edge clamp (`assert!(nx < native_w as u16 && ny < native_h as u16)`), and the center — so a regression in rounding or clamping fails here by name.</action>
  <verify>
    <automated>cargo test -p rdpilot-mcp --test scale_to_native</automated>
  </verify>
  <done>scale_to_native is pure and unit-testable; the BLOCKING test asserts precision at corners/near-corners/edges/center; no advertised-edge input ever produces a value >= native width/height.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ComputerAction schema (computer_20250124 shape) + required session field</name>
  <files>crates/rdpilot-mcp/src/computer/mod.rs</files>
  <read_first>.planning/phases/14-mcp-server-surface/14-RESEARCH.md (§Code Examples: the ComputerAction serde-tagged enum + ScrollDirection; §Computer-Use Action → IPC Verb Mapping table for the exact per-action param names; Pitfall 3 verbatim-field-names; Pitfall 4 session-field), crates/rdpilot-mcp/src/handler.rs (Parameters<T> tool-input convention)</read_first>
  <behavior>
    - `{"action":"screenshot"}` deserializes to the Screenshot variant.
    - `{"action":"left_click","coordinate":[10,20]}` deserializes with `coordinate == [10,20]` and `text == None`.
    - `{"action":"scroll","coordinate":[1,2],"scroll_direction":"down","scroll_amount":3}` deserializes with `ScrollDirection::Down`.
    - An unknown `action` tag (e.g. `{"action":"teleport"}`) is a hard serde deserialize error (strict schema, no silent fallback).
    - The `computer` tool's top-level args carry a required `session: String` field alongside `action` (D-29).
  </behavior>
  <action>In `computer/mod.rs`, define `ComputerAction` as a `#[derive(serde::Deserialize, schemars::JsonSchema)]` enum with `#[serde(tag = "action", rename_all = "snake_case")]`, one variant per `computer_20250124` action with field names mirroring Anthropic VERBATIM (Pitfall 3): `Screenshot`; `MouseMove { coordinate: [u32;2] }`; `LeftClick`/`RightClick`/`MiddleClick`/`DoubleClick`/`TripleClick { coordinate: [u32;2], text: Option<String> }`; `LeftClickDrag { start_coordinate: [u32;2], coordinate: [u32;2] }`; `LeftMouseDown`/`LeftMouseUp { coordinate: [u32;2] }`; `Key { text: String }`; `Type { text: String }`; `Scroll { coordinate: Option<[u32;2]>, scroll_direction: ScrollDirection, scroll_amount: u32, text: Option<String> }`; `HoldKey { text: String, duration: f64 }`; `Wait { duration: f64 }`; `CursorPosition`. Define `ScrollDirection { Up, Down, Left, Right }` with `#[serde(rename_all = "lowercase")]`. Define the tool-input struct `ComputerArgs { session: String, #[serde(flatten)] action: ComputerAction }` — session is a REQUIRED extra top-level field (Pitfall 4: the Anthropic schema has no session concept, but this server defines its own schema and D-29 mandates a per-call session on every tool). Add inline tests asserting the deserialization behavior vectors above, including the unknown-tag hard rejection. Write an example-rich `schemars` doc/description surface on the enum so a computer-use-trained model's learned field conventions transfer (Pitfall 3).</action>
  <verify>
    <automated>cargo test -p rdpilot-mcp computer</automated>
  </verify>
  <done>ComputerAction mirrors computer_20250124 field names; ScrollDirection parses lowercase; unknown actions hard-reject; ComputerArgs requires session (D-29).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: computer action dispatch (verb mapping + 3 gap rejections + caps) + register the tool</name>
  <files>crates/rdpilot-mcp/src/computer/dispatch.rs, crates/rdpilot-mcp/src/handler.rs</files>
  <read_first>.planning/phases/14-mcp-server-surface/14-RESEARCH.md (§Computer-Use Action → IPC Verb Mapping full table incl. dy=scroll_amount*120*sign, triple_click=3 sequential clicks, screenshot base64 passthrough; §Gap summary; Pitfall 5 duration caps), crates/rdpilot-mcp/src/computer/scale.rs (scale_to_native), crates/rdpilot-mcp/src/connect.rs (round_trip_bounded), crates/rdpilot-mcp/src/timeouts.rs (FAST), crates/rdpilot-ipc/src/input.rs (WireMouseAction/WireKeyAction/WireButton + parse_wire_key), crates/rdpilot-ipc/src/request.rs (Request::Mouse/Key/Screenshot/DesktopSize), crates/rdpilot-ipc/src/response.rs (WireResponse::Screenshot/DesktopSize)</read_first>
  <behavior>
    - A `left_click` at `[1279,799]` sources the session's native dims via Request::DesktopSize, scales the coordinate through scale_to_native, and issues `Request::Mouse { Click { .., button: Left } }` with the scaled coordinate.
    - `screenshot` issues Request::Screenshot and returns the base64 PNG as an rmcp image content block (direct passthrough, no re-encode).
    - `key` with `text:"ctrl+s"` splits on `+`, maps each token via `rdpilot_ipc::parse_wire_key`, and issues `Request::Key { Combo([Ctrl,S]) }`; an unknown token yields a legible tool error (never a panic/silent drop).
    - `scroll` `down`/`up` maps to `WireMouseAction::Scroll { dy = scroll_amount*120*sign }`; `scroll` `left`/`right` is an EXPLICIT tool error (horizontal scroll unsupported, D-3.3).
    - `left_mouse_down`, `left_mouse_up`, and `cursor_position` each return an EXPLICIT tool error naming the unsupported action (and, for the mouse-down/up pair, naming `left_click_drag` as the supported drag alternative) — NEVER a silent Ack/no-op.
    - `wait`/`hold_key` reject a `duration` exceeding the cap (100s, matching Anthropic's reference) with a clear tool error; `wait` performs a bounded `tokio::time::sleep` with NO daemon round-trip; `hold_key` is approximated as an atomic combo press+release (documented limitation).
  </behavior>
  <action>Create `computer/dispatch.rs` with `dispatch_computer(&self, args: ComputerArgs) -> Result<CallToolResult, McpError>` implementing the full research mapping table. For every coordinate-bearing action: first `round_trip_bounded(Request::DesktopSize { session }, timeouts::FAST)` to obtain native `(w,h)`, then `scale_to_native` each coordinate (both endpoints independently for `left_click_drag`), then issue the corresponding `Request::Mouse`/`Request::Key`/`Request::Screenshot` via `round_trip_bounded(.., timeouts::FAST)`. Map: mouse_move→Move; left/right/middle_click→Click{button}; double_click→DoubleClick; triple_click→THREE sequential Click round trips ~100ms apart (no atomic wire primitive); left_click_drag→Drag; key→`text.split('+')` through `parse_wire_key` into `WireKeyAction::Combo` (map the `Err(String)` to a legible `McpError`); type→`WireKeyAction::Type`; scroll up/down→`WireMouseAction::Scroll { dy = i16 from scroll_amount*120*direction_sign }`; screenshot→`Request::Screenshot` then build an rmcp image content block from the returned `png_base64` (base64 PNG passthrough, no decode/re-encode). Implement the three EXPLICIT rejections (binding direction 5, Pitfall/Gap summary): `left_mouse_down`/`left_mouse_up` (no half-click primitive — error names `left_click_drag` as the alternative), `cursor_position` (no SDK pointer getter — error says to screenshot to observe the pointer), and `scroll_direction: left|right` (D-3.3 horizontal scroll deliberately absent). Cap `wait` and `hold_key` `duration` at 100s (Pitfall 5) rejecting larger values; implement `wait` as a bounded `tokio::time::sleep` (no round trip); implement `hold_key` as an atomic `WireKeyAction::Combo` press+release (research A3 approximation — document it). Register the `computer` tool as a `#[tool(...)]` method on `RdpilotMcpHandler` in handler.rs taking `Parameters<ComputerArgs>`, with an example-rich description string documenting the supported actions AND the three unsupported ones (so a driving LLM understands the boundary). Add unit tests for the mapping and EACH rejection, and for the scroll-direction sign and the duration-cap rejection.</action>
  <verify>
    <automated>cargo test -p rdpilot-mcp</automated>
  </verify>
  <done>Every computer_20250124 action maps to the correct Request(s) or an explicit tool error; the three gaps reject legibly (asserted by name); wait/hold_key caps enforced; the `computer` tool is registered and advertised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model→`computer` tool | LLM-supplied action JSON + coordinates in the advertised 1280x800 space |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | coordinate-boundary drift → mis-click near edges | high | mitigate | `scale_to_native` round-then-clamp (Pitfall 2), proven by the MCP-04 BLOCKING edge/corner test; never emits an out-of-bounds coordinate |
| T-14-08 | Denial of Service | unbounded `wait`/`hold_key` duration parks a task | medium | mitigate | 100s duration cap (Pitfall 5) + rejection of larger values; `wait` uses a bounded sleep |
| T-14-09 | Tampering | malformed/unknown action or key token | low | mitigate | serde strict deserialization hard-rejects unknown `action`; `parse_wire_key` returns a legible error for unknown key tokens (no silent drop/panic) |
| T-14-10 | Spoofing | silent no-op on an unsupported action misleads the driving LLM | medium | mitigate | the three gaps (left_mouse_down/up, cursor_position, horizontal scroll) are EXPLICIT tool-error rejections, asserted by test |
| T-14-SC | Tampering | cargo installs | low | accept | No new package-manager dependencies added in this plan (rdpilot-mcp manifest unchanged from Plan 14-02) |
</threat_model>

<verification>
- MCP-04 BLOCKING: `cargo test -p rdpilot-mcp --test scale_to_native` green (edges/corners/center + exclusive-edge clamp).
- MCP-02: `cargo test -p rdpilot-mcp` green — computer action mapping + the three explicit rejections asserted by name.
- The `computer` tool appears in the handler's advertised tool set (covered by Plan 14-04's tools/list schema test).
</verification>

<success_criteria>
MCP-02: a single schema-discriminated `computer` tool maps every computer_20250124 action onto the ipc input/capture verbs, with the three genuine gaps rejected explicitly. MCP-04 [BLOCKING]: one pure `scale_to_native` bridges the fixed 1280x800 space to native 96-DPI pixels, verified precise at edges/corners.
</success_criteria>

<output>
Create `.planning/phases/14-mcp-server-surface/14-03-SUMMARY.md` when done.
</output>
