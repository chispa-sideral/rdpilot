---
phase: 12-session-daemon
plan: 04
type: execute
wave: 4
depends_on: [12-02, 12-03]
files_modified:
  - crates/rdpilot-daemon/src/ipc/mod.rs
  - crates/rdpilot-daemon/src/ipc/unix.rs
  - crates/rdpilot-daemon/src/ipc/framing.rs
  - crates/rdpilot-daemon/src/dispatch.rs
  - crates/rdpilot-daemon/tests/ipc_security.rs
autonomous: true
requirements: [DAEMON-02, SESSION-03]
must_haves:
  truths:
    - "The Unix IPC listener lives under a 0700-mode runtime directory created atomically"
    - "Every accepted connection's peer uid is checked against the daemon's effective uid; a mismatch is rejected"
    - "Length-prefixed JSON framing round-trips Request/WireResponse over an accepted stream"
    - "Dispatch routes Connect (atomic open) / List / Disconnect / operational verbs to the registry and returns typed WireResponses"
    - "`list` reports name/id, target host, status, connected-since, and last-activity (SESSION-03)"
    - "A different-uid client is rejected at the IPC boundary (DAEMON-02, SC#4)"
  artifacts:
    - path: "crates/rdpilot-daemon/src/ipc/unix.rs"
      provides: "0700 runtime-dir socket + peer_cred() uid authorization"
      contains: "peer_cred"
    - path: "crates/rdpilot-daemon/src/ipc/framing.rs"
      provides: "length-prefixed serde_json read/write"
      contains: "framing"
    - path: "crates/rdpilot-daemon/src/dispatch.rs"
      provides: "Request -> registry -> WireResponse dispatch incl list"
      contains: "WireResponse"
    - path: "crates/rdpilot-daemon/tests/ipc_security.rs"
      provides: "SC#4 [BLOCKING] different-uid rejected test"
      contains: "PermissionDenied"
  key_links:
    - from: "crates/rdpilot-daemon/src/ipc/unix.rs"
      to: "tokio::net::UnixStream::peer_cred"
      via: "per-connection uid check"
      pattern: "peer_cred"
    - from: "crates/rdpilot-daemon/src/dispatch.rs"
      to: "registry::Registry"
      via: "Connect/List/Disconnect -> open/list/close"
      pattern: "\\.(open|close|list)\\("
---

<objective>
Build the local-user-scoped IPC transport and the request dispatcher: the Unix `0700` runtime-dir socket with a per-connection `peer_cred()` uid check (DAEMON-02, binding direction 1 — tokio-native, NOT `interprocess`), length-prefixed JSON framing, and the dispatch layer that maps every `Request` verb (Connect/List/Disconnect + the six operational verbs) onto registry operations and returns typed `WireResponse`s including the SESSION-03 `list`. Prove local-user scoping with the BLOCKING different-uid-rejected test (SC#4).

Purpose: DAEMON-02 is a BLOCKING security criterion. The Unix path is fully offline-testable on this Linux host (`0700` dir + peer-uid check). The Windows DACL path (`create_with_security_attributes_raw` + `first_pipe_instance(true)`) requires the pinned Windows machine and the `[ASSUMED]` `windows-permissions` crate — it is deferred to the live-gate Plan 07; `ipc/windows.rs` remains a `#[cfg(windows)]` stub here. SESSION-03's `list` reporting is delivered by dispatch and unit-tested against a directly-seeded registry.

Output: `ipc/{mod,unix,framing}.rs`, `dispatch.rs`, and the BLOCKING `ipc_security.rs` test (Unix native).
</objective>

<execution_context>
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/workflows/execute-plan.md
@/home/marc/.claude/perficio/plugins/perficio/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-daemon/12-RESEARCH.md
@crates/rdpilot-daemon/src/registry.rs
@crates/rdpilot-daemon/src/seams.rs
@crates/rdpilot-daemon/src/error_map.rs
@crates/rdpilot-ipc/src/request.rs
@crates/rdpilot-ipc/src/response.rs
@crates/rdpilot-config/src/resolved.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Length-prefixed JSON framing + dispatch (Connect/List/Disconnect/operational)</name>
  <files>crates/rdpilot-daemon/src/ipc/framing.rs, crates/rdpilot-daemon/src/dispatch.rs</files>
  <read_first>
    crates/rdpilot-ipc/src/request.rs (the Request verbs incl the new Connect/List/Disconnect and SessionScoped::session() -> Option)
    crates/rdpilot-ipc/src/response.rs (WireResponse variants incl Connected, SessionList)
    crates/rdpilot-daemon/src/registry.rs (open/close/list APIs)
    crates/rdpilot-daemon/src/error_map.rs (DaemonError -> WireError)
    crates/rdpilot-config/src/resolved.rs (the ResolvedConfig->ConnectionConfig field set the daemon builds from Connect)
  </read_first>
  <behavior>
    - `write_frame(stream, &WireResponse)` then `read_frame::<Request>(stream)` round-trips a value across a duplex pipe.
    - A frame whose declared length exceeds a sane cap is rejected (no unbounded allocation).
    - Dispatching `Request::Connect { name, host, .. }` calls `registry.open(name, host, cfg)` and returns `WireResponse::Connected { session }` on success, `WireResponse::Error(DuplicateSession)` on collision.
    - Dispatching `Request::List {}` returns `WireResponse::SessionList { sessions }` with one entry per session, each carrying id/name/host/status/connected_since/last_activity (SESSION-03).
    - Dispatching `Request::Disconnect { session }` calls `registry.close(&session)` and returns `Ack`, or `Error(SessionNotFound)`.
    - Dispatching an operational verb (Ping/Screenshot/etc.) for an unknown session returns `Error(SessionNotFound)`.
  </behavior>
  <action>
    In `framing.rs`, implement `async fn write_frame<T: Serialize>(w, &T)` and `async fn read_frame<T: DeserializeOwned>(r) -> Result<T>` using a 4-byte big-endian length prefix + `serde_json` body over any `AsyncRead`/`AsyncWrite` (works for both `UnixStream` and, later, the Windows pipe). Enforce a maximum frame length (reject oversized declared lengths before allocating — prevents an untrusted client from forcing a huge allocation; V5 input validation). Return `DaemonError::Io` on transport/parse failure.
    In `dispatch.rs`, implement `async fn dispatch(registry: &Registry, req: Request) -> WireResponse`. Match on `req`: `Connect { name, host, port, username, password, domain, accept_invalid_certs }` → build a `rdpilot::ConnectionConfig` (via `ConnectionConfig::new(host, username, password)` + builder methods for port/domain/accept_invalid_certs) — note per research Open Question 3 that `sensor_binary_path`/`share_root` are NOT part of this phase's success criteria and are intentionally omitted (the connect/list/disconnect/soak/security criteria do not require a working sensor DVC channel); call `registry.open(name, host, cfg).await`, map `Ok(id)` → `WireResponse::Connected { session: id }`, `Err(e)` → `WireResponse::Error(e.into())`. `List {}` → `WireResponse::SessionList { sessions: registry.list() }`. `Disconnect { session }` → `registry.close(&session).await` → `Ack` / `Error`. The six operational verbs (Ping/Screenshot/LaunchProcess/SetForeground/Put/Get) → resolve the session via `registry` and return `Error(SessionNotFound)` if absent; a full operational-verb implementation (driving `Session::ping` etc.) is NOT in this phase's success criteria (research Open Question 3) — dispatch these to a `WireResponse::Error(WireErrorCode::Internal, "not implemented in Phase 12")` OR wire the trivial ones if the live session exists, but do NOT expand scope; keep the match EXHAUSTIVE (no wildcard) so future verbs force a compile error. CRITICAL (D-31): do NOT log the raw `Request` (the `Connect` variant carries a plaintext password) — if any tracing is added, redact the password field.
    Add inline unit tests: framing round-trip over `tokio::io::duplex`; oversized-length rejection; dispatch of Connect/List/Disconnect against a `Registry` built with the FakeConnector pattern (seed via `open`), asserting the returned `WireResponse` shapes and that `SessionList` entries carry the expected fields.
  </action>
  <acceptance_criteria>
    - framing.rs contains `write_frame` and `read_frame` with a 4-byte length prefix and a max-length guard
    - dispatch.rs `dispatch` match has no `_ =>` wildcard arm
    - A test asserts `Connect` → `WireResponse::Connected` and a duplicate → `WireResponse::Error` with DuplicateSession
    - A test asserts `List` → `SessionList` whose entries include name, host, status, connected_since, last_activity
    - dispatch.rs contains no `println!`/`tracing` line that prints the raw `Connect` password
  </acceptance_criteria>
  <verify>
    <automated>RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu cargo test -p rdpilot-daemon --target x86_64-unknown-linux-gnu dispatch:: framing::</automated>
  </verify>
  <done>Framing round-trips with a size cap; dispatch routes all verbs to the registry and delivers the SESSION-03 list; no wildcard arm; password never logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unix 0700 runtime-dir listener + peer-uid authorization</name>
  <files>crates/rdpilot-daemon/src/ipc/unix.rs, crates/rdpilot-daemon/src/ipc/mod.rs</files>
  <read_first>
    crates/rdpilot-daemon/src/ipc/framing.rs (the frame read/write used per connection)
    crates/rdpilot-config/src/paths.rs (the `directories::BaseDirs` reuse pattern — mirror it for the runtime dir)
    .planning/phases/12-session-daemon/12-RESEARCH.md (the Unix listener code example: DirBuilder mode(0o700), UnixStream::peer_cred(), the geteuid comparison)
  </read_first>
  <behavior>
    - `socket_dir()` creates `<runtime_dir>/rdpilot` (falling back to cache_dir) with mode `0700` applied ATOMICALLY at creation (DirBuilder.mode), no post-hoc chmod.
    - `bind()` creates the listener at `<dir>/daemon.sock`, cleaning up a stale socket file left by a killed predecessor (connect-then-unlink-then-rebind), never blindly reusing it.
    - `accept_and_authorize()` accepts a connection, reads `peer_cred()`, and returns `Err(PermissionDenied)` when `peer.uid() != effective_uid`.
    - The effective uid is obtained via `libc::geteuid()` (the Cargo.toml unix dep), not an env var.
  </behavior>
  <action>
    In `unix.rs`, implement `fn socket_dir() -> io::Result<PathBuf>` using `directories::BaseDirs::new()` → `runtime_dir()` (fallback `cache_dir()`) joined with `rdpilot`, created via `std::fs::DirBuilder::new().recursive(true).mode(0o700).create(&dir)` (`std::os::unix::fs::DirBuilderExt`) — atomic mode-at-creation, closing the TOCTOU window (research Pitfall 5 / V4). `fn socket_path()` = `socket_dir()?.join("daemon.sock")`. `async fn bind() -> io::Result<UnixListener>` implementing the stale-socket cleanup: if the path exists, attempt a connect; if the connect fails (no live listener), `std::fs::remove_file` it, then `UnixListener::bind`; if the connect SUCCEEDS, another daemon is live → return an `AddrInUse` error (Plan 06's bind-as-mutex uses this to exit cleanly). `async fn accept_and_authorize(listener: &UnixListener) -> io::Result<UnixStream>`: `accept().await`, call `stream.peer_cred()` (stable tokio API), compare `peer.uid()` against `unsafe { libc::geteuid() }` (wrap in a small `#[allow(unsafe_code)]` helper `fn effective_uid() -> u32` — the ONLY unsafe in this crate, justified and localized), return `io::Error::new(PermissionDenied, "peer uid mismatch")` on mismatch. In `ipc/mod.rs`, cfg-gate: `#[cfg(unix)] pub use unix::{bind, accept_and_authorize, socket_path};` and keep the `#[cfg(windows)]` re-export pointing at the windows stub (filled in Plan 07). Expose a small `pub(crate)` connection-serving helper `async fn serve_connection(stream, registry)` that loops `read_frame::<Request>` → `dispatch` → `write_frame::<WireResponse>` (used by Plan 06's accept loop).
    Add inline unit tests (Unix-native, offline): `socket_dir()` returns a dir whose mode is `0700` (stat the created dir and assert `mode & 0o777 == 0o700`); `accept_and_authorize` accepts a same-uid connection (the test process connects to itself → uid matches → Ok). The genuine different-uid rejection is the BLOCKING integration test in Task 3.
  </action>
  <acceptance_criteria>
    - unix.rs uses `DirBuilder` with `.mode(0o700)` (not a post-creation chmod)
    - unix.rs calls `stream.peer_cred()` and compares against `libc::geteuid()`
    - unix.rs `bind()` performs stale-socket connect-then-unlink cleanup and returns AddrInUse when a live listener already holds the path
    - The only `unsafe` block is the localized `effective_uid()` helper with an `#[allow(unsafe_code)]`
    - A unit test asserts the runtime dir is created with mode 0700
  </acceptance_criteria>
  <verify>
    <automated>RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu cargo test -p rdpilot-daemon --target x86_64-unknown-linux-gnu ipc::</automated>
  </verify>
  <done>The Unix listener binds under a 0700 dir with stale-socket cleanup and rejects a peer-uid mismatch; same-uid connections are authorized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Different-uid client rejected — SC#4 [BLOCKING]</name>
  <files>crates/rdpilot-daemon/tests/ipc_security.rs</files>
  <read_first>
    crates/rdpilot-daemon/src/ipc/unix.rs (bind / accept_and_authorize / peer-uid logic under test)
    .planning/phases/12-session-daemon/12-RESEARCH.md (Environment Availability: "Second local OS user account" fallback options — real cross-account preferred; a simulated/injected UCred is weaker but acceptable interim)
  </read_first>
  <behavior>
    - A same-uid client connecting to the daemon socket is accepted (control case).
    - A connection whose peer uid differs from the daemon's effective uid is rejected with PermissionDenied.
    - The socket directory is not traversable/usable by another account (mode 0700 asserted as necessary-but-not-sufficient evidence alongside the uid check).
  </behavior>
  <action>
    Create the BLOCKING SC#4 integration test. Primary path (preferred, real evidence): if the test can run as / spawn a genuinely different uid — detect a second unprivileged account or a `sudo -u`/container-provided alternate uid — have that different-uid client attempt to connect and assert the daemon rejects it. Gate this real-cross-account variant behind an env flag (e.g. `RDPILOT_SECOND_UID`) and `#[ignore]` when the second account is unavailable, mirroring the live-test opt-in convention, so CI without a second account does not fail.
    Fallback path (always-runnable offline evidence, weaker but present per research): exercise `accept_and_authorize`'s decision function directly by factoring the uid comparison into a pure `fn authorize_uid(peer_uid: u32, our_uid: u32) -> Result<(), io::Error>` in `unix.rs` (add it in Task 2 if not present) and asserting `authorize_uid(our+1, our)` is `PermissionDenied` while `authorize_uid(our, our)` is `Ok`. ALSO assert the real socket dir is mode `0700` (necessary-but-not-sufficient — research warns a stat-only test is insufficient, so this accompanies the uid-decision assertion, it does not replace it).
    Document at the top of the file: the FULL cross-account proof (a real other OS account rejected end-to-end) is available via the `RDPILOT_SECOND_UID` path here AND is re-confirmed on Windows (DACL/other-account) in the live-gate Plan 07; the always-on fallback proves the authorization decision logic offline.
  </action>
  <acceptance_criteria>
    - tests/ipc_security.rs asserts `authorize_uid(our_uid + 1, our_uid)` yields a `PermissionDenied` error
    - tests/ipc_security.rs asserts `authorize_uid(our_uid, our_uid)` is `Ok`
    - A real-cross-account variant exists, gated behind `RDPILOT_SECOND_UID` + `#[ignore]`
    - The socket-dir mode-0700 assertion is present as accompanying (not sole) evidence
  </acceptance_criteria>
  <verify>
    <automated>RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu cargo test -p rdpilot-daemon --target x86_64-unknown-linux-gnu --test ipc_security</automated>
  </verify>
  <done>The IPC boundary rejects a different-uid peer (decision logic proven offline; real cross-account path gated for CI/live) — DAEMON-02, SC#4 [BLOCKING] offline portion.</done>
</task>

</tasks>

## Artifacts this plan produces

- `ipc::unix::{socket_dir, socket_path, bind, accept_and_authorize, effective_uid, authorize_uid}` — 0700 dir + peer-uid gate
- `ipc::framing::{write_frame, read_frame}` — length-prefixed JSON with a size cap
- `ipc::serve_connection` — per-connection read→dispatch→write loop
- `dispatch::dispatch` — Request → registry → WireResponse, incl SESSION-03 `list`
- `crates/rdpilot-daemon/tests/ipc_security.rs` — SC#4 [BLOCKING] offline proof

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local other-user process → daemon socket | An unprivileged different-account process must not reach the daemon (DAEMON-02) |
| untrusted client bytes → framing/deserialize | Client-controlled frame length and JSON cross into the daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Elevation of Privilege | Another local account connecting to the daemon socket (Pitfall 5a) | critical | mitigate | `0700` runtime dir created atomically (mode-at-creation) + per-connection `peer_cred()` uid check against effective uid; SC#4 test asserts rejection |
| T-12-10 | Spoofing | Stale socket file after `kill -9` reused by a malicious process | high | mitigate | `bind()` connect-then-unlink-then-rebind, never blind reuse; AddrInUse if a live listener holds it |
| T-12-11 | Denial of Service | Oversized declared frame length forcing huge allocation | medium | mitigate | `read_frame` enforces a max-length cap before allocating |
| T-12-12 | Information Disclosure | `Connect` plaintext password logged during dispatch | high | mitigate | Dispatch must not log the raw `Request`; acceptance criterion greps for absence of a password-printing log line |
| T-12-13 | Elevation of Privilege | Windows null/default DACL + pipe squatting | high | transfer | Deferred to live-gate Plan 07 (pinned Windows machine): explicit owner-SID DACL + `first_pipe_instance(true)`; `ipc/windows.rs` stub only here |
</threat_model>

<verification>
- `RUSTUP_TOOLCHAIN=stable-x86_64-unknown-linux-gnu cargo test -p rdpilot-daemon --target x86_64-unknown-linux-gnu` (framing/dispatch/ipc unit + ipc_security) green.
- `cargo test -p rdpilot-daemon --test ipc_security` passes the offline authorization-decision + mode-0700 assertions.
- Manual: `ipc/windows.rs` remains a `#[cfg(windows)]` stub (Windows DACL is Plan 07).
</verification>

<success_criteria>
- SC#2: `list` reports name/id, target host, status, connected-since, last-activity (SESSION-03).
- SC#4 [BLOCKING] offline: a different-uid peer is rejected; the 0700 dir + peer-uid decision is proven on this Linux host (DAEMON-02).
- Framing is bounded and dispatch routes every verb without a wildcard arm.
</success_criteria>

<output>
Create `.planning/phases/12-session-daemon/12-04-SUMMARY.md` when done.
</output>
